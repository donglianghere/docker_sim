# 真机调试与飞行测试问题总结报告

**覆盖时间**：2026-09-02 ～ 2026-09-04（三天）
**覆盖范围**：真机（NX01/NX02，PX4 v1.16 + Jetson Orin NX，MICOAIR_H743_V2飞控）离线日志分析 + 现场实时排查 + 三次真机起飞现场抓取
**报告性质**：技术备忘录，按问题主题归类，非按时间顺序流水账

---

## 一、执行摘要

三天里一共排查出四类相互独立、但部分存在因果关联的问题：

| 类别 | 结论性质 | 当前状态 |
|---|---|---|
| ① 2号电机/电调整条链路故障，导致多次真实炸机 | **已实锤**（多份ulog交叉验证） | 未修复，待返厂/更换硬件 |
| ② 外部定位源（DLIO/vision_pose）间歇失效、位置跳变、进程崩溃 | **已实锤**，根因链条已追到底层物理层 | 部分修复已写入源码但**未部署**；最新根因（网线故障）**未修复** |
| ③ UWB双标签测向偏置大、串口卡死 | **已实锤** | 参数已改（待重启生效）；串口卡死根治方案已写入entrypoint（待重建镜像） |
| ④ DLIO处理性能随会话时长单调下降 | **现象已实锤，根因链条本次三天内两次被推翻/深化** | 部分优化已实测生效（CPU占用打了对折），核心"关键帧无界增长"修复待部署 |

**全篇最重要的一条新结论**：本次系列现场排查最初怀疑软件bug（GICP配准失败、DLIO内存泄漏）是外部定位源反复异常的根因，但层层深入后，09-04当天最终在**物理层**找到了一个此前完全没被怀疑过的因素——**Jetson到Mid-360激光雷达之间的以太网物理链路本身不稳定（网卡`r8168`反复link down/up，`ethtool`显示信号完整性错误持续增长），第三次起飞时该链路直接断线且写本报告时仍未自愈**。这条物理链路故障很可能是②③④里相当一部分现象的共同上游诱因，建议作为下一步排查的最高优先级。

---

## 二、问题一：2号电机/电调链路故障——多次真实炸机（09-02）

### 现象
根目录下4份+另5份更早的真机ulg日志，记录同一晚（20:03~20:26）连续3~4次炸机/失控翻滚，另有同一开机session里更早（起飞后7~14分钟）的故障早期表现。

### 定位方法
用pyulog离线解析`actuator_outputs`/`vehicle_torque_setpoint`/`vehicle_attitude`/`battery_status`等topic，跨多份日志交叉核对时间线，人工分析方法后来固化成自动化脚本`scripts/motor_health_check.py`。

### 根因（已实锤，多份独立日志交叉验证）
**2号电机（后左，`output[1]`）整条链路故障**，表现形式不固定——有时飞行中途推力骤降（约100ms内从700+崩到idle值109），有时起转阶段直接卡死在109（最长3.9秒，从当晚**第一次成功起飞的解锁瞬间**就已存在）——呈现"渐进式劣化/间歇性接触不良"特征，不是单次性故障。

**已排除的假设**：
- 电调大电容不足——早期异常窗口电压完全平稳，无毛刺
- 电调/飞控标定问题——DShot数字协议不存在传统标定概念，飞控几何映射对称无异常
- 手动模式更安全——同一套控制律代码，手动模式没炸机只是当次发作较轻（掉到492-538，未触底），不是模式本身的保护

**额外发现**：PX4自身的单电机失效检测器因未开DShot双向遥测（`DSHOT_BIDIR_EN=0`）**完全失效**（已读参数确认）——这条硬件故障本可以被飞控自己拦截，但因为一个配置项没开，完全没起作用。

### 后续动作
1. `scripts/motor_health_check.py`——喂ulg输出电机健康度体检报告，已用6份日志交叉验证结论一致
2. 给出量产QA建议5条：出厂逐电机对比测试、振动台/晃动测试+拉长地面测试时长、自动化自检固化、开DShot双向遥测做闭环拦截、来料老化筛选

---

## 三、问题二：外部定位源间歇失效、位置跳变、进程崩溃（09-04贯穿全天）

这是三天里investigate最深、证据链最完整的一条主线，按"发现顺序=排查深度递进顺序"呈现，因为每一层新证据都在推翻/收窄上一层的假设。

### 3.1 历史ulog里的初步发现

- 起飞后外部定位源（`ev_pos`/`ev_yaw`）**间歇性完全丢失**（已实锤，多份ulog独立验证），主因不是电机/电调硬件故障（跟问题一是两件独立的事，当时曾怀疑过是同一次事故的不同表现，已排除）
- 重连时会**注入几十米量级的位置跳变**（已实锤）——这次是纯手动飞行才没酿成事故，如果是自动模式下依赖这个位置源做控制，后果会更严重
- 机载计算机链路丢包、CAN总线报错**在同一~11秒窗口同时异常加剧**，且与`ev_pos`长时间失锁窗口高度重合（已实锤时间相关性；已排除电池电压跌落作为共因）——当时的记录明确提示"这是两条独立总线同时打嗝，应优先怀疑物理上的共同路径"，这个提示在09-04后半天被证实是对的（见3.4节的网线发现）

### 3.2 现场实时排查（真机SSH，未起飞状态）

- Mid-360雷达自带IMU与飞控IMU（BMI088/BMI270）是完全独立的两套硬件、两条独立数据通路（已实锤）
- 在真机历史rosbag里实锤了一次DLIO"冻结几秒→跳变几十米，反复循环"的灾难性发散事件（flight1，本次三天时间窗口之前的历史数据），且发生在**武装/自稳飞行状态下**（不是抱着走的静止状态）
- 读DLIO源码找到当时的触发机制猜测：IMU积分`propagateState`拒绝率随会话时长单调爬升、重启清零——这条猜测在09-04后半天被证明**不完整**（见3.5节）

### 3.3 现场实时抓取起飞瞬间（第一次真机起飞，本报告新增数据）

在起飞前部署自研脚本`scripts/takeoff_capture_logger.py`（实时订阅DLIO里程计、两路IMU、vision_pose，加冻结/跳变检测）+ 宿主机`tegrastats`，在真实起飞瞬间**首次直接逮到DLIO odom_node段错误崩溃（exit code -11）**的完整前因链条：
`propagateState`积分dt暴涨到36秒（说明处理管线卡了36秒没处理新数据）→密集刷屏"Low number of points in the cloud"→GICP配准0对应点彻底失败→`repub_odom`拦下了一帧隐含速度143m/s的异常数据没让它传给PX4（这是这次没有真炸机的关键）→**`dlio_odom_node`进程段错误退出，且没有自动重启，此后到落地全程约22秒零里程计反馈**。

宿主机`tegrastats`独立佐证：崩溃瞬间内存从2842MB骤降到2649MB后不再变化，全部8核CPU频率同步从满速跌到729~960MHz——跟"进程真的死了"完全吻合，且温度全程正常、内存无泄漏式增长，**排除了"CPU长期过载/内存耗尽"这类资源枯竭式假说**，指向一次由退化点云触发的具体代码路径bug。

### 3.4 第二次起飞：确认"低点数"是真实数据丢失，不是处理积压；且发现跟"是否武装"精确同步

用户质疑"真的是点云数量不够吗？桨叶振动还影响点云数量？"——为此新增原始点云每帧点数+处理延迟统计、两路IMU振动量化。第二次起飞实测：

- 原始点云每帧点数反复掉到固定的**96个点**（正常约10000点），共1051次，**延迟始终只有5~30毫秒**（不是消息积压变旧了才处理，是当场就只收到这么点数据）——96这个数字精确重复上千次，形态像"只收到一个UDP包"
- **跟武装状态精确同步**：武装前点云/雷达IMU全程零掉线，飞控加速度std锁死0.04（静止基线）；武装后（螺旋桨转起来）飞控加速度std迅速抬升，第一次掉线恰好发生在武装后8秒，此后反复出现，掉线严重程度与飞控测到的振动幅度大致同步；解除武装后立刻恢复干净
- **关键对照组**：同一时段飞控IMU（独立UART链路，跟雷达无物理关联）绝大多数时候不受影响——**排除了"整个系统CPU喘不过气"这个最简单的解释**，问题精确指向雷达这条数据通路本身
- 这次DLIO在同样甚至更严重的掉线频率下**没有崩溃**（跟第一次形成对比，样本量还太小，不能下"修好了"的结论）

### 3.5 用户追问逼出更精确的机制：GICP"伪收敛"bug，以及第二次起飞其实真的发散过一次

- **"为什么拦不住"**：读DLIO源码确认，GICP配准只在"0个对应点"时触发保护性回滚（`num_correspondences < 64`才算LOST TRACK），如果退化点云勉强凑出几十个（但仍然是烂的）对应点，配准会收敛到一个**形式上"成功"但内容错误**的位姿，且这个错误位姿会被写进关键帧/地图，成为下一帧配准的先验——一步错步步错。第二次起飞的完整数据回放里，真的抓到了这样一次发散：位置从武装后t+112s起滚雪球式跳变（3m→10m→20m→…→40m），最终冻结在偏离真实位置100余米的坐标上，**一直僵持到落地都没有恢复**，全程DLIO进程本身没有崩溃，只是永久卡在错误状态
- 更精确的历史根因（读源码在注释里发现，**这条修复已经在当前源码里生效**，flight1事故发生时大概率还不存在）：GICP在完全无匹配点时，`rho`公式算出`0/0=NaN`，C++里`rho<0`判断对NaN恒为false，代码因此误判"优化成功"、报告"误差0/已收敛"，实际早已发散——现在用`num_correspondences`识破这种伪装，但**只堵住了"0个对应点"这一种退化情况，堵不住"少量但非零"这种更隐蔽的情况**（就是3.5节这次真实复现的模式）
- `repub_odom`的隐含速度阈值防线（15m/s）本身也有缝：如果前面刚好冻结了几秒（dt被拉大），后面即使跳了十几米，算出来的"隐含速度"也可能刚好落在阈值以内被当成合理数据放行——这次数据里确实有几次15~19米的跳变被判定为"合理"实际发布了出去

### 3.6 第三次起飞：找到目前为止最底层的根因——物理网络链路故障

追问"这个掉线到底发生在哪一层？"——在宿主机（不进容器）新增两路诊断：`dmesg -w`实时监听内核网卡事件、定期轮询`ethtool -S`错误计数器。结果：

- **`enP8p1s0`（Jetson接Mid-360那张192.168.1.5网卡，`r8168`驱动）反复"link down"/"link up"**，`ethtool`显示`rx_errors`/`align_errors`持续增长（align_errors是电气信号完整性问题的典型指纹）——**这条证据把问题定位到物理链路层，不是ROS2/DDS软件层，也不是雷达内部固件**
- **这条链路在飞机完全静止、未武装的时候依然在闪断**——说明不是纯粹由飞行振动触发的，虽然振动大概率会让接触更差、发作更频繁
- 三个独立数据源精确对上同一秒：flight1那次36秒发散+DLIO崩溃，对应的正是一次持续18秒的link down事件，起点跟DLIO进程崩溃时间戳、ROS话题侧三路话题同时归零的时刻严丝合缝
- **第三次起飞（今天最后一次）：链路在起飞后8~18秒内彻底断线，此后一直到落地、到写这份报告的此刻（相隔近半小时）都没有自己恢复过**，现场直接查证`ethtool enP8p1s0`：**`Link detected: no`**

### 3.7 本条主线的结论

原本层层深入的"DLIO内存泄漏/CPU过载/GICP配准bug"排查，最终收敛到一个具体的、可现场验证的物理故障：**Jetson到Mid-360之间的网线/接头连接不可靠**。此前查到的软件层问题（GICP伪收敛、关键帧无界增长、repub_odom阈值缝隙）依然是真实存在、值得修的缺陷，但**很可能只是这条物理故障的下游放大器，不是这一连串故障现场最原始的病灶**。

---

## 四、问题三：UWB定位系统问题

### 4.1 双标签测向偏置大（09-03 L3实验实锤）
- UWB静态位置噪声实测σ=0.0105m，比论文假设的0.05m小3~5倍，视距下野值率0.21~0.41%，基本无野值（体现UWB测距本身精度不差）
- 但双标签测向σ_yaw达到2.06°（比论文写的13.9°小很多，但**块平均标度检验未通过**，UWB输出时间强相关，简单平均无法进一步降噪）
- **tag_b测距解算存在随头朝向、随平移位置变化的大偏置**（实测0.27~0.92m，物理距离应为0.28m），导致双标签yaw带±15°以上偏置——这是当前平台角度精度天花板的直接来源

### 4.2 基线参数配置错误（09-04实锤）
现场核实两个UWB标签实测间距约0.44~0.46m，配置文件里`baseline_length_m=0.28m`——配置值跟实际物理安装间距对不上，是`uwb_dual_tag_fusion`节点持续报警的直接原因。**已处理**：`.env`里`UWB_BASELINE_LENGTH_M`改成0.41，新增`UWB_YAW_OFFSET_DEG=135`统一钉死（此前一直靠两处不一致的默认值兜底），**下次容器重启生效**。

### 4.3 UWB串口卡死（09-04实锤）
第二次启动UWB节点必现`Input/output error`崩溃，排查确认是host内核层面CH340转串口芯片被卡死（不是docker配置问题）——第一次运行时串口没有走正常close()流程（大概率是容器停止时SIGTERM没有正确转发到子进程），必须做USB层面复位才能恢复，光重启容器/节点没用。**已处理**：`flight-stack-entrypoint.sh`加了SIGTERM信号转发处理器，**但这是镜像构建时打进去的文件，需要下次重建镜像才会生效**，本次未验证。

---

## 五、问题四：DLIO处理性能随会话时长单调下降

### 5.1 现象（已实锤，多次独立测量一致）
DLIO里程计发布频率随单次会话运行时长增长而系统性下降（09-03测得97.9→19.8→12.3Hz），重启后归零重新爬升。

### 5.2 根因排查过程中的三次自我更正（09-03，重要，反映排查方法的严谨性）
早期怀疑的三条机制都被后续证据推翻：
- ~~"定时器被点云回调阻塞"~~——源码确认雷达/IMU订阅各有独立回调组，不成立
- ~~"occupancy_inflate话题是20Hz/61MB/s大流量"~~——实测只有3.08Hz/约10MB/s
- ~~"DLIO被调度器饿死"~~——用schedstat算出的数字因DLIO线程动态创建/销毁而失真，不成立

### 5.3 真正根因（09-04读源码实锤）
`buildSubmap()`等函数对`keyframes`关键帧列表做**全量O(N)遍历**（找近邻+凸包/凹包几何计算），而`keyframes.push_back`全文只有增无减，**关键帧列表和对应点云从DLIO启动起完全没有裁剪/滑窗/淘汰机制**，精确解释了"内存持续增长(~5.6MB/分钟)+CPU长期高占用+odom频率随时间单调下降"这三条曲线同步出现的现象。跟雷达/飞控IMU数据源频率无关（两者都稳定100Hz）。

### 5.4 已完成的优化
- `pointcloud_z_filter`加限频+惰性订阅：CPU占用从101.7%降到45.7~60.1%，**已验证生效**
- DLIO关键帧搜索改成滑窗（只扫最近N帧，默认500）：**已写入源码，真机容器内实际编译验证通过（2分2秒，exit code 0）**，但**当前运行容器未加载新二进制，需要重启DLIO节点才生效**；且这次只解决了CPU/耗时，关键帧点云依然常驻内存不释放，内存增长问题未解决
- `repub_odom.py`用消息自带`header.stamp`（而非接收时wall clock）计算隐含速度：**已写入源码，同样需要重启节点/容器才生效**

### 5.5 一个待决策项
Jetson功耗模式从40W切到MAXN_SUPER，odom频率只提升6%（18.2→19.4Hz），但温度从80°C升到84~86°C，已因过热停机一次——是否切回默认功耗模式，**待用户决策，非本报告结论**。

---

## 六、修复状态一览表

| 编号 | 修复内容 | 涉及问题 | 状态 |
|---|---|---|---|
| ① | GICP零匹配点(NaN伪收敛)保护 | 问题二 | **已在真机运行代码中生效**（历史修复，本次三天开始前已存在）——但只堵住0对应点这一种退化情况 |
| ② | UWB baseline/yaw offset参数更正 | 问题三 | 已写入`.env`，**下次容器重启生效**（不需重建镜像） |
| ③ | UWB串口卡死：entrypoint加SIGTERM转发 | 问题三 | 已写入entrypoint脚本，**需下次重建镜像才生效**，未验证 |
| ④ | DLIO关键帧滑窗（限制搜索范围为最近500帧） | 问题四 | 已写入源码+真机容器内编译验证通过，**运行中容器未加载，需重启DLIO节点** |
| ⑤ | repub_odom用header.stamp算隐含速度 | 问题二 | 已写入源码，**需重启对应节点/容器生效** |
| ⑥ | `pointcloud_z_filter`限频+惰性订阅 | 问题四 | **已验证生效**（CPU占用打对折） |
| — | 网线/接头物理故障 | 问题二（最新根因） | **未处理，需现场人工检查** |
| — | 2号电机/电调链路故障 | 问题一 | **未处理，需返厂/更换硬件** |
| — | GICP"少量但非零对应点"发散防护 | 问题二 | **未修复**，需要在配准质量/一致性上加更严格校验，尚无具体方案 |
| — | DLIO关键帧点云内存不释放 | 问题四 | **未修复**，滑窗只解决了CPU，内存仍常驻增长 |

---

## 七、下一步行动建议（按优先级）

1. **【最高优先级，立即可做】** 断电检查Jetson到Mid-360之间的网线两端接头，插紧或更换，重新连接后先在地面观察`ethtool enP8p1s0`的`Link detected`是否稳定、错误计数是否还在涨，确认物理层稳定后再考虑下一步测试。这条如果解决，问题二里相当一部分现象可能直接消失。
2. **【尽快】** 与用户确认是否要重启DLIO节点/整个飞行栈容器，把④⑤两处已编译验证过的修复真正跑起来；⑥UWB串口修复需要用户决定何时重建镜像。
3. **【尽快，安全相关】** 2号电机/电调链路故障尚未处理，硬件层面的问题不建议再用当前这套动力系统起飞，直到完成返厂检修或更换。
4. **【中期】** 开启DShot双向遥测（`DSHOT_BIDIR_EN`），让PX4自身的单电机失效检测器真正发挥作用，作为电机故障的最后一道防线。
5. **【中期】** 给GICP配准结果补一条"对应点数量虽够但配准质量/跟先验一致性存疑"的校验，堵住3.5节里发现的那种"看似成功实则发散"的缝隙；同时评估DLIO关键帧点云内存的释放/降采样方案。
6. **【低优先级】** UWB tag_b测距偏置的物理根因（随头朝向/位置变化）尚未定位，可能需要重新标定或检查天线安装。
7. **【低优先级】** Jetson功耗模式（40W vs MAXN_SUPER）的取舍，待用户根据实际温度余量决定。
