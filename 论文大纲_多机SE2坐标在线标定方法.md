# 论文大纲：多机器人局部-全局坐标系SE(2)在线标定方法（方向B）

> 本大纲对应`论文选题方向建议.md`方向B。核心素材来自
> `docker_sim/多机空间坐标对齐问题_设计讨论纪要.docx`（数学推导最完整、
> 已是"整理成文"版本，建议方法章节直接以其第四节为骨架）、
> `docker_sim/DEBUG_JOURNAL.md`（2026-08-11~08-19相关条目，行号见文中
> 标注）、`docker_sim/README.md`（"起飞点一键锁定"章节，约373-452行）、
> `read_hw.md`（真机UWB双标签测向，2026-08-18，作为方法族泛化验证）。
> 素材挖掘由后台agent完成，本文档已整合其发现，**文中标注"待核实"的
> 数值/表述在写正式全文前必须对照当前代码核实一遍，不能直接照抄**。

---

## 标题（2026-08-20确定推荐，不再是并列候选列表）

### 推荐标题

**基于滑窗最小二乘的多机器人局部-全局坐标系SE(2)在线标定方法**

理由：
1. **保留"多机器人"**——虽然方法能推广到水下机器人/行人导航等单体
   场景（见1.1.2节），但本文实际验证的核心场景与问题动机的第一入口
   始终是多机器人协同，标题应反映"论文实际验证了什么"，推广空间放
   在讨论部分（8.1节）讲清楚即可，标题过度抽象反而不利于读者/检索
   系统快速定位论文的应用场景。
2. **不用"UWB"**——避免让读者误以为方法与UWB测距原理绑定，低估通用性。
3. **不用"Wahba"打头**——1.2.1节已把Horn/Umeyama重新定位为比Wahba
   问题更贴切、更贴近机器人/SLAM读者认知的理论参照，标题里突出
   "Wahba问题二维退化"会与正文表述重心不一致，且"Wahba"对多数中文
   机器人期刊读者不够熟悉。
4. "滑窗最小二乘"点出方法核心特征（在线、增量、无需停机标定），
   "SE(2)"给出精确数学范畴，"局部-全局坐标系"点出问题本质。

### 备选标题（期刊标题字数限制较严时使用，通常上限约20字）

**多机器人局部-全局坐标系SE(2)在线标定方法**（去掉"基于滑窗最小
二乘的"前缀，方法特征放进摘要第一句交代）

### 其它曾考虑、已否决的候选

- ~~稀疏绝对观测下多智能体局部-全局坐标系在线标定方法~~——"稀疏绝对
  观测"不是本领域标准术语，牺牲可读性换来的通用性收益不明显，否决。
- ~~一种硬件可迁移的多机器人SE(2)刚体变换在线估计方法~~——"硬件可
  迁移"是本文一个具体设计特点而非最核心卖点，放标题里份量不够，否决。
- ~~Wahba问题二维退化视角下的坐标系在线标定方法~~——见上方理由3，否决。
- ~~面向UWB单基线测距的多机器人地图坐标系在线对齐方法~~——见上方
  理由2，否决。

---

## 摘要要点

- **背景**：多机器人协同任务（编队避障、集中调度、共享地图）要求每个
  机器人把自己独立运行的SLAM局部坐标系统一到全局参考系。这件事在有
  GPS、有动作捕捉（mocap）或预先精确测量好的多基站UWB网络时是已解决
  问题，但这些前提在仓储物流、搜救应急、地下/隧道、临时工地等GPS
  拒止、无法预先布设固定基础设施的真实部署场景中普遍不成立。相较于
  依赖专用动捕系统或需要预先精确测量多个固定锚点的传统UWB三边定位，
  仅能获取单基线测距/绝对位置观测（而非直接姿态观测）的低成本UWB
  方案，如何在不停机做专门标定动作的前提下在线求解局部-全局SE(2)
  刚体变换，是一个实际存在、但现有开源多机器人系统普遍未妥善处理的
  工程问题（现有集群规划器或接口设计"半成品"未实现数据源，或强依赖
  mocap单次测量精度）。
- **方法**：证明该问题是经典Wahba姿态确定问题在"二维、仅航向角自由度"
  下的退化情形，推导出闭式解θ\*=atan2(S,C)（并用两种独立方法交叉
  验证），提出基于滑窗的在线估计机制（用运动过程中的位移段代替专门的
  标定动作，隐式按位移长度加权，短距离/低信噪比段自动被压低权重），
  配合一次静止条件下的UWB平移锚点标定，构成完整的SE(2)在线标定方案。
- **鲁棒性发现**：在实际部署中定位并修复了两类工程鲁棒性缺陷——
  ①启动瞬间NaN测量值污染滑窗锚点导致永久卡死（且原实现无自我恢复
  路径）；②诊断可观测性设计缺陷（"凑够样本才发布"导致无法区分"未
  收敛"与"卡死"）。进一步通过定量预测排除竞争假说的方法，精确定位并
  验证了一个重要的架构局限——标定结果的持续收敛与下游一次性消费之间
  的不同步，给出闭环修复方向。
- **验证**：仿真环境下收敛精度与噪声传播理论预期吻合；在真实Jetson
  硬件平台上验证了同一方法族的泛化能力（UWB双标签单机航向角测向）。
- **意义**：方法本质上只依赖"连续局部相对运动估计+稀疏绝对位置观测"
  这一抽象输入结构，与UWB测距原理无关，可直接迁移到水下机器人
  （DVL+USBL）、行人导航（PDR+信标）、仓储物流（AGV/AMR+UWB）等
  结构相同的场景，也可经SE(3)扩展应用于多相机外参在线标定、航天器
  编队等场景；本文以低成本UWB硬件条件下的多机器人无人机集群作为
  验证载体，给出了不需要专门标定动作、可在线持续收敛、硬件可迁移的
  完整方案，以及一套在线估计类方法在工程落地中必须处理的鲁棒性
  设计模式（含此前遗漏、本次补充的运动方向可观测性条件）。

---

## 1 引言

### 1.1 背景、真实价值与应用场景

> **本节的组织逻辑（2026-08-20按"方法应去场景化"重新梳理）**：先给出
> 脱离任何具体传感器的抽象问题结构（1.1.1），再说明UWB+SLAM+无人机
> 集群只是这个抽象问题的一个验证实例、并非方法的适用边界（1.1.2），
> 最后才落回本文的具体问题形式化定义（1.1.3）。写作时应严格按这个
> 顺序推进，不要一上来就讲UWB——那会让读者误把方法与UWB测距原理
> 绑定，低估通用性。

**1.1.1 抽象问题结构：局部相对运动 + 稀疏绝对观测**
- 本文方法真正依赖的输入只有两类，与具体传感器无关：①一个**连续的
  局部相对运动估计器**——任何能给出"某段时间内，该智能体在自己局部
  参考系下挪动了多少"的过程，可以是激光惯性里程计、轮式里程计、
  视觉里程计，也可以是纯IMU航位推算；②一个**稀疏、带噪声、但能给出
  绝对位置的观测源**——任何能间歇性给出"该智能体在全局参考系下的
  位置"的过程，不要求高频、不要求给出姿态，可以是UWB测距、偶尔一次
  的GPS/RTK定位、动作捕捉、固定摄像头对标记点的识别、声学定位。
  只要这两类输入同时存在，本文的滑窗最小二乘闭式解就能用，与观测源
  的物理原理无关——这是本文方法的真正适用边界，比"UWB+SLAM"这一
  具体实例宽得多。
- 这一抽象结构在机器人/导航领域有一个更贴切的经典参照系：**Horn
  （1987）绝对定向法/Umeyama（1991）方法**——SLAM评测工具（如evo、
  TUM RGB-D benchmark）用来对齐"估计轨迹"与"真值轨迹"的标准算法，
  本质上正是"一批局部系下的点/位移，配一批全局系下的对应点/位移，
  最小二乘求刚体变换"。本文方法与经典Horn/Umeyama方法的差异不在于
  数学本身（都是同一个最小二乘刚体对齐问题在不同自由度下的实例），
  而在于**对应关系的构造方式**：Horn/Umeyama假设已经有一批配对好的
  点，做批量对齐；本文的贡献是在"局部里程计连续输出、全局观测异步
  且稀疏到达"这一实际约束下，**在线、增量地从两路异步数据流中构造出
  配对的位移段，并随数据到达持续更新估计**，这个问题结构中还未有
  专门文献处理。相关工作章节应把Horn/Umeyama放在比Wahba问题更靠前、
  更核心的位置，因为它是本领域读者更熟悉、也更精确对应本文数学本质
  的参照系。

**1.1.2 UWB+SLAM+无人机集群只是验证实例之一：可直接推广的场景清单**
- **与本文验证场景数学结构完全相同（SE(2)，仅需替换传感器，方法
  不改）**：
  - **仓储/物流机器人集群（AGV/AMR）**：UWB基站+惯性/轮式里程计，
    工业界当前最主流的室内定位组合之一。
  - **搜救/应急响应多机器人编队**：GPS拒止、现场来不及预先精确测量
    锚点位置，需要快速部署后边动边完成标定。
  - **地下/矿井/隧道机器人**：GPS天然不可用，UWB是该领域的标准定位
    方案之一。
  - **工地多机协同**（施工机械/巡检机器人）：作业环境动态变化，固定
    多锚点网络维护成本高。
  - **水下机器人（AUV/ROV）**：DVL（多普勒计程仪）+IMU做局部航位
    推算，母船USBL声学定位偶尔给一次绝对位置——是这一抽象问题结构
    最经典的原始应用领域之一（水下没有GPS，USBL天生就是稀疏绝对
    观测源），可作为方法通用性的有力旁证写入讨论部分。
  - **室内行人导航/可穿戴设备**：脚绑IMU航位推算(PDR)+WiFi/蓝牙/
    UWB信标偶尔定位一次。
  - **室内多机编队/竞赛场景**：本文验证载体本身（双机协同无人机
    仿真+真机平台）对应的职业技能竞赛场景——桁架+网子搭建的室内
    场地，无GPS、无动捕覆盖。
- **结构相同但需要把SE(2)扩展为完整SE(3)（旋转自由度从1个航向角
  变成3个，闭式解需要换成通用Wahba/Umeyama解法，如SVD或四元数特征
  值法，失去本文"一次atan2"的简洁性，是推广时必须付出的代价，见
  1.2.1节与第8节讨论）**：
  - **多相机外参在线标定**：每个相机看到一个移动标定物在自己像素/
    局部3D系下的轨迹，配合一个独立的绝对参考（如另一套已标定好的
    追踪系统）给出该标定物的全局位置，问题结构同构，但需处理透视
    投影，不是直接照搬。
  - **航天器编队飞行**：局部相对导航+偶尔的绝对参考（GPS/地面测控），
    需要完整三维姿态而非仅航向角。
- 本文方法的真实定位：**在"只有一个廉价、粗糙的绝对位置观测源（不
  要求高频、不要求给出姿态）"这一最低配置约束下，把局部-全局坐标系
  对齐在线解决，不需要专门停机做标定动作**。这一约束组合是本文方法
  与mocap/多基站三边定位/点云配准类方法最本质的区别，也是其应用
  价值的来源——不是"又发明了一种角度估计算法"，而是在一个被现有
  方案系统性忽视的低成本硬件约束下给出可用方案。

**1.1.3 本文的具体问题形式化定义（验证实例的形式化，非方法的适用边界）**
- 给定N个机器人各自独立的局部SLAM里程计与一个共享的全局绝对定位
  观测源（本文用UWB作为具体实现，对应1.1.1节的"稀疏绝对观测源"这一
  抽象角色），在线估计每个机器人局部系到全局系的SE(2)刚体变换。

### 1.2 相关工作

> **写作前置提醒（2026-08-20文献调研结论）**：已用WebSearch/WebFetch
> 系统检索中英文文献，**未发现与本文完全相同的工作**（单一绝对位置源
> +滑窗最小二乘在线SE(2)标定+多机器人局部-全局坐标系对齐这一组合），
> 但存在多篇**高度相邻**、必须在此正面引用并讲清楚区别的工作，尤其是
> 1.2.2节第一篇（2025年10月刚挂出的arXiv论文，主题几乎撞题）——相关
> 工作一节不能只讲"经典理论+项目内部调研对比"，必须补上这几篇，否则
> 审稿人大概率会直接问"这和XX工作有什么区别"。以下按"数学基础→
> 最相邻的近期文献→其它UWB/多机定位文献→点云配准类→传统三边定位→
> 项目内部调研对比"顺序组织。

**1.2.1 数学基础：Horn/Umeyama轨迹对齐与Wahba问题**
（2026-08-20重新定位：Horn/Umeyama提到最前、作为核心参照系，Wahba
问题降为补充关联，理由见1.1.1节论证）
- **Horn（1987）绝对定向法 / Umeyama（1991）方法**：给定两组配对的
  点（或位移向量）$\{(u_i,v_i)\}$，最小二乘求解使$v_i\approx
  R\cdot u_i(+t)$的刚体变换，是SLAM社区评测工具（evo、TUM RGB-D
  benchmark等）对齐"估计轨迹"与"真值轨迹"的标准算法，本文读者群
  （机器人/SLAM领域）对这一方法远比对Wahba问题熟悉，应作为最核心的
  理论参照系。本文与Horn/Umeyama的本质区别不在最小二乘刚体对齐这一
  数学操作本身（结构相同），而在于**本文处理的是配对关系需要在线、
  增量地从两路异步数据流（连续局部里程计+稀疏绝对观测）中构造出来
  这一前置问题**，Horn/Umeyama默认配对关系已经给定、是批量离线
  操作。
- **Wahba问题及其通解**：经典三维姿态确定问题，通解涉及SVD分解或
  q-method（四元数特征值法）等（写作时补充确切文献引用），是
  Horn/Umeyama方法在"仅估计旋转、观测为方向向量而非带有平移的位置
  点"这一特化设定下的另一种历史脉络提法，两者数学上高度相关，可以
  在文中简要提及二者关系，但不需要展开成独立小节——本文处理的是这两
  条脉络共同退化到的"二维、仅航向角自由度"情形：退化后闭式解形式
  极简（一次atan2调用），通用三维解法（SVD/四元数特征值法）在该场景
  下的迭代/矩阵分解开销是不必要的过度设计，这是本文方法相对通用解法
  的效率论证。

**1.2.2 与最相邻的近期UWB/SLAM坐标标定文献的关系（重点撰写）**
- **《Coordinate-Consistent Localization via Continuous-Time Calibration
  and Fusion of UWB and SLAM Observations》**（arXiv:2510.05992，
  2025年10月）——主题最相邻的一篇，**必须正面引用并逐条讲清楚区别**：
  ①该文解决的是**单个机器人跨多次运行**之间的坐标一致性问题（SLAM
  每次重启原点会漂移，需要借助UWB把不同运行"钉"到同一参考系），
  本文解决的是**多机器人之间**局部-全局坐标系的在线对齐，问题的
  "多智能体"维度不同；②该文标定阶段采用**批量连续时间优化**（用一整
  段运行的全部测距+里程计数据、结合锚点间距离先验一次性求解锚点
  三维位置），随后用滑窗优化融合后续运行数据，本文的旋转估计从设计
  之初就是**在线递归、闭式解**（每个新位移段到达即可增量更新，不需要
  攒够一整段运行数据再批量求解）；③该文UWB配置是**多个固定锚点**
  （靠锚点间已知/可推断的相对距离恢复各锚点三维坐标），本文只要求
  **单一绝对位置观测源**（不需要锚点网络、不需要锚点间距离先验），
  对硬件部署要求更低。**定位表述建议**："该文解决的是同一机器人跨
  运行的时间维度一致性问题，本文解决的是多机器人间的空间维度一致性
  问题；即便同样面向'低成本UWB+SLAM'这一硬件约束，问题结构与解法
  路径均不同，可视为同一大方向下的互补工作，而非重复"。
- **《Autocalibration of a Mobile UWB Localization System for Ad-Hoc
  Multi-Robot Deployments in GNSS-Denied Environments》**（arXiv:
  2004.06762，2020）——标定的是**UWB锚点系统自身的位置**（让锚点
  部署更快、更准，支持移动锚点的即插即用），不是机器人里程计到全局
  坐标系的变换，是"基础设施自标定"而非"机器人坐标系对齐"，问题层面
  不同，但同属"降低UWB多机器人部署门槛"这一大方向，应作为背景动机
  的旁证引用。
- **《Aligning Coordinate Frames in Multi-Robot Systems with Relative
  Sensing Information》**（Nagavalli等，IROS 2014，CMU）——概念上
  最像"前辈"工作：提出集中式与分布式算法，通过**机器人两两之间的
  相对位置测量**（不依赖共享的外部绝对参考源）估计并对齐各机器人的
  坐标系朝向。与本文的核心区别在观测模型：该文依赖机间相对测量+
  一致性/共识类算法，本文依赖**每个机器人独立获取的、相对一个共享
  外部绝对源的位置观测**+闭式最小二乘，不需要机器人间直接互相测距/
  通信协商共识，架构上更简单、去中心化程度更高（每个机器人独立完成
  自己的标定，不依赖与其它机器人的实时交互）。
- **《基于超宽带阵列与里程计的多机器人相对定位》**（电子与信息学报，
  中文核心期刊，DOI:10.11999/JEIT210812）——每个机器人携带**多个**
  UWB节点组成阵列，通过非线性优化+滑动窗口图优化+粒子滤波估计
  **机间相对位姿**，报告的是相对定位误差（0.312m/4.903°）。与本文
  的本质区别：该文完全不处理局部坐标系到全局坐标系的对齐问题，是
  纯粹的"机间相对定位"，且需要每机多UWB节点（阵列），本文只需单个
  UWB标签+一个共享绝对参考源。写作时应引用此文作为"国内同期相关
  研究活跃度"的证据，同时明确指出问题定义不同。
- **《MOTLEE: Collaborative Multi-Object Tracking Using Temporal
  Consistency for Neighboring Robot Frame Alignment》**（arXiv:
  2405.05210，2024）——通过共享目标跟踪结果的时间一致性做相邻机器人
  帧对齐，观测模态是**目标检测/跟踪对应关系**，与本文基于原始位置
  观测的路径完全不同，仅作为"frame alignment"这一问题域的旁证列出，
  不需要深入对比。

**1.2.3 点云/子图配准类方法（ICP及变体）**
- 依赖点云重叠区域、对初始猜测敏感、计算成本高于本文方法（本文只需
  滑窗内标量运算）；本文方法适用于点云重叠不足或计算资源受限（如
  机载嵌入式平台）的场景。

**1.2.4 传统UWB多基站三边定位**
- 需要预先精确测量、固定多个锚点的绝对位置，部署与维护成本高；本文
  方法仅要求"至少一个能提供全局绝对位置观测的源"，对硬件部署要求
  更低。
**1.2.5 开源多机器人集群规划系统对跨机坐标对齐的现有处理方式**（这组
  对比是现成的、有说服力的相关工作素材，源自项目实测调研）：
  - mighty：`/frame_align`接口设计上支持完整4×4变换+持续刷新，理论
    完备，但"生产端"（原设计意图是配套的雷达配准包）从未被实际实现，
    是个语义完整、但缺数据源实现的"半成品接口"（历史话题命名
    `/lidar_registration/frame_align/...`即为佐证）。
  - ros2_px4_stack：`get_init_pose.py`一次性标定脚本，位置取mocap、
    roll/pitch取SLAM重力对齐、yaw单独取mocap欧拉角——前提是mocap
    单次测量精度足够给出准确航向角，UWB单基线测距做不到这一点，这是
    本文方法与之分化的直接技术原因。
  - ego-planner-swarm：上游完全未处理跨机坐标对齐，机间避让直接假设
    共享全局系（对应作者原始demo环境本身由mocap覆盖，跨机坐标天然
    一致，不代表问题不存在，只是被环境假设掩盖了）。
  - 用一句话总结这组对比的定位价值："现有开源集群规划器要么接口
    设计好了但没有配套实现数据源，要么强依赖UWB单基线做不到的
    观测前提，本文填补了'仅有UWB单基线绝对位置观测'这一常见低成本
    硬件条件下的在线SE(2)标定方案这一空白"。

### 1.3 本文贡献与领域价值（2026-08-20补充完整，与1.1节互补：
1.1节讲"给谁用"，本节讲"给学界添了什么"）

**1.3.1 学科定位：三个领域的交叉点**
- 本文同时与**导航定位**（UWB/IMU/里程计融合定位）、**多机器人系统**
  （协同定位、编队基础设施）、**SLAM/状态估计**（轨迹对齐、外参标定
  方法论脉络）三个领域相关，不隶属单一领域——写作时应在引言/结论
  都点出这一交叉定位，帮助读者/审稿人快速判断投稿栏目适配度。

**1.3.2 具体知识空白（本文最重要的领域价值判断，源自2026-08-20
三轮文献调研的副产品）**
- 密集查重过程中意外摸清了这个子领域的问题谱系：几乎全部可查到的
  中英文相邻文献（CMU 2014、电子与信息学报、西南科技大学专利+期刊
  论文线、IEEE T-RO 2024等，详见1.2.2节与附录A）做的都是**"机间
  相对定位"**——机器人测机器人的相对测距/方位角，估计彼此的相对
  位姿。**没有一篇做的是"每个机器人独立对齐到一个共享外部绝对参考
  系"**这件事。原因不难理解：多数研究者默认"要么用mocap（天然共享
  全局系，不存在对齐问题），要么用机间相对测距（不需要绝对参考）"，
  "只有一个粗糙外部绝对源、需要在线对齐到它"这一中间地带被系统性
  忽视了。**本文的领域价值，准确表述是"填补了这个被忽视的中间
  地带"，而不是"提出了全新算法"**——写作时应把这一分类边界
  （机间相对定位 vs 局部-全局坐标系对齐）在1.2节相关工作开篇就
  明确点出，作为定位本文问题在整个研究谱系里位置的关键论证。

**1.3.3 对经典理论的增量贡献（诚实定位为"小而具体"，不夸大）**
- Horn（1987）/Umeyama（1991）轨迹对齐是有40年历史的经典结果，但
  设定始终是**批量、离线、配对关系已知**。本文的增量贡献是将其
  改造为**在线、增量、配对关系需要从两路异步数据流现场构造**的
  版本——这是一个真实但"小而具体"的理论增量，不适合包装成重大
  理论突破，但足够支撑一篇方法类论文的理论部分。

**1.3.4 工程实践/方法论层面的贡献（论证材料最扎实的部分）**
- 三个真实鲁棒性缺陷（NaN污染永久卡死、诊断可观测性设计缺陷、双重
  实现冲突）的完整根因链条，是可迁移到其它在线估计系统的工程教训。
- 第5.2节"用定量预测排除竞争假说"是一次完整、可复现的科学论证
  范例，本身具有方法论示范价值。
- 8.1节的运动多样性可观测性条件与IEEE T-RO 2024论文独立得出的
  可观测性结论互相印证——两篇论文在不同问题上独立得出结构相似的
  可观测性结论，说明这是这类在线估计问题的共同数学根源，不是本文
  的孤立发现，这一呼应关系本身增强了本文的理论可信度。

**1.3.5 诚实的边界（不该往上贴的标签，写作时必须明确排除）**
- 不能宣称"解决了多机器人定位问题"——本文假设局部里程计已经工作
  良好、假设存在一个外部绝对观测源，只解决"如何在线对齐两者"这一
  个子问题，不涉及里程计本身的精度改进，不涉及绝对观测源本身的
  获取（如UWB测距算法）。
- 不能宣称"提出了新的姿态估计理论"——数学本身是经典结果（Wahba/
  Horn/Umeyama）在二维退化情形下的直接应用，理论新颖性有限，价值
  在于"在什么约束下用、怎么在线化、鲁棒性怎么做"这些应用/工程层面。
- 这两条边界应该在结论部分与贡献并列陈述，体现学术诚实，也是让
  审稿人更容易认同贡献真实性的写作策略（避免"一眼看穿的夸大"引发
  审稿人对全文其它论证的连带怀疑）。

### 1.4 论文组织结构

---

## 2 问题定义与数学模型

### 2.1 SE(2)变换与位移段定义
- 定义局部系位移段$u_i=(u_{ix},u_{iy})$（机器人自身里程计连续两次
  采样间的位移向量）与对应的全局系位移段$v_i=(v_{ix},v_{iy})$
  （同一时间窗口内全局绝对定位观测的位移向量），理想无噪声情形下
  满足$v_i=R(\theta)u_i$，$R(\theta)=\begin{pmatrix}\cos\theta &
  -\sin\theta\\\sin\theta&\cos\theta\end{pmatrix}$。

### 2.2 与Wahba问题的关系
- 标准Wahba问题：给定一组三维参考方向向量与对应的机体系观测向量，
  求最优旋转矩阵（3自由度），经典解法涉及SVD分解的q-method等。
- 本文场景的退化：只需要估计绕Z轴的单一航向角自由度，"参考方向对"
  退化为水平面内的位移向量对——这一退化直接把问题从"矩阵分解"降维
  到"标量代数"，是本文方法计算效率的理论来源，写作时应明确给出
  从三维通解退化到本文闭式解的推导路径（或至少给出定性论证+引用），
  以证明本文解法不是"重新发明轮子"而是对已知理论在特定约束下的
  正确简化。

### 2.3 代价函数与闭式解推导（完整摘录已有推导，写作时核实符号一致性）
- 最小二乘代价函数：
  $$J(\theta)=\sum_i \|v_i-R(\theta)u_i\|^2$$
  展开为
  $$J(\theta)=\sum_i\left[(v_{ix}-u_{ix}\cos\theta+u_{iy}\sin\theta)^2+(v_{iy}-u_{ix}\sin\theta-u_{iy}\cos\theta)^2\right]$$
- 对$\theta$求导并令其为零，二次项相互抵消，得
  $$\sin\theta\cdot\sum_i(u_{ix}v_{ix}+u_{iy}v_{iy})=\cos\theta\cdot\sum_i(u_{ix}v_{iy}-u_{iy}v_{ix})$$
- 定义$C\triangleq\sum_i(u_{ix}v_{ix}+u_{iy}v_{iy})$（各段位移点积和，
  衡量局部/全局位移方向的一致程度）、
  $S\triangleq\sum_i(u_{ix}v_{iy}-u_{iy}v_{ix})$（各段位移叉积和，
  衡量需要转多少角度才能对齐），得闭式解
  $$\theta^*=\operatorname{atan2}(S,C)$$
- **交叉验证**：该推导已用两种独立路径验证一致——①上述实数域直接
  求导展开；②复数域下把Wahba问题写成标准形式后代入求解。写作时
  两种推导都应给出，作为方法正确性的双重证明，提升论文的数学严谨度
  观感。
- **物理直觉**：代入公式可见，每段位移的隐式权重正比于
  $|u_i|\cdot|v_i|$——长距离、信噪比好的位移段自动获得更大权重，
  短距离、噪声主导的段自动被压低，不需要手动设计加权方案，这是该
  闭式解相对"简单平均多段角度估计"这类朴素方法的内生优势，值得在
  方法部分专门论证一段。

### 2.4 平移锚点与SE(2)整体重构
- 静止条件下的平移锚点标定（见第3.3节展开），得到某一时刻的局部/
  全局位置对$(local\_lock, uwb\_lock)$。
- 每次$\theta^*$更新后，复用同一对锚点重算平移：
  $$t=uwb\_lock-R(\theta^*)\cdot local\_lock$$
  保证整体SE(2)变换$(R(\theta^*),t)$内部自洽——旋转与平移不是分别
  独立估计再拼接，而是以固定锚点为基准协同更新。
- 下游目标点正/逆变换公式（用于把全局坐标换算成各机器人局部坐标，
  实测代码路径）：
  $$w_x=t_x+l_x\cos\theta^*-l_y\sin\theta^*,\quad w_y=t_y+l_x\sin\theta^*+l_y\cos\theta^*$$
  （z轴仅平移不参与旋转，因为本文场景SE(2)只处理水平面）。

---

## 3 在线估计机制设计

### 3.1 分段策略
- 最小位移阈值$d_{min}$（实测取值0.5m）：机器人每移动超过该阈值才
  记录一个新的位移段，避免距离过短时观测噪声主导估计（对应2.3节
  "权重正比于位移长度"的论证，此阈值是对该原理的工程实现——某种
  意义上是"预先过滤掉理论上权重就会很低的样本，减少无效计算"）。

### 3.2 滑窗更新
- 滑窗大小$W$（实测取值50段）：维护最近$W$段位移对，$C$、$S$在滑窗
  内滚动求和，新段加入、最老段移出——**这是本文方法与"批量（离线）
  最小二乘"的关键区别**，滑窗设计使方法具备跟踪时变外部效应（如
  SLAM累积漂移导致的局部-全局映射关系缓慢改变）的能力，而非收敛到
  一个固定不变的历史平均值（详细对比实验设计见第6.1节）。

### 3.3 首次可信判据与静止平移锚点标定
- 触发方式：机器人通电静止在起飞/起始点后，主动触发一次静止条件
  标定（对应实现是`set_origin_from_uwb`服务）。
- 可信性判据：取最近2秒（`sample_window_sec`=2.0s）内的绝对定位
  观测样本，数量$\geq5$且标准差$\leq0.15m$才认为可信，与SLAM里程计
  位置求均值差得到该时刻的平移锚点。
- 持久化：标定结果落盘，进程/容器重启后自动恢复，不需要重新标定
  （工程细节，体现方法在实际部署中的可用性设计）。

### 3.4 收敛性与噪声传播分析（**需要在成稿前补充正式推导，当前只有
    数值链条，缺公式化的方差传播模型**）
- 现有数值证据：UWB测距噪声标准差实测$\sigma\approx0.05m$
  **（待核实：本次挖掘未定位到该数值的独立出处行号，需要在最终成稿
  前用仿真配置文件`range_noise_std`默认值反查确认，不能仅凭此前已
  提炼的结论直接引用）**；单段最小位移0.5m对应的角度噪声量级
  $\approx\arcsin(\sigma/d_{min})\approx8°$；滑窗$W=50$段平均后，
  按独立噪声假设理论收敛到$\approx1$~$2°$，与实测诊断话题输出吻合。
- **需要补充的正式推导**：把上述数值链条形式化为闭式的方差传播
  公式——把$S$、$C$视为$W$个独立、方差为$\sigma_u^2$（单段角度噪声
  对应的方差）随机变量之和，用一阶泰勒展开在真实$\theta$附近对
  $\operatorname{atan2}(S,C)$做线性化，得到$\hat\theta$估计量的近似
  方差表达式（形式上类似于圆形统计中冯·米塞斯分布集中参数与角度
  估计方差的关系，或可直接借用该领域现成结果，写作时查阅相关文献
  确认最贴切的推导路径）——这是把"数值上吻合"升级为"理论上可预测"
  的关键一步，是B方向能否达到方法类核心期刊数学严谨度要求的核心
  缺口，优先级最高。

---

## 4 工程鲁棒性问题与修复（基于实测bug，方法论价值高）

> 本节的核心论点：在线估计类方法在实验室条件下"算法正确"与在真实
> 长期运行系统里"工程健壮"是两回事，本节呈现的三个真实案例分别
> 对应"输入异常处理""可观测性设计""多实现共存"三类通用工程教训。

### 4.1 NaN污染锚点导致永久卡死
- **现象**：θ\*诊断话题从未发布过，两机分段计数长期停留在0。
- **排查过程（体现严谨的故障定位方法论，值得完整呈现）**：独立编写
  诊断脚本复刻同一分段判定逻辑单独订阅原始数据流，25段正常记录、
  证明算法逻辑本身没有问题；但生产环境里长期运行的真实节点实例依然
  卡在0——这一对照直接把嫌疑范围从"算法逻辑错误"收窄到"该实例内部
  状态被污染"。
- **根因**：DLIO/UWB节点启动瞬间可能发出一帧NaN值，若被当作分段锚点
  记录下来，之后每次`math.hypot(...)`计算恒为NaN；Python里
  `NaN < 阈值`表达式恒为`False`（既不真也不抛异常）——距离判断
  条件永远无法满足，且原实现锚点只在"成功记录到新段之后"才更新，
  **不存在任何自我恢复路径**，一旦第一帧被污染就永久卡死直至进程
  重启。
- **修复**：在分段判定函数中加入有限数值校验（`isnan`/`isinf`检查），
  当前锚点或输入观测任一为非有限值时，判定为"当前无可用锚点"并用
  下一帧有限值重新设定锚点——不仅防住启动瞬间的污染，还赋予了
  运行时任意时刻发生的坏数据自动恢复的能力。
- **通用教训**：任何"用连续两次观测计算增量"的在线估计逻辑，都应该
  在增量计算前显式校验两次观测本身的有效性，而不能只依赖"增量结果
  是否合理"做事后判断——本文这个bug里增量结果（NaN）恰好无法用
  常规的阈值比较判断出"不合理"，是这类校验缺失最隐蔽的一种表现
  形式。

### 4.2 可观测性设计缺陷
- 诊断话题设计为"只在凑够$rotation\_min\_segments$段之后才第一次
  发布"，导致运维/调试人员无法区分"系统正常运行、只是还没攒够样本"
  与"系统已经卡死"这两种状态。
- **通用原则（可直接写入论文的方法论建议）**：在线估计类方法的诊断
  接口应当"无论内部状态是否达到可信阈值，都先发布当前进度"（如
  "当前已累积N/W段，估计值为X，是否达到可信阈值：是/否"），而不是
  等达到阈值后才开始发布——这让"未收敛"和"已卡死"在外部观测者眼里
  可区分，是在线估计系统可观测性设计的一条通用准则。

### 4.3 双重实现冲突：权威数据源唯一性原则
- **现象**：$\theta^*$正确估计出44.7°（与机器人45°预设航向角吻合），
  但下游可视化（RViz）显示的相对位姿仍是恒等旋转，未被纠正。
- **根因**：系统内同时存在**两条独立的**跨机对齐实现——本文方法
  （`origin_setter_node`广播的`world -> {ns}/map`）与一个更早期、
  仅做粗略平移假设的兜底实现（`broadcast_map_tf()`，旋转部分写死
  单位四元数，注释显式说明"单基线测距给不出朝向，这里简化为不做
  相对姿态估计"）。两者同时向TF树发布结果，造成"同一子frame存在
  两个不同父frame"的树结构冲突，可视化消费的是未被本文方法覆盖的
  那一条。
- **修复与架构结论**：彻底删除兜底实现，确保跨机坐标对齐只有唯一
  权威数据源。
- **通用教训**：迭代开发过程中遗留的"简化版兜底实现"，即使功能上
  已被更完善的新方法取代，只要没有被显式移除，就可能在特定场景下
  被下游消费者优先选中，是在线估计类方法从原型走向生产的常见陷阱，
  与`docker_sim/论文大纲_容器化机器人软件集成一致性故障分类方法.md`
  里讨论的"通信一致性——权威数据源唯一性"存在概念呼应（第8.3节展开
  两篇论文的关系）。

---

## 5 已知架构局限：持续标定与下游消费不同步

### 5.1 问题描述
- 目标点从操作界面（RViz点击/输入面板）到规划器内部目标的转换是
  **一次性**TF查找与坐标变换：收到目标点消息那一刻查一次当前
  $(R(\theta^*),t)$，变换结果固定发布，此后$\theta^*$的任何后续
  持续修正都不会追溯应用到已发布的目标点上。

### 5.2 定量分析：用预测排除竞争假说（论文里最具说服力的一段实证）
- **背景**：一次双机穿越路径测试中，两机最终停留位置与预期目标点
  存在系统性偏差，最初被误判为"SLAM里程计（DLIO）漂移"。
- **重新论证过程**：用真实预设航向角（30°/-45°）重新计算"如果目标点
  变换完全不做旋转补偿，会落在全局系哪个位置"，与两次独立实测结果
  对照：
  - 机器人1：预测落点(3.60,4.97)，两次独立实测(3.67,4.88)与
    (3.64,4.91)，误差量级0.1m
  - 机器人2：预测落点(6.70,4.95)，两次独立实测(6.84,5.03)与
    (6.75,4.89)，误差量级0.1~0.15m
- **结论**："旋转补偿缺失"这一假说对两次独立实验都给出了精确、可
  重复的定量预测，而"SLAM漂移"假说不具备这种可重复预测能力（漂移
  量本应随时间/路径累积变化，不会恰好两次都落在同一个可由固定角度
  偏差解释的位置）——这是一段完整的"用可证伪的定量预测在竞争假说间
  裁决"的科学论证范例，直接可作为论文的核心案例分析段落，同时也是
  对"复杂系统故障排查中，先入为主的归因（漂移）可能掩盖真实的更
  简单的根因（架构缺口）"这一方法论教训的具体示范。

### 5.3 与硬件可迁移实现的对照
- 同一系统内，跨机坐标对齐的硬件可迁移实现`frame_align_bridge_node`
  采用了**持续周期性重新查询**TF（默认10Hz，与它替代的旧实现发布率
  一致）的设计，天然不受本节问题影响——因为设计者预见到$\theta^*$
  会持续收敛、变换不是一次性静态值。
- **方法论对照价值**：同一个系统里，一个消费者（`frame_align_bridge_
  node`）做对了"持续重新查询"，另一个消费者（目标点变换节点）做错了
  "一次性查询"——这不是算法能力问题，而是**架构设计模式选择**问题，
  值得作为独立小节讨论"在线估计方法的消费者应当默认采用持续查询
  模式，除非有明确理由（如需要固定不变的历史快照）才使用一次性
  查询"这一设计准则。

### 5.4 修复方向与建议实验（写全文前应当实施，把"已知局限"转化为
    "已解决贡献"，性价比高）
- 修复思路：目标点消费端改为持续跟踪目标点在全局系下的"真值"，每次
  $\theta^*$/TF更新都重新变换、重新发布局部目标，直到机器人真正
  到达为止。
- 建议实验：实现该修复后，重复第5.2节的双机穿越路径测试，对比修复
  前后的落点偏差；如果修复有效，第5.2节的定量证据链正好构成"问题
  发现→定量验证→修复→效果验证"的完整闭环，是本文最强的实证贡献
  之一。

---

## 6 与现有方法的比较（**当前最大缺口，需要补实验，见下方标注**）

### 6.1 与批量（离线）最小二乘的比较——**建议补做**
- 批量方法：用全部历史位移段一次性求解$\theta$，理论上稳态精度可能
  与滑窗方法接近甚至更优（样本量更大），但无法跟踪时变的局部-全局
  映射关系变化（如SLAM缓慢累积漂移导致的映射关系微小漂移）。
- **实验设计**：真实的SLAM缓慢漂移特性在短时间仿真中很难自然复现，
  需要人为向局部位移观测注入一个缓慢变化的模拟偏置（如每分钟偏置
  角度线性增加0.5°），对比批量方法与滑窗方法对这一时变偏置的跟踪
  滞后与稳态误差，量化二者的差异——这是能够直接证明"为什么选择在线
  滑窗而不是更简单的批量方法"的关键实验，当前完全缺失，是B方向
  说服力最大的补充项。

### 6.2 与传统UWB多基站三边定位的比较
- 部署成本：三边定位需要预先精确测量并固定至少3个锚点的绝对位置；
  本文方法只需要1个能提供绝对位置观测的移动/固定源（即便测距噪声
  较大也可通过在线滑窗平均降低影响）。
- 精度特性：三边定位精度取决于锚点几何布局（GDOP），本文方法精度
  取决于机器人自身运动路径的位移段数量与长度分布——二者的误差
  特性来源不同，可以做一张定性对比表，如条件允许可做定量对比。

### 6.3 与ICP/点云子图对齐的比较
- ICP依赖两机点云地图有实际重叠区域、对初始猜测敏感、计算成本
  （尤其在机载嵌入式平台上）显著高于本文方法（本文每次更新仅需
  滑窗内的标量代数运算，$O(1)$增量更新）；本文方法完全不依赖点云
  重叠，适用于机器人分散作业、地图无重叠的场景。

### 6.4 与三条上游开源集群规划系统处理方式的对比
- 复用1.2节的对比内容，此处从"方法特性对比"角度重新组织成一张表
  （对比维度：是否需要专门标定动作/是否持续在线更新/硬件前提/
  计算成本），作为方法定位的收尾论证。

---

## 7 实验验证

### 7.1 实验环境与仿真时间换算说明（**重要的方法论陷阱，写作时必须
    提醒读者**）
- 仿真UWB真值发布配置的`update_rate`参数值不等于真实发布频率，需要
  乘以仿真实时因子（RTF）才是真实Hz——实测RTF在双机高负载场景下
  在0.27~0.53之间波动，直接拿配置值当真实频率使用会造成显著误判。
  这一提醒同时是本文实验设置部分的严谨性论证，也是给同领域读者的
  一条通用避坑提示。

### 7.2 收敛精度验证
- $\theta^*$诊断话题实测收敛曲线（段数 vs 估计角度），与3.4节噪声
  传播分析（补充推导后）的理论预测曲线对照。

### 7.3 实验设计陷阱：验证必须使用非零真值场景
- 明确指出并论证：早期验证曾在两机预设航向角均为0的默认场景下
  测试，该场景下真值$\theta$本身就是0，"即使算法实现存在符号错误
  或系数计算错误，测试结果也会看似正常"——这是一条必须写入方法论文
  的实验设计原则：验证旋转对齐类算法必须预先建立非零已知真值场景，
  不能用默认对齐场景测试专门解决"对齐"问题的算法。可对照说明本文
  开发过程中借鉴了mighty原作者自己代码库里`test_frame_alignment.
  launch.py`故意设置45°偏航角的测试用例先例，作为方法论共识的佐证。

### 7.4 参数敏感性分析——**建议补做**
- 扫描滑窗大小$W$与最小位移阈值$d_{min}$的不同取值组合，在仿真
  平台（可获取地面真值）上测量收敛速度与稳态精度的变化，绘制敏感性
  曲线。当前完全缺失，是继6.1节之后第二个建议优先补充的实验。

### 7.5 与批量方法的对比实验——即6.1节实验设计的具体执行与结果呈现

### 7.6 真机侧方法族泛化验证：UWB双标签定位定向融合
- 场景：单机机体航向角测量（与本文主线的"多机地图对齐"是同一
  "UWB驱动的在线坐标/姿态标定"方法家族的另一个应用，用于证明方法
  思路的泛化能力，不是本文的核心贡献，作为附加验证呈现）。
- 方法：双标签几何反正切测向，$yaw=\operatorname{normalize}
  (\operatorname{atan2}(dy,dx)+90°)$，水平位置取双标签中点。
- **误差传播设计范例（可作为3.4节噪声传播推导的一个独立佐证/类比
  案例）**：基线长度现场实测=0.29m，容差参数设计依据——UWB噪声
  $\sigma\approx0.05m$，两站独立测距，组合误差
  $\sqrt2\times0.05\approx0.07m$，容差取一倍多裕量（0.15m）——这是
  一段完整的、可直接引用的误差传播工程推导实例。
- 鲁棒性设计：陈旧数据超时检测（0.3秒无新数据则跳过发布，避免用
  过期数据持续输出看似正常但已失效的估计值）；协议类型鲁棒性（UWB
  标签协议帧类型会在重新上电/被配置工具修改后改变，不能假设一成
  不变，需要运行时自适应识别）。
- **泛化论证**：同一套"低成本UWB驱动的在线姿态/坐标标定"方法论，从
  "多机地图SE(2)对齐"（本文主线）泛化到"单机双标签测向"（本节），
  且都体现了相同的工程纪律（超时检测、协议/输入鲁棒性、参数依据
  可推导而非拍脑袋），是本文方法族价值的旁证。

---

## 8 讨论

### 8.1 方法的适用边界
- 需要至少一个能提供全局绝对位置观测的源（不要求持续高频，静止
  标定阶段需要短暂窗口的稳定观测）；不适用于完全没有绝对定位、仅有
  机器人间相对测距的场景（这类场景需要额外的相对定位/协同SLAM方法，
  超出本文范围）。
- **运动方向多样性/可观测性条件（此前大纲版本遗漏，2026-08-20补充，
  是一条真实存在的数学局限，写作时必须列入）**：若机器人的位移段
  长期集中在单一或近似平行的方向上（如长时间沿直线飞行/行驶），
  旋转角$\theta$在数学上是**不可观测或病态**的——直觉上类似"只用
  一组方向的向量对，无法唯一确定一个二维旋转"，此时$S$、$C$的比值
  对噪声高度敏感，估计方差会显著大于3.4节噪声传播分析给出的理论
  预期。方法要求机器人的运动轨迹在滑窗覆盖的时间范围内包含**至少
  两个方向不共线的位移段**才能把$\theta$可靠约束住。工程上的应对
  措施：①诊断层面监测滑窗内位移段的方向分布/条件数，条件数过大时
  在诊断话题中显式标注"当前估计不可信、需要更多样的运动"；②本文
  真机验证阶段的`auto_calibration_flight.py`起飞后自动执行"前进
  一段+返回"的机动，客观上制造了方向多样性，可以在此处回溯性地
  论证这一自动机动设计恰好满足了可观测性条件，不是巧合（若时间
  允许，可补做一次"仅单方向直线运动"的对照实验，量化不可观测/
  病态条件下估计方差的实际膨胀程度，作为本条局限的实证支撑）。
  **文献佐证（2026-08-20文献调研补充）**：这一"运动多样性影响可
  观测性"的现象并非本文孤立发现，与近期IEEE T-RO文献
  （Multi-Robot Relative Pose Estimation in SE(2) With Observability
  Analysis, 2024, arXiv:2401.15313）在机间相对位姿估计问题中给出
  的可观测性条件（"两机器人均需非零线速度"）在方法论精神上高度
  呼应——虽然两篇处理的是不同的估计问题（该文是机间相对位姿，本文
  是局部-全局坐标系对齐），但"运动状态的充分激励/多样性决定了
  旋转类参数是否可观测"这一底层原理相通。写作时可以引用这一呼应
  关系，一方面增强本条局限论证的学术根基（不是自说自话），另一方面
  也从侧面印证了1.2.2节"这是不同问题、非重复"的判断——两篇论文
  在不同问题上独立得出结构相似的可观测性结论，恰恰说明这是两个
  真实不同、但有共同数学根源的问题。

### 8.2 消费端同步问题的一般性
- 呼应第5节，提炼为一般性论断："任何在线估计方法的实用价值，不仅
  取决于估计本身的收敛性，还取决于其全部下游消费者是否被设计为
  持续查询最新估计值——这是在线估计类方法工程化时经常被忽视的一半"。

### 8.3 与方向A（一致性框架）的关系
- 本文方法是`论文大纲_容器化机器人软件集成一致性故障分类方法.md`
  框架中"空间一致性"维度下"多机局部-全局坐标系对齐"这一具体问题的
  深化研究；第4.3节"权威数据源唯一性"与第5节"持续查询vs一次性查询"
  两条工程教训，也可视为方向A"通信一致性"（消费者-生产者契约）维度
  在具体方法层面的例证。若两篇独立投稿，此处应明确注明互相引用关系，
  避免被审稿人质疑内容重叠而不清楚归属。

---

## 9 结论与展望

- **先回扣1.3节的领域价值判断**：本文的核心贡献不是"提出新算法"，
  而是"填补了'局部-全局坐标系在线对齐'这一被现有文献系统性忽视的
  中间地带"（1.3.2节结论），辅以对经典Horn/Umeyama理论的在线化增量
  扩展（1.3.3节）与一组可迁移的工程鲁棒性教训（1.3.4节）。这一表述
  应该是结论开篇第一句话，而不是先罗列技术细节再补一句"意义"——
  审稿人对"这篇论文到底贡献了什么"的第一印象由此形成。
- 技术贡献总结：数学贡献（Wahba/Horn-Umeyama 2D退化闭式解及两种
  方法交叉验证）、工程鲁棒性贡献（NaN修复、可观测性设计原则、权威
  数据源唯一性）、架构局限的定量发现与修复（一次性变换问题，第5节
  "用预测排除竞争假说"论证）、方法族泛化验证（UWB双标签测向）四方面。
- **诚实边界**（复用1.3.5节，结论部分必须再次明确，不能只在引言
  提一次就不再出现）：不解决多机器人定位问题本身，不是姿态估计
  新理论，只解决"已有局部里程计+已有外部绝对观测源，如何在线对齐"
  这一具体子问题。
- 局限：当前对比实验（第6节）除定性论证外缺乏定量数据，需要在成稿
  前完成7.4/7.5节标注的补充实验；噪声传播分析（3.4节）需要补充正式
  推导（含误差传播/Delta method的完整数学推导，见写作前核实清单）。
- 展望：向三维（SE(3)，含roll/pitch的完整姿态而不仅是yaw）场景的
  推广；与方向A框架的联合应用（作为其空间一致性维度诊断清单里的
  标准化处理方案）；可观测性条件（8.1节）与IEEE T-RO 2024论文呼应
  关系的进一步理论化，或是否值得作为独立的后续工作展开。

---

## 写作前必须核实/补充的事项清单

1. **`rotation_min_segments`参数取值矛盾**——`DEBUG_JOURNAL.md`记为
   5段，`README.md`当前版本记为1段（"第一段就发布"），两处不一致，
   写论文前必须对照当前代码确认真实生效值，不能凭旧记录直接引用。
2. **UWB噪声标准差$\sigma\approx0.05m$的独立出处未定位到**——本次
   挖掘只确认这是此前已提炼的结论，未找到对应的原始行号或配置文件
   默认值，成稿前需要反查`src/uwb_sim`里`range_noise_std`参数的
   实际配置值做交叉确认，不能直接沿用未经验证的数值。
3. **`多机空间坐标对齐问题_设计讨论纪要.docx`内嵌的架构图（图1/图2/
   图3）本次仅提取了文本，未提取嵌入图片**——如果全文需要复用这几张
   图的设计思路作为插图基础，需要另外解压docx（docx本质是zip包）
   提取`word/media/`目录下的图片文件。
4. **第6节全部对比实验（批量最小二乘、UWB三边定位、ICP）、第7.4节
   参数敏感性分析目前均为"建议补做"，尚未执行**——是本方向当前
   离"方法类核心期刊要求的定量对比"最大的缺口，优先级：6.1节批量
   对比 > 7.4节参数敏感性 > 6.2/6.3节与传统方法的定量对比（后两者
   如果时间有限，可先做定性对比表，定量对比作为后续工作）。
5. **第3.4节噪声传播的正式数学推导尚未完成**，当前只有数值链条，
   是数学严谨度层面的核心缺口，优先级最高（不需要新实验，纯推导
   写作工作量）。
6. **第5.4节修复实验尚未实施**——如果时间允许，强烈建议做，能把
   一个"已知局限"转化为完整的"发现问题→验证→解决"闭环贡献。
7. **文献调研已做三轮WebSearch/WebFetch排查（详见附录A），覆盖较充分，
   但机构级CNKI/万方复核仍需在定稿1.2节相关工作前完成——不是"投稿前"
   才做，那样排序错误（写完整篇初稿后才发现撞题，等于白写）**。三轮
   检索累计核实近10篇/项最相邻候选（含标题风险评估最高的IEEE T-RO
   2024论文、专利CN112833876B等），均确认问题结构不同（核心判据："机间
   相对定位"vs"局部-全局坐标系对齐"两类问题的边界清晰、本文属于后者，
   目前查到的候选全部属于前者）。**正确的执行顺序**：①现在（大纲
   定稿、正文动笔前）——由有知网/万方机构账号权限的人员按附录A.1
   关键词清单做一轮正式检索，重点复核附录A表格中标记"风险已排除"的
   几项、以及是否有本次免费渠道覆盖不到的文献；②确认无撞题后再定稿
   1.2节相关工作的具体文字；③方法推导（第2-3节）、鲁棒性案例（第4节）、
   实验设计（第7节）等不依赖这一步的章节可以与①②并行撰写，不需要
   等复核结果才能动笔。同时需要补查Horn（1987）/Umeyama（1991）的
   确切文献信息（期刊/卷期/DOI）以及arXiv:2510.05992是否已有正式
   发表版本，1.2.1节已把Horn/Umeyama提到相关工作最前面，引用信息
   必须准确。
8. **运动方向可观测性条件（8.1节新增）目前只有理论论证，没有实测/
   仿真数据支撑**——建议补一次"仅单方向直线运动"场景下的对照仿真
   实验，量化病态条件下$\theta$估计方差相对3.4节理论预期的膨胀程度，
   优先级与7.4节参数敏感性分析相当，可以合并成同一轮补充实验（都是
   在现有仿真平台上改变机器人运动轨迹模式、复用已有的地面真值评估
   流程，不需要新增实验设施）。
9. **1.1.1节"配对关系需要在线增量构造"这一与Horn/Umeyama的差异化
   表述，需要在写正文时进一步核实是否真的是"该问题结构未有专门文献
   处理"**——本次WebSearch调研没有专门针对"在线增量轨迹对齐"
   （incremental/online trajectory alignment，区别于批量Horn/Umeyama）
   这一更精确的关键词组合做检索，成稿前建议补一轮定向检索确认这一
   差异化论证站得住，不要凭已有检索结果外推下结论。

---

（大纲完，预计正文5000-6500字，图表建议：①SE(2)标定流程示意图；
②$\theta^*$收敛曲线；③NaN卡死问题的状态机/时序示意图；④第5.2节
双机穿越测试的预测vs实测落点对照图；⑤参数敏感性曲线（补做后）；
⑥与批量方法的漂移跟踪对比曲线（补做后）。）

---

## 附录A：外部文献调研存档（2026-08-20，供撰写1.2相关工作时核对引用）

| 文献 | 关键信息 | 与本文关系 |
|---|---|---|
| Coordinate-Consistent Localization via Continuous-Time Calibration and Fusion of UWB and SLAM Observations, arXiv:2510.05992 (2025-10) | 单机器人跨多次运行的坐标一致性；批量连续时间优化标定锚点3D位置+滑窗融合；多固定锚点UWB配置 | **最相邻，必须正面引用并逐条区分**（单机跨运行 vs 本文多机；批量 vs 本文在线闭式；多锚点 vs 本文单一绝对位置源），见正文1.2.2节 |
| Autocalibration of a Mobile UWB Localization System for Ad-Hoc Multi-Robot Deployments in GNSS-Denied Environments, arXiv:2004.06762 (2020) | 在线自动标定UWB锚点自身位置，支持移动锚点即插即用部署 | 问题层面不同（标定基础设施 vs 本文标定机器人坐标系），作背景动机旁证引用 |
| Aligning Coordinate Frames in Multi-Robot Systems with Relative Sensing Information, Nagavalli et al., IROS 2014 (CMU) | 集中式/分布式算法，依赖机间相对位置测量对齐坐标系朝向 | 概念最像"前辈"，但观测模型不同（机间相对测量+共识 vs 本文单机独立对齐共享外部绝对源），见正文1.2.2节 |
| 基于超宽带阵列与里程计的多机器人相对定位，电子与信息学报，DOI:10.11999/JEIT210812 | 每机多UWB节点阵列+非线性优化+滑窗+粒子滤波，估计机间相对位姿，报告相对误差(0.312m/4.903°) | 中文核心期刊同期相关工作，但完全不处理局部-全局坐标系对齐（纯机间相对定位），问题定义不同 |
| MOTLEE: Collaborative Multi-Object Tracking Using Temporal Consistency for Neighboring Robot Frame Alignment, arXiv:2405.05210 (2024) | 通过共享目标跟踪时间一致性做相邻机器人帧对齐 | 观测模态不同（目标跟踪对应关系 vs 本文原始位置观测），仅作问题域旁证 |
| 基于单UWB融合里程计的多机器人相对定位方法，邓忠元、刘冉、曹志强、肖宇峰，《计算机应用研究》2023年第40卷第3期839-844页，DOI:10.19734/j.issn.1001-3695.2022.07.0381（2026-08-20直接WebFetch核实摘要确认） | 单UWB+里程计，滑窗截取历史机间测距+里程计预测位姿构建非线性最小二乘、EKF估计里程计协方差加权、图优化融合；6m×12m实测相对位置精度0.32m/相对角度精度4.16° | **已直接核实关键区别，风险已排除**：①UWB是**机器人间直接相对测距**，不是与固定外部参考的绝对定位；②论文明确"不涉及统一到全局坐标系"，是纯粹的机间相对位姿估计；③批量滑窗优化，非在线闭式递归。与本文"单一绝对位置源+局部-全局SE(2)对齐"问题结构不同，但选题、期刊定位（计算机应用研究，中文核心）高度参考价值，可作为"国内同期研究活跃、且本文问题定义与之互补"的直接证据引用 |
| 专利CN112833876B《一种融合里程计与uwb的多机器人协作定位方法》，西南科技大学，刘冉/陈凯翔/肖宇峰等，2020年申请/2022年授权（2026-08-20WebFetch核实权利要求） | 机器人间直接测距+Levenberg-Marquardt批量非线性优化求相对位姿（x,y,θ），未明确处理全局坐标系绝对对齐 | 与上一条《计算机应用研究》2023年论文同一团队（西南科技大学刘冉/肖宇峰组），检索该团队2024/2025年后续成果未发现新论文，该研究线目前仍停留在"机间相对定位"问题，未转向本文的"局部-全局对齐"问题，风险已排除，可作为"同一团队持续深耕相邻但不同问题"的旁证 |
| Multi-Robot Relative Pose Estimation in SE(2) With Observability Analysis: A Comparison of Extended Kalman Filtering and Robust Pose Graph Optimization, IEEE T-RO 2024, arXiv:2401.15313（2026-08-20WebFetch核实摘要，**标题风险评估最高的一篇，含"可观测性分析"字样与本文8.1节新增内容高度相似**） | 机器人间range-only/bearing-only观测+里程计共享，EKF/PGO两种方法比较，可观测性条件是"两机器人均需非零线速度" | **已核实关键区别，风险已排除**：估计的是**机器人间相对位姿**（非各机器人到共享全局参考系的变换），观测量是机间测距/方位角（非外部绝对位置源），无闭式解（EKF/PGO数值方法）。**但其可观测性条件（运动线速度非零）与本文8.1节"运动方向多样性"条件在方法论精神上高度呼应**——建议在讨论部分正面引用这一呼应关系，作为"可观测性约束是这类在线估计问题的共性、不是本文孤立发现"的佐证，增强8.1节论证的学术根基 |
| Online Multi-Sensor Calibration Method for Unmanned Surface Vehicle Swarms in Complex and Contested Environments, Drones (MDPI), 2026, DOI:10.3390/drones10030161（2026-08-20核实摘要） | 水面无人艇集群，紧耦合因子图+滑窗，融合雷达/光电/IMU/GNSS做**单车传感器安装外参**在线标定，用集群互相观测约束漂移 | 问题不同（单车多传感器外参标定 vs 本文多机局部-全局里程计对齐），风险低，仅作"UWB/多传感器+集群+滑窗"这一方法论社区活跃度的旁证 |
| 谷歌学术"多机器人 局部坐标系 全局坐标系 在线标定"检索命中的工业机器人基坐标系标定类文献（如《双机器人基坐标系标定方法的研究》2021、《基于多传感器协同的智能装配系统标定方法研究》2025等） | 工业机械臂（固定基座）多机器人基坐标系标定，应用场景是产线协同装配 | 领域不同（固定基座工业机械臂 vs 本文移动机器人/无人机在线里程计对齐），核心区别是工业机械臂场景通常有精确的机械结构约束可用、不存在"里程计"这一环节，风险低，不需要在正文中专门讨论区别，仅记录排除过程 |

**检索结论（2026-08-20，共三轮检索：初筛+针对性补充+百度学术/谷歌学术
延伸追查）**：未发现"单一绝对位置源+滑窗最小二乘在线SE(2)递归闭式解
+多机器人局部-全局坐标系对齐"这一完整组合的现成文献。三轮检索累计
直接核实了近10篇/项最相邻候选的完整摘要或权利要求（含期刊论文、
专利、arXiv预印本、IEEE T-RO正刊论文），标题上"看起来最像"的几篇
（如IEEE T-RO 2024那篇，标题同时出现"SE(2)"和"observability
analysis"）逐字核实后均确认是**机间相对定位**问题（观测机器人之间
的相对测距/方位角），与本文**独立对齐到共享外部绝对参考系**的问题
结构不同——这一区分（"机间相对定位" vs "局部-全局坐标系对齐"）
贯穿了几乎所有排除案例，值得在1.2节相关工作的开篇就明确点出这一
分类界限，帮助读者快速理解本文问题定位在这个研究谱系里的准确位置。
1.2.2节几篇高相邻文献（尤其arXiv:2510.05992）仍必须在正式引言/
相关工作中正面处理，不能回避。

**检索方法与残余风险（如实告知，不夸大也不低估）**：本次调研通过
WebSearch/WebFetch，覆盖了通用网络搜索、arXiv、IEEE Xplore摘要页、
Google Patents、Google Scholar、百度学术、知网/万方/维普可公开访问
的标题列表页，**已经是免费公开渠道能做到的较充分程度**，用户已确认
"百度学术/谷歌学术查一查应该就能基本排除"这一判断得到了验证——三轮
交叉检索均未发现直接撞题的文献。**残余风险**：CNKI/万方部分文献的
完整摘要需要机构登录才能看到（本次遇到多次403/空内容），存在检索
盲区的可能性依然存在，但经过如此密集的关联词/引证追溯/研究团队
追踪之后，残余风险已明显低于最初评估。**排序建议（回应"投稿前才查
等于白写"这一合理顾虑）**：不需要等到"投稿前"才做机构级复核，而是
**在正式撰写1.2节相关工作之前**（即现在，大纲阶段与写作阶段之间）
安排一次机构级复核最合适——其它章节（方法推导、鲁棒性案例、实验
设计）不依赖这一步，可以并行推进，不需要整体阻塞；只有1.2节的最终
文字需要等复核结果定稿。

### 附录A.1：知网/万方正式查重检索关键词清单（2026-08-20整理，
待有机构账号权限的人员执行）

检索范围建议勾选"篇关摘"（标题+关键词+摘要），不要只勾"关键词"——
作者用词差异大，只查关键词字段容易漏掉措辞不同但问题相同的文献。
时间范围建议近5-8年；学科类别建议限定自动化技术/自动控制、无线电
电子学、计算机软件及计算机应用；文献类型建议期刊论文+学位论文都查
（很多同类工作会先以硕博学位论文形式出现）。

**A组：核心问题词（先查这组，看有没有直接撞题）**
- `多机器人 坐标系 标定`
- `多机器人 坐标系 对齐`
- `无人机集群 坐标系 标定`
- `无人机集群 坐标系 对齐`
- `局部坐标系 全局坐标系 标定`
- `局部坐标系 全局坐标系 在线`
- `地图坐标系 对齐 多机器人`
- `多机 建图坐标系 统一`

**B组：方法特征词（缩小范围，专查有没有完全一样的算法）**
- `滑动窗口 最小二乘 旋转角 估计`
- `在线标定 旋转 平移 最小二乘`
- `SE(2) 在线标定`
- `SE(2) 刚体变换 估计`
- `偏航角 在线估计 最小二乘 多机器人`

**C组：传感器/应用场景组合（查有没有完全一样的具体实现）**
- `UWB 里程计 坐标系 对齐`
- `UWB SLAM 坐标 一致`
- `UWB 单基站 多机器人 定位`
- `无人机 UWB 起飞点 标定`
- `无人机集群 UWB 世界坐标系`

**D组：顺藤摸瓜（比关键词穷举更容易找到隐藏在不同措辞下的同类工作）**
- 直接搜篇名`基于单UWB融合里程计的多机器人相对定位方法`（邓忠元等，
  《计算机应用研究》2023），打开后查看"被引用"与"参考文献"两个列表
- 直接搜篇名`基于超宽带阵列与里程计的多机器人相对定位`（电子与信息
  学报），同样查看引证关系

**E组：英文关键词（知网/万方均有外文文献库，防止漏掉中文期刊用英文
关键词索引、但中文全文检索词不完全对应的文章）**
- `multi-robot coordinate frame alignment online`
- `SE(2) online calibration sliding window`
- `local global frame alignment UWB odometry`

**查证结果记录表（待补，查完后把每组的检索结果概况填进来，作为
投稿前"已完成正式查重"的存档证据）**

| 组别 | 检索平台 | 命中相关文献数 | 是否发现高风险撞题 | 备注 |
|---|---|---|---|---|
| A组 | 知网 | 待填 | 待填 | |
| A组 | 万方 | 待填 | 待填 | |
| B组 | 知网 | 待填 | 待填 | |
| B组 | 万方 | 待填 | 待填 | |
| C组 | 知网 | 待填 | 待填 | |
| C组 | 万方 | 待填 | 待填 | |
| D组 | 知网 | 待填 | 待填 | |
| E组 | 知网/万方 | 待填 | 待填 | |

## 附录B：原始素材来源存档（供撰写正文时逐条溯源核对）

| 内容 | 来源文件 | 位置 |
|---|---|---|
| 完整数学推导（代价函数展开、闭式解、Wahba问题关系、复数域交叉验证） | `docker_sim/多机空间坐标对齐问题_设计讨论纪要.docx` | 第四节（**最高优先级素材，方法章节直接以此为骨架改写**） |
| 三条上游开源算法（mighty/ros2_px4_stack/ego-planner-swarm）处理跨机坐标对齐方式的调研对比 | 同上 | 第二节 |
| 情景一（UWB+IMU替代SLAM）设计与EKF2复用决策 | 同上 | 第三节 |
| 验证方法论陷阱（spawn yaw=0致假阳性）+ mighty上游`test_frame_alignment.launch.py`45°测试用例先例 | 同上 | 第七节 |
| 平移锚点公式、θ\*更新后重算平移逻辑 | `docker_sim/DEBUG_JOURNAL.md` | 约11008-11026行（"情景二实现：origin_setter升级成在线SE(2)旋转估计"条目） |
| 目标点SE(2)正/逆变换公式实现 | `docker_sim/DEBUG_JOURNAL.md` | 约11414-11421行 |
| 全部参数取值与依据（`rotation_seg_min_disp_m`/`rotation_window_size`/`rotation_min_segments`等） | `docker_sim/DEBUG_JOURNAL.md` | 约11008-11019行；另见`docker_sim/README.md`"起飞点一键锁定"章节（约373-452行，**与前者`rotation_min_segments`取值不一致，见下方核实事项**） |
| UWB样本可信判据（≥5样本、标准差≤0.15m、2秒窗口） | `docker_sim/DEBUG_JOURNAL.md` | 约10190行（`set_origin_from_uwb`服务实现） |
| NaN污染锚点永久卡死问题：现象/独立诊断脚本复刻/根因/修复 | `docker_sim/DEBUG_JOURNAL.md` | 约11055-11104行 |
| 44.7°标定正确但可视化未纠正：双重实现冲突问题 | `docker_sim/DEBUG_JOURNAL.md` | 约11235-11267行 |
| 权威数据源唯一性修复（彻底删除`broadcast_map_tf()`兜底实现） | `docker_sim/DEBUG_JOURNAL.md` | 约11269-11307行 |
| 一次性目标点变换架构缺口：完整定量论证过程（预测vs两次独立实测对照） | `docker_sim/DEBUG_JOURNAL.md` | 约14628-14680行（"更正上一条'DLIO漂移2.6米'的结论，找到真正的架构性根因"条目） |
| `frame_align_bridge_node`硬件可迁移实现技术细节 | `docker_sim/DEBUG_JOURNAL.md` | 约11167-11233行 |
| UWB采样率仿真配置值与真实Hz换算陷阱（RTF 0.27~0.53波动） | `docker_sim/DEBUG_JOURNAL.md` | 约12946-13098行（`update_rate`调整/回退相关条目） |
| UWB双标签定位定向融合模块（`dual_tag_fusion_node.py`）完整实现 | `read_hw.md` | 约14-46行（2026-08-18条目） |
| 关键话题帧率验证+测距仪话题路径排查等真机侧配套发现 | `read_hw.md` | 约48-75行 |

**注**：上表位置为后台证据挖掘时的大致定位，成稿前务必逐条对照原文
件复核，尤其是"写作前必须核实/补充的事项清单"中标注的两处数值矛盾
（`rotation_min_segments`取值、UWB噪声标准差出处）。

（附录完）
