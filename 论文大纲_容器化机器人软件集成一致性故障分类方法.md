# 论文大纲：容器化机器人软件系统集成故障的一致性分类方法（方向A）

> 本大纲对应`论文选题方向建议.md`里的方向A，是把
> `论文大纲_双机协同飞行仿真平台.md`第3节（三重/四重一致性框架）
> 抽离出来、去场景化、独立成篇的版本。双机无人机仿真+真机平台从"论文
> 主体"降级为"案例验证载体之一"，核心贡献是框架本身。素材来源同前，
> 另需补充第2节相关工作里的经典分布式系统故障分类文献（写作时检索，
> 本大纲不臆造引用）。

---

## 标题（候选，按风格分组）

**偏理论/框架导向**（投方法类栏目优先选）：
1. 容器化机器人软件系统集成故障的一致性分类框架
2. 分布式机器人软件系统容器化集成中的隐式契约破坏与一致性故障分类
3. 面向容器化分布式机器人软件集成的一致性故障分类与诊断方法

**偏工程实践导向**（投工程应用类栏目优先选）：
4. 一种容器化多机器人系统集成故障的一致性分类方法及诊断实践
5. 异构机器人软件栈容器化集成中的时空-通信-并发一致性故障研究

**偏具体技术栈**（若目标期刊希望标题体现ROS2/DDS等具体技术栈以提高检索命中率）：
6. 面向ROS2容器化部署的机器人软件集成故障分类框架：一致性视角

**建议**：候选1最简洁、覆盖面最广，作为默认首选；候选3把"诊断方法"
一并点出，更贴合"不仅分类、还给方法"的实际内容，若期刊偏好"方法"类
标题可换这个；候选6适合目标期刊/审稿人明确以ROS2机器人系统为受众的
情况，用具体技术名词提高被检索到的概率。定稿前建议按目标期刊过往
录用标题的用词风格再筛一次。

---

## 摘要要点

- **背景**：容器化（Docker/Kubernetes等）已成为部署异构机器人软件栈
  （SLAM、规划、控制、仿真器等，通常来自不同团队、不同版本、独立发布）
  的默认方式，尤其在多机器人/多智能体系统中——每个智能体一份容器实例
  是常见架构模式。但容器化把这些软件模块从"单进程"环境搬进"多容器、
  多主机"的分布式现实，产生的集成故障目前主要依赖工程师个体经验
  排查，缺少系统性的分类与诊断方法。
- **方法**：基于对一个实际的容器化多机器人系统（双机协同无人机仿真
  平台，后延伸至真实Jetson硬件平台）在约两周高强度集成开发期间的
  全部技术记录做归纳分析，提出容器化机器人软件集成故障的"一致性"
  分类框架：识别出四类被容器化/分布式部署打破的隐式契约——时间一致性、
  空间一致性、通信一致性、并发一致性——每类给出形式化定义、可判定的
  失效特征与通用诊断检查清单，并讨论该框架与经典分布式系统故障模型
  （崩溃/遗漏/时序/拜占庭故障）的对应关系。
- **验证**：在案例系统中识别出的全部集成故障均可被四类框架无遗漏
  分类（穷尽性验证），并给出至少一个跨环境（仿真→真实硬件）迁移验证
  的实例，证明框架不依赖具体仿真工具链；讨论框架用于故障预判/加速
  排障的实际效果。
- **意义**：为容器化机器人软件系统的集成测试与故障预判提供了不依赖
  具体机器人应用、可迁移到其他多智能体/多传感器容器化系统的方法论
  工具。

---

## 1 引言

### 1.1 研究背景
- 容器化在机器人系统中的应用现状：模块化部署、CI/CD、跨硬件架构
  复现（写作时补充文献，如ROS2官方容器化实践、云机器人/群体机器人
  部署案例）。
- 问题：容器化解决了"依赖冲突/环境不一致"这个表层问题，但引入了一层
  新的、往往是**静默**的集成故障——不报错、只是数据不通或数值错误，
  比传统的编译/运行时错误更难定位。
- 现状缺口：现有资料（官方文档、社区经验帖、厂商最佳实践）多是"点状"
  的具体坑（如"记得加`ipc: host`"），缺少能覆盖整个问题空间、指导
  "遇到新问题时该往哪个方向查"的系统性分类。

### 1.2 相关工作
- **经典分布式系统故障模型**：崩溃故障（crash fault）、遗漏故障
  （omission fault）、时序故障（timing fault）、拜占庭故障（Byzantine
  fault）等经典分类（Cristian等对故障模型的分类工作，写作时补充确切
  引用）——本文框架与这套经典理论的关系需要在此处讲清楚：不是重新
  发明分布式故障理论，而是**把经典理论应用到"容器化+机器人软件栈"
  这个特定语境下、结合该语境特有的隐式假设（namespace唯一性、传感器
  外参、发布线程模型等）做出的领域化细化**，这是本文与经典理论的
  差异化定位，务必写清楚避免被审稿人质疑"新瓶装旧酒"。
- **机器人中间件与容器化部署实践**：ROS2 DDS通信机制、Docker/K8s在
  机器人系统中的应用案例（XTDrone、云端仿真平台等，写作时补充引用）。
- **软件缺陷/故障分类学**：通用软件工程领域的缺陷分类标准（如IEEE
  Std 1044、ODC正交缺陷分类）——本文的"一致性维度"可以类比为面向
  "容器化机器人系统集成"这一细分领域的一套领域特定缺陷分类法，写作
  时可以引用ODC等方法论作为"分类学构建方法"的方法论依据。

### 1.3 本文贡献
1. 提出容器化机器人软件集成故障的**四维一致性分类框架**（时间/空间/
   通信/并发），并论证其与经典分布式故障模型的对应关系与领域特化点。
2. 给出每一维度的**形式化定义、失效判据与通用诊断检查清单**，具备
   跨具体技术栈（不限ROS2/Docker）的可迁移性。
3. 提出支撑该框架长期演进、可持续积累案例的**工程范式**（patch化
   构建体系），讨论其对框架"从个案到分类学"这一归纳过程的支撑作用。
4. 用一个实际运行、跨越仿真与真实硬件两个环境、开发周期内累计记录
   40例集成类故障的容器化双机协同系统作为案例验证，证明框架的穷尽性
   （全部案例均可归入四维框架，仅1例未定论如实标注）；并通过完整的
   复发时间线证据（同一失效模式在仿真、真机两个环境跨越约11天、
   经三次不同代码路径复发），论证"点状修复无法根除类别性风险，需要
   系统性审查"这一更精确的方法论结论，取代"仿真结构性无法触发"这一
   过强论断。

### 1.4 论文组织结构

---

## 2 问题背景与研究方法

### 2.1 案例系统概述（简化描述，细节让位于第6节）
- 一句话交代案例系统是什么（容器化双机协同无人机仿真+真机平台），
  说明选它作为归纳基础的原因：跨越仿真/真实硬件两种环境、集成六个
  独立第三方软件模块、开发过程有完整时间序列的技术记录可供归纳分析
  ——这三点是选择该案例作为"素材库"而非"应用背景"的方法论依据，要
  在此处讲清楚，呼应"这不是一篇无人机论文"的定位。

### 2.2 研究方法：基于案例的归纳分类法
- 说明分类框架的构建方法：对案例系统开发过程中记录的全部集成类故障
  （区别于纯算法/业务逻辑bug，只统计"模块间协作失败"这一类）做
  开放编码（open coding）式归纳，识别每个故障"被打破的隐式假设"，
  归并出稳定重复出现的几类模式，即本文四个一致性维度——这是一种
  质性研究方法（类似软件工程里的grounded theory/根植理论方法），
  写作时应明确交代方法本身，提升方法论严谨性、便于审稿人评估分类法
  的构建过程是否可信，而不是"作者拍脑袋分的类"。
- 统计信息（写作前需要实际统计，不臆造）：案例系统开发周期内记录的
  集成类故障总数、四个维度各自的故障数量分布，可以做成一张统计表，
  作为分类法"确实覆盖了实际遇到的故障"这一穷尽性论证的数据支撑。

---

## 3 一致性故障分类框架

> 本节是全文核心，写法上比`论文大纲_双机协同飞行仿真平台.md`3.0节
> 更强调"形式化定义"与"与经典理论的映射"，具体案例大幅压缩篇幅
   （每个只保留1-2句话的高度概括，详细描述移到第6节）。

### 3.1 核心命题与形式化定义
- 复用已有的核心命题（隐式单进程假设→分布式部署→契约破坏），补充
  形式化表述：定义一个软件模块$M$在单体环境下依赖的隐式契约集合
  $C(M)=\{c_1,...,c_n\}$，容器化部署将$M$迁移到分布式环境
  $E'$，当且仅当$\exists c_i \in C(M)$在$E'$中不再天然成立且未被
  显式重建时，产生集成故障——这个形式化定义本身不需要复杂数学，但
  以符号语言呈现能显著提升论文的方法论严谨度观感，是"文章升格"的
  低成本高收益动作。

### 3.2 四维分类表与经典故障模型映射
- 呈现`论文大纲_双机协同飞行仿真平台.md`3.0节的表格，新增一列
  "对应经典分布式故障模型"：

| 一致性维度 | 隐式假设 | 打破方式 | 失效特征 | 对应经典故障模型 |
|---|---|---|---|---|
| 时间一致性 | 共享时钟源、时间戳信息完整 | 时钟源分裂、消息转换丢失时间信息、端到端延迟超限 | 卡死或缓慢发散，不崩溃 | 时序故障（timing fault） |
| 空间一致性 | 唯一坐标系/唯一身份 | 命名空间未支持、外参未建模、多坐标系需显式对齐 | 崩溃或"收敛到错误但稳定的解" | 可归入更广义的"状态一致性故障"，经典模型中较少专门讨论此类身份/坐标语义冲突，是本文领域特化的部分 |
| 通信一致性 | 发布必达 | QoS策略不匹配、传输通道不可用、环境未初始化 | 双方"已发现"但静默丢包，无报错 | 遗漏故障（omission fault） |
| 并发一致性 | 单一执行路径访问共享状态 | 异步发布模式下的detached线程无保护写共享结构 | 间歇性、负载相关的内存/状态损坏，动态检测工具可能反而观测不到 | 竞态条件/同步故障，经典并发理论中已有研究，本文贡献在于指出其在"发布-订阅式异步发布模式"下的特殊触发条件 | 

- 讨论"空间一致性"是四类中与经典分布式故障模型映射最弱、领域特化
  程度最高的一类，值得单独一段展开论证——这恰恰是本文相对经典理论
  最有增量价值的部分，建议写作时着重笔墨。

### 3.3 各维度详细展开
- 复用`论文大纲_双机协同飞行仿真平台.md`3.1~3.3节的内容结构（判据+
  实例），但把每个实例压缩到1-2句话（如"时钟源分裂导致的进程级
  死锁：见第6.2节案例"），详细技术细节全部移到第6节案例验证部分，
  避免第3节被具体案例喧宾夺主、失去"通用框架"的文风。
- **3.3.4 并发一致性单独成节，核心论点已根据完整证据挖掘结果修正**
  （见下方"证据修订说明"）——不是"仿真从未触发"，而是"同一类故障
  因点状修复、缺乏系统性审查而反复复发"，这是比最初版本更准确、也
  更有力的论点，为第7节讨论埋伏笔。
- **并发一致性的两个子类**（据完整案例挖掘新增的细化，写作时应在
  3.3.4节明确拆开）：
  - **A类：共享状态竞态**——多个执行路径（通常是detached线程）无
    同步地并发读写同一共享数据结构，典型修法是互斥锁/原子操作。
  - **B类：阻塞调用致执行器饥饿**——单线程executor模型下，某个回调
    内部发生阻塞式调用（如同步`input()`、`lookup_transform`在数据
    未就绪时的阻塞等待），导致整个事件循环冻结、其余回调全部无法
    执行，表现为"系统看似挂起但没有任何一处发生数据竞争"。典型修法
    是换用多线程executor、把阻塞调用移出回调、或改为非阻塞轮询。
  - 两个子类根因不同、修复手段不同，但共享同一个上位判据："单线程/
    单一执行路径"是原始隐式假设，多线程/异步回调把它打破——因此仍
    归入同一个"并发一致性"维度，只是需要在诊断决策流程（3.4节）里
    分两条支路处理。

### 3.3.5 证据修订说明（重要，写作时须体现）
- **最初版本的论点**（写在核心论点/摘要草稿中）是"并发一致性问题
  仅在真机时序特征下触发、仿真从未复现"。**完整案例挖掘推翻了这个
  强论断**：仿真阶段（约2026-08-08）已经记录过一次独立的并发案例
  ——DLIO的`debug()`detached线程直接调用`this->gicp`的非线程安全
  方法，与主线程发生数据竞争导致段错误（`DEBUG_JOURNAL.md:905-918`，
  详见6.5节）。真机阶段的`publishToROS()`/`publishKeyframe()`线程
  竞态是**同一子系统（DLIO）、同一失效模式（detached线程无保护访问
  共享状态）在不同代码路径上的第二次复发**；而`publish_ros_mutex_`
  修复本身又引出了第三次变种（detached线程排队积压致线程数暴涨到
  85个，改用`try_lock`丢帧解决）。
- **修正后的核心论点**（应据此改写摘要/1.1核心命题/7.3节）：并发
  一致性故障不是"仿真环境结构性无法触发"，而是**"点状修复掩盖了
  类别性风险"**——第一次出现时（仿真阶段）只修了`debug()`这一个
  具体线程，没有系统性审查代码库里其它同样采用"每次数据到达就
  detach一个新线程"模式的调用点；真机阶段在完全不同的代码路径
  （`publishToROS`/`publishKeyframe`）上复发，且真机阶段的负载/
  时序抖动特征（`LIDAR_SCAN_FREQ`从10Hz提到20Hz后帧间隔砍半）
  客观上提高了竞态窗口重叠概率、加速了复发速度——这是一个更细致、
  也更可辩护的论点：**仿真验证不是完全没用，而是"点状验证"天然
  无法覆盖"类别性"风险，除非配合系统性代码审查**。截至真机验证
  结束，`metrics_thread`/`debug_thread`两个同样采用detached模式的
  线程仍未被系统审查过（`read_hw.md`待办事项第12条），即该失效
  模式**可能仍未被根除**，这一"未完成的审查"本身也是论文应如实
  报告的局限（呼应第8节）。

### 3.4 诊断决策流程
- 新增内容（`论文大纲_双机协同飞行仿真平台.md`里没有的部分）：
  把四个维度的判据整理成一张**诊断决策流程图**——给定一个"模块间
  协作失败但无报错日志"的症状，按什么顺序排查（建议顺序：先查通信
  一致性——最容易用工具确认（QoS/发现状态可查）；再查空间一致性——
  次容易确认（namespace/frame_id可枚举检查）；再查时间一致性——
  需要主动构造时间戳追踪；最后查并发一致性——最难，需要静态代码
  审查+负载相关性测试）。这张流程图是本文除分类表外第二个具体的
  方法论产出，建议画成正式图表，是审稿人评估"这篇论文提供的是否是
  可操作的方法而不只是事后归纳的标签"的关键证据。

---

## 4 支撑框架演进的工程范式（precise对应原大纲3.4节，压缩篇幅）
- patch化构建体系（三层代码组织+diff叠加构建）作为"让归纳出的每个
  故障分类都有可追溯、可复现的具体证据"的工程基础设施，一段话交代
  即可，不需要展开成独立大节（这部分在方向D里是重头戏，在方向A里
  只是辅助论证"框架的构建过程有真实工程记录支撑，不是凭空归纳"）。
- 提及一个真实的框架自我验证的反例证据：真机开发中发现的
  "staging/live_src/镜像三者代码不一致"（11处遗漏差异）——这恰好是
  patch化范式本身失效的一个实例（工程纪律被违反导致的一致性问题），
  可以作为"框架具有自反性——工程范式本身也可能违反一致性契约"的
  一个有趣的元讨论点，加深论文的思辨深度。

---

## 5 与现有分类方法的比较讨论
- 与通用软件缺陷分类（ODC等）的比较：本文分类法是"集成故障"这一
  子类的领域特化，不是通用缺陷分类法的替代品，二者可以配合使用
  （先用ODC分出"这是集成类故障"，再用本文框架进一步分类）。
- 与容器化/DevOps领域已有的"常见坑列表"（如社区博客、官方FAQ）的
  比较：本文提供的是有理论抓手的分类，而非纯经验列表，抓手的价值
  在于面对**新的、未见过的具体故障**时仍有指导意义（可预判、可
  检索），而经验列表只能覆盖已知案例。

---

## 6 案例验证：容器化双机协同无人机系统

> 本节把双机无人机仿真+真机平台的具体技术细节集中呈现，是全文唯一
> 保留详细案例描述的地方，起"实证支撑"作用。写法上以"验证框架"为
> 陈述主线（如"以下故障验证了3.2节时间一致性维度的判据"），不要
> 写成流水账式的项目开发历程。

### 6.1 案例系统概述
- 简要架构介绍（复用`论文大纲_双机协同飞行仿真平台.md`第2节精简版）。

### 6.2 时间一致性维度验证
- 仿真时钟/挂钟时间基准分裂导致DLIO死锁；真机上IMU厂商SDK单位（g vs
  m/s²）未转换导致的物理量纲错误（可以论证这也是"时间一致性"外的
  一个新子类——量纲/单位一致性，值得在讨论里提及是否应该拆成第五
  维度，或归入"空间一致性"的物理量建模范畴，交给第7节讨论）；
  uwb_imu模式反馈频率上限48Hz与控制律稳定性要求冲突。

### 6.3 空间一致性维度验证
- DLIO命名空间缺失导致跨机数据串扰；LiDAR-IMU外参未建模导致重力
  对齐错误（含仿真30°与真机实测25°两次独立标定的对照，是"外参必须
  真实标定、不能假设理论安装角"这一论点的双重证据）；SE(2)在线
  标定方法及其"一次性目标点变换未追溯修正"的已知局限。

### 6.4 通信一致性维度验证
- QoS Reliability策略不匹配；跨容器共享内存/dev/shm不可用；进程
  环境初始化契约（bash严格模式、Gazebo渲染资源路径）。

### 6.5 并发一致性维度验证：一个模式、三次复发（8个案例）
> 本节是全文实证分量最重的部分，按"复发时间线"而非"简单罗列"组织，
> 突出"同一失效模式反复出现"这一叙事，比单个案例更有说服力。

- **第一次（仿真阶段，约2026-08-08，A类-共享状态竞态）**：DLIO的
  `debug()`detached线程直接调用`this->gicp`的非线程安全方法，与主
  线程数据竞争导致段错误（`DEBUG_JOURNAL.md:905-918`）。当时的修复
  只针对这一个具体线程，未触发对代码库其它detached线程的系统性审查
  ——这是"点状修复"的第一个实例。
- **第二次（真机阶段，2026-08-19，A类-共享状态竞态，主案例）**：
  完整呈现`publishToROS()`/`publishKeyframe()`发布线程竞态的排查
  过程（ASAN插桩后bug消失→静态审查定位"每帧detach不节流的新线程
  对共享`std::vector`无保护`push_back`"→互斥锁修复→6轮RViz2重启
  循环12分钟零崩溃验证），与第一次案例对比：同一子系统、不同代码
  路径、相隔约11天、跨越仿真到真机两个环境——直接证明这不是偶发
  孤例，而是该子系统里一种反复出现的编码模式带来的类别性风险。
- **第三次（真机阶段，同期，A类-共享状态竞态的修复副作用）**：
  互斥锁修复后，detached线程改为排队而非丢弃，高频场景下线程数
  暴涨到85个、拖垮100Hz定时器，改用`try_lock`+丢帧策略解决——这个
  "修复引入新问题"的案例本身也值得讨论：对异步发布模式的并发问题，
  "加锁"不是免费的，需要同时考虑"锁等待是否会造成积压"这一新的
  设计维度。
- **B类子案例（阻塞调用致执行器饥饿，3例，与A类并列但根因不同）**：
  `dual_goal_input.py`用`input()`阻塞冻结整个单线程rclpy spin循环
  （`DEBUG_JOURNAL.md:3757-4317`）；`gt_cloud_bridge_node`单线程
  executor下`lookup_transform`自陷等待（`DEBUG_JOURNAL.md:10337-
  10348`）；tmux窗口启动时序竞态，CycloneDDS discovery未就绪即
  启动依赖它的节点（`DEBUG_JOURNAL.md:7045-7063`）。
- **元层面案例（工程流程层，不是运行时软件并发，但结构同构，可作为
  框架"跨层级适用性"的旁证）**：`staging/`热改代码与`docker build`
  流程本身产生的"竞态"——开发者手改`staging/`源码调试时，如果此时
  有人跑`docker compose build`，构建会读到不完整的中间状态
  （`DEBUG_JOURNAL.md:2049-2067`）。
- **本节结论**：并发一致性问题在案例系统中共记录8例（含A类3例核心
  复发链条+3例B类阻塞饥饿+1例元层面），是四个维度中样本量最小但
  "复发证据链"最完整的一类，其核心方法论价值不在数量而在于清晰
  展示了"点状修复→类别性风险仍未消除→不同代码路径复发"这一完整
  过程，第7.3节应重点围绕这条复发链条展开讨论。

### 6.6 穷尽性统计（据完整证据挖掘更新，共40例）

| 一致性维度 | 案例数 | 说明 |
|---|---|---|
| 时间一致性 | 10 | 含时钟源分裂×2独立触发点、消息转换丢失时间戳、反馈延迟超限、多处硬件/固件限速问题 |
| 空间一致性 | 10 | 含命名空间冲突、外参未建模（仿真30°+真机25°双重实证）、spawn yaw非零导致的坐标换算错误×3处独立发现、SE(2)标定相关 |
| 通信一致性 | 12 | 含QoS不匹配、共享内存不可用/资源耗尽、进程环境初始化契约、真机WiFi网卡场景下`ipc:host`验证无效的反例 |
| 并发一致性 | 8 | 含A类共享状态竞态×3（一个模式三次复发）、B类阻塞调用执行器饥饿×3、元层面案例×1 |
| **合计** | **40** | 全部可归入四维框架，其中1例（两机初始配准误差收敛到"错误但稳定"的固定解）作者本人也标注为"怀疑但未验证"，如实保留为未定论候选，不强行归类 |

- 候选第五维度（量纲/单位一致性）证据挖掘结论：仅发现1个强案例
  （真机IMU厂商SDK输出g未转m/s²）+1个证据力较弱的案例（规划器
  重试上限的单位从"次数"改为"秒"，属单模块内部设计问题而非跨模块
  集成故障）——**证据不足以支撑独立成第五维度**，建议维持四维框架，
  将IMU单位问题保留在"时间一致性"维度下、作为"消息转换丢失物理
  语义"判据的具体实例，第7节讨论部分应明确交代这一"考虑过但否决"
  的决策过程，体现分类法构建的审慎性（审稿人常见质疑"是否穷尽了
  所有可能维度"，主动交代否决过程比被动等待质疑更有说服力）。
- 通信一致性维度里的真机WiFi案例（`DEBUG_JOURNAL.md:14682-14750`）
  是一个有价值的**反例**：discovery成功但数据死活送不到，这次
  `ipc: host`（仿真阶段的标准修复手段）验证无效，根因是WiFi网卡
  场景下RMW实现并非FastDDS、失效机制不同——提醒"通信一致性"维度
  内部不同实例的具体修复手段不能一概而论，诊断决策流程（3.4节）
  需要考虑"先确认当前RMW实现/传输介质，再选择对应检查清单"这一
  前置分支。

---

## 7 讨论

### 7.1 框架的适用边界
- 目前只在一个案例系统（且只有一个应用领域）上验证，穷尽性论证的
  说服力受限于样本；讨论框架向其他机器人应用领域（地面机器人集群、
  多摄像头视觉系统、工业物联网容器化部署等）迁移的可能性与预期挑战
  （没有条件真做，但可以做合理的类比论证）。
- 四个维度并非严格正交（如外参标定同时涉及时间与空间维度），讨论
  这种"软边界"是分类法的固有特性还是需要进一步细化的信号。

### 7.2 关于"并发一致性"是否应独立成第四维度的讨论
- 正反两方论证：支持独立成维度的理由（触发机制、根因、修复手段都
  与其他三类本质不同，值得单列；且内部可进一步拆分A类共享状态竞态/
  B类阻塞调用执行器饥饿两个子类，细粒度上具备独立理论内容）；反对
  意见（也可以论证它是"通信一致性"在进程内的推广，是否要合并需要
  看审稿意见，写作时保留两种处理方式的可能性，视具体期刊/审稿反馈
  决定最终取舍）。
- 候选第五维度（量纲/单位一致性）的否决过程也应在此处交代（详见
  6.6节），体现分类法构建方法的完整性——不仅要论证"已采纳的维度
  为什么成立"，也要论证"考虑过的候选为什么被否决"。

### 7.3 仿真验证的覆盖能力：从"能否触发"到"能否根除"
- **本节论点已根据6.5节的完整证据链修正，不能简单写成"仿真无法
  触发并发故障"**——案例系统的完整记录显示，并发一致性故障在仿真
  阶段就已经出现过一次（DLIO `debug()`线程竞态），说明仿真环境
  并非结构性地不可能触发此类问题。真正值得讨论的论点是：**点状修复
  （只处理触发瞬间暴露的那一个具体调用点）无法根除类别性风险**——
  同一种编码模式（detached线程+无保护共享状态访问）在代码库的其它
  位置依然存在，等待下一次被不同的触发条件（更换代码路径、提高
  采样频率、接入新订阅者、更高的真机CPU负载）激活。真机阶段的复发
  与后续截至真机验证结束仍未完成的`metrics_thread`/`debug_thread`
  审查，都是这一论点的直接证据。
- 由此给出的一般性建议不再是"必须用真机压力测试才能发现并发问题"
  （这个结论过强、不被证据支持），而是更精确的：**遇到一类集成故障
  时，除了修复触发该次故障的具体代码位置，还应该系统性搜索代码库
  中是否存在采用同一编码模式的其它位置**（本文案例中即"所有detached
  线程调用点"）——这是一条对任意一致性维度都成立的通用工程建议，
  不局限于并发一致性，可以作为第8节结论里独立于四维分类表之外的
  一条"元建议"。

---

## 8 结论与展望
- 总结四点贡献（对应1.3节），如实说明局限（单案例验证、四维度
  可能不完全正交、并发一致性维度样本量为1）。
- 展望：在更多机器人应用领域验证框架的可迁移性；把诊断决策流程
  （3.4节）工具化（如做成一个交互式排查清单或轻量诊断脚本）；
  探索是否需要拆分出第五个维度（量纲/物理量一致性）。

---

## 与方向D（系统设计与实现论文）的内容复用说明

- 本大纲第3、4节直接复用`论文大纲_双机协同飞行仿真平台.md`第3节的
  已有分析，只是重新组织呈现顺序（框架先行、案例后置）与压缩案例
  篇幅；第6节等于把D大纲第3节里"实例"部分的详细描述搬过来集中呈现。
- **写作顺序建议**：由于两篇高度共享素材，建议**先写A**（当前这篇）
  ——把三重/四重一致性框架的形式化定义、诊断流程图、与经典理论的
  映射关系这些"新增工作量"一次性做完；D大纲第3节届时可以大幅精简，
  改成"详见另文[A]，本节仅概述"+引用，避免同一套内容在两篇论文里
  重复展开导致自我抄袭（self-plagiarism）风险，两篇投不同期刊时
  务必注意互相引用、明确各自的核心贡献边界。

---

（大纲完，预计正文5000-7000字（比D更聚焦，篇幅可以更短），图表
建议：①四维分类表；②诊断决策流程图；③穷尽性验证统计表；④并发
一致性案例的时序竞态示意图。）

---

## 附录：完整案例证据表（后台证据挖掘原始结果存档，供撰写第6节时
逐条核对引用，不需要全部写进正文，正文按6.2~6.5节的精选叙事呈现）

> 以下四表为对`docker_sim/DEBUG_JOURNAL.md`（14768行）与`read_hw.md`
> （1294行）全部标题的系统性排查结果，每条均已核对能在原文件对应
> 位置找到出处，未包含无法溯源的猜测性归类。行号为大致定位，成稿前
> 建议按行号复核原文避免引用错位（原始挖掘基于当时文件版本，若文件
> 后续有编辑，行号可能漂移）。

### 表1：时间一致性（10例）

| 日期 | 来源 | 一句话描述 | 关键引用 |
|---|---|---|---|
| 08-08左右 | DEBUG_JOURNAL.md:758-832 | DLIO仿真时钟vs挂钟基准分裂致死锁（正文6.2主案例） | "sim time被误判成挂钟" |
| 08-08 | DEBUG_JOURNAL.md:3897 | global_mapper_ros里同样"假epoch vs墙钟"，第二个独立触发点 | "根源在global_mapper_ros自己身上" |
| 08-09 | DEBUG_JOURNAL.md:7271 | rviz_goal_bridge_node的tf2查询两个时钟域相差一年半 | "两个时钟域相差一年半" |
| 08-08 | DEBUG_JOURNAL.md:6316 | Gazebo实时因子仅0.44拖累下游速率假设 | "实时因子只有0.44" |
| 08-13 | DEBUG_JOURNAL.md:12969 | gazebo_mavlink_interface硬编码250Hz锁步限速里程计 | "硬编码250Hz锁步" |
| 08-12 | DEBUG_JOURNAL.md:10817-10818 | PX4固件MAVLink流速率写死压到30Hz | "写死压到30Hz" |
| 08-08 | DEBUG_JOURNAL.md:6024-6043 | mavros/imu卡50Hz：脚本与PX4固件初始化线程竞态覆盖速率（与并发维度交叉，两处均可引用） | "运行时脚本覆盖...竞态" |
| — | read_hw.md:93-126 | 真机IMU厂商SDK输出g未转m/s²，时序积分全错（正文6.2主案例） | "SDK这颗IMU原始输出是g为单位" |
| — | DEBUG_JOURNAL.md（GICP链路，约1266行前后） | Livox点云offset_time字段丢失致deskew失败（正文6.2主案例） | "offset_time直接被扔掉了" |
| — | DEBUG_JOURNAL.md:10944,10979 | uwb_imu反馈经EKF2+MAVLink往返上限48Hz致炸机（正文6.2主案例） | "反馈频率不够" |

### 表2：空间一致性（10例，含1未定论候选）

| 日期 | 来源 | 一句话描述 | 关键引用 |
|---|---|---|---|
| 08-07 | DEBUG_JOURNAL.md:508 | DLIO命名空间缺失致双机撞名串扰（正文6.3主案例） | "发现几个节点名冲突" |
| 08-07 | DEBUG_JOURNAL.md:249 | global_mapper_ros查TF用的frame名与Gazebo插件对不上 | "frame名字...对不上" |
| 多处 | 仿真30°+read_hw.md:84-160真机25° | LiDAR-IMU外参未建模致重力对齐错误（正文6.3主案例，双重实证） | "重力对齐从初始化第一步就是错的" |
| 08-08 | DEBUG_JOURNAL.md:3518 | UWB真值坐标变换少一项修正，量级几米 | "坐标变换少了一项修正" |
| 08-12 | DEBUG_JOURNAL.md:11309 | gt_odom_bridge_node同样假设spawn yaw≈0（第三处同类假设错误） | "同样假设spawn yaw≈0" |
| 08-12 | DEBUG_JOURNAL.md:11383 | goal窗口换算：45°spawn yaw导致纯平移公式算错 | "纯平移公式算错" |
| 08-09 | DEBUG_JOURNAL.md:3806-3897 | RViz全局坐标显示双机，两bug都在NX01/map→NX02/base_link链路 | "都在这条跨机链路上" |
| 08-08 | DEBUG_JOURNAL.md:5632-5635 | term_goal绕开AGENT_INDEX*3换算层，误落地图原点 | "完全绕开了这层换算" |
| 多处 | 仿真θ*(11008起)+真机 | SE(2)在线标定+一次性目标点变换不追溯修正（正文6.3主案例，另见方向B大纲详细展开） | "θ*=atan2(S,C)" |
| — | TODO.md#9末段 | 未定论候选：两机初始配准误差被锁死成"错误但稳定"的固定解 | "怀疑...还没验证" |

### 表3：通信一致性（12例）

| 日期 | 来源 | 一句话描述 | 关键引用 |
|---|---|---|---|
| 08-07 | DEBUG_JOURNAL.md:223 | occupancy_grid的QoS不匹配（正文6.4主案例） | "rclcpp的一个经典陷阱" |
| — | TODO.md#9第二层 | DLIO点云RELIABLE vs Livox BEST_EFFORT（正文6.4主案例） | — |
| 08-08 | DEBUG_JOURNAL.md:621 | RViz接入后DDS共享内存资源耗尽（区别于"完全不可用"的另一种失效表现） | "DDS共享内存资源耗尽" |
| — | TODO.md#9第三层 | 跨容器ipc:host共享内存丢包（正文6.4主案例） | — |
| 08-08 | DEBUG_JOURNAL.md:6052 | CycloneDDS在host网络下枚举全部虚拟网卡拖累速率 | "枚举了全部宿主机虚拟网卡" |
| 08-11 | DEBUG_JOURNAL.md:10679 | build重启后QoS不兼容致三症状同时出现 | — |
| — | TODO.md#0 | entrypoint bash严格模式与ament setup冲突（正文6.4主案例） | — |
| — | TODO.md#11 | Gazebo GUI环境变量未初始化致OGRE空指针崩溃（正文6.4主案例） | — |
| 08-19 | read_hw.md（GCS真机WiFi网卡节，2026-08-20条目附近） | discovery成功但单播数据死活送不到，`ipc:host`此处验证无效（反例，正文6.6节专门讨论） | "discovery能看到对方...数据包死活送不到" |
| 08-13 | DEBUG_JOURNAL.md:12449 | Livox-SDK2版本pin过旧+上游build.sh吞掉失败退出码 | "隐蔽坑" |
| 08-18 | DEBUG_JOURNAL.md:13716 | gcs镜像缺PCL开发库致build失败 | — |
| 多处 | — | 诊断工具自身故障干扰排障：ros2 daemon被高频docker exec弄挂，误判成"消息没到" | "诊断工具本身先坏了" |

### 表4：并发一致性（8例，含A/B两个子类+1元层面案例）

| 日期 | 来源 | 一句话描述 | 关键引用 | 子类 |
|---|---|---|---|---|
| 08-08左右 | DEBUG_JOURNAL.md:905-918 | 仿真侧第一个独立并发案例：DLIO的`debug()`detached线程直接调`this->gicp`方法，与主线程数据竞争致段错误 | "debug线程读的时候，主线程恰好正在改" | A（第一次） |
| — | read_hw.md:788-826 | DLIO callbackPointCloud发布线程无保护并发push_back共享vector（正文6.5主案例） | "两个线程同时对同一个std::vector并发push_back" | A（第二次） |
| — | read_hw.md（`publish_ros_mutex_`修复副作用节） | 修复后detached线程排队积压致线程数涨到85个、拖垮100Hz定时器，改用try_lock丢帧解决 | "线程数涨到85个" | A（第三次/修复衍生） |
| 08-08 | DEBUG_JOURNAL.md:3757-4317 | dual_goal_input.py: input()阻塞冻结整个单线程rclpy spin循环 | "冻结整个单线程事件循环" | B |
| 08-11 | DEBUG_JOURNAL.md:10337-10348 | gt_cloud_bridge_node单线程executor下lookup_transform自己等自己（死锁） | "自己等自己" | B |
| 08-08 | DEBUG_JOURNAL.md:7045-7063 | tmux窗口启动时序竞态：CycloneDDS discovery未就绪即启动 | "确凿的启动时序竞态证据" | B |
| 08-08 | DEBUG_JOURNAL.md:6024-6043 | mavros/imu 50Hz：运行时脚本vs PX4固件task_main线程竞态（与时间一致性表1交叉引用） | "竞态" | A/B边界 |
| 08-08 | DEBUG_JOURNAL.md:2049-2067 | 元层面：staging/热改代码与用户docker build流程产生竞态（工程流程层，非运行时软件） | "会跟...docker build产生竞态" | 元层面 |

### 候选第五维度（量纲/单位一致性）证据（结论：不独立成维，见正文6.6节）

| 日期 | 来源 | 一句话描述 | 备注 |
|---|---|---|---|
| — | read_hw.md:93-126 | 真机IMU厂商SDK输出g未转m/s²（已计入表1时间一致性） | 建议保留在时间一致性维度 |
| — | DEBUG_JOURNAL.md:1975-2003 | 规划器重试上限设计单位从"次数"改成"挂钟时间(秒)" | 证据力弱——单模块内部设计问题，非跨模块集成故障，仅作旁证 |

（附录完）
