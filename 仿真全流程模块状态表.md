# 仿真全流程程序模块状态表

统计口径：docker_sim项目三个容器——仿真栈（`sim-world`）、仿真飞行栈（`flight-stack`）、选手栈（`contestant-sdk`，含`mission_program/`任务程序）。"完成度"指代码是否已实现，"测试情况"指是否有过实际仿真验证（引用`DEBUG_JOURNAL.md`记录），两者是独立维度——代码写完不等于测过，测过一次也不等于没有遗留缺口。

## 一、仿真栈（sim-world）

| 模块 | 功能 | 完成度 | 测试情况 | 备注/缺口 |
|---|---|---|---|---|
| gzserver + fire_drill_room.world | 场地/墙体/立柱/障碍物/标记物 | 完成 | 已测试 | 立柱碰撞体、墙体位置09-17实测核实过 |
| gen_iris_mid360_sdf.py | 无人机+雷达+相机SDF生成 | 完成 | 已测试 | NX01真实相机反复验证；NX02 logical_camera生成逻辑沙盒验证过 |
| gazebo_ros_logical_camera插件 | 几何视锥检测传感器（cherry-pick） | 部分完成 | 测试未通过 | 编译加载正常，但跨容器消息投递问题排查一整个会话未解决，NX02已改回不挂相机，插件代码保留未使用 |
| gazebo_ros_camera_set_enabled_topic补丁 | 运行时动态开关相机 | **已删除** | — | 09-17确认会间歇导致相机假死，用户要求整体删除，不再保留 |
| mission_judge_node.py | checkpoint自动裁判（读Gazebo真值） | 完成 | 已测试 | 历次测试checkpoint完成日志稳定 |
| scenario_reset_node.py | 场景重置/立柱朝向随机化 | 完成 | 已测试 | 09-15修过静态/运动学标签bug，此后稳定 |
| uwb_ground_truth_node | UWB真值仿真+frame_align数据源 | 完成 | 已测试 | 双机模式下frame_align真实payload核实过 |

## 二、仿真飞行栈（flight-stack）

| 模块 | 功能 | 完成度 | 测试情况 | 备注/缺口 |
|---|---|---|---|---|
| MAVROS + PX4 SITL | 飞控连接 | 完成 | 已测试 | 每次测试稳定连接 |
| DLIO/FAST-LIO SLAM（uwb_slam模式） | 真实SLAM定位 | 已实现 | 近期未测试 | 当前大赛固定用`uwb_imu`（大房间SLAM会失效），SLAM路径09-14后未再实测 |
| uwb_imu_fusion_node | UWB+IMU融合定位（当前默认） | 完成 | 已测试 | 本次会话全程使用，09-14修过起飞点锁定竞态bug |
| origin_setter_node.py | 起飞点锁定+SE(2)旋转估计 | 完成 | 已测试 | 每次开机锁定成功；uwb_imu模式下θ*正确强制为0 |
| frame_align_bridge_node.py | 跨机坐标系对齐 | 完成 | 已测试 | 双机模式验证正常；单机模式下确认不工作（预期内，缺对端） |
| gt_odom_bridge（4个节点） | 定位/点云桥接、RViz标注 | 完成 | 已测试 | local_position_readback_node历史修过2个真实bug |
| ego_planner + traj_server（12+补丁） | 路径规划核心 | 基本完成 | 已测试，遗留已知缺口 | D3长航线（单段最长约20米）验证通过；`segment_point`概率性重规划死锁只修了崩溃，触发条件未根治 |
| pt4ctrl / px4ctrl_bridge | 飞控控制器 | 完成 | 已测试 | 无未解决问题 |
| position_cmd_relay_node.py | 位置指令中继/仲裁 | 完成 | 已测试 | relay_mode切换bug 09-14修过 |
| qr_apriltag_detect_node.py | 真实相机QR/AprilTag检测 | 完成 | 已测试，有已知限制 | front稳定；down历史性稀疏未根治；遗留临时诊断代码未清理 |
| logical_camera_bridge_node.py | logical_camera→检测结果桥接 | 已实现 | 未验证通过 | 崩溃/命名空间/QoS问题修过，但跨容器投递从未验证成功，当前未使用 |
| precision_servo_node.py | 精准降落/居中视觉伺服 | 完成 | 已测试，新发现缺口 | E6全流程（精降+抓取+复飞+返航）验证过；新发现"急停后丢失目标视野导致居中超时" |
| fire_pillar_aim_node.py | 高层火情立柱瞄准位姿广播 | 完成 | 已测试 | D6绕飞+识别流程内验证正常 |
| formation_follower_node.py | 编队跟随 | 已实现 | 仅单元测试 | 09-13合成测试过，今天大量相机/SDF改动后未回归验证 |
| actuator_action_node.py | 符号化动作执行（抓取/投放/发射） | 完成 | 已测试 | E6链路内验证过 |
| pillar_detector_node.py | 感知式立柱候选检测 | 已实现 | **未集成** | 发布`pillar_candidates`但`mission.py`从未订阅，实际是死代码路径（真实流程用题目给定坐标） |
| ground_scan_helper.py / orbit_flight_helper.py / fire_pillar_approach_helper.py | 弓字形扫描/绕飞航点生成 | 完成 | 已测试 | D3/弓字形扫描/绕飞飞行今天多次验证 |

## 三、选手栈（contestant-sdk）

### DroneSDK能力（`contest_sdk/capabilities.py`）

| 模块 | 完成度 | 测试情况 | 备注/缺口 |
|---|---|---|---|
| takeoff / land | 完成 | 已测试 | 标准化前置检查+5秒悬停流程09-17验证通过 |
| goto / goto_direct / cancel_goto | 完成 | 已测试，有已知限制 | 中短距离可靠；长距离偶发概率性规划死锁（见ego_planner条目） |
| world_to_local / local_to_world | 完成 | 已测试 | 稳态可靠；曾出现一次无法解释的异常返回值，未复现 |
| wait_for_detection / center_on_target | 完成 | 已测试，新发现缺口 | `center_on_target`急停后可能丢失目标视野，导致超时 |
| precision_land_and_confirm / precision_land_at | 完成 | 已测试 | E6全流程验证过 |
| do_action / start_formation_follow / stop_formation_follow | 完成 | 仅单元测试 | 未跟随近期改动回归验证 |
| send_to_teammate / on_teammate_event（`reliability.py`） | 完成 | 仅单元测试 | **从未做过双机真实跨进程收发验证** |
| set_mission_state / get_mission_state | 完成 | 已测试 | — |
| set_yaw_mode_velocity/constant/point | 完成 | 已测试 | 起飞掉高根因（yaw目标写死0.0）09-17修复并验证 |
| trigger_alarm / play_sound_light / mute_sound_light | 完成 | 已测试 | 20条事件表已定稿，仅起飞/降落2条接入`mission.py`，其余18条未接线（用户明确deprioritize） |
| reset_aim / read_fire_pillar_staging_pose / read_fire_pillar_aim_pose | 完成 | 已测试 | D6/E6流程内验证过 |
| ~~set_camera_enabled~~ | **已删除** | — | 09-17确认底层patch会导致相机假死+空操作误导任务逻辑，用户要求整体删除 |

### 任务程序（`mission_program/mission.py`等）

| 模块 | 完成度 | 测试情况 | 备注/缺口 |
|---|---|---|---|
| recon_main（D3航线→D4调度→D5/D6子流程→D7同步） | 基本完成 | 部分测试 | D3航线+弓字形扫描起步09-18首次完整跑通；D5地面火情完整链路在recon_main内从未跑完（曾被疑似误触发打断）；D6高层火情绕飞+识别独立验证+回归测试都通过 |
| supply_main | 完成 | **从未测试** | 任务机整条流程代码写完，仿真里一次都没跑过 |
| 双机同时真实运行（recon_main+supply_main并行） | 完成 | **从未测试** | 今天所有测试都是NX01单独跑，NX02最多是陪跑待命 |
| 双机返航互相确认收尾（等`_NOTIFY_SUPPLY_LANDED`+`done:mission_complete`） | 完成 | **从未测试** | 依赖上一条，尚未验证 |
| mission_geometry.py | 完成 | 已测试 | 专门单元测试`test_mission_geometry.py`+插值往返验证 |
| mission_vocab.py + 声光事件对照表 | 完成 | 未独立测试 | 作为常量被上述模块消费，随宿主模块间接验证 |

## 四、汇总结论

**已完成且测试通过、可信度较高**：仿真栈全部模块、飞行栈定位/控制/中继链路、单机D3航线+D6绕飞识别、精准降落E6全流程、声光反馈表（仅起降2条接线）。

**已完成但缺关键验证**：`send_to_teammate`跨进程真实收发、`supply_main`、双机并行运行、双机收尾同步、`formation_follower_node`近期回归、`center_on_target`视野丢失问题的修复验证。

**已实现但未集成或已废弃**：`pillar_detector_node`（死代码路径）、`logical_camera_bridge_node`+`gazebo_ros_logical_camera`插件（当前未使用）、`set_camera_enabled`（已删除）。

**明确缺失/未开始**：D5地面火情在`recon_main`真实调度路径下的完整闭环验证；真正的双机端到端全流程回归。
