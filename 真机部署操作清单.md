# 真机部署操作清单

> Jetson Orin NX Super 16G（JetPack 6.2）单机首飞部署，本机（Ubuntu 24.04）
> 作为地面站。本清单是对初版清单的复查修正版——修正了一处顺序依赖倒置
> （"迁移方法"依赖"Jetson系统级准备"先完成），补上了检查时发现的缺失项
> （存储空间/build联网需求/QGC预配置等），并把"先在本机一次性准备好所有
> 真机文件"这个不现实的假设拆成"本机搭骨架 + 上机后填真实值"两段。
> 阶段2的rsync排除清单也补充过一版：原版只排除了体积最大的几个仿真专属
> 目录，漏了`docker-compose.yml`/`Dockerfile.sim-world`等几个"传了不会
> 报错但容易被误操作"的文件，现已补全。
> 阶段1.2列的骨架代码（2026-08-14）已经全部写成实际文件（只做了语法/
> 静态检查，没有`docker build`真正编译过、没有连过真实硬件），下面
> "[x]"标注对应的文件已经存在，可以直接看代码，不用从头写。骨架代码落地
> 之后又反过来检查了一遍"这些新文件对阶段2迁移有没有影响"，补了两条
> 排除规则（`gcs/`+`docker-compose.override.yml`），阶段3.8（原3.7）也
> 换成了引用现成的systemd模板而不是让你从头写。
> 2026-08-15新增：3.7"启动流程说明"（entrypoint.sh的完整执行顺序+哪些
> 全自动/哪些要人工触发）、4.6"起降与起飞点标定操作"（具体命令）、
> 阶段5"网页端地面站控制面板"（可选后续规划，纯讨论未实施，详细方案+
> 实施步骤已单独成文，见《网页端地面站控制面板方案》）。阶段2拆成
> 2.1/2.2/2.3三个方案并列（rsync/一键打包脚本`package_hw_deploy.sh`/git
> 远程仓库），排除清单三者共用同一份；1.2的`docker-compose.hw.yml`那条
> 补了双机部署的参数化说明。
>
> 背景方案见同目录《真机部署与Docker开发知识梳理》。

---

## 阶段 0：Jetson 系统级准备（前置阶段，必须最先完成）

> 这一步是"迁移方法"（阶段2）能执行的前提——`rsync`/`ssh` 都要求 Jetson
> 已经联网、能被连上，原始清单把这一步放在"迁移方法"之后，属于顺序错误，
> 这一版提到最前面。

- [ ] JetPack 6.2 烧录（⚠️ 本机 Ubuntu 24.04 不在 NVIDIA SDK Manager 官方
  支持的烧录 host 列表内，官方只支持 20.04/22.04——需要另外准备一台
  22.04 环境，或确认载板是否支持免 host 烧录）
- [ ] 设静态 IP / 路由器 DHCP 保留
- [ ] 确认 SSH server 已开启（`systemctl status ssh`）
- [ ] `chrony`/NTP 对时（GCS 或路由器做时间源）——**提前在这一步做**，
  不要拖到最后：后续裸机联调阶段如果要拿 GCS 和 Jetson 的日志/rosbag
  时间戳交叉对照，没对时会很麻烦
- [ ] 检查 Jetson 存储空间是否够用（源码约2.6G + ROS2底座 + 各模块编译
  产物，实际可能占到一二十G，eMMC/SD 方案要格外注意，优先选 NVMe）
- [ ] 确认 `rsync` 已安装（两端都要检查，`which rsync`，没有则
  `apt install rsync`）
- [ ] 本机确认/生成 SSH key，`ssh-copy-id` 到 Jetson 配好免密登录，
  `~/.ssh/config` 起个别名（如 `nx01`）

---

## 阶段 1：本机文件内容准备（搭骨架，不强求一次到位）

### 1.1 确认迁移范围

| | 内容 | 处理 |
|---|---|---|
| 需要 | `docker/`、`patches/`、`src/`、`vendor/`、`config/`、`docker-compose.yml`、`fetch_sources.sh` | 迁移 |
| 需要 | `staging/` 里的 `dlio_ws_src`/`decomp_ws_src`/`mighty_ws_src`/`ros2_px4_stack`/`ego-planner-swarm`/`livox_ws_src`/`pointlio_ws_src`/`nlink_ws_src`（约2.6G） | 迁移 |
| 排除 | `runtime_logs/`（73G，纯仿真日志） | 不迁移 |
| 排除 | `staging/gazebo_models_external`（3.9G）、`staging/PX4-Autopilot`（2.6G，真实飞控走独立烧录固件）、`staging/PX4-SITL_gazebo-classic`（312M） | 不迁移 |
| 排除 | `docker-compose.yml`（原3服务版）、`docker/Dockerfile.sim-world`、`docker/entrypoints/sim-world-entrypoint.sh`、`docker/scripts/gen_iris_mid360_sdf.py` | **不迁移**——这几个凑一块儿会让人在Jetson上误敲`docker compose up sim-world`/裸`docker compose build`，浪费时间/引发混淆，属于真正该排除的一类 |
| 排除（2026-08-14新增） | `gcs/` 整个目录、`docker-compose.override.yml`（真正生效的那份，不是`.example`） | **不迁移**——`gcs/`是地面站（本机）专用容器，跟`docker-compose.yml`一样是"留在Jetson上会被误操作"的一类；`docker-compose.override.yml`可能挂载了只存在于本机的调试路径，对Jetson没有意义。⚠️ `.gitignore`不影响rsync，这两个哪怕已经被git忽略，rsync一样会原样传过去，必须显式排除 |
| 可选 | `scripts/`（宿主机运维脚本，假设双机同host，Jetson上大概率跑不起来）、`patches/`里17个sim-world专属patch、`src/uwb_sim/`、`docker-compose.override.yml.example`（调试模板，Jetson用不上但传了无害） | 传了也不会被`Dockerfile.flight-stack`用到（没有`COPY`过），纯属占地方，可排除可不排除 |

### 1.2 真机专属文件——本机只搭"骨架"，占位符标注哪些值要上机后才能定

> ⚠️ 修正点：以下几项的**具体数值**（真实网卡名、真实串口设备路径、雷达
> 真实IP）只有登上 Jetson 之后才能确定，本机阶段只能先把文件结构/逻辑
> 框架写好，值留成占位符/TODO，**不要求这一步就"完成"**——阶段3上机后
> 还有一轮编辑。
>
> ✅ **2026-08-14 更新：以下骨架代码已经全部落地**（只写了文件+语法/静态
> 检查，没有 `docker build` 真正编译过，没有连过真实硬件），逐项对应关系
> 见下面每条后面标注的文件名。

- [x] 新建 `docker-compose.hw.yml` 骨架（单容器，去 GPU reservations，
  `devices:` 直通 PX4/UWB 串口，路径占位默认值）。2026-08-15追加：
  `NAMESPACE`/`AGENT_INDEX`/`NUM_AGENTS` 从硬编码字面量改成可覆盖，
  双机部署时两台 Jetson 跑同一份文件靠传参区分身份（`NX01`/`NX02`），
  不用手改文件；双机协同飞行还要记得把 `LOCALIZATION_SOURCE` 从默认的
  `single_uwb_slam` 换成不带 `single_` 前缀的 `uwb_slam`（配合
  `NUM_AGENTS=2` 才会真正启用机间避让逻辑，`single_*` 语义是"单机、
  无编队感知"）
- [x] `flight-stack-entrypoint.sh` 新增 `DEPLOY_TARGET`（`sim`/`hw`）环境
  变量串联真机分支（没有另建独立文件，直接在现有 entrypoint 里加条件
  分支，避免两份文件重复维护 DLIO/mighty/ego_planner/px4ctrl 这些跟
  sim/hw 无关的公共逻辑）：
  - `fcu_url` 改读真实串口（`FC_SERIAL_DEVICE`/`FC_BAUD_RATE`，路径占位）
  - `INIT_X/Y/Z` 固定为 0
  - CycloneDDS 网卡从 `lo` 换成 `HW_NETWORK_INTERFACE` 变量+可选
    `GCS_PEER_ADDRESS` 静态 Peer
  - 新增真实 Mid-360 驱动启动（`docker/entrypoints/hw_launch/
    mid360_real.launch.py`，新建文件，话题名假设未经真实设备验证）
  - 新增真实 UWB 驱动启动（`nlink_uwb_bridge.launch.py`，复用已有文件）
  - `DEPLOY_TARGET=hw` 下 `CONTROLLER=so3ctrl/pt4ctrl`、`LOCALIZATION_
    SOURCE=gt`、`SLAM_BACKEND=point_lio` 三种目前没有对应真机实现的组合
    会直接报错退出（fail fast，不会静默退回不安全的仿真配置）
- [x] `docker/entrypoints/source_all.sh` 补全 `source /opt/nlink_ws/
  install/setup.bash`（当前遗漏，已修）
- [x] 新建 `src/px4ctrl_bridge/launch/px4ctrl_hw.launch.py`：`no_RC` 改
  `False`，其余逐字照抄仿真版
- [x] `px4_param_relax.py` 三个失控保护参数按 `DEPLOY_TARGET` 区分取值
  （真机版 `COM_DISARM_PRFLT` 不放宽、`COM_OF_LOSS_T` 收紧到 2.0 秒——
  这个数字仍是未经真实链路验证的起点值，上机联调必须重新评估）
- [ ] `config/MID360_config.json`：IP 占位，等雷达真实接线后再改（依赖
  硬件信息，还没做，见 3.2）
- [ ] `config/vehicle_profile.yaml`：`mass`/`hover_thrust` 先留仿真占位值，
  标记"待真机称重+标定后覆盖"（依赖硬件信息，还没做）
- [x] 额外新建（原清单没单列，属于同一批骨架代码）：
  `docker-compose.override.yml.example`（volume调试模式模板）、
  `docker/systemd/flight-stack-hw.service`（开机自启模板）、
  `gcs/Dockerfile`+`gcs/docker-compose.yml`+`gcs/cyclonedds_gcs.xml.
  example`（GCS轻量容器骨架，对应阶段4）

#### 新文件用途速查（2026-08-14新增）

| 文件 | 作用 |
|---|---|
| `docker-compose.hw.yml` | 真机单机部署的compose文件，单容器，`DEPLOY_TARGET=hw`，锁定确认的首飞组合默认值，串口设备直通 |
| `docker/entrypoints/hw_launch/mid360_real.launch.py` | 启动真实Mid-360驱动，把上游默认的CustomMsg格式改成DLIO需要的标准PointCloud2格式 |
| `src/px4ctrl_bridge/launch/px4ctrl_hw.launch.py` | px4ctrl真机版启动文件，跟仿真版唯一实质差异是`no_RC=False` |
| `docker-compose.override.yml.example` | volume调试模式的**仿真侧**模板，日常在本机改代码调试用，跟真机部署本身无关 |
| `docker/systemd/flight-stack-hw.service` | 开机自启模板，见3.7的安装步骤 |
| `gcs/Dockerfile` | 地面站轻量容器镜像定义，只装ROS2底座+`quadrotor_msgs`/`traj_utils`两个消息包，不编译算法实现 |
| `gcs/docker-compose.yml` | 起GCS容器用的compose文件，`network_mode: host`，配好`ROS_DOMAIN_ID`/`RMW_IMPLEMENTATION`跟Jetson一致 |
| `gcs/cyclonedds_gcs.xml.example` | GCS侧CycloneDDS静态Peer配置模板 |

### 1.3 网络/代理准备（原清单遗漏项）

- [ ] 确认 Jetson 现场是否需要代理才能访问外网——**`docker build` 期间
  `apt-get install`/`pip install` 仍然需要联网**（不是访问 GitHub，是访问
  Ubuntu/pip 镜像源），"用 rsync 不用 Jetson 联网"这句话**只对源码传输
  成立，对 build 步骤不成立**，这条容易被误解，专门列出来提醒
- [ ] 如果需要代理，提前把代理地址/端口记下来，build 时当 `HTTP_PROXY`/
  `HTTPS_PROXY` 传给 `docker build`

---

## 阶段 2：迁移方法

三选一，默认用方案A；现场网络差/需要离线传输选方案B；想要版本历史选
方案C。三者排除的文件范围是同一份决策，只是实现方式不同。

### 2.1 方案A：rsync（默认，需要现场跟 Jetson 网络连通）

- [ ] `ssh nx01 echo ok` 确认能连通（依赖阶段0已完成）
- [ ] 首次全量同步（⚠️ 排除清单比初版多了`docker-compose.yml`等4项——这几个
  是原3服务仿真compose+sim-world专属文件，留在Jetson上容易让人手滑跑
  `docker compose up sim-world`，属于真正该排除的一类，不是占地方那么
  简单；2026-08-14又补了`gcs/`+`docker-compose.override.yml`两项，理由
  同上——`gcs/`是地面站专用容器，`docker-compose.override.yml`可能挂了
  只存在于本机的调试路径，都不该出现在Jetson上，`.gitignore`不影响
  rsync，必须显式排除；`scripts/`/仿真专属patch/`src/uwb_sim`/
  `docker-compose.override.yml.example`传了也不会被`Dockerfile.
  flight-stack`用到或者只是仿真侧调试模板，可排除可不排除，这里选择
  一并排除保持目录干净）：
  ```bash
  rsync -avz --progress \
    --exclude 'runtime_logs' \
    --exclude 'staging/gazebo_models_external' \
    --exclude 'staging/PX4-Autopilot' \
    --exclude 'staging/PX4-SITL_gazebo-classic' \
    --exclude 'docker-compose.yml' \
    --exclude 'docker/Dockerfile.sim-world' \
    --exclude 'docker/entrypoints/sim-world-entrypoint.sh' \
    --exclude 'docker/scripts/gen_iris_mid360_sdf.py' \
    --exclude 'gcs' \
    --exclude 'docker-compose.override.yml' \
    --exclude 'scripts' \
    --exclude '.git' \
    /home/robots/ai_uav/docker_sim/ \
    nx01:~/docker_sim/
  ```
  其余新增的骨架文件（`docker-compose.hw.yml`、`docker/entrypoints/
  hw_launch/`、`src/px4ctrl_bridge/launch/px4ctrl_hw.launch.py`、
  `docker/systemd/flight-stack-hw.service`）都在已经整体同步的目录下，
  不需要额外加排除/包含规则，这条命令会自动带上它们。
- [ ] 传输量较大（约2.6G+）时，现场网络差可以先用 U 盘拷，或 `iperf3`
  测一下实际带宽
- [ ] 后续有改动，同一条命令重新跑一遍即可（自动增量，只传变化部分）

### 2.2 方案B：一键打包脚本（2026-08-15新增，tar 归档，适合离线/U盘传输）

> 排除清单跟方案A的 rsync 完全一致（同一份决策换个实现），好处是**不需要
> 现场跟 Jetson 有网络连通才能"打包"这一步**——先在本机打包好，U盘/随便
> 什么方式传到 Jetson 都行，不依赖 rsync 那种"边连边传"的实时链路。

- [ ] 仓库根目录已有 `package_hw_deploy.sh`，直接跑：
  ```bash
  ./package_hw_deploy.sh              # 只打包，产出放上一级目录
  ./package_hw_deploy.sh nx01:~/      # 打包后顺手 scp 到 Jetson（需要网络连通）
  ```
  ⚠️ 输出文件不能放在被打包目录（`docker_sim/`）自己里面——第一版踩过
  这个坑，`tar` 遍历过程中会把正在被写入、体积持续增长的输出文件当成
  "读取途中发生变化"报错退出，脚本已经改成输出到上一级目录，这个坑
  不需要你再踩一次。
- [ ] 脚本自动生成 `.sha256` 校验文件，传输完在 Jetson 上先校验完整性：
  ```bash
  sha256sum -c docker_sim_hw_deploy_<时间戳>.tar.gz.sha256
  tar -xzf docker_sim_hw_deploy_<时间戳>.tar.gz -C ~/
  cd ~/docker_sim
  ```
- [ ] 后续有改动，重新跑一遍脚本即可（每次都是全量打包，不像 rsync 有
  增量优势，改动频繁时更适合用方案A）

### 2.3 方案C：git 远程仓库（备选，需要现场有稳定外网+想要版本历史）

- [ ] 先建 git 远程仓库，`git push`（本机）+ `git clone`（Jetson）代替
  rsync/打包脚本

---

## 阶段 3：真机安装

### 3.1 Docker + NVIDIA Container Toolkit 安装
```bash
sudo apt update
sudo apt install -y docker.io docker-compose-plugin
sudo usermod -aG docker $USER && newgrp docker
sudo apt install -y nvidia-container-toolkit   # 具体包名现场核实
sudo nvidia-ctk runtime configure --runtime=docker
sudo systemctl restart docker
sudo docker run --rm --runtime nvidia nvcr.io/nvidia/l4t-base:r36.4.0 uname -a   # 验证
```
（flight-stack 本身不依赖 GPU，`docker-compose.hw.yml` 的 `build`/`up` 都
不需要 `--runtime nvidia`，这一步是为后续相机/视觉阶段预留）

### 3.2 上机后第二轮编辑——把阶段1的占位符换成真实值

> 骨架代码已经把这些值都做成了环境变量（`docker-compose.hw.yml` 里
> `${VAR:-占位默认值}` 写法），不需要改代码本身，直接在 `docker-
> compose.hw.yml` 或者一个不进 git 的 `.env` 文件里改值就行。

- [ ] `ip a` 确认 Jetson 真实网卡名，改 `HW_NETWORK_INTERFACE`（默认占位
  `eth0`），同时把 GCS 的静态 IP 填进 `GCS_PEER_ADDRESS`
- [ ] 确认 PX4 飞控串口实际设备路径（`/dev/ttyTHS1` 或 USB 的
  `/dev/ttyACM0`，视接线方式），改 `FC_SERIAL_DEVICE`
- [ ] 确认 Mid-360 雷达实际 IP，改 `staging/livox_ws_src/livox_ros_
  driver2/config/MID360_config.json`（`host_net_info`/`lidar_configs`
  两处）
- [ ] 确认 UWB 模块实际串口设备路径，改 `UWB_SERIAL_DEVICE`（默认占位
  `/dev/ttyCH343USB0`）

### 3.3 QGC 直连飞控预配置（原清单遗漏项，且必须在 3.5 之前完成）

> ⚠️ `MAV_1_CONFIG`/`SER_TELx_BAUD`/`RC_MAP_*` 这几个 PX4 参数要靠 QGC
> 连上飞控才能改——正常流程是先用笔记本/GCS **通过 USB 直连飞控**配置好，
> 飞控才会真的在对应串口上给 Jetson 输出 MAVLink。这一步原清单完全没有，
> 不补上 3.5 的 MAVROS 连接测试大概率连不上。

- [ ] QGC 通过 USB 直连飞控
- [ ] 设置 `MAV_1_CONFIG` 指向连 Jetson 的那个 TELEM 口
- [ ] 设置该口 `SER_TELx_BAUD`，跟 `fcu_url` 里的波特率（921600）一致
- [ ] 遥控器接收机接好后，按之前的映射表设置 `RC_MAP_*`（ch0-3对应
  Roll/Pitch/Yaw/Throttle，channels[4]/[5]/[7]对应mode/gear/reboot）

### 3.4 硬件接线（可与 3.5 构建镜像并行，不必强制串行）

- [ ] PX4 飞控串口接 Jetson
- [ ] Mid-360 雷达接网线
- [ ] UWB 模块接串口
- [ ] 遥控器接收机接 PX4

### 3.5 构建镜像

```bash
docker compose -f docker-compose.hw.yml build
```
- [ ] ⚠️ **按 16G 内存调小 `BUILD_JOBS`**——Jetson Orin NX Super 只有
  16G 内存，`docker-compose.yml` 现有注释警告过"核数远超内存的机器容易
  把内存吃穿、系统死机"，Jetson 上这个风险比32核开发机更大，**不能直接
  沿用开发机默认值**，例如 `docker compose -f docker-compose.hw.yml build
  --build-arg BUILD_JOBS=4`，具体数值现场根据实际内存占用调
- [ ] 记录 build 耗时
- [ ] 如果某个依赖没有 aarch64 版本导致编译失败，现场排查（查有没有
  aarch64 wheel/换源/本地编译），这不是简单"记录一下"就能过去的，可能
  要花实际排查时间，提前预留余量

### 3.6 裸机联调（不装桨，按顺序验证）

> ⚠️ **"不装桨"不等于"电机不会转"**——只要 ESC 通电、飞控解锁，电机照样
> 会转，没有桨叶时后果轻很多，但高速裸转的电机/桨毂仍有风险。第一次测试
> `no_RC=false`/failsafe 触发这类涉及解锁的步骤时，考虑要不要额外断开
> ESC 供电或做好物理约束，不要只靠"没装桨"当作绝对安全。

- [ ] `docker compose -f docker-compose.hw.yml up` 起容器
- [ ] `ros2 topic echo /mavros/state` 确认 `connected=true`
- [ ] 确认真实 Mid-360 点云话题有数据，`xfer_format`/话题名跟 DLIO 期望
  对齐（见知识梳理文档第1.5节的格式不匹配提示）
- [ ] DLIO 收敛复核（已在仿真确认过，建议用真实点云再核实一次）
- [ ] UWB 驱动联调：核对 `nlink_pose_bridge_node` 的3个未验证假设（协议
  帧类型/quaternion顺序/单位），跑通 `set_origin_from_uwb` 一键定原点
- [ ] `no_RC=false` 后，人为断开遥控器信号，验证 failsafe 真的生效
- [ ] 真机称重+桨盘推力测算，覆盖 `vehicle_profile.yaml`/`MPC_THR_HOVER`
  的占位值

### 3.7 启动流程说明（容器启动后到起飞前，各模块谁负责、什么时机跑）

> 2026-08-15新增。回答"真机各模块启动流程是怎样、有没有专门负责的文件、
> 启动时机是什么"这几个问题，供 3.6 联调时对照排查用——不是新的操作项，
> 是对 3.6 检查清单背后逻辑的解释。

**专门负责的文件**：`docker/entrypoints/flight-stack-entrypoint.sh`（容器
`ENTRYPOINT`，PID 1）是总指挥，按顺序调用下面这些各管一摊的 launch 文件：

| 子模块 | 负责的 launch 文件 |
|---|---|
| 飞控连接 | `mavros`（官方 `px4.launch`） |
| 激光雷达驱动 | `hw_launch/mid360_real.launch.py` |
| SLAM | `direct_lidar_inertial_odometry`（`dlio.launch.py`） |
| 规划器 | `ego_planner_bridge`（`ego_planner_docker_sim.launch.py`） |
| 板外控制器 | `px4ctrl_bridge`（`px4ctrl_hw.launch.py`） |
| UWB 驱动 | `nlink_uwb_bridge`（`nlink_uwb_bridge.launch.py`） |
| 起飞点标定/多机对齐 | `uwb_origin_bridge`（`uwb_origin_bridge.launch.py`） |

**启动流程**（容器启动后，实际执行顺序）：

1. **阶段A 环境搭建**（全自动，几秒内）：`source_all.sh` 挂载所有
   workspace → `PLANNER=ego_planner` 切 CycloneDDS（真机网卡+GCS Peer）→
   `NAMESPACE`/`INIT_X/Y/Z` 等基础变量就绪
2. **阶段B 飞控连接**（全自动发起，连没连上要另外确认）：MAVROS 用真实
   串口启动，脚本 `sleep 3` 就继续，**不等实际连接成功**
3. **阶段C 定位链路**（全自动，有依赖顺序）：真实 Mid-360 驱动启动
   （`sleep 2`）→ DLIO 启动订阅点云（`sleep 3`）
4. **阶段D 规划+控制**（全自动启动，起来后待命不会自己飞）：`ego_planner`
   → `dynus_mavros.launch.py` 支撑节点 → `px4ctrl_hw.launch.py`（含
   `px4_param_relax`——**唯一有超时机制**的部分，最长等 `mavros/param/set`
   服务120秒，超时放弃但不连累其它节点；含 `takeoff_gate`——见阶段F）
5. **阶段E UWB+起飞点标定**（驱动自动启动，"标定"动作需要人工触发）：
   `nlink_uwb_bridge` 起来发布 `uwb/pose_abs` → `uwb_origin_bridge` 的
   `origin_setter_node` 自动做在线旋转估计，但"锁存当前位置为起飞原点"
   要手动调一次 service（见 4.6）
6. **阶段F 起飞触发**（**唯一真正"卡住等人"的地方**）：`takeoff_gate`
   从阶段D起来后一直轮询容器内 `/tmp/takeoff_go` 文件，**没有超时**，
   故意设计成"容器起来不会自己飞上天"（见 4.6 的触发方式）

### 3.8 开机自启（可选，全部验证通过后再做）

- [ ] `docker-compose.hw.yml` 里 `restart: "no"` 改 `restart: unless-stopped`
- [ ] 或用现成的 systemd unit 模板（`docker/systemd/flight-stack-hw.
  service` 已经写好，不需要从头写）：
  ```bash
  sudo cp docker/systemd/flight-stack-hw.service /etc/systemd/system/
  # 编辑 /etc/systemd/system/flight-stack-hw.service 里的
  # WorkingDirectory=/home/jetson/docker_sim 改成实际部署路径
  sudo systemctl daemon-reload
  sudo systemctl enable flight-stack-hw.service
  sudo systemctl start flight-stack-hw.service   # 立即测试一次，不用等重启
  ```

---

## 阶段 4：本机地面站（GCS）设计

### 4.1 决定监控方式

| 方案 | 优点 | 缺点 |
|---|---|---|
| A. 跨机 DDS | 可编程、能同时监控多机 | 要解决跨机发现，仿真期间没验证过 |
| B. NoMachine 远程桌面 | 绕开DDS跨机配置 | 不适合长期跑重型可视化（Jetson CPU紧张） |

单机阶段图省事可以先选 B，双机阶段需要"一屏同时看两架飞机+GCS自己跑脚本"
时再上 A。

### 4.2 方案A：跨机 DDS

> ✅ 2026-08-14 更新：骨架代码已落地在 `gcs/` 目录（`Dockerfile`+
> `docker-compose.yml`+`cyclonedds_gcs.xml.example`），下面清单对应实际
> 用法，不再是"过于笼统"的描述。

- [x] GCS 本机装一个轻量 ROS2 Humble 容器（`ros:humble-ros-base-jammy`
  基础镜像），`network_mode: host`——`gcs/Dockerfile`已建好，`cd gcs &&
  docker compose build`
- [x] **已梳理清楚需要哪些自定义消息包**（原清单这里过于笼统，已核实
  package.xml 确认这两个包"干净"、不拖带算法实现）：`quadrotor_msgs`
  （`vendor/px4ctrl_ros2`）+`traj_utils`（ego-planner的Bspline等消息，
  `staging/ego-planner-swarm/src/planner`）+ apt 装的 `ros-humble-
  mavros-msgs`。如果以后要监控 mighty 特有话题，需要另外加
  `dynus_interfaces`/`fla_interfaces` 两个包（当前确认组合用不到）
- [x] 装 `ros-humble-rmw-cyclonedds-cpp`，`RMW_IMPLEMENTATION=
  rmw_cyclonedds_cpp`，`ROS_DOMAIN_ID` 跟 Jetson 一致——`gcs/docker-
  compose.yml` 已配好这几个环境变量
- [ ] `cp gcs/cyclonedds_gcs.xml.example gcs/cyclonedds_gcs.xml`，改成
  真实 IP（GCS 本机网卡 IP + Jetson 静态 IP），这一步依赖真实网络信息，
  还没做
- [ ] 配 CycloneDDS 静态 Peer（GCS↔Jetson 双向写对方IP），双机阶段升级
  成 Discovery Server
- [ ] 改造 `launch_control.py`/`status_monitor.py` 等运维脚本，去掉
  `docker exec`/`docker cp` 假设，改走 DDS 话题/service call 或 SSH

### 4.3 方案B：NoMachine（原清单描述过于笼统，这里补齐具体步骤）

- [ ] NoMachine 不在 Ubuntu 默认 apt 源里，需要去官网下载对应架构的
  `.deb` 包（Jetson 是 arm64，GCS 是 x86_64，两边下载的包不同）
- [ ] Jetson 上：`sudo dpkg -i nomachine_*_arm64.deb`，装完确认服务
  `systemctl status nxserver`
- [ ] GCS 上：装 NoMachine client（同样是 `.deb` 或官方安装脚本）
- [ ] **注意授权条款**：免费版对多人同时连接/商业用途有限制，确认现在
  的使用场景（个人调试 vs 团队多人协作）符不符合免费版范围
- [ ] 约定好只在上面跑轻量监控（`docker logs`/`status_monitor.py`/
  `ros2 topic echo`），不长期开重型 3D 可视化（rviz2点云）

### 4.4 QGroundControl

- [ ] GCS 装 QGC（官方 AppImage）
- [ ] 优先配独立数传链路（不复用机载 WiFi）；没有独立数传就用 mavros
  `gcs_url` UDP 转发（会绑定在同一条 WiFi 上，需接受这个单点故障）

### 4.5 网络基础复核

- [ ] 确认 GCS 和 Jetson 同网段（阶段0已配置，这里做最终复核）
- [ ] `chrony` 对时状态复核（阶段0已提前做，不是这里才第一次做）

### 4.6 起降与起飞点标定操作（2026-08-15新增）

> 对应 3.7 阶段E/F 提到的两个"需要人工触发"的动作。只要跨机 DDS（4.2）
> 已经打通，下面两条命令都能直接在 GCS 本机执行，**不需要 SSH 到
> Jetson**。

- [ ] **起飞点一键锁定**（飞机在起飞位置静止不动时执行一次，必须先于
  起飞）：
  ```bash
  ros2 service call /NX01/set_origin_from_uwb std_srvs/srv/Trigger
  ```
- [ ] **起飞/降落触发**（推荐方式——长驻订阅，可反复触发，降落后还能
  再起飞）：
  ```bash
  ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 1}"   # 起飞
  ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 2}"   # 降落
  ```
- [ ] 备选方式（跨机 DDS 还没配好时的退路，**只能用一次**——`takeoff_gate`
  节点触发完就退出，降落后没法用这个方式重新起飞）：
  ```bash
  ssh nx01 "docker exec <容器名> touch /tmp/takeoff_go"
  ```
- [ ] 起飞前监控确认（不需要主动干预，确认即可）：`/mavros/state` 里
  `connected: true`、DLIO 里程计收敛、`uwb/pose_abs` 有数据

---

## 阶段 5（可选，后续规划）：网页端地面站控制面板

> 2026-08-15新增，**纯讨论，尚未决定是否实施，不是必须完成的部署步骤**。
> 用来回答"能不能做个网页代替现在的 tmux+curses+`docker exec`"这个问题，
> 记录下来供后续参考。

### 5.1 是什么、需要几个容器

不是一个程序，是"两个后端服务+一个静态前端"拼起来的小系统，建议拆成
**2个容器**：

| 容器 | 内容 | 权限 |
|---|---|---|
| 容器A：`rosbridge` | 扩展 `gcs/` 镜像，加装 `ros-humble-rosbridge-suite`，把话题/service/参数转成WebSocket JSON | 无特殊权限，只需要DDS网络访问（`network_mode: host`） |
| 容器B：设备控制后端 | 自己写的小后端（如FastAPI），负责`docker compose up/down`、查日志，顺带serve前端打包出的静态文件（不用再起第3个容器） | **唯一需要特权访问**——挂`/var/run/docker.sock`或SSH key，是安全边界所在，要单独收紧 |

浏览器本身不在容器里，在操作员自己的笔记本/平板上打开网页。

### 5.2 覆盖范围与阻塞项

| 需求 | 技术路径 | 状态 |
|---|---|---|
| 监控真机/模块状态 | rosbridge订阅话题 | 现成套路，`status_monitor.py`现在做的事搬上网页 |
| 起降/起飞点锁定 | rosbridge转发4.6那两条service call/话题发布 | 已经是标准操作，直接复用 |
| 启动真机端程序 | 容器B的自定义API | 需要新写，功能简单 |
| 设置真机端环境变量参数 | 容器启动时生效，网页管不了运行中容器 | 只能通过"改配置+重启容器"实现 |
| 设置飞控参数 | rosbridge转发`mavros_msgs/srv/ParamSet` | 技术可行，**建议白名单+二次确认，不做成自由参数表** |
| 缩略视频 | MJPEG/WebRTC，浏览器原生支持 | ⚠️ **阻塞**——前视/下视相机尚未集成（阶段8），做不了 |

### 5.3 建议的实施顺序（如果决定做）

- [ ] 先针对**本机仿真的3个容器**（`sim-world`+`flight-stack-nx01/nx02`）
  做出可用版本——`rosbridge`只关心"是否同一DDS域"，不区分本机容器还是
  跨机Jetson，前端代码完全复用，风险更低、能立刻开始不用等真机
- [ ] 验证通过后切到真机场景，主要改动是把 rosbridge/后端服务指向的
  地址从本机换成 Jetson 的 IP
- [ ] 这实质上是替代此前"真机侧改造清单"里搁置的"运维脚本改造"
  （`launch_control.py`/`watch_sim.sh` 去掉 `docker exec`/`docker cp`
  假设）那一项，做成之后这一项可以视为完成

### 5.4 Web vs App 结论

网页端，不建议做原生 App——单/少数操作员内部工具，不需要 App 才有的能力
（离线优先、推送、应用商店分发），网页配合 PWA（加到主屏幕）或后续用
Electron 包一层壳，就能有"像 App"的体验，不需要重新开发一套。

---

## 本清单未覆盖的范围

- 真实 UWB anchor 的现场部署（3-4个已知坐标覆盖起飞区）——这是"试飞场地
  准备"，不是"给飞机装软硬件"，属于阶段6（真实UWB接入验证）的范畴，见
  《真机部署与Docker开发知识梳理》第1.2节路线图。
- 首飞安全协议、预飞检查清单、双机组网试飞——同样属于后续试飞阶段，本
  清单止于"系统能连通、跑起来"，不包含实际试飞流程。
