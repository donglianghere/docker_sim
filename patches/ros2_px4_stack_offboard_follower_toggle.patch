diff --git a/launch/dynus_mavros.launch.py b/launch/dynus_mavros.launch.py
index 9825bf8..5803a18 100644
--- a/launch/dynus_mavros.launch.py
+++ b/launch/dynus_mavros.launch.py
@@ -10,6 +10,26 @@ def generate_launch_description():
     init_x, init_y, init_z = os.environ.get("INIT_X"), os.environ.get("INIT_Y"), os.environ.get("INIT_Z")
     init_roll, init_pitch, init_yaw = os.environ.get("INIT_ROLL"), os.environ.get("INIT_PITCH"), os.environ.get("INIT_YAW")
     veh = os.environ.get("VEH_NAME")
+    # CONTROLLER=px4ctrl/so3ctrl时，flight-stack-entrypoint.sh会额外export
+    # RUN_OFFBOARD_FOLLOWER=false——这个launch文件本来把"发setpoint给PX4的
+    # track_dynus_traj节点"和"repub_odom/mocap_to_livox_frame/静态TF"这些
+    # 跟具体走哪个板外控制器无关的职责捆在同一个LaunchDescription里，两者
+    # 现在要能分开：px4ctrl/so3ctrl接管发setpoint这部分职责时，track_dynus_traj
+    # 必须停摆（不然两边同时喊mavros/setpoint_raw/attitude、mavros/cmd/arming，
+    # 谁的指令生效完全看竞态），但repub_odom这些跟controller无关的部分要
+    # 继续跑（px4ctrl/so3ctrl自己不做DLIO里程计到mavros/vision_pose_cov的转发）。
+    run_offboard_follower = os.environ.get("RUN_OFFBOARD_FOLLOWER", "true").lower() != "false"
+
+    nodes = []
+    if run_offboard_follower:
+        # Run dynus node
+        nodes.append(Node(
+            package='ros2_px4_stack',
+            executable='track_dynus_traj',
+            name='track_dynus_traj_py',
+            namespace=namespace,
+            output='screen',
+        ))
 
     return LaunchDescription([
         # Declare launch arguments
@@ -20,16 +40,7 @@ def generate_launch_description():
         DeclareLaunchArgument('fcu_url', default_value='/dev/ttyACM0:921600'),
         DeclareLaunchArgument('respawn_mavros', default_value='false'),
         DeclareLaunchArgument('odom_type', default_value='livox'),
-
-        # Run dynus node
-        Node(
-            package='ros2_px4_stack',
-            executable='track_dynus_traj',
-            name='track_dynus_traj_py',
-            namespace=namespace,
-            output='screen',
-        ),
-
+    ] + nodes + [
         # Static transforms
         # 只保留 world_mocap -> {veh}/init_pose 这一条——mocap_to_livox_frame.py
         # 的 get_gf_to_af_tf() 真的用 tf2 查了 "{veh}/init_pose" <-> "world_mocap"，
