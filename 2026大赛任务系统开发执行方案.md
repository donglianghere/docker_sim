# 2026大赛任务系统开发执行方案

> 2026-09-06。本文档是《2026大赛任务场景选手定位与完善方案.md》
> （下称"设计方案"）的可执行版本——按阶段列出具体要创建/修改的文件、
> 归属容器、验证方法。每个阶段结束都有独立可验证的成果，不需要等
> 全部完成才能看到效果。**真机侧硬件排查（电机/电调故障）与本文档
> 的软件开发工作并行推进，互不阻塞**——阶段0~10全部在仿真侧完成，
> 阶段11（真机移植）才需要真机重新可用。

---

## 阶段0：准备工作

- [ ] 确认`docker_real/docker_sim/`参考快照可用（`docker_real/
  SYNC_NOTES.md`有同步范围说明），需要更新时重新跑一遍其中记录的
  `rsync`命令。
- [ ] **锁定竞赛默认组合**：`PLANNER=ego_planner`（`mighty`没有可
  编程航点接口，见设计方案5.1）、`CONTROLLER=px4ctrl`或`so3ctrl`
  （支持反复起降，`ros2_px4_stack`架构上只能起飞一次）、
  `LOCALIZATION_SOURCE=uwb_slam`（仿真阶段用于验证含DLIO的完整链路；
  也可视调试阶段需要临时切`gt`跳过SLAM）。
- [ ] 在`docker_sim/src/`下新建本次任务专属的ROS2包骨架（建议命名
  `contest_mission`，`ament_python`包，参照`uwb_origin_bridge`的目录
  结构：`contest_mission/`+`launch/`+`config/`+`resource/`+
  `setup.py`+`setup.cfg`+`package.xml`）——阶段2~7的大部分新节点都
  放在这个包里，避免每个功能都单独建一个包造成管理负担。

**验证**：`colcon build --packages-select contest_mission`能空跑
成功（先建一个只有`__init__.py`的空包占位）。

---

## 阶段1：场景资产（仿真侧，独立于真机）

对应设计方案第三章。

- [ ] 1.1 新建`staging/mighty_ws_src/mighty/worlds/fire_drill_room.
  world`（**不覆盖`simple_room.world`**），复用`simple_room.world`
  的地面/天花板/物理参数结构（20×20×5米→改成20×25×6米，注意天花板
  z坐标、四面墙尺寸都要跟着改）。
- [ ] 1.2 起降点/物资点/着火点地面标记：先用纯色薄圆盘占位（
  `Gazebo/Green`起飞、`Gazebo/Yellow`降落、`Gazebo/Red`着火点），
  只有物资点需要贴真实可解码的AprilTag/二维码（见1.4）。
- [ ] 1.3 独立障碍物圆柱（直径0.5米，高6米）+ 3根方形/矩形立柱
  （截面建议0.6×0.6米，高6米，两两间距>2米），坐标写入独立配置文件
  （不写死在world里，方便后续参数化/随机化）。
- [ ] 1.4 生成真实可解码的AprilTag图片——本机已验证`opencv-
  python 4.11`自带`cv2.aruco.DICT_APRILTAG_36H11`，可以直接用
  `cv2.aruco.generateImageMarker()`生成有效tag，不需要联网下载。
  物资点和火情立柱各生成一个不同ID的tag（比如ID 0=物资点，ID 1=
  火情立柱），保存为PNG。
- [ ] 1.5 写一个Ogre材质脚本（`worlds/media/materials/scripts/
  contest_markers.material`+对应`textures/`目录存放1.4生成的PNG），
  参照现有`file://media/materials/scripts/gazebo.material`的引用
  方式；需要把这个新`media/`目录加进`Dockerfile.sim-world`的
  `GAZEBO_RESOURCE_PATH`（不是`GAZEBO_MODEL_PATH`，两者含义不同，
  见`README.md`"编译内存不够"章节附近关于Gazebo环境变量的历史记录）。
- [ ] 1.6 接入`base_mighty.launch.py`的`world_mapping`字典（新增
  `'fire_drill_room': 'fire_drill_room.world'`）+ `Dockerfile.
  sim-world`里`COPY`这个新world文件+对应patch（新建`patches/
  mighty_fire_drill_room_world.patch`，走项目既定的"编辑staging+
  git diff生成+git checkout还原"流程）。

**验证**：`WORLD_ENV=fire_drill_room ./start.sh`起两机，Gazebo/RViz
里能看到全部道具、双机能正常spawn，静止5分钟不崩溃（冒烟测试标准
参照README里"三路相机稳定性冒烟测试"的要求）。

---

## 阶段2：仿真侧感知栈（对齐真机`vision-stack`接口）

对应设计方案4.2、六章。

- [ ] 2.1 先解决相机链路：新建`patches/mighty_generic_camera_
  namespace_fix.patch`，修复`generic_camera.urdf.xacro`的多机命名空间
  问题（上游`TODO: Not sure why but can't put them in namespace`），
  在NX01挂前视+下视两路，NX02视需要挂下视一路。**先做纯稳定性冒烟
  测试**（起飞前静止跑5分钟不崩溃）再进入下一步。
- [ ] 2.2 `contest_mission`包里新增`qr_apriltag_detect_node.py`：
  订阅相机`sensor_msgs/Image`话题，用`pyzbar`解码二维码/用
  `apriltag`库（Python绑定，或直接vendor`apriltag_ros`的C++实现，
  二选一，优先选Python绑定以减少编译依赖）解码AprilTag，检测结果
  发布**`vision_msgs/Detection2DArray`**——这是跟真机`vision-stack`
  对齐的关键，不需要自定义消息包，直接`apt install ros-humble-
  vision-msgs`即可。
- [ ] 2.3 把这个节点接入`docker/Dockerfile.flight-stack`（依据设计
  方案5.3的容器归属原则，归`flight-stack-nx01/nx02`，不需要独立
  容器），新增依赖`python3-pyzbar`（apt装，同真机做法）。

**验证**：手动`ros2 topic echo /NX01/vision/detections`能看到物资点
和火情立柱两个AprilTag被正确识别，`bbox`/`class_id`字段有意义。

---

## 阶段3：`position_cmd`中继/仲裁节点

对应设计方案5.2。这是精降和环绕共用的基础设施，建议先做，阶段4/6
都依赖它。

- [ ] 3.1 `contest_mission`包新增`position_cmd_relay_node.py`：默认
  订阅`traj_server`发布的`position_cmd`原样转发到px4ctrl/so3ctrl
  订阅的话题名，新增一个`~/relay_mode`参数（`normal`/`precision_
  land`/`orbit_yaw_override`）通过service动态切换。
- [ ] 3.2 `precision_land`模式：改为订阅精伺服节点（阶段6.2）发布的
  `position_cmd`格式消息，完全接管转发。
- [ ] 3.3 `orbit_yaw_override`模式：仍订阅`traj_server`的
  `position_cmd`，但用当前里程计位置+一个"环绕中心点"参数实时算
  `yaw = atan2(cy-y, cx-x)`，覆写`yaw`/`yaw_dot`两个字段后转发，其余
  字段透传。
- [ ] 3.4 新增一个`~/set_relay_mode`service（或订阅一个模式话题），
  供任务状态机（阶段7）调用切换模式。
- [ ] 归入`flight-stack-nx01/nx02`容器。

**验证**：
1. `normal`模式下常规导航行为跟改造前完全一致（回归测试，比如重跑
   README里"起飞-跟踪-降落"验证流程，确认没有引入延迟或行为变化）。
2. 手动切`orbit_yaw_override`模式+给一个虚构中心点，观察飞机沿现有
   航点飞行时机头朝向确实指向该点，而不是沿轨迹切线方向。

---

## 阶段4：立柱检测 + 环绕飞行

对应设计方案3.2、5.1。依赖阶段3。

- [ ] 4.1 `contest_mission`包新增`pillar_detector_node.py`：订阅
  规划器已经在维护的occupancy grid话题，在已知房间边界内裁剪，做
  柱状聚类（简单方法：按栅格连通域+高度延伸判断，不需要引入重的
  点云聚类库），按直径阈值过滤掉独立障碍物圆柱，输出候选立柱坐标
  列表。
- [ ] 4.2 新增`orbit_flight_helper.py`（纯几何生成，不是ROS节点，
  是给任务逻辑调用的辅助函数）：输入圆心+半径，输出一圈离散航点。
- [ ] 4.3 环绕飞行的任务逻辑（在阶段7的任务状态机里实现，不是独立
  节点）：切`position_cmd_relay`到`orbit_yaw_override`模式→用4.2
  生成的航点走`waypoint_queue`接口→同时订阅阶段2的检测结果话题→
  一旦检测到目标AprilTag立即中断环绕、退出该模式。

**验证**：仿真里对3根立柱手动触发检测，聚类结果应该正好是3个、
且不包含独立障碍物圆柱；环绕飞到贴标签那根立柱时能在飞完整圈之前
提前停止。

---

## 阶段5：地面着火点搜索

对应设计方案3.2。

- [ ] 5.1 新增`ground_scan_helper.py`（纯几何生成）：输入房间边界
  矩形顶点、相机FOV、飞行高度，输出弓字形固定航点序列（保证相邻
  扫描行有重叠）。
- [ ] 任务逻辑（阶段7实现）：走这条固定路径的同时持续订阅阶段2的
  检测结果，命中即中断剩余航点。

**验证**：生成的航点序列在RViz里可视化确认能覆盖整个20×25米区域；
仿真飞一遍确认扫描过程中经过着火点标记附近时能被检测到。

---

## 阶段6：执行器动作 + 精准降落

对应设计方案5.1、5.2、六章2.9条目。

- [ ] 6.1 `contest_mission`包新增`actuator_action_node.py`：提供一个
  `perform_action(action_name)`service，内部只是记录状态+做一个约定
  好的飞行动作（比如短暂升降代表"投放"），不做任何物理建模。
- [ ] 6.2 新增`precision_servo_node.py`：订阅阶段2的AprilTag检测
  结果（相对相机的位姿），换算成机体系水平偏差，用简单P/PID算出
  修正后的`position_cmd`（位置微调+固定速率下降），发布给阶段3的
  中继节点（`precision_land`模式消费这个话题）。
- [ ] 任务逻辑（阶段7）：粗对准（走`waypoint_queue`到降落点上方）→
  切`position_cmd_relay`到`precision_land`模式→精伺服接管→水平误差
  收敛后开始下降→接近地面/`vehicle_land_detected`确认后切
  `AUTO.LAND`或直接disarm→中继节点切回`normal`模式。

**验证**：仿真里手动走一遍粗对准+精降全流程，最终水平误差应该收敛
到设计方案没有特别要求的情况下<0.1~0.2米量级（可参照真机
`apriltag_stack`的现成参数做起点）。

---

## 阶段7：任务状态机 + 自动裁判 + 场景重置

对应设计方案4.3节的"任务语义层"接口、五章5.1、5.3（这三个节点归属
`sim-world`容器，不是`flight-stack`）。

- [ ] 7.1 定义任务状态话题：`contest_mission`包新增
  `MissionState`消息或直接用`std_msgs/String`+约定的字符串枚举
  （`idle/searching/loading/enroute/aiming/done`），避免过早引入
  自定义消息包的编译成本（呼应设计方案4.3的"不用为每个任务定义
  `.msg`"建议）。
- [ ] 7.2 新增`mission_judge_node.py`（`sim-world`容器）：订阅
  Gazebo模型状态（`/gazebo/model_states`一类接口）+飞机位姿，按赛题
  判据（比如"水平距离≤1米+朝向误差≤阈值，连续满足3秒"）自动判定
  任务完成/失败，发布到一个话题。
- [ ] 7.3 新增`scenario_reset_node.py`（`sim-world`容器）：提供一个
  service，把飞机瞬移回起飞点（`gazebo_msgs/SetModelState`一类接口）、
  清空任务状态机、重新生成阶段1.3里目标物的随机坐标（如果决定要
  随机化）。
- [ ] 7.4 用阶段2~6的节点+7.1~7.3拼出完整的双机任务状态机（放在-
  `contest_mission`包，作为一个或两个"参考任务逻辑"节点——这一步
  同时也是阶段10要用的参考实现的雏形）。

**验证**：完整跑一次任务（找立柱→找火情→取物资→精降/动作→降落），
裁判节点应该能自动输出正确的完成判定；调用场景重置service后飞机
位置/任务状态应该恢复到初始，不需要重启容器。

---

## 阶段8：选手SDK + 网关

对应设计方案4.3、五章5.3。

- [ ] 8.1 新增网关服务（独立容器，仿`gcs/backend`的两容器模式）：
  一个轻量HTTP/WebSocket服务，内部用`roslibpy`连接现有`rosbridge`，
  只暴露语义化接口（`POST /takeoff`、`POST /fly_to`、`POST /orbit`、
  `GET /detect_target`、`POST /perform_action`、`GET /mission_
  status`……），不透传完整ROS话题空间。
- [ ] 8.2 新增选手Python SDK包（比如`contest_sdk`，纯Python，
  `pip install`本地可安装的小包）：封装对8.1网关的HTTP/WebSocket
  调用，提供`connect()`/`takeoff()`/`fly_to(x,y,z)`/
  `orbit_and_scan(center, radius)`/`detect_target()`/
  `precision_land()`/`perform_action(name)`/`get_pose()`/
  `mission_status()`这组函数，内部处理好合理的超时/重试。
- [ ] 8.3 SDK内部读一个环境变量/配置文件（比如`CONTEST_TARGET=sim/
  hw`）决定连哪个网关地址，选手代码不需要写判断分支。

**验证**：写一个几十行的最小示例脚本（起飞→飞到一个点→降落），
用SDK跑通，全程不出现任何ROS2/Docker相关的调用或报错细节。

---

## 阶段9：GCS修改

对应设计方案第八章。

- [ ] 9.1 `gcs/backend/app.py`新增鉴权（区分"监考/教练"和"选手"两种
  来源，不需要复杂的用户体系，能分清楚谁在发指令即可）。
- [ ] 9.2 落地仿真/真机模式互斥切换（切换时真实断开/重连对应DDS
  后端，不是只做前端过滤）。
- [ ] 9.3 新增场景重置/系统自检按钮（调用阶段7.3的service+一个
  简单的传感器状态自检汇总）。
- [ ] 9.4 前端新增任务状态展示（订阅阶段7.1的任务状态话题）+
  `position_cmd`中继模式的可见性（是否处于`precision_land`/
  `orbit_yaw_override`等非常规模式）。

**验证**：按项目现有GCS验证方式（`curl`/`gcs_ui_sim.py`一类的
rosbridge模拟脚本）逐项确认新接口可用。

---

## 阶段10：参考实现 + 端到端冒烟测试

- [ ] 10.1 用阶段8的SDK写一份完整的参考选手方案（双机协同：侦察机
  找立柱+找火情+上报，行动机取物资+飞向火情+精准动作），作为"这套
  接口设计到底好不好用"的验证，也作为以后回归测试用例保留。
- [ ] 10.2 统计这份参考实现的代码行数，跟设计方案第二章"300~500行"
  的估计做对照，如果明显超出，回头检查是不是某个环节的SDK封装
  不够到位（选手代码里出现了不该有的底层细节）。
- [ ] 10.3 端到端跑几轮完整任务，验证阶段7的场景重置在真实使用节奏
  下够不够快、够不够干净。

---

## 阶段11：真机移植（等电机/电调硬件问题解决、真机重新可用后）

- [ ] 11.1 `vision-stack`真机已有且接口本来就对齐（`vision_msgs/
  Detection2DArray`），不需要重写，只需要确认`docker-compose.hw.yml`
  里`vision-stack`服务照常起来。
- [ ] 11.2 阶段3的`position_cmd`中继/仲裁节点、阶段4的立柱检测/环绕、
  阶段5的地面搜索、阶段6的精降/执行器节点，原样移植到`flight-stack-
  hw`镜像（走`DEPLOY_TARGET=hw`分支，参照项目现有entrypoint的
  sim/hw分支写法）。
- [ ] 11.3 阶段8的网关/SDK改指向真机地址（读`CONTEST_TARGET=hw`）。
- [ ] 11.4 场景重置服务（阶段7.3）在真机没有对应物（物理世界不能
  程序化瞬移），改成"提示工作人员把飞机搬回标记点+跑传感器自检"的
  人工流程，裁判节点（阶段7.2）真机侧的实现需要另外设计（真实检测
  结果驱动的判定，或人工判定）。
- [ ] 11.5 双机协同真机化验证——此前从未做过，需要独立的验证计划，
  不能假设仿真验证过就等价于真机可行。

---

## 依赖关系速览

```
阶段0 → 阶段1（场景） ──┬─→ 阶段2（感知）──┐
                        │                  ├─→ 阶段4（立柱+环绕）──┐
阶段0 → 阶段3（position_cmd中继）──────────┘                        ├─→ 阶段7（状态机/裁判/重置）─→ 阶段8（SDK/网关）─→ 阶段9（GCS）─→ 阶段10（端到端）
                                                                    │
                        阶段2 ──────────────→ 阶段5（地面搜索）─────┤
                        阶段2+阶段3 ─────────→ 阶段6（精降/执行器）──┘
```

真机侧硬件排查全程与阶段0~10并行，只在进入阶段11时才成为前置条件。
