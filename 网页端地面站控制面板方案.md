# 网页端地面站控制面板方案

> 2026-08-15。这是一份**纯设计方案，尚未决定是否实施、没有创建任何代码/
> 容器定义文件**。整理自与《真机部署操作清单》阶段5相关的几轮讨论，单独
> 成文方便后续参考/评审。背景部署方案见同目录《真机部署与Docker开发
> 知识梳理》和《真机部署操作清单》。

---

## 一、背景与目标

现有的 `launch_control.py`/`watch_sim.sh` 这套 tmux+curses+`docker exec`
操作台，对"仿真里3个容器全在一台机器上"这个假设依赖很深——真机分布式
部署（GCS 和飞机物理分离）之后，这套东西大部分功能会失效（详见
DEBUG_JOURNAL.md 2026-08-12 那次评估）。

目标：做一套**网页控制面板**，同时满足：
- 替代现有仿真操作台的日常调试需求
- 能直接用于真机单机/双机部署，不需要另外维护一套工具
- 起降、状态监控、参数设置、启停容器这几类操作都能覆盖

---

## 二、总体架构：2 个容器

不是一个程序，是"两个后端服务 + 一个静态前端"拼起来的小系统：

| 容器 | 内容 | 权限 |
|---|---|---|
| **容器A：`rosbridge`** | 扩展现有 `gcs/` 镜像，加装 `ros-humble-rosbridge-suite`，把 ROS2 话题/service/参数转成 WebSocket JSON | 无特殊权限，只需要 DDS 网络访问（`network_mode: host`） |
| **容器B：设备控制后端** | 自己写的小后端（如 FastAPI），负责 `docker compose up/down`、查日志这类 rosbridge 管不了的操作，顺带 serve 前端打包出的静态文件（不用再起第3个容器） | **唯一需要特权访问**——挂 `/var/run/docker.sock` 或 SSH key，是安全边界所在，要单独收紧、独立审计 |

浏览器本身不在容器里，在操作员自己的笔记本/平板上打开网页。

**为什么不是1个容器**：`rosbridge` 需要完整 ROS2 Humble 环境（几百MB+），
更新频率低、纯粹是桥接组件；容器B 持有特权访问、代码是自己写的业务逻辑、
迭代频率高。两者生命周期和权限需求都不一样，绑在一起没有好处，只会让
"改个按钮"和"动特权访问代码"共享同一次镜像重建。

---

## 三、覆盖范围与阻塞项

| 需求 | 技术路径 | 状态 |
|---|---|---|
| 监控真机/模块状态 | rosbridge 订阅话题 | 现成套路，`status_monitor.py` 现在做的事搬上网页 |
| 起降/起飞点锁定 | rosbridge 转发 service call/话题发布 | 已经是标准操作（见第七章），直接复用 |
| 启动真机端程序 | 容器B 的自定义 API | 需要新写，功能简单 |
| 设置真机端环境变量参数 | 容器启动时生效，网页管不了运行中容器 | 只能通过"改配置+重启容器"实现 |
| 设置飞控参数 | rosbridge 转发 `mavros_msgs/srv/ParamSet` | 技术可行，**建议白名单+二次确认，不做成自由参数表** |
| 缩略视频 | MJPEG/WebRTC，浏览器原生支持 | ⚠️ **阻塞**——前视/下视相机尚未集成，做不了 |

---

## 四、核心设计原则：两条独立的轴

这是这份方案里最容易漏掉、也是最关键的一点：**"几台物理机"和"几架飞机"
是两条独立的轴**，只在真机场景下才会重合。

| 轴 | 决定什么 | 仿真场景 | 真机场景 |
|---|---|---|---|
| **网络拓扑轴**（几台物理机） | CycloneDDS 配置（锁`lo`/静态Peer/Discovery Server） | 永远1台机器，不管模拟单机还是双机，**配置不变** | 单机=2台机器（GCS+1Jetson），双机=3台机器，**配置要跟着变** |
| **机队规模轴**（几架飞机） | `docker compose` 起哪些服务 + `NUM_AGENTS`/`LOCALIZATION_SOURCE` | 单机=只起`sim-world`+`flight-stack-nx01`；双机=三个都起 | 单机=1台Jetson跑`single_uwb_slam`；双机=2台Jetson各跑`uwb_slam`+`NUM_AGENTS=2` |

真机场景两条轴重合（一台 Jetson 必然对应一架飞机），仿真场景不重合
（一台机器可以模拟1或2架飞机）——这就是为什么"网络配置"和"机队配置"
必须设计成两套独立的配置文件，不能合并成一个"场景"变量对应一份配置。

---

## 五、四种部署场景

`SCENARIO` 环境变量取4个值，对应4份机队配置（`fleet/`），但只对应3份
网络配置（`cyclonedds/`，`sim-single`/`sim-dual` 共用一份）——数量不
对等是刻意的，不是遗漏。

### 5.1 目录结构

```
gcs/
├── Dockerfile                  # 容器A：rosbridge
├── backend/                    # 容器B：设备控制后端 + 前端静态文件
│   ├── Dockerfile
│   ├── app.py                  # 起降/标定转发、docker/SSH控制接口
│   ├── fleet/
│   │   ├── sim-single.yaml     # services: [sim-world, flight-stack-nx01]
│   │   │                       # env: NUM_AGENTS=1, LOCALIZATION_SOURCE=single_uwb_slam
│   │   ├── sim-dual.yaml       # services: [sim-world, flight-stack-nx01, flight-stack-nx02]
│   │   │                       # env: NUM_AGENTS=2, LOCALIZATION_SOURCE=uwb_slam
│   │   ├── hw-single.yaml      # target: 1台Jetson(SSH)，docker-compose.hw.yml
│   │   │                       # env: NAMESPACE=NX01 AGENT_INDEX=1 NUM_AGENTS=1
│   │   │                       #      LOCALIZATION_SOURCE=single_uwb_slam
│   │   └── hw-dual.yaml        # targets: 2台Jetson(SSH)各一份
│   │                           # env: NAMESPACE=NX01/NX02 AGENT_INDEX=1/2 NUM_AGENTS=2
│   │                           #      LOCALIZATION_SOURCE=uwb_slam
│   └── static/                 # 前端build产物
├── frontend/                   # 前端源码（React/Vue），单独开发，build后扔进backend/static
├── cyclonedds/
│   ├── sim.xml                 # sim-single、sim-dual 都用这一份，锁lo
│   ├── hw-single.xml.example   # 真实网卡+1个Peer（已有模板，纳入这套体系）
│   └── hw-dual.xml.example     # Discovery Server配置，还没写
└── docker-compose.yml          # 按 SCENARIO 挂载对应的 fleet/cyclonedds 配置
```

### 5.2 切场景

```bash
SCENARIO=sim-single docker compose -f gcs/docker-compose.yml up
SCENARIO=sim-dual   docker compose -f gcs/docker-compose.yml up
SCENARIO=hw-single  docker compose -f gcs/docker-compose.yml up
SCENARIO=hw-dual    docker compose -f gcs/docker-compose.yml up
```
容器B 读 `SCENARIO` 决定用哪份 `fleet/*.yaml`；容器A 读 `SCENARIO` 的
**前缀**（`sim-`/`hw-single`/`hw-dual`）决定挂哪份 `cyclonedds/*.xml`。

---

## 六、前端设计原则：动态发现，不硬编码

前端不写"如果是双机就……"这类场景分支，只做一件事：**订阅 DDS 域里
实际存在的命名空间，看到几架就渲染几张飞机卡片**。这条原则天然覆盖了
全部4种场景（甚至以后扩展到3机、4机也不用改前端代码），也是为什么
"哪些是真正要新写的代码"里前端本身工作量相对固定、不随场景数量增长。

---

## 七、地面站操作命令参考（已验证可行、可直接被 rosbridge 转发）

以下命令来自真机部署阶段单独梳理的起降流程，只要跨机 DDS 打通，
可以在 GCS 本机直接执行，不需要 SSH：

```bash
# 起飞点一键锁定（起飞前，飞机静止不动时执行一次）
ros2 service call /NX01/set_origin_from_uwb std_srvs/srv/Trigger

# 起飞/降落（长驻订阅，可反复触发）
ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 1}"   # 起飞
ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 2}"   # 降落

# 备选（跨机DDS没配好时的退路，takeoff_gate只能触发一次）
ssh nx01 "docker exec <容器名> touch /tmp/takeoff_go"
```
网页上的"起飞"/"降落"/"标定原点"按钮，本质就是容器B把这几条命令转成
API 调用，通过 rosbridge 发出去。

---

## 八、详细实施步骤

### 8.1 技术选型（这次补充确定，之前只写了"如FastAPI"这种待定语气）

| 用途 | 选型 | 理由 |
|---|---|---|
| 后端框架 | Python + FastAPI + uvicorn | 跟这个项目其它ROS2桥接节点/脚本一致用Python，不引入新语言 |
| 后端↔容器A通信 | **`roslibpy`**（PyPI包，rosbridge协议的纯Python客户端） | ⚠️ 关键决定：**不用`rclpy`**——如果后端直接用`rclpy`，就必须把完整ROS2 Humble环境装进容器B，等于把"轻量、无特殊依赖"这条设计原则废掉了。`roslibpy`纯Python实现rosbridge协议，`pip install`即可，容器B不需要任何ROS2组件 |
| 前端框架 | Vue 3 + Vite | 小型面板场景下比React样板代码少，上手快；如果团队更熟悉React也完全可行，`roslib`（rosbridge的JS客户端）不绑定任何框架 |
| 前端↔容器A通信 | `roslib`（npm包，即roslibjs） | rosbridge官方JS客户端 |
| 机队远程控制 | 直接 `subprocess` 调 `ssh` 命令行 | 不引入`paramiko`这类SSH库——复用《真机部署操作清单》阶段0已经配好的`~/.ssh/config`别名（`ssh nx01`），跟4.6节的操作命令是同一套凭证/别名，不用重新管理一套SSH认证 |

### 8.2 分阶段实施步骤

> 每个阶段都有明确的"验证通过"标准，前一阶段验证不过不建议进入下一阶段
> ——这是个小项目，但没必要跳步。
>
> ⚠️ **2026-08-20更新**：阶段1/3/4/5实际落地时对原方案做了简化，跟下面
> 写的步骤有出入——没有用Vue+Vite（改成一个不需要构建步骤的静态
> `gcs/frontend/index.html`，直接引入`roslib.min.js`），也没有起阶段2
> 那个独立容器B/FastAPI后端（阶段5这三个动作`rosbridge`本身就能直接从
> 浏览器发起，不需要后端转发；容器B真正必要的是阶段6机队启停的
> `subprocess`/`ssh`能力和阶段7参数白名单的服务端校验，这两件事到了
> 对应阶段再补）。下面的checkbox保留原文不改，实际完成情况见每个阶段
> 末尾的"实际完成"备注。

**阶段0：开发环境准备**
- [ ] 确认本机有 Node.js（`node -v`，没有装 LTS 版，前端构建用）
- [ ] 确认本机有 Python 3 + pip（本地跑一份 backend 方便调试，不进容器）
- [ ] Docker 环境已具备（前面几轮工作已确认）

**阶段1：容器A（rosbridge）——独立验证，不涉及任何自定义代码**
- [ ] 改 `gcs/Dockerfile`：加 `RUN apt-get install -y ros-humble-rosbridge-suite`
- [ ] 改 `gcs/docker-compose.yml`：起容器时跑
      `ros2 launch rosbridge_server rosbridge_websocket_launch.xml`，
      暴露 `9090` 端口
- [ ] 新建 `gcs/cyclonedds/sim.xml`（复制现有 `sim-world`/`flight-stack`
      锁 `lo` 的那份 CycloneDDS 配置逻辑，单独抽成文件供 gcs 容器挂载）
- **验证**：起本机仿真（`sim-single` 或 `sim-dual` 都行）+ gcs 容器，用
  `wscat -c ws://localhost:9090` 或几行 `roslibpy` 脚本连上，能订阅到
  `ros2 topic list` 里看得到的话题
- **实际完成（2026-08-18落地，2026-08-20端到端验证）**：`gcs/Dockerfile`
  装了`ros-humble-rosbridge-suite`，`gcs/entrypoint.sh`同时起
  `rosbridge_websocket`（9090）和静态文件服务（8080）。没有另建
  `gcs/cyclonedds/sim.xml`——现有`gcs/cyclonedds_gcs.xml`是真机部署
  专用（绑真实网卡IP），本地单机仿真联调时需要临时换成`lo`接口，
  见DEBUG_JOURNAL.md 2026-08-20"WiFi网卡DDS丢包"那次记录，这是本地
  测试时的已知操作步骤，不是缺一个文件。

**阶段2：容器B 后端骨架**
- [ ] 新建 `gcs/backend/`：`requirements.txt`
      （`fastapi`/`uvicorn[standard]`/`roslibpy`/`pyyaml`）、`Dockerfile`
      （`python:3.11-slim` 基础镜像，**不需要 ROS2**）、`app.py`
      （最小应用：`/healthz` 端点 + serve `static/` 目录）
- [ ] 新建 `gcs/backend/fleet/{sim-single,sim-dual,hw-single,hw-dual}.yaml`
      （内容见第五章目录结构那段）
- **验证**：`docker compose up backend`，`curl localhost:8000/healthz`
  返回 200
- **实际完成（2026-08-20落地，比原计划提前——不是阶段6触发的，是用户
  直接提出"把网络IP配置+飞行栈重要参数也放进GCS网页"这个需求）**：
  `gcs/backend/`（`app.py`+`Dockerfile`+`requirements.txt`）建起来了，
  但跟这里写的骨架不完全一样——没有`roslibpy`（这个后端不碰任何ROS2
  话题/service，起降那些还是走前端直连rosbridge，这个后端只管"改配置
  文件+重启"这类浏览器做不到的操作）、没有`fleet/*.yaml`（`nx01`/
  `nx02`两个SSH目标直接写死在`app.py`的`HW_TARGETS`字典里，规模小
  没必要抽成配置文件）、也没有serve`static/`目录（前端继续走容器A
  自己的`http.server`，两个容器分工没变）。实际做的是两个具体接口：
  `GET/POST /api/gcs-network`（本地改`cyclonedds_gcs.xml`+
  `docker restart gcs`自己）和`GET/POST /api/hw-params/{ns}`（SSH改
  Jetson上的`.env`+远程重建`flight-stack-hw`，8个规划/避障调参白名单，
  明确排除模式选择/机身份/硬件接线这几类参数），详见DEBUG_JOURNAL.md
  2026-08-20记录。⚠️当前完全没有鉴权，只适合受信内网。

**阶段3：前端骨架**
- [ ] `gcs/frontend/`：`npm create vite@latest . -- --template vue`
- [ ] `npm install roslib`
- [ ] `App.vue` 里 `new ROSLIB.Ros({url: 'ws://<gcs-ip>:9090'})`，订阅
      任意一个话题打印到 console，验证链路通不通
- **验证**：`npm run dev` 本地跑，浏览器 console 能看到实时话题数据
- **实际完成（2026-08-18落地）**：没用Vue/Vite，改成`gcs/frontend/
  index.html`一个静态文件+`roslib.min.js`（build阶段`curl`进镜像，不
  依赖运行时CDN），`new ROSLIB.Ros(...)`直接写在`<script>`里。

**阶段4：状态监控面板（打通 `sim-single` 场景）**
- [ ] 前端调用 rosbridge 自带的 `rosapi`（`/rosapi/topics` 服务）枚举
      当前话题列表，正则提取命名空间（`/NX01/...` → `NX01`）——这一步
      就是第六章"动态发现"原则的具体实现
- [ ] 每个命名空间订阅关键状态话题：`mavros/state`、
      `dlio/odom_node/odom`、`uwb/pose_abs`
- [ ] 渲染成卡片组件
- **验证**：跑 `sim-single`，网页显示 1 张 `NX01` 卡片，字段随仿真运行
  实时更新
- **实际完成（2026-08-18落地，2026-08-20端到端验证通过）**：`discover()`
  函数按方案做的，`STATUS_TOPICS`常量正是这三个话题，`renderCard()`
  渲染卡片。2026-08-20第一次真正端到端验证（之前只做过静态代码审查），
  过程中发现并修好了本地仿真联调必需的DDS网络配置问题（见阶段1备注）。

**阶段5：起降与标定控制**
- [ ] `backend/app.py` 加三个接口：`POST /api/{ns}/takeoff`、
      `POST /api/{ns}/land`、`POST /api/{ns}/lock-origin`——内部用
      `roslibpy` 连容器A，分别对应第七章的 `TakeoffLand` 话题发布和
      `Trigger` service call
- [ ] 前端加三个按钮，调用这几个接口
- **验证**：`sim-single` 场景下点"起飞"，仿真飞机真的起飞（复现之前
  手动敲 `ros2` 命令验证过的效果，这次从网页触发）
- **实际完成（2026-08-20）**：没有加后端接口——`gcs/frontend/index.html`
  的每张卡片直接加了三个按钮，用`ROSLIB.Service`/`ROSLIB.Topic`直接对
  rosbridge发起调用，不经过容器B。起飞按钮有`confirm()`二次确认，降落/
  锁定起飞点不加（安全/幂等操作）。**验证方式**：没有真实浏览器（用户
  选择跳过Claude in Chrome扩展安装），用一个手写Python脚本（跟roslib.js
  同一套rosbridge v2协议，模拟浏览器按钮点击的真实请求）代替，三个
  按钮全部验证通过：起飞→`armed:true`+`OFFBOARD`，降落→`armed:false`+
  `AUTO.LOITER`，锁定起飞点→service正常返回成功。

**阶段6：机队启停控制**
- [ ] `backend/app.py` 加 `POST /api/fleet/start`、`POST /api/fleet/stop`
      ——读取当前 `SCENARIO` 对应的 `fleet/*.yaml`：`sim-*` 场景本地
      `subprocess` 跑 `docker compose up/down`（容器B需要挂载
      `/var/run/docker.sock` + 装 docker CLI 客户端）；`hw-*` 场景
      `subprocess` 跑 `ssh <目标别名> "docker compose -f
      docker-compose.hw.yml up/down"`
- [ ] 前端加启停按钮 + 简单日志滚动窗口（轮询或转发 `docker logs`）
- **验证**：网页点启动，本机仿真容器真的从无到有起来
- **实际完成（2026-08-20落地，语法+仿真侧本地验证通过，待build+真机
  端到端验证）**：接口路径跟没按`fleet/*.yaml`+`POST /api/fleet/start`
  这个原方案设计——`nx01`/`nx02`直接写死在已有的`HW_TARGETS`（规模小
  没必要抽配置文件，跟阶段2备注同样的理由），拆成
  `GET/POST /api/fleet/sim/{status,start,stop}`（仿真三容器）+
  `GET/POST /api/fleet/hw/{ns}/{status,start,stop}`（真机按机身分开，
  不是"一个fleet按钮控制全部"，方便只重启出问题的那台Jetson）。**仿真侧
  刻意偏离原方案**：没有用`docker compose up/down`，改成对已存在容器
  直接`docker start`/`docker stop`——原因和sibling-container路径挂载
  复杂度见TODO.md第17项。没有做日志滚动窗口（这次没做，如果以后需要
  可以加`GET /api/fleet/{sim|hw}/logs`转发`docker logs`/`ssh ... docker
  compose logs`）。前端新增独立的"🚀 机队启停"面板（`toggleFleetPanel`/
  `fleetCtl`/`renderFleetPanel`），跟已有的GCS网络设置面板、每张卡片的
  调参面板是同一种"折叠面板+按需加载状态"模式。**已验证**：`app.py`/
  `index.html`内联JS语法检查；仿真`docker start`/`stop`两个方向直接对
  这台开发机上真实存在的三个容器（`docker_sim-sim-world-1`等）完整跑过
  一轮（`exited`→`running`→`exited`，全部符合预期）。**未验证**：还没
  经过重新build的`gcs-backend`镜像（当前跑着的容器还是旧`app.py`）；
  没有真实/模拟浏览器点击过新面板；`hw`那组接口这台开发机没有
  `nx01`/`nx02`SSH别名，SSH路径本身没法端到端测，复用的是阶段2已验证
  过的`_ssh_run`同一套逻辑。

**阶段7（可选）：飞控参数白名单编辑**
- [ ] `backend/app.py` 加 `GET/PUT /api/{ns}/params/{name}`，白名单校验
      （只允许改规划/避障这类非安全关键参数，明确排除
      `COM_DISARM_PRFLT` 这类失控保护参数，见第三章的提醒）
- [ ] 前端加参数表单 + 二次确认弹窗

**阶段8：切到真机场景验证**
- [ ] `hw-single`：真机部署后设 `SCENARIO=hw-single`，验证同一套前后端
  代码不改一行，只换 `cyclonedds`/`fleet` 配置就能工作——这是检验
  "配置驱动场景切换"这个核心设计是否成立的关键一步
- [ ] `hw-dual`：补 `cyclonedds/hw-dual.xml`（Discovery Server 配置，
  目前还没写），验证双机

**阶段9：打磨收尾**
- [ ] rosbridge websocket 断线重连——真实网络环境下这是最容易被忽略
  但最影响使用体验的一块，前端要有明确的"连接中断/正在重连"状态提示
- [ ] 开机自启（复用 `docker_sim/真机部署操作清单.md` 3.8 节的
  systemd 套路）
- [ ] 可选：PWA manifest，支持"添加到主屏幕"

### 8.3 Web vs App 结论

**网页端**，不建议做原生 App——单/少数操作员内部工具，不需要 App 才有
的能力（离线优先、推送、应用商店分发）。想要"像 App"的体验，网页配合
PWA（加到主屏幕）或后续用 Electron 包一层壳即可，不需要重新开发一套。

---

## 九、当前状态

（2026-08-20更新，上一版"纯设计方案，尚未创建任何文件"已过期）

**阶段1/3/4/5/6已落地**（阶段6截至2026-08-20是"语法+仿真侧本地验证
通过，待build+真机端到端验证"，其余阶段端到端验证通过），阶段7（可选，
飞控参数白名单）/8（切真机）/9（打磨收尾）还没做——见8.2节每个阶段
末尾的"实际完成"备注。当前`gcs/`目录实际内容：
- `gcs/Dockerfile`+`gcs/docker-compose.yml`+`gcs/entrypoint.sh`：
  容器A（rosbridge，9090端口）+ 静态文件服务（8080端口）合一，不是
  方案原文里独立的"容器A"，是简化后的实现。
- `gcs/frontend/index.html`：静态HTML+`roslib.min.js`，动态发现飞机
  命名空间、渲染状态卡片（阶段4），每张卡片带起飞/降落/锁定起飞点
  三个按钮直接对rosbridge发起调用（阶段5），没有经过任何后端；顶部
  另有GCS网络设置面板（阶段2）、机队启停面板（阶段6）、每张卡片的
  规划/避障调参面板（阶段2同批新增），这三处都走容器B。
- `gcs/backend/`（`app.py`+`Dockerfile`+`requirements.txt`，2026-08-20
  新增，比原方案阶段2骨架晚、由用户直接提出的调参需求触发）：不带
  `roslibpy`/不serve静态目录（跟原方案骨架不同，见8.2节阶段2备注），
  当前提供`/healthz`、`GET/POST /api/gcs-network`、
  `GET/POST /api/hw-params/{ns}`、`GET/POST /api/fleet/sim/{status,
  start,stop}`、`GET/POST /api/fleet/hw/{ns}/{status,start,stop}`。
  ⚠️同时挂了宿主机`/var/run/docker.sock`+`~/.ssh`，当前完全没有鉴权，
  只适合受信内网。
- `gcs/cyclonedds_gcs.xml`（真机部署站点专属值，不进git）+
  `gcs/rviz/`（rviz2配置，2026-08-19新增）。

**一个值得记住的环境坑**：`gcs/cyclonedds_gcs.xml`是真机部署配置
（绑真实网卡IP），本地单机仿真联调（GCS+sim-world+flight-stack全在
同一台开发机）时不能直接用——需要临时把`NetworkInterface`换成`lo`
才能让GCS容器跟仿真那边的DDS图真正互通（用真实WiFi网卡会出现"能
发现对方、但数据包送不到"的静默丢包，详见DEBUG_JOURNAL.md 2026-08-20
记录），测完要记得换回真机部署的值。
