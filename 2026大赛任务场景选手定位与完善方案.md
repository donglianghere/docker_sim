# 2026年人工智能大赛场景任务设定——选手定位评估与项目完善方案

> 2026-09-06全面重整版。依据：仿真侧`docker_sim/TODO.md`+`DEBUG_JOURNAL.md`
> （截至2026-08-23）；真机侧`fcu/`炸机复盘、`docker_sim/真机调试与飞行
> 测试问题总结报告.md`、`read_hw.md`（截至2026-09-05）；真机代码参考
> 快照`docker_real/`（2026-09-06从`nx01`/192.168.2.104同步，见
> `docker_real/SYNC_NOTES.md`）；`docker_sim/赛项适配度评估报告.md`
> （赛项四大任务权重）。本版是对2026-09-05当天多轮讨论的**归纳整理**，
> 不是聊天记录的时间顺序转录——讨论过程中被推翻/收窄的中间结论（如
> "覆盖式规划算法"最终判定不需要）直接体现为下面的最终结论，不再展示
> 推翻过程，历史过程见对话本身。

---

## 一、现状核实

### 1.1 仿真侧（`docker_sim`，双机 Gazebo 仿真）

- **导航/避障/双机协同主线相对成熟**：定位（DLIO/UWB/真值三选一）、
  规划（mighty/ego-planner二选一）、板外控制（ros2_px4_stack/px4ctrl/
  so3ctrl）多种组合已实测跑通完整起飞-跟踪-降落流程。
- **两个已知未解决问题**：①`hard_forest`密集障碍物场景下MAVROS与PX4
  间歇断连，根因指向Gazebo lockstep单步物理延迟；②多机坐标对齐架构
  缺口——θ*旋转在线估计收敛需要时间，已发出的目标点不会随后续收敛
  结果修正，会导致偏差（已复现，用户已决定暂缓修复架构本身）。
- **视觉感知：完全空白**——前视相机因上游插件多机崩溃bug被禁用，下视
  相机从未存在。
- **GCS网页地面站**：状态监控、起降/标定控制、机队启停、部分调参
  已落地并端到端验证过，但**当前完全没有鉴权**，仿真/真机数据源也
  没有互斥隔离。

### 1.2 真机侧（单机，Jetson Orin NX + PX4 v1.16 + MICOAIR_H743_V2）

- **已经做到的事**：单机已完成多次真实起降/飞行；仿真侧的tmux监控、
  目标点输入、起降控制台、RViz可视化已移植到真机并验证可用；UWB双
  标签定位定向融合已实现，配套SE(2)在线标定做过真机实验（θ*精度
  RMSE 2.83±0.83°）；DLIO在真实数据上做过大量排查修复。
- **当前唯一最高优先级、尚未解决的问题——动力系统（电机/电调）硬件
  故障**：2026-09-02晚间连续多次真实炸机，09-04/09-05复飞复现（不同
  电机通道），根因锁定在电调板/接线/装配工艺的系统性缺陷。**截至
  2026-09-05的结论是"不建议再次上电试飞"**，真机处于安全封存状态。
- **其它未解决问题**：Jetson-Mid360以太网物理链路间歇故障；DLIO长
  时间运行内存/频率下降（部分修复已编译未生效）；UWB基站坐标未标定；
  RC链路未接实机验证。
- **已有但本地`docker_sim`仓库未同步的能力——`vision-stack`**：
  2026-09-06确认真机侧已实现独立的感知栈容器，集成YOLO目标检测
  （`ultralytics`导出的YOLO26n，TensorRT推理）+ 二维码识别（`pyzbar`）
  + AprilTag精准降落检测（`ros-humble-apriltag-ros`，纯CPU），检测
  结果统一发布**标准`vision_msgs/Detection2DArray`消息**（二维码解码
  文本装在`class_id`字段，**没有为本项目自定义检测消息包**）。这份
  代码已同步到本地`docker_real/docker_sim/`供参考，见第六章。
- **仍然明确未覆盖**：双机协同飞行从未在真机上验证过，目前全部真机
  工作只针对单机。

### 1.3 大赛任务设定与赛项要求

选手用仿真系统+真机系统完成大赛任务，4小时内完成传感器调试、Python
程序编写、任务逻辑仿真测试、真机飞行验证。四个实操任务权重：部件
安装测试25% / 目标信息采集与AI建模20% / 智能功能集成验证25% / 系统
联调与智能化作业25%，另有职业素养5%。技术主线是**视觉感知+任务级
作业**，不是底层算法本身。

---

## 二、三个评估问题的结论

### 1. 选手适合操作飞机内部代码吗？——不适合

仿真侧/真机侧的调试记录（namespace撞名、点云时间戳基准错误、IMU单位
换算错误、多次段错误崩溃、`staging`/`live_src`/镜像三方代码不一致、
`git reset --hard`误删未提交改动）证明这一层的排查深度远超4小时能
承受的范围；真机当前处于硬件安全封存状态，进一步坐实"底层/硬件问题
不该由选手承担"。赛项考核重心也在应用层（视觉感知20%+系统联调
25%），不在飞控内部代码。

**划界**：选手碰"任务逻辑层"——订阅/发布平台封装好的接口写Python；
不碰规划器、SLAM、板外控制器、飞控安全参数，这些由赛事保障方预先
调好、封装成黑盒。

### 2. 选手开发平台应该选在哪一侧？——仿真侧开发调试，真机侧只做验证

仿真可重复、可回放、无安全风险；真机现状（硬件故障未解决、处于禁飞
状态）进一步说明真机只适合做"已在仿真验证过的逻辑"的最终确认，且
提供给选手的真机必须是独立于当前原型机、经过硬件排查确认安全的平台。
选手代码通过`DEPLOY_TARGET=sim/hw`风格的配置开关在两侧复用，不为
真机重写逻辑。

### 3. 选手开发代码量多大合适？——1~2个ROS2节点/Python脚本，300~500行

4小时刨去传感器调试、飞行验证时间，实际编码时间约1.5~2小时。**这个
估计成立是有前提的**：必须是SDK把识别/环绕/精降/搜索这些能力都封装
成单次函数调用，选手写的只是编排逻辑（详见第四章）。按两台飞机协同
任务估算：连接初始化~60行+两台飞机各自状态机~250~300行+通信/异常
处理~80行，合计300~550行，与估计吻合。

---

## 三、比赛场景设计（定案）

### 3.1 场景规格

20×25×6米室内场景，在`simple_room.world`的地面/天花板/物理参数结构
基础上改造，**不直接覆盖`simple_room.world`本身**（它是项目当前默认
场景，被其它调试流程依赖）——新建一个独立world文件。

道具清单：

| 道具 | 数量 | 规格 | 说明 |
|---|---|---|---|
| 起飞点标识 | 2 | 地面标记 | 各机一处 |
| 降落点标识 | 2 | 地面标记 | 各机一处 |
| 物资点标识 | 1 | 贴AprilTag | 二维码/AprilTag标识材料类型 |
| 着火点地面标识 | 1 | 地面标记 | 位置未知，需搜索 |
| 独立障碍物圆柱 | 1 | 直径0.5米，高6米 | 常规避障基准物 |
| 模拟高楼立柱 | 3 | 两两间距>2米 | 其中一根竖直面贴AprilTag标识火情 |

**立柱应做成方形/矩形截面（不是圆柱）**——AprilTag需要一个平整的
竖直面才能被正确解码，圆柱曲面会让贴图扭曲失真。

坐标/贴图/`GAZEBO_RESOURCE_PATH`接线等实现细节留给执行方案文档。

### 3.2 立柱与地面着火点的定位方式

- **立柱坐标未知，但场景边界矩形已知**——不需要覆盖式路径规划，靠
  现有规划器已维护的occupancy grid做柱状聚类，在已知边界内裁剪、按
  直径过滤掉独立障碍物圆柱，得到3根候选坐标；逐个环绕、检测到
  AprilTag即提前终止。
- **地面着火点位置未知、场景边界同样已知**——不需要正式的覆盖路径
  规划算法/库（边界规则、无需处理未知形状），用房间尺寸+相机FOV+
  飞行高度直接参数化生成弓字形固定航点（几十行胶水代码），边扫边做
  视觉检测，命中即提前终止。

---

## 四、选手外部接口与SDK架构（定案）

### 4.1 接口清单（最终版）

| 接口 | 方向 | 说明 |
|---|---|---|
| 点到点/航点序列导航 | 选手→平台 | 内建自动避障（不单独暴露"全局规划接口"，规划是导航接口的内建能力） |
| 起降控制 | 选手→平台 | 必须支持反复起飞-降落（见4.3控制器选型） |
| 环绕/绕飞 | 选手→平台 | 参数化通用原语（中心点+半径+平面），不写死成某个特定道具专属 |
| 精确对准/精准降落 | 选手→平台 | 封装成通用组件，选手只决定"何时调用、传什么目标"，不写伺服算法 |
| 感知（传感器原始数据 / 检测结果） | 选手→平台，双向 | 见4.2，需区分"考核对象"与"平台基线能力" |
| 执行器（符号化动作） | 选手→平台 | 无真实物理机构，用约定动作代替 |
| 机间通信 | 选手↔选手 | 通用JSON payload消息总线，不为每个任务单独定义`.msg`类型 |
| 位姿/状态查询 | 选手←平台 | 位置、姿态、电量、飞行模式 |
| 任务完成判定/裁判反馈 | 选手←平台 | 见五章5.1 |
| 安全/异常处理（悬停/返航/中止） | 选手→平台 | 出错兜底路径，不能没有 |
| 传感器自检/状态 | 选手←平台 | 对应任务1"部件安装测试" |
| 仿地飞行/高度跟随 | 选手→平台 | 赛项任务4明确要求，当前项目完全没有对应能力，需新增 |

### 4.2 感知能力的仿真/真机分工——已解决此前的评分归属顾虑

**仿真和真机在"识别"这一层承担不同职责，不是同一件事的两种实现**：

- **仿真**验证的是任务逻辑+控制逻辑，识别用二维码/AprilTag代替——
  可靠、零训练成本，不是偷懒，因为仿真侧要考核的从来不是识别能力。
- **真机**是任务2"目标信息采集与AI建模"真正被考核的地方，用真机
  已有的`vision-stack`（YOLO+二维码+AprilTag）做真实图像识别。

这样此前"识别调用接口会不会替选手做了任务2该做的工作"这个顾虑就不
存在了——只要选手的识别工作量压在真机侧、仿真侧的核心考核点是逻辑/
控制而非感知，两边职责天然不冲突。

**接口一致性要求**：`detect_target()`这类感知函数对选手暴露的签名
和返回格式，在仿真（AprilTag/二维码解码）和真机（YOLO推理）两侧必须
完全一致——**真机侧已经把检测结果统一成标准`vision_msgs/
Detection2DArray`消息**（见1.2节、第六章），仿真侧的"假识别节点"
直接复用同一个消息类型即可，不需要另外设计。选手代码换到真机不需要
改一行，只是这个函数背后的实现从"解码AprilTag"换成"跑YOLO推理"，
这个切换由平台根据部署目标自动决定。

**遗留的选手代码稳健性问题**（不是接口设计能完全解决的）：AprilTag/
二维码解码基本是"有/没有"的干净结果，YOLO在真机上会有多个候选、
置信度、偶发误检——选手代码如果只按仿真的"干净"结果写、没做阈值/
多帧确认，代码能跑但真机表现会变差，需要在SDK文档里提醒选手写防御
性代码。

**Jetson算力提醒**：YOLO选型用YOLO26n这类轻量模型（真机已经这么做了），
Jetson同时还要跑DLIO/规划器，避免选大模型抢计算资源（项目已有过温度
过热停机的先例）。

### 4.3 SDK技术路线与选手隔离架构

**技术路线**：不用原生`rclpy`+自定义ROS2消息包（编译成本高、选手
4小时经不起）。复用GCS后端已验证的路线——`rosbridge`（WebSocket把
话题/服务转JSON）+`roslibpy`纯Python客户端，选手`pip install`即可，
不装ROS2、不编译。

**安全边界不在SDK库本身**（库就是明文`.py`文件，挡不住选手改），
**在库背后连的服务选手账号碰不碰得到**：

1. **账号隔离**：选手用无sudo、不在`docker`组的受限账号登录，容器/
   ROS2栈由另一个服务账号常驻跑，选手账号没有到容器的shell访问。
2. **受限任务API网关**：选手SDK不直连现有GCS前端那种"无限制"的
   rosbridge访问范围，而是连一个新增的、只暴露语义化任务接口
   （`takeoff/land/fly_to/orbit/detect_target/perform_action/
   mission_status`）的网关——即使选手绕开SDK自己手写请求，能做的事
   也不超过网关暴露的范围。这跟GCS现有"容器B持特权、前端间接调用"
   的隔离思路是同一个模式（见第七章）。
3. **库本身可选再加一层"不好改"**（辅助手段，不是主防线）：编译成
   `.pyc`或装到选手账号只读路径。

**下一场次复原**：选手工作区固定目录、直接删除重拷模板（秒级）；
任务/场景状态靠"场景重置服务"（见五章5.1）；仿真侧容器用已验证的
`docker start`/`stop`重启（不需要重新`docker compose build`，几秒到
十几秒）；真机侧额外需要人工把飞机搬回标记点+跑一次传感器自检。

### 4.4 代码量估算（见第二章问题3，此处不重复）

---

## 五、算法与控制架构（定案）

### 5.1 算法模块清单（3类）

1. **AprilTag/二维码识别**：仿真侧新写检测节点（复用`vision_msgs/
   Detection2DArray`），真机侧直接用已有的`vision-stack`（含YOLO），
   两者接口一致（见4.2）。
2. **立柱检测+环绕飞行**：点云/occupancy grid聚类检测立柱候选坐标
   （检测有算法含量）+ 参数化环绕航点生成（纯几何，复杂度低）。
3. **精确对准/精准降落**：见5.2的`position_cmd`中继架构。

地面着火点搜索（弓字形固定航点生成）是这三类之外的一段轻量胶水代码，
不单独算作"算法模块"。

### 5.2 `position_cmd`中继/仲裁架构——精降与环绕共用，不新增跟px4ctrl/so3ctrl并列的控制器

- **不建议**精伺服控制器自己算姿态/推力、直连PX4——要重新实现
  px4ctrl/so3ctrl已经验证过的失控保护/RC接管逻辑，还要在"谁真正握着
  PX4"这个最安全攸关的层面做硬切换。
- **建议**：精伺服控制器只往`position_cmd`话题发消息，跟`traj_server`
  是同一个角色（顶替轨迹优化器，不是跟px4ctrl/so3ctrl并列）。
- 新增一个**中继/仲裁节点**，插在`traj_server`输出和px4ctrl/so3ctrl
  输入之间：
  - **精准降落**：任务状态机切到"精降模式"，中继节点完全改为转发
    精伺服节点的输出（全接管），因为贴近地面阶段厘米级精度优先于
    避障能力。
  - **环绕飞行**：平移部分仍走`waypoint_queue`喂给ego-planner正常
    避障，中继节点只覆写`yaw`/`yaw_dot`两个字段（朝向立柱中心），
    其余字段原样转发。
- 两种情形都是**新增中继节点**，不修改`ego_planner`/`mighty`/
  `px4ctrl`/`so3ctrl`任何一方核心代码，不走
  `feedback_mighty_core_patch_authorization`授权流程。

### 5.3 容器归属原则与清单

沿用真机侧`flight-stack-hw`/`vision-stack`拆分时已经验证过的三条
判断标准（依赖树是否不同 / 失败域是否需要隔离 / 权限面是否不同），
逐项判断这次新增内容该放哪：

| 新增内容 | 归属 | 理由 |
|---|---|---|
| `position_cmd`中继/仲裁节点 | `flight-stack-nx01/nx02` | 依赖极轻、是低延迟控制链路，拆到独立容器反而引入跨容器DDS延迟/发现风险 |
| 立柱点云聚类、环绕/地面搜索航点生成、执行器符号化动作 | `flight-stack-nx01/nx02` | 轻量逻辑节点，不需要特殊权限 |
| 仿真侧"假识别节点"（二维码/AprilTag） | `flight-stack-nx01/nx02` | 纯CPU、依赖轻，三条标准一条都不命中，不需要为了跟真机形式对称而单独开容器 |
| 场景重置服务、自动裁判节点 | `sim-world` | 需要读写Gazebo模型状态，这类权限只有`sim-world`容器有，判断依据是"谁有访问权限" |
| 选手SDK网关 | **独立容器/服务** | 唯一命中"权限面不同"——需要跟rosbridge（监考完整访问）分开权限边界，直接复用GCS现有`容器B`模式 |

---

## 六、真机代码参考（`docker_real/`）与可复用清单

2026-09-06已从`nx01`（192.168.2.104）同步真机`docker_sim`代码到本地
`docker_real/docker_sim/`（排除`staging`/`live_src`/`runtime_logs`等
体积大的噪声目录，约29M），详情见`docker_real/SYNC_NOTES.md`。这是
**只读参考快照**，不是可构建部署副本。

关键定位：

- `docker_real/docker_sim/src/vision_stack/`——真机感知栈ROS2包：
  `yolo_detector_node.py`+`yolo_trt_infer.py`（YOLO推理）、
  `qr_detect_node.py`（二维码）、`apriltag_stack.launch.py`+
  `config/tags.yaml`（AprilTag精准降落，用`ros-humble-apriltag-ros`）。
- `docker_real/docker_sim/docker/Dockerfile.vision-stack`——独立镜像，
  `FROM ros:humble-ros-base-jammy`，不跟flight-stack共享基础镜像，
  理由（依赖树/失败域/权限面三条）已经在Dockerfile注释里写清楚，正好
  印证第五章5.3的容器归属原则不是凭空提出的，是真机侧已经验证过的
  实践。
- `docker_real/docker_sim/models/`——YOLO26n权重（`.pt`/`.onnx`/
  `.engine`三种格式）+ 测试图片/二维码样张。
- `docker_real/docker_sim/docker-compose.hw.yml`——`flight-stack-hw`
  和`vision-stack`两个服务定义。
- `docker_real/docs/vision_stack.md`+`视觉感知vision-stack方案.md`——
  感知栈设计文档+风险清单，`docker_real/docker_sim/read_hw.md`是真机
  完整调试日志。

**可直接复用/参考，不需要仿真侧重新设计的部分**：检测结果消息类型
（`vision_msgs/Detection2DArray`）、AprilTag检测库选型（
`ros-humble-apriltag-ros`）、二维码检测库选型（`pyzbar`）、
`flight-stack`/`vision-stack`容器拆分的判断依据。

**仿真侧需要独立开发、不能照搬真机代码的部分**：YOLO推理链路（真机
是TensorRT+Jetson GPU专属，仿真侧不需要也用不上）、真机`vision-stack`
的相机取流方式（GStreamer appsink，绑定Jetson硬件相机管线，仿真侧
应该直接订阅Gazebo相机插件发布的`sensor_msgs/Image`）。

---

## 七、仿真侧/真机侧完善任务清单

### 仿真侧

**P0 稳定性收口**
1. 修复`hard_forest`场景MAVROS间歇断连。
2. 把"发目标点"接口封装成能自动应对θ*收敛延迟的形式（持续查TF而
   非一次性发布）。

**P1 视觉感知与任务层基础设施**
3. 补齐相机使能（前视/下视，`generic_camera`命名空间修复+双机稳定性
   验证）。
4. 落地第三~五章的场景/接口/算法设计。

**P2 GCS选手可用性收口**（见第八章）

### 真机侧

**P0 安全归零——当前一切真机工作的前置阻塞项**
1. 动力系统（电机/电调）硬件故障排查与修复，最高优先级，与软件开发
   并行推进、互不阻塞。
2. 收口定位链路已知问题（以太网物理链路、DLIO内存/频率下降、UWB
   基站标定、RC链路实机验证）。

**P1 补齐能力缺口**
3. 前视/下视相机链路（真机侧独立验证，不能假设仿真方案直接适用）。
4. 双机协同飞行真机化验证（此前从未做过）。
5. 保证选手接口在`DEPLOY_TARGET=hw`下无需改代码直接复用。

**P2 竞赛现场保障**
6. 提供给选手的真机必须是独立于当前原型机、经过硬件排查确认安全的
   平台。
7. 备用机制——已发生多次真实炸机事故，真机验证环节必须配备用飞机
   和明确的现场安全/紧急处置流程。

---

## 八、GCS地面站需要的修改

角色变化：GCS从"开发者调试工具"变成"选手能直接摸到的东西"，之前的
设计假设操作者可信，现在需要一层选手与监考/教练的权限边界。

**新增后端能力**：场景重置接口、系统就绪自检接口、假识别节点/
vision-stack状态监控（复用现有"VisionStack"状态列的模式）。

**权限/隔离架构（最实质的调整）**：新增受限任务API层（选手SDK只连
这一层，不直连现有前端那种无限制的rosbridge访问）；操作系统账号隔离
落到具体设备；补上鉴权（此前标记"当前无鉴权，只适合受信内网"，这次
选手直接操作GCS，鉴权不再可选）。

**前端/监控面板新增**：精降/环绕这类临时接管`position_cmd`的特殊
模式要在GCS上可见（安全相关）；选手代码异常的可见性（选手脚本不是
ROS2节点，日志不进现有监控体系，需要转发或约定日志文件）；仿真/真机
模式互斥切换（`project_gcs_competition_context`记忆条目：防误操作
优先于信息全面，选手直接操作后这条更重要）。

**不需要大改**：现有四场景机制（`sim-single`/`sim-dual`/`hw-single`/
`hw-dual`）跟"两台无人机"设定本来就对得上，不用重新设计；起降/标定/
状态监控这些"教练/监考"功能保留，只是访问权限要跟选手SDK区分开。

---

## 九、待确认的政策性问题

大部分政策性顾虑已经通过第四章4.2节的"仿真/真机识别分工"设计解决。
仍需跟赛项方/组织方确认的：

1. 任务2"AI建模"评分是否认可"仿真侧用标准视觉标志验证任务逻辑、
   真机侧才是真实识别"这种分工，还是要求仿真侧也考核识别难度。
2. 立柱/着火点坐标要不要作为"已知场景参数"直接告诉选手，还是必须
   靠选手自己感知发现——这决定"立柱检测"这部分代码选手要不要写。

---

## 十、优先级速览

```
仿真侧：P0稳定性收口 → P1场景/接口/算法落地 → P2 GCS选手可用性
真机侧：P0动力系统安全排查（当前唯一阻塞项，与软件开发并行）
        → P0定位链路收口 → P1相机/双机能力补齐 → P2现场保障
```

真机侧P0是当前最紧迫的事项——在电机/电调硬件故障解决之前，"真机飞行
验证"这个赛题环节本身无法安全开展；但这不阻塞仿真侧的开发工作，两条
线应该并行推进，具体执行顺序见配套的《2026大赛任务系统开发执行
方案.md》。
