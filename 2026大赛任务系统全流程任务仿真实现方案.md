# 2026大赛任务系统全流程开发方案

版本：2026-09-13 v3-g（同一次执行前一致性核查的第二轮订正——用户进一步明确了真机`uwb_imu`定位融合的真实实现方式，导致1.1节`agl_hold`模式方案被推翻重做，改成直接修`uwb_imu_fusion_node`的z融合逻辑，2.1节第5条SDK能力相应删除——本版替换v3-f）
适用场景：`fire_drill_room` + 双机（NX01/NX02）+ `PLANNER=ego_planner` + `CONTROLLER=pt4ctrl` + `LOCALIZATION_SOURCE=uwb_imu`

**v3-f→v3-g的变化**（同一次执行前一致性核查过程中，讨论A2设计前提时发现的更深层问题）：
1. **`uwb_imu`模式下PX4的高度状态本质是什么，之前完全没有查证过**——查`docker/Dockerfile.sim-world:48`确认这套仿真固定配置`EKF2_EV_CTRL=15`+`EKF2_HGT_REF=3`，PX4的高度主参考源直接是我们发布的`vision_pose`里的z（不是气压计），这个配置全局生效、不分`LOCALIZATION_SOURCE`。原1.1节`agl_hold`模式方案假设了一个从未存在的前提——"PX4内部有一个稳定的世界系Z，AGL是需要叠加的修正量"——用户确认真机上`uwb_imu`这套定位方案的z本来就是"姿态真值（真机对应AHRS）+测距雷达`range`合成"，不是"UWB测的z"，仿真这边`uwb_imu_fusion_node.py`之前直接拿UWB模拟位置的z喂给PX4，没有对齐真机实现。
2. **结论：`agl_hold`模式方案整体作废，全程AGL定高改成在`uwb_ground_truth_node.py`/`uwb_imu_fusion_node.py`（`uwb_origin_bridge`/`uwb_sim`包，不是`contest_mission`包）修复z融合逻辑**——修完之后`uwb_imu`模式下PX4的高度状态天然就是AGL，不需要任何"切换模式"的开关。新增1.1a节描述具体改法（姿态从Gazebo姿态真值取、z从测距雷达range配合水平倾斜补偿算出、近地边界单独处理）。`position_cmd_relay_node.py`不用改，回到"维持不变"名单；2.1节第5条`sdk.set_altitude_mode('agl'|'world_z')`这条SDK能力整个删除（没有"world_z"这个可选项了）；4.2节等处调用`sdk.set_altitude_mode('agl')`的地方一并删除。
3. **这类改动虽然碰了`contest_mission`包之外的文件（`uwb_sim`/`uwb_origin_bridge`），用户明确要求这次一起做**，不留成后续遗留问题——这是执行前核查而不是执行中期发现的好处，提前把设计前提问题解决掉，避免阶段A做到一半发现地基有问题被迫推倒重来。

**v3-e→v3-f的变化**（执行前一致性核查发现的问题，不是推翻已有结论，是订正几处跟实际代码/项目验证记录不符的表述）：
1. **`CONTROLLER`选型订正：`px4ctrl`→`pt4ctrl`**——核查时发现之前版本"适用场景"这行一直写的是`CONTROLLER=px4ctrl`，但查`README.md`的控制器兼容性记录，`LOCALIZATION_SOURCE=uwb_imu`+`px4ctrl`（以及`so3ctrl`）是**明确记录过的禁止组合，2026-08-12实测炸机**（根因：这两个板外几何姿态控制器需要companion computer持续高频喂姿态目标，`uwb_imu`模式下位置反馈经PX4内部EKF2+MAVLink往返，速率上限~48Hz，喂不上）——继续用`px4ctrl`会直接导致阶段A/E所有实测炸机，必须先订正。用户确认这次任务改用`pt4ctrl`（2026-08-13新增，复用px4ctrl状态机、删掉推力模型估计+姿态解算两步、改走`mavros/setpoint_trajectory/local`交给PX4固件自己解算姿态，因此同时具备"支持反复起降"+"对更新率不敏感、兼容`uwb_imu`"两个优点），**用户明确这个组合真机侧已经验证可行，仿真侧现有`px4ctrl`/`pt4ctrl`/`so3ctrl`三个控制器也都各自验证过没问题**——比`README.md`当前"`pt4ctrl`从未在这套docker_sim仿真里实际飞过、完全未知"的记录更新，`README.md`那条记录已经过期，需要另外找时间同步更新（不在本次方案修订范围内，见下面"未决问题"）。这一订正直接影响1.4.2/2.1/4.2节里跟θ*/坐标对齐相关的表述，见下面第3条。
2. **`precision_servo_node.py`需要拆成两种模式，不是只有现有的"精降"这一种**——用户直接给出的设计：模式A"仅居中定位"（检测到目标→修正水平误差→收敛后**发送当前位置信息、不下降**，交还控制权），给RECON角色发现地面火情、读自己坐标广播给SUPPLY用；模式B"精降抓取"（抵达目标→修正水平误差→收敛后自动下降→到`min_descent_z`后**触发真正的autoland（disarm）**→完成后发通知），给SUPPLY角色抓取物资用。查了现有代码（226行）确认：现在只有模式B的雏形，且雏形本身也不完整——下降到`min_descent_z`后只是**停在那个高度悬停**，并不会触发真正的PX4自主降落，需要补上"到达min_z后调用现有降落触发接口(`TakeoffLand`一类)+等armed变false+发通知"这一段。模式B完成抓取后SUPPLY还要再起飞飞去投放/继续任务——这正是第1条订正为`pt4ctrl`（支持反复起降）的必要性所在，如果没有这条订正，模式B这个设计在`uwb_imu`+`px4ctrl`/`ros2_px4_stack`任何一个现有验证过的组合下都跑不通（`px4ctrl`+`uwb_imu`直接炸机，`ros2_px4_stack`架构上只支持起飞一次）。新增1.7节详细描述这两种模式的接口设计。
3. **`uwb_imu`模式下不存在θ*收敛这个环节，之前版本的表述需要订正**——`origin_setter_node`（在线SE(2)旋转估计+θ*收敛）是`LOCALIZATION_SOURCE=uwb_slam`专属的节点，`uwb_imu`模式下这个节点根本不会起，`local_position_readback_node`读回的坐标本身已经是同一个UWB锚点下的全局系坐标，两机local frame天然对齐，不需要任何旋转对齐步骤。之前1.4.2节"转换成跟随者局部系（通过`origin_setter_node`/`frame_align_bridge`两机共享的全局参考系对齐）"、2.1节/4.2节"确认两机θ*收敛稳定判据满足后"这几处表述都是**假设了`uwb_slam`模式的机制、套用到`uwb_imu`场景上**的错误类比，本版订正为"`uwb_imu`模式下两机局部系=全局系，`formation_follower_node`直接消费长机odom坐标即可，不需要坐标转换、不需要等待任何收敛判据"。0.3节那条"必须现场用`tf2_echo`核实局部系原点"的警告仍然保留（不能省略实测核实这一步），但不再暗示"可能需要θ*对齐"。
4. **`sdk.goto()`系列调用的z坐标口径明确为两类，不是从`fire_drill_room_layout.yaml`读**——核查时发现该yaml里`supply_point`/`pillars`等点位本身**不带独立z字段**（高度信息在顶层单独字段，如`pillar_height`），4.2/4.3节多处`sdk.goto(supply_point)`/`sdk.goto(消息里的坐标)`这类调用如果假设能从yaml读到z会读不到。订正：全流程程序里所有`goto()`的z参数只有两类取值——**普通航线巡航AGL高度**（4.2节的1.5米）、**立柱绕飞AGL高度**（4.3.2节`generate_orbit_waypoints`的2.0米），4.3.1/4.3.2节涉及的具体goto调用统一按这两类中的一类取值，不需要从yaml读z。

**未决问题（v3-f新增，不是本版能替你决定的）**：`README.md`里`pt4ctrl`控制器那条兼容性记录（"从未在这套docker_sim仿真里实际飞过，完全未知"）已经过期，需要找时间同步更新成"`uwb_imu`+`pt4ctrl`真机+仿真均已验证"，避免以后有人照着`README.md`当前文字误判风险等级——这个更新不在本方案范围内，是`README.md`自己的维护工作，提醒一下不要漏掉。

**v3-d→v3-e的变化**（用户提出两个具体问题，讨论后确定了两个之前方案里没有定下来的实现细节）：
1. **抓取/投放/发射这三个动作，明确要通过mavros复用遥控器开关（`mavros/rc/override`话题）真实触发，且仿真环境也要走这条真实链路（不是继续用现有的纯状态记录）**——这推翻了v3-d及之前版本第7节"`sdk.do_action()`只做状态记录+固定等待模拟耗时，不做真实机构仿真"这条排除范围里"不做真实触发"的部分（"不模拟机构本身的物理动力学"这半句结论不变，Gazebo里没有抓取臂/投放舱的物理仿真，这次只是让**触发信号链路**本身是真实的）。查了本机`PX4-Autopilot`源码（`mavlink_receiver.cpp`/`rc_update.cpp`）+mavros源码（`rc_io.cpp`）确认了一个关键事实：**PX4收到`RC_CHANNELS_OVERRIDE`后直接写入`input_rc`这个uORB话题，消费它的`rc_update`模块是订阅回调驱动、不是定时轮询，PX4底层没有"隔多久没收到override就自动判失效"的机制**——如果真实遥控器接收机还在工作，它会用远高于override的硬件帧率持续把这个话题"抢回去"（你的信号很快被真实的、没被拨动的开关状态覆盖）；如果没有真实接收机，停止发布也不会自动复位，必须显式发一帧释放（`CHAN_NOCHANGE=65535`）。因此实现上**必须持续（建议≥10Hz）发布到动作需要的时长，动作完成后显式发一帧释放**，这个"持续发布+完成后释放"的回路参照1.0节原则应该放在flight-stack内（跟`formation_follower_node`同一类判断——这次不是全程持续，是有限时长的一次性动作，但期间仍然是需要持续publish的回路，不适合让SDK跨网络维持），具体是把现有`actuator_action_node.py`从"纯状态记录"改造成真正驱动`mavros/rc/override`的节点，SDK这边`sdk.do_action()`接口形状完全不变（还是"设置action_name参数+等action_status确认done"这套一次性触发+轮询结果的模式），见新增1.6节。
2. **地面站声光装置由选手程序主动控制**（不是GCS后端自动响应通知触发）——这意味着选手/全流程程序需要一条新的网络链路联系到GCS所在机器去驱动这个硬件，0.5节拓扑总览要补第4条链路，2.1节SDK能力清单要新增一项，见新增的"链路D"相关段落。

**v3-c→v3-d的变化**（用户要求的9个维度里，双机对称/双机通信/通信延迟三项v3-c已经覆盖，本版补齐剩下6项里实际有缺口的部分——封装完整性/接口通用性/选手易用性/天地通信/基础设施权限/换选手复位）：
1. **天地通信维度之前跟其它链路混着讨论**——新增0.5节"系统拓扑总览"，明确区分链路A（选手程序→飞机，控制）/链路B（双机→双机）/链路C（GCS→飞机，监控展示），说明链路C可靠性要求最低、不需要跟A/B一样的ACK+重发协议，并厘清2.3节引用`gcs/backend/app.py`只是"链路A的历史实现案例"，不代表GCS监控功能本身也要同等严格设计。
2. **接口通用性维度发现一处缺口**——`orbit_flight_helper.py`/`ground_scan_helper.py`是`contest_mission`包内的纯函数，第四部分程序运行在独立的`contest_sdk`容器里不能直接`import`，新增2.1节第8条，把这两个helper vendor进`contest_sdk`暴露成`sdk.generate_orbit_waypoints()`/`sdk.generate_ground_scan_waypoints()`。
3. **封装完整性维度之前没有专门讲**——新增2.2.1节，明确选手在任何情况下（含出错时）都不应接触裸rclpy异常/ROS消息类型/TF frame_id概念，新增`contest_sdk/exceptions.py`统一转换异常。
4. **选手易用性维度新增可调试性要求**——新增2.2.2节，SDK阻塞调用要主动打印中文进度、超时异常要给出可能原因提示，不能让选手必须会用`ros2`命令行才能排查问题。
5. **"基础设施选手能用不能改"之前完全没讨论**——新增3.1节，明确这次靠容器级文件系统隔离（选手镜像里根本没有飞行栈源码）作为实际生效的防线，SROS2/DDS细粒度访问控制列入排除范围。
6. **"换选手容易恢复系统"之前完全没讨论**——新增3.2节，指出`scenario_reset_node`/`mission_judge_node`只覆盖道具位置和判分进度，覆盖不到飞行栈运行时状态（悬停未降落、agl_hold残留、编队跟随残留等），推荐换选手直接`docker compose restart`三个仿真栈容器这种简单可靠的方案，不做精细化的局部复位服务。
7. 顺带完成一次全文NX01/NX02硬编码表述的一致性检查，修正了两处遗漏（示例日志前缀、延迟预算讨论里的事件名）。

**v3-b→v3-c的变化**（用户一次性问了4个问题，都是之前版本没考虑周全的地方，不是推翻已有结论）：
1. **编队的几何队形+控制策略都要做成可插拔接口，不要焊死这次选的"纵向+长机-僚机"**——`formation_follower_node`按策略模式拆成`FormationGeometry`/`FormationControlStrategy`两层抽象接口，这次任务只实现纵向+长机-僚机这一种组合，但接口预留其它组合，见1.4.1节。
2. **两架飞机硬件/软件完全对称，之前的文档一直用"NX01程序"/"NX02程序"暗含了固定角色分工，这个假设没有依据**——改成"角色（role）是运行时参数，跟机身编号（namespace）解耦"，代码不能出现按`namespace`分支的逻辑，只能按`role`分支，角色靠启动参数静态指定（这次任务用的方案）或动态协商（更复杂，本方案不做），见新增1.5节。
3. **真机场景3根候选立柱只给坐标、不给编号，之前文档默认沿用仿真配置文件的"1#/2#/3#"标签，这个假设不成立**——改成程序自己按坐标做确定性排序（先比X再比Y）自行编号，不依赖外部给的标签，仿真里沿用的"1#/2#/3#"字样只是方便对照场景说明、跟程序内部真实编号不需要一致，见4.3.2节。
4. **双机通信之前只讨论了协议层可靠性（ACK+重发），没有讲清楚物理链路怎么连、有没有专门算过这条链路的延迟**——补充了两种物理拓扑（经地面站路由器转发 vs 双机直连），给出这次默认选路由器转发的理由+双机直连作为已知的可靠性升级方向；延迟这块说明了ACK+重发协议本身是延迟无关的，真正要关注的是`send_to_teammate()`的超时值要设得够宽松（建议30-60秒），见2.3节"链路B"扩写部分。

**v3→v3-b的变化**：v3把"编队跟随"这个持续、高频（5-10Hz）的实时控制回路放进了SDK（`sdk.start_formation_follow()`），用户指出这个判断错了——**SDK提供的应该是决策功能，实时控制回路必须放在飞行栈（机载）里，因为选手程序/地面站到无人机之间存在不小的网络延迟，一个需要持续闭环的实时回路如果每个控制周期都要跨网络一来一回，控制质量会明显下降**。v3-b把编队跟随的实际计算/控制回路挪回第一部分（飞行栈内的ROS2节点，机载运行、访问自己的odom零延迟），SDK这一层只保留一个"启动/停止编队跟随"的一次性触发调用（决策层面的开关，不是控制回路本身），详见第一部分1.4节+第二部分2.1节第7条的修正说明。这次修正同时把"实时控制回路 vs 决策层"这条边界判断标准明确写成了一条通用原则（1.0节），以后任何新能力该放Part1还是Part2，都按这条原则判断，不再逐个凭感觉分。

**v2-b→v3的变化**（技术内容大部分沿用v2-b已经确认过的结论，这次主要是"东西该放在哪一层"这个架构归属的调整，不是重新推翻技术方案）：

之前v2/v2-b把"任务编排"设计成一个`mission_fsm_node.py`——放进`contest_mission`包、跟着flight-stack容器一起跑的**过渡性质**节点（明确写过"这是把阶段7.4提前做一个临时版本"）。用户这次明确表态**不要这个临时节点**，直接把"SDK"作为全流程开发的**前置依赖部分**，"全流程任务程序"本身才是最终交付物——这意味着任务编排逻辑（状态机、双火情响应、立柱选择规则等）**不再是contest_mission包里的一个ROS2节点，而是选手/开发者用SDK写的一份独立程序**，跑在选手自己的环境里，不跟着flight-stack容器一起起。

按这个思路，整篇方案重新分成4个部分，**第4部分就是全流程任务程序本身**：

- **第一部分：仿真栈基础能力扩展**——contest_mission包里几个ROS2节点需要补的、纯粹是"能力"而跟"这次具体怎么用"无关的底层扩展（比如新增一种定高模式），这一层做完之后，不管以后写什么任务，这些能力都能复用。
- **第二部分：选手SDK（`contest_sdk`）**——把第一部分的能力、以及ROS2原生的检测/通信/动作接口，封装成选手好用的Python API，这一层同样是通用的，不含任何"地面火情"、"物资点"这类这次任务专属的概念。
- **第三部分：选手开发/运行环境**——选手怎么装、怎么跑SDK，尽量不增加操作门槛。
- **第四部分（最终交付物）：全流程任务程序**——用第二部分的SDK写出来的、真正实现题目描述的三段任务的完整程序，这次任务专属的所有词汇（事件名、动作名、状态子状态、立柱选择规则等）都在这一层定义，不污染前三部分的通用性。

这样分层的好处：第一/二/三部分做完之后是可以**复用**的基础设施，以后换一个任务题目，只需要重写第四部分，不需要跟着重新做一遍SDK或者仿真栈扩展；而且第四部分作为一份普通的选手程序，跟这个项目"contest_mission包提供能力、选手在外部消费"这个既有分工思路是一致的，不会出现"任务逻辑到底该放contest_mission里还是选手程序里"这种归属不清楚的问题。

---

## 0. 背景与现状核查（全篇的事实基础，不是独立交付物）

### 0.1 已有能力模块（跟之前版本一致，不重复展开）

起飞/降落触发（`TakeoffLand`消息）、`waypoint_queue`多点导航接口、`vision/detections`视觉检测话题、`position_cmd_relay_node`的模式仲裁机制、`actuator_action_node`符号化动作占位、`mission_judge_node`的5个checkpoint、`mission_state.py`的话题契约（目前没有发布方）——这些跟之前版本盘点的结论一样，不再重复，需要用到时直接引用。

### 0.2 高层火情检测：维持点云聚类+法线吸附，不用视觉位姿解算（真机兼容性，这条结论定死不再变）

**这是之前一版方案被用户当场纠正的一处方向性错误，结论定下来之后不会再变，这里保留完整理由**：曾经考虑过"用AprilTag真实6DOF位姿解算代替点云聚类"，这个方案在**仿真里**技术上可行，但**真机上高层火情用的是火焰图像模拟高层火源，不是AprilTag**——真机那条视觉链路是YOLO一类的2D检测器识别火焰，只能给2D bbox，解不出深度/朝向，这跟`fire_pillar_aim_node.py`文件头一开始就写清楚的理由完全一致（"不做单目6DOF位姿解算，理由...这条路在真机上根本走不通"）。这个项目的目标是仿真代码以后要能原样搬到真机上跑（`CLAUDE.md`里的Jetson Orin NX移植目标），仿真这边不能依赖"真机根本没有"的能力。

结论：`pillar_detector_node.py`（点云聚类识别立柱朝向）+`fire_pillar_approach_helper.snap_to_nearest_face()`继续使用、不动；`qr_apriltag_detect_node.py`不扩展位姿解算，视觉侧永远只提供"看到了/没看到"这个二值触发信号。地面火情坐标获取同理，用"下视相机居中悬停（`precision_servo_node.py`的模式A`center_only`，见1.7节）+读自己当前局部坐标"的方式，不做PnP解算——见第四部分3.2节。

### 0.3 定位源：`uwb_imu`（已真机验证）

`LOCALIZATION_SOURCE=uwb_imu`时跳过DLIO/SLAM，起`uwb_imu_fusion_node`（UWB位置+IMU的roll/pitch直接当`vision_pose`喂给PX4自己的EKF）+`local_position_readback_node`（把PX4融合后的`local_position/odom`读回来、改名重发成跟`gt`/`uwb_slam`两种模式**同名同格式**的`<namespace>/odom -> <namespace>/base_link` TF链，下游`ego_planner`/`rviz_goal_bridge_node`不用区分定位源）。

**用户确认这套模式已经在真机上验证过**，可信度比本项目仿真侧现场验证过的`gt`/`uwb_slam`两种模式更高——风险清单里这条只是"这次容器化仿真环境这一层的wiring没跑过"，不是怀疑这个定位方式本身（第5节风险清单有这条）。

⚠️ `<namespace>/odom`这个局部系原点具体对应世界系哪个点——**必须在真实运行的容器里用`ros2 run tf2_ros tf2_echo`现场核实**，不能假设"UWB模式局部系=世界系"就直接拿yaml坐标硬编码，这条约束不因为换了定位源就能省略（v3-f订正：`origin_setter_node`的在线θ*旋转对齐是`uwb_slam`模式专属机制，`uwb_imu`模式下不存在这一步，不要再假设两者有关联；但"局部系原点具体在哪"这个核实动作本身跟θ*无关，仍然要做）。

### 0.4 场景约束：障碍物/仿地模块坐标视为未知，立柱/物资点/地面火情点坐标已知或需现场发现

- **障碍物圆柱、仿地模块坐标不允许写进任何代码**——不能读`fire_drill_room_layout.yaml`当"上帝视角"，必须靠传感器实时感知/规避：障碍物避让靠`ego_planner`本身基于mid360点云的局部避障（不需要任何额外代码，选手不知道位置这件事对它没有影响）；仿地穿越靠全程AGL定高（见第一部分1.2节），不需要知道仿地模块具体在哪、多高，AGL定高天然会跟着地形起伏走。
- **3根候选立柱、物资点坐标视为已知**（题目原文允许）——**执行前一致性核查订正：这几个坐标应该由选手直接写成第四部分程序里的字面量/常量，不是"引用`fire_drill_room_layout.yaml`对应字段"**（之前版本这句话是错的：第四部分程序跑在`contest_sdk`容器里，根本没有`contest_mission`这个包、没有这份yaml文件，见3.1节"选手容器里根本不包含仿真栈源码"的隔离设计，物理上就不存在"引用"这个操作；真机部署时也是题目/主办方直接口头或书面给出这几个坐标数值，选手照抄进代码，从来不涉及读取任何配置文件）。仿真环境里这几个数值恰好跟`fire_drill_room_layout.yaml`里的配置一致，只是巧合，不代表程序应该去读那份yaml——但立柱**朝向**每次随机、必须现场探测（见0.2节）。
- **地面火情、高层火情的具体位置视为需要现场发现**——不能直接读`ground_fire_point`/`fire_apriltag_marker`的配置坐标，"发现"这个动作本身就是任务的一部分。

### 0.5 系统拓扑总览：三条链路要分清楚，不要混着讨论（这次重新梳理时补的）

之前几版文档讨论"通信"/"延迟"的时候，"选手程序"和"GCS"经常被放在一起讲（比如引用`gcs/backend/app.py`的例子来论证SDK该怎么设计），容易让人误以为这是同一条链路的两种叫法。实际上这次方案涉及的通信参与方一共有4个，构成**3条性质不同的链路**，讨论可靠性/延迟的时候要分清楚是哪一条：

```
                    ┌─────────────────┐
                    │   GCS(地面站网页)  │
                    │   +声光装置(新增)  │◀────────────┐
                    └────────▲────────┘              │ 链路D
                             │ 链路C                    │ (选手→GCS机器，
                             │ (监控展示)                 │  驱动声光装置)
┌──────────────┐    链路A（控制）    ┌──────────────┐   │
│ 全流程任务程序 │ ──────────────────▶│   无人机 A    │   │
│ (选手/contest_sdk)│───────────────────────────────────┘
└──────────────┘                    └──────┬───────┘
                                            │ 链路B
                                            │ (双机通信)
                                     ┌──────▼───────┐
                                     │   无人机 B    │
                                     │ (flight-stack)│
                                     └──────────────┘
```

- **链路A（选手程序→飞机，"控制链路"）**：全流程任务程序对飞机下达指令（起飞/导航/动作），2.3节"链路A"专门讨论过，可靠性要求高（指令必须确认执行），延迟容忍度中等（一次性指令，几十到上百毫秒延迟不影响正确性，只影响响应快慢）。
- **链路B（飞机↔飞机，"双机通信"）**：2.3节"链路B"专门讨论过，可靠性要求最高（协同动作直接依赖这条链路，消息丢了任务会卡住），延迟容忍度中等偏高（本方案设计的都是"决策层"一次性事件，不是实时控制，见2.3节延迟预算分析）。
- **链路C（GCS↔飞机，"监控链路"，这次重新梳理时明确区分出来的）**：GCS是给人看的监控面板（第4.4节提到"GCS这一侧要新增订阅节点转发给前端"），**只读为主**（展示`mission_state`/`mission_events`），可靠性要求相对最低——GCS上一条通知晚几秒钟显示、甚至偶尔丢一条中间状态，不影响任务本身能不能成功完成（人看到的只是"进度条卡了一下"，跟"任务真的失败了"是两回事），**不应该跟链路A/B用同一套"阻塞直到确认+超时报异常"这种严格可靠性设计**，用简单的"尽力而为"订阅+定期兜底轮询一次全量状态就够，没必要给GCS也套一层ACK+重发协议，那是不必要的复杂度。
- **链路D（选手程序→GCS机器，"声光装置控制链路"，v3-e新增，用户明确要选手程序主动控制而不是GCS后端自动响应）**：地面站电脑连接一个物理声光装置（发光发声，具体接口方式——USB继电器/串口/GPIO——留给实现时按实际采购的硬件确定），选手程序需要在任务跑到某些"通知"节点时主动触发它。这条链路方向和链路A/B都不一样（不是"选手→自己的飞机"也不是"飞机→飞机"，是"选手→GCS所在的另一台机器"），可靠性要求参照链路A（触发指令要有确认，不能"发了就当作成功"，不然选手容易怀疑装置到底有没有响应），但频率极低（只在4.4节几个关键通知点触发，不是持续/高频控制），延迟容忍度高（声光提示晚个几十上百毫秒不影响任务判定，纯粹是人感知层面的体验）。具体封装见2.1节新增的SDK能力第9条。

**这条区分对之前几节的影响**：2.3节引用`gcs/backend/app.py`的`_publish_takeoff_land()`/`_recent_px4ctrl_feedback()`作为"设计SDK可靠性层"的参考依据仍然成立——**不是因为GCS和选手程序是同一条链路**，而是因为GCS后端触发起飞这个动作时，它此刻扮演的角色就是链路A（GCS当时充当了"控制指令发起方"，跟选手程序发指令是同一种角色），这个precedent本身没问题，只是要理解成"链路A的一个历史实现案例"，不代表GCS本身的监控展示功能（链路C）也需要一样严格的可靠性设计。

---

## 第一部分：仿真栈基础能力扩展

这部分是`contest_mission`包（跟着flight-stack容器跑的现有ROS2节点集合）需要新增的、纯粹是"能力"、不含任务专属逻辑的扩展。做完这部分，SDK（第二部分）才有东西可以封装。

### 1.0 架构原则：实时控制回路放飞行栈（机载），决策/一次性指令放SDK（用户提出，v3-b新增）

这是v3-b新增的一条通用判断标准，之前判断"某个能力该放Part1还是Part2"只考虑了"是否必须插在flight-stack内部管道里"（比如`agl_hold`必须改`position_cmd_relay_node`的内部转发逻辑，SDK进程在外部拦不到），**这次用户指出还漏了一个更根本的维度：延迟**。

选手的SDK/全流程程序不保证一定跟飞行栈跑在同一台机器上——真机部署场景下，选手程序很可能跑在地面站电脑（第二部分2.3/2.4节讨论的"路由器互联"场景），到无人机机载电脑之间有真实的、不可忽略的网络延迟（WiFi往返几十到上百毫秒量级是常态）。任何**需要持续、高频闭环**的控制逻辑（比如编队跟随需要5-10Hz"读队友位置→算目标点→发新目标"这个回路），如果放在选手程序里跨网络做，每个控制周期都要承受一次网络延迟，会直接体现成"响应慢半拍"，编队跟随这种对实时性敏感的行为会明显变差、抖动变大，是真实的、会影响任务成功率的问题，不是理论上的洁癖。

**判断标准**：
- **需要持续闭环、频率明显高于"一次指令+偶尔查询状态"的控制逻辑**（编队跟随、定高、避障这类）——放第一部分，作为flight-stack内的ROS2节点，机载运行，访问自己的odom/传感器数据零延迟，选手程序只负责"决定要不要启用这个行为、什么时候启用/停用"这个一次性开关。
- **一次性指令+等待/轮询结果的模式**（起飞、导航到某个点、执行一个动作、发一条消息给队友）——放第二部分SDK里，即使跨网络也只是"发一次、等最终结果"，网络延迟只影响"响应快慢"这个用户体验层面的东西，不影响控制回路本身的稳定性，可以接受。
- 简单说：**"控制"留在飞机上，"决策"可以放选手程序里**，这也是很多真实无人机系统的通用分工原则，不是这个项目专有的设计。

按这条原则重新核对了一遍第二部分列的能力，只有"编队跟随"这一项之前判断错了（见1.4/2.1节的修正），其它能力（起飞/导航/等检测/发消息/执行动作）本身就是"一次性指令+轮询"模式，不受这条原则影响，维持原判断不变。

### 1.1 全程AGL定高：改在`uwb_imu_fusion_node`修z融合逻辑，不新增`agl_hold`模式（v3-g订正，废弃原`position_cmd_relay_node`方案）

**这一节是执行前一致性核查过程中被推翻重做的一节，原因记录在这里，避免以后有人对着旧版文档奇怪`agl_hold`哪去了**。原方案（v3-e及之前）打算给`position_cmd_relay_node`新增一个`agl_hold`模式，通过增量换算把测距雷达`range`补偿进`position.z`——这个设计**建立在一个当时没有查证的假设上：PX4内部的高度状态（`local_position.z`）是一个稳定的"世界系Z"，AGL是需要额外叠加的修正量**。

查了本机flight-stack的PX4航空框架patch（`docker/Dockerfile.sim-world:48`）确认：这套仿真固定配置`EKF2_EV_CTRL=15`+`EKF2_HGT_REF=3`——**PX4的高度主参考源直接就是我们发布的`vision_pose/pose_cov`里的z，不是气压计，这个配置不分`LOCALIZATION_SOURCE`、全局生效**。这意味着：`uwb_imu`模式下，PX4内部`local_position.z`到底是不是"稳定世界系Z"，完全取决于`uwb_imu_fusion_node`往`vision_pose/pose_cov`里塞的z是什么——**而不是一个独立于localization实现的、天然存在的物理量**。

**用户确认了真机的实际实现**：真机上`uwb_imu`这套定位方案的z本来就是"Gazebo姿态真值（仿真侧，真机对应AHRS姿态）+测距雷达`range`合成"（不是"UWB测的z"）——之前`uwb_imu_fusion_node.py`直接把仿真里`uwb_ground_truth_node`模拟的UWB绝对位置的z分量（Gazebo真值+高斯噪声，见0.3节/A0新增的现状核查）当z喂给PX4，这一步本身就没有对齐真机实现，需要修。修完之后，`uwb_imu`模式下PX4的`local_position.z`**天然就是AGL**（离地高度，不是世界系固定Z）——`normal`模式下发的`position_cmd.z`本身就已经在按AGL工作，飞过仿地模块时高度状态自己会跟着地形走，**不需要任何"切换到agl_hold模式"这个开关**，因为压根不存在"AGL vs 世界系Z"这两种互斥语义可以切换：世界系Z这个概念在`uwb_imu`模式下不再存在。

**结论：原1.1节`position_cmd_relay_node`新增`agl_hold`模式的方案整体作废**，不新增这个模式，`position_cmd_relay_node`维持现有4个模式不变（回到"1.3维持不变的模块"名单）。全程AGL定高改成在定位融合层（`uwb_imu_fusion_node.py`，`uwb_origin_bridge`包，不是`contest_mission`包）一次性修好，具体见新增的A0节（原本按项目惯例这类改动不在"仿真栈基础能力扩展"第一部分范围内，但这次因为A2的设计前提直接依赖它，执行前核查发现后，用户明确要求这次一起做，不留成后续遗留问题）。

对应地，2.1节第5条`sdk.set_altitude_mode('agl'|'world_z')`这条SDK能力**整个删除**——`uwb_imu`模式下从来没有"world_z"这个可选项，"选择AGL还是世界系Z"这个二选一在这次任务的定位源下没有意义，不需要一个只有单一效果的开关接口（1.0节"没有必要不引入接口"的精神）；全流程程序里对应的`sdk.set_altitude_mode('agl')`调用也一并删除，不需要任何显式调用来"进入AGL模式"，AGL全程默认生效。如果以后要在`gt`/`uwb_slam`定位源下也支持AGL/世界系Z切换，需要重新设计（不在这次任务范围）。

### 1.1a `uwb_imu_fusion_node.py`的z融合逻辑修复（新增，属于A0，跟原1.1节是替代关系）

**改动范围**：`uwb_origin_bridge`包的`uwb_ground_truth_node.py`+`uwb_imu_fusion_node.py`，不是`contest_mission`包，执行时不受"第一部分只改`contest_mission`包"这条边界限制（这条边界本来是为了风险隔离，但A0这个改动是A2的设计前提本身出了问题，不属于"顺手改别的包"，是这次任务必须先修的地基）。

1. **`uwb_ground_truth_node.py`**：`uwb/pose_abs`话题的姿态分量，从"roll/pitch强制置0（占位值）"改成直接取Gazebo `model_states`里对应model的真实姿态四元数分量（不是`pose_truth`那条"评估专用、机上模块不许订阅"的旁路——那条线的"不许机上订阅"规则不变，这次改的是`pose_abs`本身，`pose_abs`一直就是"机上可以订阅的模拟传感器读数"这条线，只是之前roll/pitch这两个分量没有真的填数据）。这不是"开天眼"：真机上UWB+IMU/AHRS融合系统本来就能给出精度足够好的roll/pitch，仿真里直接用Gazebo姿态真值模拟这一路传感器，跟"直接读地图坐标当自己位置"这种作弊行为性质不同——是模拟一个真实存在、精度良好的传感器输出，理由类比0.2/0.4节区分"传感器能力可以直接建模"和"上帝视角信息不能写代码里"的原则。
2. **`uwb_imu_fusion_node.py`**：
   - roll/pitch的来源从订阅`mid360/imu`（Gazebo IMU传感器插件，噪声/零偏建模）改成直接从`uwb/pose_abs`的姿态分量取（跟x/y/yaw同一个消息来源，不用再单独维护一路IMU姿态解算）——`mid360/imu`话题不用整个丢弃，线加速度/角速度部分仍然要用（现有`_p_state`/`_v_state`互补滤波传播逻辑不变）。
   - 新增订阅`mavros/hrlv_ez4_pub`（`sensor_msgs/Range`），新增参数`range_min_valid_m`（跟原`agl_hold`设计一样，测距雷达最小量程0.19-0.2米，留量取0.25）。
   - z这一路的"外部观测修正值"来源从"`uwb/pose_abs`的z分量（Gazebo真值+噪声）"改成"`range × cos(roll) × cos(pitch)`"（水平倾斜补偿——测距雷达测的是沿机体轴向下的斜距，飞机有俯仰/横滚角时不修正会系统性高估高度，这个修正开销很小，不应该省）。**x/y两路的观测修正值来源不变**（仍然是`uwb/pose_abs`的x/y分量），现有代码里x/y/z是同一个`for i in range(3)`循环处理，需要把z拆出来单独处理，改用独立的观测源+独立的有效性判断。
   - 近地/量程外的边界处理：`range.range < range_min_valid_m`（或者传感器仿真给出的"无回波"哨兵值）时，本次跳过z这一路的"外部观测修正"步骤，只保留现有的IMU纯积分传播（`_p_state[2]`/`_v_state[2]`继续用线加速度积分，不因为range暂时无效就让z跳变或停止更新）——x/y两路的修正不受影响（数据源没变）。这条边界处理本质上是把原方案`agl_hold`里"读数不可信时不要用不可信的range硬算Z"这条设计原则，从"某个可选flight-mode里的判断"搬到了"定位融合层永远生效的判断"，重要性不降反升（因为这次是全程唯一的高度来源，没有retreat到"world_z"的余地）。

**验收标准变化**（原方案5节风险清单第2条"agl_hold起降边界阈值"对应调整为）：容器里全程飞行（起飞→巡航→降落），确认`local_position/odom`的z读数在飞过仿地模块时能跟着地形起伏、贴地/起降瞬间不因range读数跌出量程而产生跳变，跟原方案A2的验收标准实质等价，只是验证对象从"agl_hold模式切换行为"变成"z融合层全程行为"。

### 1.2 `fire_pillar_aim_node`补发`~/fire_pillar_aim_pose`

现有实现只把锁定的悬停点发到`~/fire_pillar_aim_cmd`控制自己飞行，没有对外广播这个坐标值本身。第四部分的程序需要知道"RECON角色锁定的瞄准点具体在哪"才能告诉SUPPLY角色"投射完之后飞到同一个位置接着投"，所以补发一个话题。这是本方案对现有节点**唯一**必要的代码改动，其它节点都不动。

### 1.3 维持不变的模块

`pillar_detector_node.py`、`qr_apriltag_detect_node.py`、`mission_judge_node.py`——都不改，理由见0.2/0.4节。`flight-stack-entrypoint.sh`要确保`pillar_detector_node`正常启动（立柱朝向检测依赖它）。（`actuator_action_node.py`v3-e起不再属于"维持不变"，见新增1.6节。`position_cmd_relay_node.py`v3-g订正后重新回到"维持不变"名单——原来1.1节打算给它新增`agl_hold`模式，现在改成直接修`uwb_imu_fusion_node.py`的z融合逻辑，`position_cmd_relay_node.py`不用动，见1.1/1.1a节。）

### 1.4 新增`formation_follower_node`：编队跟随的实时控制回路（v3-b修正：从SDK挪回这里）

**先答用户问的"双机编队有哪些编队模式"，再讲这次选的模式+为什么控制回路要放这里。**

编队一般从两个相互独立的维度分类，实现时是两个维度的组合，不是单选一个就够：

**维度A：几何队形**（两机/多机之间保持什么样的相对位置关系）
- **纵向编队（tandem/column，本方案选的）**：一前一后、单路纵队，后机沿前机的飞行路径跟随，横向间距为0（只有前后距离）。优点是所需的规避走廊最窄（适合狭窄空间/密集障碍场景），实现也最简单（跟随目标只需要沿一条一维的"路径弧长"追踪，见下面）；缺点是万一前机遇到需要横向机动的紧急避障，后机反应链路更长（要等前机先完成机动、这个动作反映到路径缓冲区里，后机才能跟上）。
- **横向编队（line-abreast，并排）**：两机并排、垂直于飞行方向保持固定横向间距。优点是两机视野/传感器覆盖范围横向拉开，适合并排搜索这类任务；缺点是需要的空间走廊比纵向宽，且计算"侧向偏移量"要考虑航向实时变化（v2初稿最早试过这个方案，撤回了，见DEBUG_JOURNAL.md 2026-09-12历史记录）。
- **楔形/斜向编队（echelon，介于纵向和横向之间）**：后机在前机的斜后方，跟军机编队的"三角队形"是同一个思路，两机既有一定纵向距离也有一定横向距离，是纵向和横向的折中方案。
- （3机以上还有三角形/菱形/环形等，这次只有2架机，这些队形对2机没有意义，不展开。）

**维度B：控制策略**（怎么让"跟随者"的实际位置贴近"应该在哪"这个目标）
- **长机-僚机/跟随式（leader-follower，本方案选的）**：跟随者持续读长机的实时位置/轨迹，算出"我现在应该在哪"这个目标点去追。实现简单、去掉长机不影响跟随者的基本导航能力（跟随者本身还是能独立导航，只是这一路会尽量往目标点靠），但精度依赖跟随算法本身（比如本方案强调的"必须沿长机实际走过的弧线追、不能走直线抄近道"这个坑）。
- **虚拟结构法（virtual structure）**：整个编队被当成一个刚体，所有飞机相对一个"虚拟中心点"保持固定的刚性偏移，中心点的轨迹单独规划，所有飞机同步跟中心点走。精度更高（队形几乎不会变形），但需要一个额外的编队级规划器协调所有飞机，实现复杂度明显更高，双机场景下用长机-僚机就够，不需要上这一档。
- **基于行为法（behavior-based）**：每架飞机根据"跟目标的距离"、"跟队友的距离"、"避障"等几条简单行为规则的加权叠加决定自己的运动，类似鸟群/鱼群仿真的思路，更适合大规模集群、不需要精确保持队形的场景，这次任务需要相对精确的位置关系（比如通知点17"SUPPLY抵达RECON瞄准点位置"这种需要精确坐标的场景），不适合选这个。

**这次任务的选择**：几何队形=纵向（用户已经定的，任务描述里"编队飞行、先后经过...点"这个措辞本身也更像纵队而不是并排），控制策略=长机-僚机的路径跟随（不是虚拟结构法，双机场景没必要上更复杂的编队级规划）。

### 1.4.1 接口设计：几何队形和控制策略都做成可插拔的（用户要求，不要把这次的选择焊死）

上面维度A（几何队形）、维度B（控制策略）不是只做一个选项，`formation_follower_node`要按**策略模式（strategy pattern）**设计成两层可替换接口，这次任务只**实现**纵向+长机-僚机这一种组合，但**接口**要能塞进其它组合，以后换任务/换编队需求时不用重新设计这个节点：

```python
class FormationGeometry:
    """几何队形接口：给定"我"当前的角色参数，从长机的轨迹缓冲区里
    算出"我现在应该在哪个目标点"。不同队形只是这个方法的不同实现。"""
    def compute_target(self, leader_path: LeaderPathBuffer, params: dict) -> Pose2D:
        raise NotImplementedError

class TandemGeometry(FormationGeometry):
    """纵向：沿长机实际轨迹回溯固定弧长（这次任务实际使用的实现，
    就是之前设计的point_at_arc_length_behind()逻辑）。"""
    def compute_target(self, leader_path, params):
        return leader_path.point_at_arc_length_behind(params['follow_distance_m'])

class LineAbreastGeometry(FormationGeometry):
    """横向：在长机当前朝向的法线方向偏移固定距离（v2初稿试过又撤回
    的方案，这次作为一个可选实现保留接口，不在这次任务里启用——
    保留是因为"接口留着、任务不用"和"接口都没有、以后要用重新设计"
    是两回事，成本很低，不该因为这次不用就不留）。"""
    def compute_target(self, leader_path, params): ...

class FormationControlStrategy:
    """控制策略接口：拿到FormationGeometry算出来的目标点之后，
    决定怎么把这个目标发出去（比如要不要做平滑/预测，用什么频率、
    什么接口发布）。"""
    def track(self, target: Pose2D, params: dict) -> None:
        raise NotImplementedError

class LeaderFollowerStrategy(FormationControlStrategy):
    """长机-僚机：直接把目标点当前一次性目标发给~/rviz_goal_world，
    这次任务实际使用的实现。"""
    def track(self, target, params): ...

class VirtualStructureStrategy(FormationControlStrategy):
    """虚拟结构法：预留接口，不在这次任务里实现（双机场景没必要，
    见上面维度B的讨论），以后如果编队规模变大/需要更刚性的队形，
    在这里加一个新的策略类，不用改FormationGeometry那一层。"""
    def track(self, target, params): ...
```

`formation_follower_node`初始化时按参数（比如`geometry_type='tandem'`、`strategy_type='leader_follower'`）选择具体用哪个`FormationGeometry`+`FormationControlStrategy`的实例，节点主循环只认这两个抽象接口，不关心具体是哪个实现——这样以后要加新队形/新策略，只需要新增一个子类，不用碰节点主逻辑。

### 1.4.2 为什么这个控制回路必须放在飞行栈，不能放SDK

（1.0节原则的具体应用）：编队跟随需要"持续读长机实时位置→计算目标点→发布新目标"这个回路以5-10Hz频率跑，如果这个回路放在选手程序里、选手程序又跑在跟无人机有网络延迟的地面站电脑上，每个控制周期都要承受一次往返延迟，跟随的响应会明显滞后、变得不稳定。这个回路必须放在**机载**运行——具体来说是**跟随角色所在那架飞机**的flight-stack容器里常驻一个`formation_follower_node.py`（跟长机的odom是同一个局域网/同一台宿主机内部的话题通信，不经过选手程序所在的那台机器，没有额外的跨网络延迟）。

节点逻辑：①订阅长机的odom（跨命名空间订阅，`/<长机namespace>/dlio/odom_node/odom`，这个话题名在`gt`/`uwb_slam`/`uwb_imu`三种定位源下都统一，见0.3节）喂进`LeaderPathBuffer`；②以5-10Hz频率调用当前配置的`FormationGeometry.compute_target()`；③**这一步在这次任务实际用的`uwb_imu`模式下不需要坐标转换**——`origin_setter_node`（在线θ*旋转对齐）是`uwb_slam`模式专属的节点，`uwb_imu`模式下两机的odom本身就是同一个UWB锚点下的全局系坐标，长机局部系=跟随者局部系=世界系，直接拿长机odom的x/y喂给跟随者自己的目标点即可（v3-f订正，之前版本这里假设了跟`uwb_slam`一样需要对齐，是错误类比）；④调用当前配置的`FormationControlStrategy.track()`把目标发出去。

**节点常驻但默认不生效**：`formation_follower_node`跟其它flight-stack节点一样常驻运行，但默认处于"待命"状态（不发布任何目标），需要收到一次启用信号（下面SDK那个一次性触发调用，带上"谁是长机、用哪个geometry/strategy"这几个参数）才开始真正工作——这样即使选手程序在决定"什么时候开始编队"上有延迟（网络延迟或者选手自己代码逻辑的时间），也只影响"编队什么时候开始"这个决策时机，不影响编队跟随本身的控制质量，符合1.0节"决策可以有延迟，控制不能有延迟"这条原则。**每架飞机的flight-stack容器都要起这个节点**（不是只在"跟随角色"那架飞机上起）——理由见1.5节的双机对称性讨论，角色是运行时决定的，不能只给某一架飞机装这个能力。

### 1.5 双机硬件/软件完全对称，角色必须是运行时参数，不能焊死在NX01/NX02身上（用户直接问，之前版本没考虑到）

**用户问的原话**："无论仿真还是真机，两架飞机都是完全一样的软硬件配置，本方案考虑了这一点吗？双机角色互换还能实现一样的功能吗？"——**之前v3/v3-b的文档没有认真考虑这一点**，第四部分一直是"NX01程序"/"NX02程序"这种按机身编号分别描述的写法（"NX01做航线+双火情响应主导，NX02做编队跟随+补给+第二次投射"），这种写法暗含了"NX01永远是侦查角色、NX02永远是任务角色"这个假设，但两架飞机硬件/软件完全一样，这个假设**没有任何物理/软件上的依据**，纯粹是文档描述习惯上的偷懒，需要纠正。

**修正原则：任务程序按"角色"编程，不按"机身编号"编程**——`contest_mission`/`contest_sdk`/第四部分的全流程程序里，任何逻辑分支都不应该出现"如果`namespace=='NX01'`就做A、`namespace=='NX02'`就做B"这种写法，应该是"如果`role=='recon'`就做A、`role=='supply'`就做B"，`role`是一个**运行时参数**，跟`namespace`（NX01/NX02，只是飞机的身份标识/命名空间前缀）完全解耦。第四部分的"NX01程序"/"NX02程序"这个说法在这次修正之后应该理解为**"这次运行时分配到侦查角色的那架飞机所执行的程序"**/**"分配到任务角色的那架飞机所执行的程序"**，不是"NX01这个身份专属的程序"——两架飞机跑的是**同一份代码**，只是启动时传入的`role`参数不同。

**角色怎么分配**（这次方案给一个简单、确定性的方案，不做复杂的动态协商）：
- **静态配置（推荐，这次任务用这个）**：启动全流程程序时，通过一个启动参数/环境变量显式指定这次运行谁是`recon`、谁是`supply`（比如`python3 mission.py --namespace NX01 --role recon`跟`python3 mission.py --namespace NX02 --role supply`），由地面操作员/发起任务的人决定，不是程序自己判断。这样两架飞机的实际身份（NX01/NX02）可以任意组合角色，今天NX01当侦查机、明天NX02当侦查机都只是改一个启动参数，代码不用动一行。
- **动态协商（可选，更复杂，不建议作为这次任务的第一版）**：两架飞机启动后先互相通信一轮，按某个确定性规则（比如"哪架机先完成起飞就当侦查机"或者"命名空间字符串小的当侦查机"）自动分配角色，不需要人工指定。这个方案的好处是操作员不用记得每次指定角色，坏处是多引入一层"角色协商"逻辑本身也要处理消息丢失/超时等可靠性问题（2.3节讨论的可靠性问题在这里会再出现一次），这次任务范围内不建议做，用静态配置就够，需要的话以后可以单独加。

**这条修正对前面几节的影响**（回过头顺一遍，确保没有遗漏）：
- 1.4节`formation_follower_node`：已经改成"每架飞机都起这个节点、角色运行时决定"，跟这条原则一致，不用再改。
- **2.1节SDK能力，这里发现一处遗漏，顺带修正**：`sdk.send_to_teammate()`底层要往`/<队友namespace>/mission_events`这个话题发消息，**必须知道队友的`namespace`是NX01还是NX02**——如果程序本身只知道`role`（自己是recon还是supply）、不允许硬编码`namespace`，那"队友的namespace是什么"这件事就不能再假设"我是NX01所以队友肯定是NX02"这种写法（这种写法本身就是变相的namespace硬编码，只是换了个地方硬编码）。修正：`DroneSDK`初始化时除了`namespace`（自己的身份，这个跟物理飞机绑定、不是运行时可选的，必须传）+`role`（自己这次运行分配到的角色）之外，**还要传一个显式的`teammate_namespace`参数**（比如`DroneSDK(namespace='NX01', role='recon', teammate_namespace='NX02')`），跟角色分配一起由启动这次任务的人/脚本决定并传给两边程序（两边程序分别拿到`teammate_namespace='NX02'`和`teammate_namespace='NX01'`），SDK内部`send_to_teammate`/`on_teammate_event`用这个参数拼话题名，不自己猜。这样"角色可以互换"和"知道往哪发消息"这两件事互不冲突，都是启动时配置决定，不是代码里硬编码的假设。
- 4.1-4.3节：这次修订统一把"NX01程序"改成"侦查角色程序"、"NX02程序"改成"补给角色程序"的表述，具体见4.0节新增的角色命名约定。

### 1.6 `actuator_action_node.py`改造：真实驱动`mavros/rc/override`（v3-e新增，用户明确抓取/投放/发射要真实触发）

**结论先行**：这三个动作（`grab_supply`/`drop_supply`/`horizontal_launch`）不再是"纯状态记录"，改成真正往`mavros/rc/override`话题（`mavros_msgs/OverrideRCIn`，18通道`uint16`数组）发布数据、复用飞机上已经接好的遥控器开关通道触发真实机构——**仿真环境也走这条真实链路**（哪怕Gazebo里没有对应的抓取臂/投放舱物理模型去响应这个信号，链路本身——SDK触发→节点发布override→PX4接收→写入`input_rc`——在仿真里就要能完整跑通、跟真机部署时是同一套代码路径，不需要额外适配）。

**为什么必须持续发布+完成后显式释放，不能"发一次就不管"**（这次直接查了本机`/home/robots/PX4-Autopilot`源码+mavros源码`rc_io.cpp`确认，不是猜的）：
- mavros的`rc/override`插件收到一条ROS消息就转发一条MAVLink `RC_CHANNELS_OVERRIDE`，**自己不会重发**，发布节奏完全靠调用方。
- PX4收到`RC_CHANNELS_OVERRIDE`后直接把值写进`input_rc`这个uORB话题（`rc_lost=false`），消费它的`rc_update`模块是**订阅回调驱动**（`SubscriptionCallbackWorkItem`），不是定时轮询——**PX4底层没有"隔多久没收到override消息就自动判失效/回退"这个机制**。
- 如果飞机上的真实遥控器接收机同时在正常工作（大概率会，作为安全后备），它会用远高于override频率的硬件帧率持续往`input_rc`发布数据，等于不断把这个话题"抢回去"——只发一次override，很快就会被下一帧真实的、没被拨动的开关状态覆盖回去。
- 如果没有真实接收机在工作，停止发布也不会自动复位，必须显式发一帧把该通道设成`CHAN_NOCHANGE`（65535）交还控制权。

**因此这个"持续发布+完成后释放"的短时回路必须放在flight-stack内、机载执行**（1.0节原则的又一次应用——虽然这次不是"全程持续"，是有限时长（几秒量级）的一次性动作，但动作期间仍然要以稳定频率持续publish，跨网络维持这种短时高频回路一样有"网络抖动导致提前掉线、机构没触发完就被真实RC抢回去"的风险，不适合放SDK里跨网络做）：

- `actuator_action_node.py`保留现有的触发接口不变（`~/set_parameters`设置`action_name`参数，完成后`action_status`广播`done:<name>`）——**SDK这边`sdk.do_action()`的调用形状完全不用改**，只是这个节点内部从"记录状态"换成"真的发布override"。
- 新增参数化的动作→硬件映射表（每个`action_name`对应一个RC通道号+目标PWM值+持续时长，比如`action_rc_map`参数或者每个动作单独几个参数），**不要硬编码具体通道号/PWM值**——这几个数字是真实硬件接线决定的，方案层面拿不到，需要跟做机构/接线的人确认后再填，具体见下面"未决问题"。
- 收到触发后：以≥10Hz频率发布`OverrideRCIn`（只改配置的那个/那几个通道，其余通道全部设成`CHAN_NOCHANGE=65535`，不影响别的通道），持续到配置的`hold_duration_s`，然后**显式发一帧把该通道也设回`CHAN_NOCHANGE`**释放控制权，最后发布`action_status`为`done:<name>`。
- `setup.py`/`package.xml`不用新增文件，但`package.xml`要加`<depend>mavros_msgs</depend>`（现在缺这一条）。

**未决问题（这次会话没有答案，需要后续明确，不是本方案能替你决定的）**：
1. **三个动作分别对应哪个RC通道、什么PWM值触发、需要保持多久**——这是真实遥控器/机构接线的硬件细节，不属于软件设计范围，需要跟负责机构/遥控器接线的人确认。方案里先把这几个值做成可配置参数，不写死，等硬件确定了直接改参数、不用改代码。
2. **仿真环境下"没有真实机构响应"这件事要不要在`action_status`之外，另外提供一个人工可观察的验证手段**（比如在`ros2 topic echo /<ns>/mavros/rc/override`能看到值变化，作为"链路本身通了"的验证依据）——建议作为验证阶段的手段，不需要额外写代码。

### 1.7 `precision_servo_node.py`改造：拆成"仅居中定位"/"精降抓取"两种模式（v3-f新增，用户直接给的设计）

**现状核查发现的问题**：现有`precision_servo_node.py`（226行）只有一种写死的行为——检测到目标tag后，水平误差未收敛只修水平，收敛后开始按固定速率下降，下降到`min_descent_z`参数（默认0.15米）之后**停在那个高度悬停、持续发布`precision_land_cmd`**，不会触发任何"降落完成"的信号，也没有对外广播"我现在的位置"这个接口。这个行为只适合"精降到底、原地悬停等后续处理"这一种用途，D5地面火情响应子流程里RECON角色"命中后悬停居中、读自己坐标广播给SUPPLY"这个用法（不需要下降，只需要居中）现有代码支撑不了。

**改造方案：加一个模式选择参数（比如`servo_mode`，取值`'center_only'`/`'precision_land'`），两种模式共享同一套水平误差收敛逻辑，收敛之后的行为分叉**：

- **模式A `center_only`（仅居中定位，新增）**：水平误差收敛到`horizontal_converge_threshold_m`以内并稳定`hold_duration_s`秒（新增参数，避免瞬间抖动误判为"已收敛"）后，**不下降**，直接把当前`_odom_xy_z_yaw`的x/y广播到一个新话题（比如`~/centered_pose`，`geometry_msgs/PointStamped`或复用`PositionCommand`），同时发一次"完成"通知（跟`actuator_action_node`的`action_status`同款字符串广播模式，方便SDK统一轮询），之后**停止发布**`precision_land_cmd`（交还控制权给`position_cmd_relay`当前的其它模式，不继续闭环控制）。给RECON角色用：命中地面火情标记后触发这个模式，收敛完成时读到的x/y就近似等于地面火情的世界坐标（理由见0.2节，不做单目位姿解算）。
- **模式B `precision_land`（精降抓取，现有逻辑+补完）**：水平误差收敛后开始下降（现有逻辑不变），下降到`min_descent_z`后，**新增**：不再是"停在那个高度悬停"，而是调用现有的降落触发接口（复用0.1节已有的`TakeoffLand`消息触发降落，跟第四部分`sdk.land()`底层调的是同一个接口，不需要新造一个降落机制），等待`armed`变`false`确认真正落地，然后发一次"完成"通知。给SUPPLY角色抓取物资用：飞到物资点上方触发这个模式，落地后`sdk.do_action('grab_supply')`抓取，完成后**重新`sdk.takeoff()`**飞去投放（v3-f第1条订正为`CONTROLLER=pt4ctrl`正是为了让这次重新起飞能成立）。

**这两种模式对应两个不同的SDK能力**（2.1节需要补充，具体见新增第10条）：模式A是"触发居中+等待返回坐标"这种一次性调用（跟`wait_for_detection`类似的阻塞语义），模式B是"触发精降+等待落地确认"这种一次性调用（跟`sdk.land()`类似，但多了"先精确对准再降"这一步）。两种模式都不是持续控制回路本身（回路本身还是在`precision_servo_node`内部按1.0节原则跑在机载），SDK只负责"触发+等结果"。

---

## 第二部分：选手SDK（`contest_sdk`）

这部分是通用能力库，不含这次任务专属的事件名/坐标/动作名，以后换任务题目也能复用。

### 2.1 SDK要封装的能力（对应第一部分+现有ROS2原生接口）

1. **统一的检测结果接口**——封装成"等待某个class_id出现"的阻塞式/回调式API，底层订阅`vision/detections`过滤`class_id`。
2. **任务状态发布/查询**——封装`mission_state.py`契约的发布/订阅，选手调用`sdk.set_mission_state('searching:orbit_pillar_2')`这种语义化接口，不用自己拼消息。
3. **跨机通信**——封装成`sdk.send_to_teammate(event, **kwargs)`/`sdk.on_teammate_event(event_name, callback)`，底层是`/<teammate_namespace>/mission_events`话题+2.3节的可靠性协议，**事件名本身是选手自定义的字符串，SDK不限定具体有哪些事件**（这是跟之前版本的一个区别——事件词汇表这次放进第四部分，SDK只提供通用的"发消息给队友"能力）。**队友的`namespace`不是SDK自己猜的**，是`DroneSDK`初始化时显式传入的`teammate_namespace`参数（见1.5节的修正说明），SDK内部只负责拼话题名+可靠性协议，不做任何"我是NX01所以队友是NX02"这种假设。
4. **动作执行**——封装`actuator_action_node`的`~/set_parameters`+`~/action_status`轮询，选手调用`sdk.do_action('grab_supply')`（动作名同样是选手自定义字符串，SDK不限定）。
5. ~~飞行模式开关——封装`sdk.set_altitude_mode('agl'|'world_z')`~~（**v3-g订正：整条删除**——1.1节`agl_hold`模式方案已作废，改在`uwb_imu_fusion_node`层修z融合逻辑，`uwb_imu`模式下AGL全程默认生效、没有"world_z"这个可选项，不需要这个开关接口）。
6. **坐标系转换**——封装`sdk.world_to_local(x, y, z)`，底层走TF查询（0.3节提到的陷阱，选手不应该自己手写TF查询代码）。
7. **编队跟随开关（v3-b修正：只是个一次性触发调用，不是控制回路本身）**——`sdk.start_formation_follow(follow_distance_m=3.5)`/`sdk.stop_formation_follow()`，底层就是调一次`~/set_parameters`/一个简单service，启用或停用1.4节新增的、常驻在flight-stack里的`formation_follower_node`。SDK这一层**不做**任何持续订阅odom/计算目标点的工作，那些全部在机载节点里，理由见1.0节的架构原则——这是v3-b相对v3的修正，v3曾经把这整个控制回路错放进了SDK，用户指出"实时功能得放到飞行栈，因为地面站到无人机有延迟"，详见1.4节。
8. **通用航点生成helper（这次重新梳理时补的一处打包遗漏）**——`orbit_flight_helper.generate_orbit_waypoints()`/`ground_scan_helper.generate_ground_scan_waypoints()`这两个纯函数目前是`contest_mission`包（跑在flight-stack容器里）的一部分，第四部分的程序里直接写了"用`orbit_flight_helper.generate_orbit_waypoints(...)`"，但**全流程程序实际跑在第三部分的选手容器里，跟flight-stack是两个不同的容器/Python环境，不一定能直接`import contest_mission`这个包**——这是之前版本没检查出来的一处接口通用性/封装完整性漏洞。修正：这两个纯函数（本身就跟ROS无关，是纯几何计算）**打包进`contest_sdk`**，作为`sdk.generate_orbit_waypoints(cx, cy, radius, z, num_points)`/`sdk.generate_ground_scan_waypoints(...)`这两个静态方法/独立函数对外暴露（具体做法是`contest_sdk`直接vendor一份这两个函数的代码，或者把它们从`contest_mission`包里抽出来做成一个两边都依赖的独立小包`contest_geometry_helpers`——哪种做法更好留给实现时决定，但**必须让选手容器能直接调用到，不能要求选手额外装`contest_mission`这个跟ROS2节点耦合的大包**）。同理也提醒一下：4.2/4.3节提到的"长距离分段插值"、"立柱确定性编号排序"这些纯函数逻辑，既然是全流程程序自己要用的通用几何计算，也应该放进这个"通用几何helper"的范畴，不需要依赖`contest_mission`包，直接写在全流程程序自己的代码里或者也收进`contest_sdk`都可以，不构成新的打包问题（这两个本来就是这次任务专属逻辑，不是复用现有`contest_mission`文件，没有跨容器import的风险）。
9. **驱动地面站声光装置（v3-e新增，用户明确要选手程序主动控制）**——`sdk.trigger_alarm(pattern, duration_s=None)`，底层通过0.5节新增的"链路D"联系到GCS所在机器触发硬件（具体接口方式——串口/USB继电器/HTTP调GCS后端一个小接口——留给实现时按实际硬件+GCS现有技术栈确定，这份方案不假设GCS后端已经有对应的HTTP/WebSocket接口，需要实现时跟GCS那边一起补），`pattern`是选手自定义的字符串（比如`'fire_found'`/`'mission_complete'`，跟事件名一样不由SDK限定具体取值）。可靠性要求参照链路A（要有"触发成功"的确认，不能发了不管），但**这是纯粹的人体验层面的提示，不是任务逻辑的一部分**——`sdk.trigger_alarm()`调用失败/超时不应该让整个任务程序崩溃或者阻塞后续任务逻辑，建议这个方法内部把异常吞掉、只打印一条警告（违反2.2.1节"不能吞异常"的一般原则是刻意的例外，因为声光装置纯粹是锦上添花的人机提示，不应该让一个外设故障拖垮真正的任务执行）。
10. **精确对准两种模式的触发（v3-f新增，对应1.7节`precision_servo_node`的改造）**——`sdk.center_on_target(class_id, timeout)`：触发模式A`center_only`，阻塞直到节点广播"已居中"+返回收敛时的坐标，或超时抛异常；`sdk.precision_land_and_confirm(class_id, timeout)`：触发模式B`precision_land`，阻塞直到节点确认真正落地（`armed`变`false`），或超时抛异常。两者都是"一次性触发+等待结果"模式，符合1.0节架构原则，控制回路本身仍在`precision_servo_node`内部。RECON发现地面火情用第一个，SUPPLY抓取物资前用第二个。
11.（实现阶段D写`mission.py`时补的三个能力，v3-g追加）**`sdk.read_fire_pillar_staging_pose(timeout)`/`sdk.read_fire_pillar_aim_pose(timeout)`**——D6需要读`fire_pillar_aim_node`锁定成功后持续广播的等待点/瞄准点坐标（`~/fire_pillar_staging_pose`/`~/fire_pillar_aim_pose`，1.2节/A1新增），B3清单原来只顾着"触发+等完成信号"这类一次性调用，漏了"持续订阅一个状态话题、取最新值"这类能力（跟`get_mission_state()`/`centered_pose`是同一种模式，不是新发明的接口风格），读不到时抛`DetectionTimeoutError`。**`sdk.get_local_position(timeout)`**——暴露`_odom_xyz`这个原本只在SDK内部（B6进度打印）用的状态，返回自己当前局部坐标`(x,y,z)`，选手需要"知道自己在哪"这类需求（比如判断队友是否已经飞完一段航线）原来没有正式接口可用，补一个薄封装。

### 2.2 SDK总体架构（分层设计）

```
contest_sdk/
  __init__.py          # 对外只暴露DroneSDK这一个类+几个异常类，选手不需要理解rclpy
  exceptions.py         # 见2.2.1节，封装完整性要求：选手拿到的异常必须是
                        #     这里定义的类型，不能是裸rclpy/DDS异常
  _rclpy_runtime.py     # L1：rclpy生命周期封装（后台spin线程、节点初始化/关闭）
  capabilities.py       # L2：2.1节7类能力的具体实现（takeoff/goto/
                        #     wait_for_detection/send_to_teammate/do_action/
                        #     world_to_local/start_formation_follow等，
                        #     每一项都是"一次性指令+等待/轮询结果"这种
                        #     模式，符合1.0节的架构原则，没有一项是
                        #     需要持续高频闭环的控制回路）
  reliability.py        # L3：2.3节可靠性机制（重试/ACK/状态确认），
                        #     被L2内部调用，选手完全无感
```

### 2.2.1 封装完整性：选手不应该在任何情况下接触到裸ROS2/rclpy的东西

这是这次重新梳理时补的一条明确要求，之前版本没有专门讲——不是"选手一般情况下不用碰rclpy"，是**任何情况下（包括出错的时候）都不应该让选手看到rclpy/DDS原始的异常类型、日志格式、消息类型**：

- **异常类型**：`contest_sdk/exceptions.py`定义一套自己的异常（比如`TakeoffTimeoutError`、`TeammateUnreachableError`、`ActionFailedError`），L2代码捕获底层rclpy可能抛出的异常之后统一转换/包装成这些类型再往外抛，选手写`except`的时候只需要认`contest_sdk`自己的异常类，不需要知道`rclpy`异常长什么样。
- **返回值类型**：`sdk.wait_for_detection()`返回的`det`对象是SDK自己定义的简单数据类（比如`det.local_x`/`det.local_y`/`det.class_id`），不是`vision_msgs/Detection2D`原始消息对象——选手不应该需要知道ROS消息字段叫`bbox`还是`results[0].pose`这种细节。
- **`sdk.world_to_local()`的参数/返回值只是`(x, y, z)`三元组**，不暴露`frame_id`这种TF概念——SDK内部知道自己的`namespace`该用哪个frame，选手不需要传。
- 之前版本已经做到的：不暴露`rclpy.init()`/`spin()`（1.0节/2.2节已有）、事件名/动作名是选手自己定义的字符串（不是ROS消息类型）。

选手（第四部分程序）写的代码大概长这样（这次同步更新了`DroneSDK`的构造参数，见1.5节修正——多了`role`和`teammate_namespace`两个参数，`namespace`还是必须传，因为这是跟物理飞机绑定的身份标识，不是运行时可选的角色）：

```python
from contest_sdk import DroneSDK

sdk = DroneSDK(namespace='NX01', role='recon', teammate_namespace='NX02')
sdk.takeoff()                                    # 阻塞直到armed确认或超时报错
                                                  # （v3-g订正：不再需要sdk.set_altitude_mode('agl')，
                                                  # uwb_imu模式下AGL全程默认生效）
if sdk.role == 'supply':
    sdk.start_formation_follow(follow_distance_m=3.5)   # 只有supply角色调用，
                                                         # 不是"只有NX02调用"
sdk.goto(7, -10, 1.5)                             # 阻塞直到到达确认或超时报错
det = sdk.wait_for_detection('apriltag:2', timeout=60)
sdk.send_to_teammate('ground_fire_found', x=det.local_x, y=det.local_y)  # 阻塞直到对方ack或超时
sdk.do_action('grab_supply')                      # 阻塞直到action_status确认done
```

### 2.2.2 可调试性：选手不该需要会用`ros2`命令行才能看懂发生了什么（这次重新梳理时补的，对应"选手易用性"）

选手是竞赛参赛者，不是这个项目的开发者，不能假设选手会主动开一个终端敲`ros2 topic echo`去排查"为什么`goto()`一直不返回"。SDK的L2/L3在每一步阻塞等待的过程中要主动往标准输出打印**中文人话进度**，而不是安静等待直到超时才报一个异常：

- `sdk.takeoff()`/`sdk.goto()`这类阻塞调用，等待过程中定期（比如每2-3秒）打印一行状态，例如"[RECON] 起飞中…当前高度0.8m，目标1.5m"，而不是全程无输出直到成功或超时。
- 超时异常的提示文本要给出**可能原因**而不是单纯报"超时"——比如`TeammateUnreachableError`的信息里除了"等待队友消息超过60秒"，还应该提示"请确认队友程序是否已经启动、两台设备是否在同一个路由器/WiFi下"，帮选手把问题定位到"我这边代码没写对"还是"网络/队友程序没起来"这两类完全不同的排查方向上，不需要选手自己再去翻2.4节的检查清单才能猜到。
- 这些提示信息本身也是`contest_sdk`的一部分（不是外挂的日志系统），意味着2.2.1节"选手不应该接触裸rclpy"这条边界同样适用于日志——打印内容用选手能看懂的话术，不直接透传`rclpy.logging`或者ROS2节点日志里的原始措辞。

### 2.3 可靠性层：路由器互联条件下怎么保证指令/消息真的送达

**这两条链路要解决的问题，这个项目里已经有真实踩过的坑+验证过的应对模式，不是凭空提议**：查`gcs/backend/app.py`发现GCS后端触发起飞时用的`_publish_takeoff_land()`函数，注释原话"连发3次而不是一次——`ros2 topic pub --once`在DDS discovery没跟上时可能白发"，同一个文件里还有一个`_recent_px4ctrl_feedback()`函数专门读`docker logs`确认"px4ctrl到底是真的进了AUTO_TAKEOFF、还是被FSM前置条件静默拒绝了"——这两个函数证明这个项目的开发者早就意识到"指令发出去≠指令被执行了"，只是应对方式（连发几次+读docker日志）比较朴素，SDK要把这套思路做得更系统、更适应"选手程序不一定能docker exec到飞机"的真实部署场景。

**链路A：选手程序 → 飞机（指令下达）**

1. **常驻publisher，不要每次指令都新起一次性进程**——`_publish_takeoff_land()`目前每次新开一个`docker exec`+新的`ros2 topic pub --once`进程，这正是"DDS discovery没跟上就白发"的根源。`DroneSDK`实例初始化时建好所有publisher并常驻，指令下达用同一个已经跟对端匹配好的publisher直接发布。
2. **一次性关键指令内部实现"发送+等待状态反馈"，不是发完就当作成功**——照抄`_recent_px4ctrl_feedback()`的思路，但不能用`docker logs`（真机/路由器互联场景选手程序访问不到飞机容器日志），改成SDK内部订阅对应的ROS2状态话题确认（`sdk.takeoff()`发完后订阅`mavros/state`等`armed`变`true`，超时抛异常）。
3. **有回复语义的指令优先用ROS2 service而不是裸topic**——SDK即使底层还是topic，也应该在L3把"topic发送+订阅状态确认"包装成"看起来像调用一个service"的阻塞语义。

**链路B：双机之间的消息（用户直接问"双机互相通信如何实现、考虑延迟没有"，这里把物理链路+延迟预算都说清楚，不只是讲协议层面的可靠性）**

**先说物理上怎么连**——两种拓扑，二选一，这次方案默认选第一种：

- **方案一（默认，简单）：两架飞机都连同一个地面站路由器/AP，双机通信实际上是"飞机A→路由器→飞机B"两跳**，走的是普通WiFi基础设施模式（跟GCS-飞机走的是同一个物理网络，只是DDS discovery让飞机A和飞机B的节点直接互相看见、互相发布订阅，数据包在链路层还是要经过路由器转发，不是绕开路由器的直连）。好处是不需要额外配置，跟GCS-飞机走同一套网络配置（2.4节的检查清单同样适用）；坏处是任何一架飞机跟路由器的连接质量都会影响双机通信（哪怕两架飞机物理上离得很近，如果其中一架信号弱，双机通信也会跟着变差），且**依赖路由器本身不掉线**——路由器如果重启/故障，不只是GCS失联，连双机互相通信都会断。
- **方案二（可选，更稳但更复杂）：两架飞机之间建立直连链路**（比如各自的WiFi网卡开一个独立的ad-hoc/mesh模式，或者用专门的图传/测控电台做机间数据链，完全绕开地面站路由器）。好处是双机通信不依赖地面站基础设施，路由器挂了双机还能继续互相协同（这次任务里恰好用得上——第四部分4.3节的双机投射流程全程都要靠双机消息，如果这时候路由器出问题，用方案一整个任务就卡死，用方案二至少双机自己能把手头的协同动作做完）；坏处是需要机载电脑多配一张网卡/多一层网络配置，工作量和复杂度都更高，这次任务先不做，方案里记一笔作为**已知的可靠性升级方向**，不是这次的必做项。

**延迟预算（这次会话真正针对"双机链路"单独算的部分，不是照搬GCS-飞机那条链路的结论）**：方案一（经路由器转发）下，双机之间的单跳WiFi延迟通常是几毫秒到几十毫秒量级（同一个局域网内、无严重干扰的情况下），比GCS笔记本到飞机的链路通常还要更短（GCS那条链路v2-b/v3讨论时说的"几十到上百毫秒"，是考虑了GCS所在网络环境可能更复杂；双机之间往往物理距离更近、都是"专用"机载WiFi模块，实测通常更干净）。**但**这次任务里`mission_events`传递的都是1.0节原则里已经归类为"决策层"的一次性事件（发现火情、等待点坐标、投射完成信号），不是需要持续闭环的控制指令，2.3节前两条已经设计的"应用层ACK+1Hz重发"协议本身就是**延迟无关**的（不管单程延迟是5毫秒还是500毫秒，重发协议都能工作，只是"确认送达"这件事花的时间长短不同）——真正需要关注的是**给`sdk.send_to_teammate()`设一个够宽松的总超时**（建议30-60秒，不是几秒钟这种量级），因为这次任务里"没在预期时间内收到队友的关键消息"（比如迟迟等不到`SUPPLY_READY`）意味着任务会卡住甚至失败，这个超时值要覆盖"正常WiFi延迟+一次路由器短暂抖动重连"这种情况，不能设得太紧张导致正常场景也频繁误报超时。

具体协议设计（跟延迟量级无关，是通用可靠性设计）：
1. **纯topic发布是"尽力而为"，不是"保证送达"**——DDS的RELIABLE QoS只保证已匹配订阅者之间不丢包，对方那一刻掉线照样永久丢失且发布方不知道。
2. **应用层ACK+重发协议**（行业标准做法）：每条事件加唯一ID，发送方以固定低频率（建议1Hz）持续重发直到收到接收方回发的ack，接收方用ID去重、避免重复处理业务逻辑。封装在`reliability.py`，`sdk.send_to_teammate()`对选手表现为"阻塞直到确认送达，超时抛异常（超时值见上面的延迟预算讨论）"。
3. **QoS设置**：`mission_events`话题用`RELIABLE`+足够`history depth`（建议10）；可加`TRANSIENT_LOCAL`durability，进程重启重新订阅也能拿到最近几条历史事件。

**链路D：选手程序→GCS机器（驱动声光装置，v3-e新增）**——跟链路A/B同样需要"发了要确认"（不能假设装置一定响应了），但触发频率极低（只在4.4节几个关键通知点触发，不是持续控制），且**失败影响范围仅限于人机提示体验，不影响任务本身能否完成**（跟链路C"只读展示"的低可靠性要求所在的理由不同——链路C低要求是因为"只是展示进度"，链路D低要求是因为"只是提示，不是任务逻辑的一部分"，两者结论相似但原因不一样，不要混为一谈）。因此`sdk.trigger_alarm()`内部可以做一次简单的确认+短超时（比如1-2次重试、总超时几秒），超时后**吞掉异常、只打日志**，不像`send_to_teammate()`那样超时要抛出去阻塞任务流程——这是2.2.1节"选手不应该接触裸异常"原则之外，**唯一一处刻意选择不向选手抛出异常的能力**，原因见2.1节第9条。

### 2.4 部署现场的网络配置检查清单

1. **AP client isolation（客户端隔离）必须关闭**——很多路由器默认开启"同一WiFi下设备互相隔离"，会导致GCS和飞机机载电脑即使连同一路由器也完全无法互通，最容易被忽视、后果最严重的一条。
2. **CycloneDDS改成单播peer list，不依赖多播发现**——很多路由器不转发或限速多播流量，`CycloneDDS`的`<Discovery><Peers>`配置把GCS和每架飞机IP显式列出来。项目现有的`docker_sim_cyclonedds.xml`配的是"lo"回环接口（容器都在同一台宿主机），真机部署到跨机器场景时必须换成实际网卡+对方IP的peer列表。
3. **系统防火墙UDP端口**——需要放行，否则会出现`ros2 topic list`看得到对方、但实际收不到数据这种隐蔽故障。
4. **`ROS_DOMAIN_ID`全场一致**（这个项目用21），建议SDK启动时主动校验，读到不一致直接报错提示。
5. **WiFi带宽/信道占用**——控制指令跟视频流（MJPEG拉流）共用带宽时可能互相挤占，控制链路不稳定时可以先关视频流排查。

---

## 第三部分：选手开发/运行环境

**核心思路：容器要存在，但选手全程不需要知道"容器"这两个字，也不需要在终端敲任何`docker`命令。** 选手能感知到的操作只有两件事：①写Python代码；②运行一个脚本看到飞机飞起来。

**方案①（推荐）：一个双击运行的启动器脚本**——文件夹里有`我的任务.py`（选手写代码的地方，预置好`import contest_sdk`的示例框架）+`运行仿真.sh`/`.bat`双击脚本，脚本内部包一行`docker run --rm --network host -v "$(dirname "$0")":/workspace contestant-sdk:latest python3 /workspace/我的任务.py`，选手全程不接触Docker概念，只需要装好Docker Desktop本身。脚本要包一层错误捕获（Docker没装/没启动时打印人话提示，不是原始报错）。

**方案②（进阶可选）：VS Code Dev Containers**——给本来就用VS Code的选手，打开文件夹后VS Code提示"在容器中重新打开"，点一下之后集成终端/代码补全/调试器全部自动切到容器环境，开发体验接近本机开发。跟方案①共用同一个底层镜像，可以同时提供、不冲突。

**不推荐**：选手裸机直接跑（技术上因为`network_mode: host`可行，但选手要自己搞定ROS2 Humble+`rmw_cyclonedds_cpp`+`CYCLONEDDS_URI`配置+自定义`.msg`包编译，环境问题比算法逻辑本身更容易卡住选手，且难以分清是代码问题还是环境问题）。

**跟仿真栈本身的关系**：`contestant-sdk`镜像跟`sim-world`/`flight-stack-nx01`/`flight-stack-nx02`三个仿真栈容器完全独立，仿真栈由赛事方按现有方式起好，选手的容器只是"多一个能看到同一套ROS2话题的旁观者+指挥者"，不需要选手碰仿真栈的启动流程。

### 3.1 基础设施边界：选手能用什么、不能碰什么（这次重新梳理时补的）

- **文件系统隔离是主要的、实际生效的防线**：`contestant-sdk`镜像里根本不包含`sim-world`/`flight-stack`的源码、`mighty`/`ego_planner`/PX4固件、world/launch文件——选手拿到的只有`contest_sdk`这个Python包（2.2节的分层结构）+自己那份任务代码，物理上就没有"改飞行栈代码"这个选项，不需要靠自觉或者权限管理去约束，选手的容器和挂载卷（`-v "$(dirname "$0")":/workspace`，见方案①）只暴露选手自己的工作目录，不会把仿真栈的仓库目录挂进去。
- **ROS2/DDS网络层面，本方案不做细粒度访问控制**：`contestant-sdk`容器和仿真栈容器在同一个`ROS_DOMAIN_ID`（21）下，技术上选手如果绕开SDK自己写rclpy代码，是可以订阅/发布任意话题、甚至给`flight-stack`发奇怪指令的——但2.2.1节已经约定SDK不会给选手暴露裸rclpy接口，正常使用SDK写任务代码的选手不会走到这条路上；真要在DDS层面做强制限制（比如用SROS2的安全策略给每个`ROS_DOMAIN_ID`参与者签发证书、限定谁能读写哪些话题），是一项独立的安全加固工作，工作量不小（证书分发、权限文件维护），**本方案不做，列入7节"明确排除范围"**，如果以后发现选手会主动绕过SDK搞破坏，再考虑升级到这一层。
- 换句话说：这次的隔离策略是"选手拿不到能改基础设施的文件"（容器级别，简单可靠），不是"选手在网络里被技术手段挡住"（协议级别，更彻底但也更重）——跟GCS一贯的"防呆优先于功能完备"取向一致。

### 3.2 换选手：如何快速恢复到"干净状态"（这次重新梳理时补的）

比赛现场大概率是多名选手依次上场测试/正式比赛，前一个选手的程序可能中途崩溃或者写出有bug的逻辑，留下的"脏状态"不只是道具位置（`scenario_reset_node`的`~/reset_scenario`已覆盖）和判分进度（`mission_judge_node`的`~/reset_checkpoints`已覆盖），还可能包括：

- 飞机还悬停在半空、没有降落；
- `formation_follower_node`还处于启用状态（1.4节）；
- `mission_state`/`mission_events`里还残留着上一位选手任务跑到一半的状态和历史消息。

这些都不是`scenario_reset_node`/`mission_judge_node`职责范围内的东西（它们管的是"场景道具"和"判分"，不是"飞行栈运行时状态"），单独再写一个"运行时状态复位"服务去覆盖这些分散的状态点，本身就容易漏掉某个新加的状态（每次1.x新增一个模式开关，这个复位服务都要跟着改，维护成本持续存在，还可能出现"复位了但漏了一个"的隐蔽故障）。

**推荐做法：换选手之间直接`docker compose restart sim-world flight-stack-nx01 flight-stack-nx02`**（只重启这三个仿真栈容器，GCS不用重启，`contestant-sdk`容器本来就是选手一次性运行、跑完自然退出，不需要额外处理）。重启是"重新加载整个进程"级别的复位，不需要逐项枚举/维护"运行时状态"清单，天然不会漏——跟[[project_gcs_competition_context]]里"GCS给参赛选手用，UI要防呆优先于功能完备"是同一个取向：**选简单粗暴但绝对可靠的方案，不为了省几秒钟重启时间去换一个可能复位不干净的精细方案**。容器重启到PX4 SITL/mighty重新完成初始化大概是十几秒到几十秒量级（跟现有开发迭代时重启容器的经验一致），比赛场景下这个等待时间可以接受。

一个更精细的"选择性局部复位"服务，作为未来如果发现十几秒重启时间成为现场瓶颈时的优化方向，本方案不做（列入7节"明确排除范围"）。

---

## 第四部分（最终交付物）：全流程任务程序

这是选手/开发者用第二部分的SDK写出来的一份普通Python程序（不是`contest_mission`包里的ROS2节点），运行在第三部分的选手环境里，真正实现题目描述的三段任务。这次任务专属的所有词汇在这一层定义，不出现在SDK或contest_mission里。

### 4.0 命名约定：按角色写，不按机身编号写（呼应1.5节）

下面几节为了叙述方便，仍然会用"NX01"/"NX02"举例，但这**只是"这次具体运行"的一个例子**，不是代码层面的硬编码——本方案实际只有**一份**全流程任务程序代码，两架飞机跑的是同一份代码，靠启动参数`role`区分行为分支。为了阅读方便，下面用**RECON**（负责航线领飞、地面/立柱搜索、率先投射）和**SUPPLY**（负责编队跟随、物资投送、递补投射）指代这两个角色，`role='recon'`/`role='supply'`是`sdk.role`的取值，读到"RECON侧做什么"就理解成"分配到recon角色的那架飞机（这次运行是NX01，换一次运行也可能是NX02）执行的逻辑"。

### 4.1 任务专属词汇表

**跨机事件名**（`sdk.send_to_teammate(event, **kwargs)`的`event`参数取值，由这份程序自己定义，SDK本身不限定）：

```python
# RECON/SUPPLY两个角色共用同一份代码里的这份词汇表定义
# （可以放进程序自己的一个mission_vocab.py里）
GROUND_FIRE_FOUND = 'ground_fire_found'    # RECON->SUPPLY，附x/y
HR_FIRE_LOCKED = 'hr_fire_locked'          # RECON->SUPPLY，附staging_x/y/yaw
RECON_LAUNCH_DONE = 'recon_launch_done'    # RECON->SUPPLY，附aim_x/y/yaw
SUPPLY_READY = 'supply_ready'              # SUPPLY->RECON，无附加字段
```

**动作名**（`sdk.do_action(name)`的`name`参数取值）：`grab_supply`（抓取物资）、`drop_supply`（投放物资）、`horizontal_launch`（水平投射破窗）。

**任务状态子状态**（`sdk.set_mission_state()`用，见4.4节20个通知点里的6个阶段级的点）。

### 4.2 第一部分任务：航线编队飞行

用户给的4个途经点`(7,-10,1.5)→(7,10,1.5)→(-8,10,1.5)→(-8,-10,1.5)`，`1.5`是AGL目标高度——**这个1.5米就是全流程程序里"巡航AGL高度"的取值，4.3节所有普通航线goto调用的z都用这个值**（跟4.3.2节立柱绕飞用的`z_agl=2.0`是两个不同的、各自固定的高度取值，全流程程序里z坐标只有这两类，不需要从`fire_drill_room_layout.yaml`读，见v3-f changelog第4条）。这条航线的作用是验证仿地定高和避障能力，不要求严格贴直线飞（AGL定高+避障重规划本身会让实际轨迹偏离直线），只要求：①全程AGL读数稳定跟随目标；②不撞障碍物圆柱；③最终到达4个途经点附近、回到各自起降点。

**长距离分段**：4个途经点两两间距18-20米量级，不能整条航线一次性丢给`sdk.goto()`底层的`waypoint_queue`——`ego_planner`长距离目标容易卡在`GEN_NEW_TRAJ`重规划死循环（本项目实测踩过的坑），程序里按最大步长3米自己插值出中间途经点，逐段调用`sdk.goto()`。

**编队**：RECON角色按用户给定航点飞；SUPPLY角色只需要确认两机都起飞完成，调用`sdk.start_formation_follow(follow_distance_m=3.5)`（一次性触发调用，实际控制回路跑在机载的`formation_follower_node`里，见1.4节）——**这次任务用`uwb_imu`定位，不存在θ*收敛判据这个环节**（`origin_setter_node`是`uwb_slam`模式专属节点，v3-f订正，见changelog第3条），不需要额外等待或判断收敛，两机局部系天然就是同一个全局系。不需要自己发航点队列——代码里是`if sdk.role == 'supply': sdk.start_formation_follow(...)`这种按角色分支，不是按`namespace`分支。

**状态判定**：RECON用`sdk.goto()`自身的到达确认（内部等`waypoint_state`）；SUPPLY没有独立的"完成"信号，RECON到达终点后自然停止移动，`formation_follower_node`的追踪目标也停在对应位置，两机是否都到位用`mission_judge/checkpoints`（`DiagnosticArray`全局话题，可订阅查询）里的`return_pad` checkpoint做权威确认（Gazebo真值，比程序自己的odom更可信）——**执行前一致性核查订正**：实际checkpoint名字是`nx01_return_pad`/`nx02_return_pad`两个带命名空间前缀的（小写），不是字面上单一的`return_pad`，查询自己对应的那一项时用`namespace.lower() + '_return_pad'`拼接（这里用`namespace`本身拼字符串是合理的，因为`namespace`是跟物理飞机绑定、必须显式传入的参数，不是1.5节禁止的"按namespace分支写业务逻辑"）。

飞完后：SUPPLY角色调用`sdk.land()`；RECON角色不降落，直接进入4.3节。

### 4.3 双火情响应阶段（第二部分任务+第三部分任务合并，事件驱动）

**为什么合并**：地面火情和高层火情哪个先被发现不做假设，只保证"发现即响应、两个都处理完才返航"这个不变量。

```
RECON角色执行的分支：
  search_itinerary = 弓字形地面航段 + 3根立柱依次绕飞航段（拼成一条完整搜索航线）
  ground_fire_done = False
  hr_fire_done = False
  同时启动一个后台线程/回调，全程监听sdk.wait_for_detection的两种class_id
      （不是"飞哪段航线就只看哪种检测"这种分段判断，两个检测器全程并行）
  while not (ground_fire_done and hr_fire_done):
      继续飞search_itinerary的下一段
      若命中'apriltag:2'（地面火情）且未处理过：
          中断当前段（sdk.cancel_goto()），执行4.3.1子流程，ground_fire_done=True，恢复剩余航段
      若命中'apriltag:1'（高层火情）且未处理过：
          中断当前段，执行4.3.2子流程，hr_fire_done=True，恢复剩余航段
  sdk.goto(*自己的起降点)
  sdk.land()

SUPPLY角色执行的分支（降落后进入STANDBY，同时监听两种消息，哪个先到都要能正确分派）：
  收到GROUND_FIRE_FOUND -> 执行4.3.1子流程里SUPPLY那部分 -> 回STANDBY继续等
  收到HR_FIRE_LOCKED -> 执行4.3.2子流程里SUPPLY那部分
```

**代码结构提示**：上面这两段分支是**同一份程序文件里`if sdk.role == 'recon': ... else: ...`的两个分支**，不是两个独立文件——这样任何一次运行给两架飞机传相反的`role`参数，行为会自动对调，不需要改代码，这是1.5节双机对称性原则在实际代码结构上的体现。

**4.3.1 地面火情响应子流程**

RECON侧：下视相机第一次看到ID2只是粗略触发信号，此时目标不一定在画面正中心。触发`precision_servo_node`的**模式A`center_only`**（1.7节新增，不是笼统说的"视觉伺服逻辑"——这里明确不需要下降，只需要居中），收敛完成后节点会把当前坐标广播出来，此时**RECON自己当前的局部坐标就近似等于目标的地面坐标**，不需要单目位姿解算（理由见0.2节）。调用`sdk.send_to_teammate(GROUND_FIRE_FOUND, x=..., y=...)`（这个坐标是RECON局部系下的值，SDK的`send_to_teammate`内部负责转换成SUPPLY能用的坐标，见2.1节第6条能力）。

SUPPLY侧：收到消息后`sdk.goto(supply_x, supply_y, 巡航AGL高度)`飞到`supply_point`附近上方（`x`/`y`是题目给的已知坐标，**由选手直接写成程序里的字面量常量，不是读`fire_drill_room_layout.yaml`**——选手容器里没有这个文件，见0.4节订正；z统一用这次任务的巡航AGL高度，不需要额外的z坐标来源），到位后触发`precision_servo_node`的**模式B`precision_land`**（1.7节新增）精确对准+下降+真实autoland，落地确认后`sdk.do_action('grab_supply')`抓取；抓取完成后**重新`sdk.takeoff()`**（v3-f订正为`CONTROLLER=pt4ctrl`就是为了让这次重新起飞能成立），飞到消息里RECON给的地面火情坐标（同样用巡航AGL高度），`sdk.do_action('drop_supply')`（投放不需要落地，悬停触发即可）；回`STANDBY`继续等。

**4.3.2 立柱绕飞+高层火情响应子流程**

**立柱编号：真机场景只给坐标、不给编号，程序自己按确定性规则编号（用户直接问的问题）**——这次仿真里"1#/2#/3#"这几个编号是`fire_drill_room_layout.yaml`这份配置文件自己起的名字，属于仿真侧的实现细节，**不能假设真机部署时主办方会给"这是1号立柱、这是2号立柱"这种带编号的信息，大概率只会给3个坐标点**（跟"地面火情/高层火情坐标要现场发现"不是一回事——立柱坐标本身题目允许提前给，但"编号"这个标签是仿真自己加的，不是题目给的）。解决办法很简单：**程序自己算一遍确定性排序，赋予编号，不依赖外部给的标签**——这3个坐标点本身（跟0.4节的`supply_point`同理）由选手直接写成程序里的字面量常量，不是从任何配置文件读取；拿到3个坐标点之后，按"先比X坐标、X相同再比Y坐标"这种字典序规则排序，排序结果的下标就是这次运行里程序自己认定的编号（0/1/2或者1/2/3都行，只要前后一致）。这个规则只需要保证**同一份坐标输入排序结果稳定可复现**，不需要跟仿真配置文件里原来的"1#/2#/3#"标签对上——反正无论真机还是仿真，这次任务真正关心的是"选中的是哪个具体坐标"，不是"选中的编号数字是几"，编号只是程序内部用来打破平局、方便调试打日志的一个标识，不是任务逻辑本身需要的信息。仿真环境里为了方便跟`fire_drill_room_layout.yaml`里原有的"1#/2#/3#"对话，可以让自定义排序规则**碰巧**跟配置文件原有编号对上（比如这次配置里`pillar_1(4,4)`/`pillar_2(0,8)`/`pillar_3(-6,2)`按X坐标升序排恰好是`pillar_3, pillar_2, pillar_1`——不完全对得上，说明"按坐标排序"和"配置文件原有编号"本来就是两套独立的编号规则，不需要强行凑一致，这次文档里后面提到的"1#/2#/3#"字样都是沿用仿真配置文件的原始编号，方便对照场景说明，实际代码里用的是程序自己排序出来的编号，两者不需要一致）。

立柱选择规则：按"离当前位置最近，同距离取（程序自己排序出来的）编号小的"选一根未绕飞过的立柱（3根依次来，不是只测一根就结束——如果绕完一圈没找到，继续下一根，直到3根都绕完或找到为止）。用`sdk.generate_orbit_waypoints(cx, cy, radius=2.5, z_agl=2.0, num_points=12)`生成绕飞航点（2.1节第8条新增的通用几何helper，不是跨容器import`contest_mission`包）。

> 平局兜底规则举例（这次仿真配置下的具体数字，编号是仿真配置文件原有的1#/3#，不是程序自己排序出来的编号，仅作为"确实存在平局这种情况、需要兜底规则"的证据）：立柱1#(4,4)、3#(-6,2)到地面火情点(0,-2)的距离精确相等（都是√52≈7.211米，不是四舍五入巧合，实算验证过），单纯"取最小值"在这种情况下没有唯一答案，取决于遍历顺序这种实现细节，所以显式加一条：距离相等（或差值小于极小阈值）时固定选编号小的那个，保证这个选择永远可复现——不管这个编号是主办方给的还是程序自己排序出来的，"有一条确定性的兜底规则"这件事本身比"编号具体是谁给的"更重要。

高层火情响应：绕飞过程中命中`apriltag:1`（这个检测本身已经由`fire_pillar_aim_node`处理——它自动锁定+算悬停点+自动接管`pillar_aim`模式，这部分不需要程序自己实现，见现有`fire_pillar_aim_node`设计），程序这边只需要`sdk.cancel_goto()`打断绕飞航点（避免跟`pillar_aim`模式抢控制权），然后订阅1.2节新增的`~/fire_pillar_staging_pose`拿等待点坐标，`sdk.send_to_teammate(HR_FIRE_LOCKED, staging_x=..., staging_y=..., staging_yaw=...)`。

SUPPLY侧：收到`HR_FIRE_LOCKED`，`sdk.goto()`飞到等待点，到位后`sdk.send_to_teammate(SUPPLY_READY)`，然后等`RECON_LAUNCH_DONE`消息。

RECON侧：等到`SUPPLY_READY`后，`sdk.do_action('horizontal_launch')`；完成后从`~/fire_pillar_aim_pose`（1.2节新增）读出自己锁定的瞄准点坐标，`sdk.send_to_teammate(RECON_LAUNCH_DONE, aim_x=..., aim_y=..., aim_yaw=...)`，调用`fire_pillar_aim_node`的`~/reset_aim`服务切回`normal`模式，标记`hr_fire_done=True`，恢复search_itinerary。

SUPPLY侧：收到`RECON_LAUNCH_DONE`，`sdk.goto()`飞到消息里的瞄准点坐标（不需要重新跑一遍检测/锁定逻辑，RECON已经把精确坐标算好传过来了），`sdk.do_action('horizontal_launch')`。

**执行阶段D写`mission.py`时发现并订正的一处顺序缺陷**：这里原文紧接着写的是"然后`sdk.goto(*自己的起降点)` + `sdk.land()`"——如果高层火情先于地面火情被发现，这一步会让SUPPLY投射完立即降落退出`STANDBY`循环，之后再也不能响应地面火情（`STANDBY`已经结束，`on_teammate_event`注册的回调不会再被触发）。**订正为：两个子流程（地面火情/高层火情）各自完成后都不立即降落，回到`STANDBY`继续等，只有`ground_fire_done`和`hr_fire_done`两个标志位都成立（这个状态在RECON侧维护，需要一个额外的跨机同步事件让SUPPLY知道"两个都做完了"，比如RECON侧`hr_fire_done`且`ground_fire_done`都成立后发一个通知事件）之后，SUPPLY才在`STANDBY`循环末尾统一`sdk.goto(*自己的起降点) + sdk.land()`一次**。4.4节20个通知点表格里的第18点"SUPPLY完成水平投射"、第19点"SUPPLY返回起降点降落"因此对应到代码里不是紧邻的两步，中间可能隔着"等RECON也确认两个火情都处理完"这一段等待。

### 4.4 全流程关键逻辑点通知清单（共20个）

**"通知"分两种粒度**：粗粒度用`sdk.set_mission_state()`（对接`mission_state.py`契约的`顶层值:子状态`写法），细粒度用`sdk.send_to_teammate()`同款机制广播（对不需要处理的事件类型，接收方直接忽略、不强制要求每种都有人消费）。GCS要看到这些通知，需要新增一个订阅节点转发给前端（具体走WebSocket还是轮询要跟GCS现有机制保持一致，这份方案没有去核实GCS现有技术选型，留给实现时确认）。

**第一部分：航线编队飞行**

| # | 逻辑点 | 触发条件 | 通知方式 |
|---|---|---|---|
| 1 | 任务开始 | 双机起飞完成（`sdk.takeoff()`内部确认`armed`） | `mission_state` |
| 2 | 编队跟随启动 | SUPPLY确认自己起飞完成，调用`sdk.start_formation_follow()`（v3-f订正：`uwb_imu`模式下不存在θ*收敛判据，见4.2节） | `mission_state` |
| 3 | 避障重规划事件（可选，见4.5节） | - | - |
| 4 | 第一部分航线飞完 | 两机都到达4个途经点对应的最终位置附近 | `mission_state` |
| 5 | SUPPLY降落完成 | `armed`变`false` | `mission_state` |
| 6 | RECON转入火情响应阶段 | 程序内部转移 | `mission_state` |

**双火情响应阶段**

| # | 逻辑点 | 触发条件 | 通知方式 |
|---|---|---|---|
| 7 | 地面火情被发现 | `GROUND_FIRE_FOUND`事件 | `mission_events`（跨机+对外双重用途） |
| 8 | SUPPLY抵达物资点并完成抓取 | `grab_supply`动作完成 | `mission_events` |
| 9 | SUPPLY抵达地面火情点并完成投放 | `drop_supply`动作完成 | `mission_events` |
| 10 | 立柱选择完成 | 选中第几根立柱 | `mission_events` |
| 11 | 立柱绕飞开始 | 绕飞航点开始执行 | `mission_state` |
| 12 | 高层火情被发现 | `fire_pillar_aim_node`锁定成功 | `mission_events` |
| 13 | 任务机等待点算出并广播 | `HR_FIRE_LOCKED`事件 | `mission_events` |
| 14 | SUPPLY抵达等待点 | `SUPPLY_READY`事件 | `mission_events` |
| 15 | RECON完成水平投射+广播瞄准点 | `RECON_LAUNCH_DONE`事件 | `mission_events` |
| 16 | RECON返回起降点降落 | `armed`变`false` | `mission_state` |
| 17 | SUPPLY抵达RECON瞄准点位置 | 导航到位判定 | `mission_events` |
| 18 | SUPPLY完成水平投射 | 动作完成 | `mission_events` |
| 19 | SUPPLY返回起降点降落 | `armed`变`false` | `mission_state` |
| 20 | 任务整体完成 | 两个火情标志位都True且两机都已降落 | `mission_state` |

20个点里，7/13/14/15这4个本来就是双机通信必需的事件（本来就要发），剩下16个是额外补充的、纯粹用于对外通知（GCS/日志能看到进度，不代表任务逻辑需要它）的点。

### 4.5 不是所有"技术上能加"的点都建议做

第3点（避障重规划事件）单独说明：技术上能做，但**目前没有干净的现成信号**——查了`ego_planner`源码（`ego_replan_fsm.cpp`），`REPLAN_TRAJ`这个FSM状态从来没有发布成ROS2话题（只有`cout<<`打印到标准输出），而且这个状态碰撞触发和周期性正常重规划共用同一个值，光看状态本身分不清"真在躲障碍物"还是"常规换段"。如果真要做，成本低的替代方案是用几何层面的间接判断（实际odom轨迹跟理论直线航段的横向偏移量超过阈值就认为在规避），成本高的方案是patch `ego_planner`把状态+触发原因结构化发布出来（这属于碰mighty/ego_planner核心代码的改动，不建议作为第一批工作）。这条不是任务逻辑的分支点（避不避障任务都会继续走），不列入核心20个，需要演示效果可以当第21个可选点。

**仿地穿越的感知**（对比参考）：跟避障不同，这个能直接用测距雷达判断（`range`读数在飞过仿地模块时会有跟0.25米高度差对应的、方向确定的阶跃变化），不需要碰任何现有节点代码，如果需要也可以加进通知列表，成本很低。

---

## 5. 风险 + 建议的验证顺序

按已有经验，**不建议一次性把四个部分串联起来直接跑全流程**，建议分段验证：

1. **第一部分（仿真栈能力扩展）先单独验证**：`uwb_imu_fusion_node`z融合逻辑单独测试（v3-g订正：不再是"agl_hold模式切换"，起飞后全程观察`local_position/odom`的z是否稳定跟随AGL、飞过仿地模块/起降边界读数是否正确）；`fire_pillar_aim_node`新话题单独测试（synthetic注入确认发布内容正确）；`formation_follower_node`的`LeaderPathBuffer`弧长插值逻辑单独用synthetic轨迹数据测试（这块逻辑复杂度较高，不要直接上真实双机飞行验证，先用单元测试跑几种典型轨迹形状——直线、直角拐弯、连续S弯——确认插值结果落在缓冲区折线上而不是抄近道）。
2. **第二部分（SDK）单独验证**：每个能力方法写单元测试/集成测试（尤其是2.3节ACK重发协议，建议先用synthetic丢包/延迟场景测试，不要直接上真实网络环境验证）。
3. **第四部分先单机验证**（只跑RECON角色的分支，不起SUPPLY那架）：重点验证长距离分段航点能不能连续飞完不卡在`GEN_NEW_TRAJ`死循环、经过障碍物圆柱和仿地模块时避障/定高是否正常。
4. **双机跑第一部分任务**：验证编队跟随在实际仿真里的表现（拐弯处是否还会抄近道；`uwb_imu`模式下不存在θ*收敛这个环节，重点看的是UWB定位噪声本身导致的抖动，不是θ*收敛前后的差异）。
5. **双火情响应阶段单独验证**（可以直接从RECON角色已经在地面火情点附近开始摆位置测试）：弓字形航点生成+检测命中中断逻辑、跨机消息传递+ACK协议、立柱选择规则（重点测试1#/3#平局情况）、双机投射顺序、**角色对调后重跑一遍**（把启动参数的`role`互换给两架物理飞机，确认行为完全对调、没有任何残留的机身编号硬编码）。
6. **最后串联全流程**，全程记录`mission_state`/`mission_events`话题日志，方便复盘。

**具体风险点**（在验证顺序基础上列几个已知的技术难点）：
1. **这次仿真环境这一层还没有实测跑过`uwb_imu`**——定位方式本身已真机验证，但这套容器化仿真环境的wiring需要单独确认。
2. **`uwb_imu_fusion_node`z融合层在起降阶段的边界处理**（v3-g订正：不再是"切回normal模式"，是z这一路观测修正的有效性判断）——测距雷达最小量程0.19-0.2米，`range_min_valid_m`这个阈值需要实测调。
3. **编队跟随依赖的跨机定位精度**——`uwb_imu`模式下两机局部系天然对齐（不经过`origin_setter_node`/θ*，v3-f订正），跟随精度主要取决于UWB定位本身的噪声水平，不再是θ*估计的实时准确性问题。
4. **"下视相机居中悬停+读自己位置"的地面火情坐标精度**——依赖`precision_servo_node`视觉伺服收敛精度，建议实测残余像素偏移换算成地面距离误差是否明显小于`mission_judge_node`的1米判定半径。
5. **双检测器并行监听+中断恢复逻辑**——需要正确处理"探测到A事件时刚好在处理B事件响应流程"这种交错情况，建议单独写单元测试模拟"检测消息按不同顺序到达"的几种情况。
6. **ego_planner长距离目标容易卡在重规划死循环**——已知的稳定性问题，长距离分段是应对措施，不是根治，建议第4步验证时重点观察。
7. **RC override通道映射/PWM值/持续时长这三个参数目前是未知数（v3-e新增）**——1.6节已经说明这是硬件接线细节，实现`actuator_action_node.py`时如果拿不到真实映射，只能先用占位参数配合仿真里`ros2 topic echo mavros/rc/override`肉眼确认链路通不通，不能假装这几个数字已经定了。
8. **override释放时机的安全性需要SITL实测确认，不能只信文档（v3-e新增）**——`CHAN_NOCHANGE`(65535)/`CHAN_RELEASE`(0)这两个特殊值在mavlink规范里的语义、PX4实际接收后的行为，建议先在SITL里针对这两个值各测一遍再定下来用哪个做"动作完成后释放"，1.6节倾向推荐`CHAN_NOCHANGE`但没有在这次会话里实测验证过。
9. **`precision_servo_node`模式B新增的"到达min_z后触发真正autoland"这一段是新代码，没有实测过（v3-f新增）**——需要单独验证：下降到`min_descent_z`时是否稳定触发降落、`armed`变`false`的确认时机是否会跟"还在下降过程中的短暂低于阈值"混淆，建议先给`min_descent_z`留足够余量、加一个短暂"持续低于阈值"的确认窗口再触发降落，不要一碰到阈值就立即触发。
10. **抓取后重新起飞这条流程完全依赖`pt4ctrl`能真正支持"落地→再起飞"，而`pt4ctrl`本身是2026-08-13才新增的代码（v3-f新增）**——用户确认真机+仿真都验证过，但建议阶段A/E实测时把"落地抓取→重新起飞"这个动作单独测一遍，不要等D5串联时才第一次验证这个反复起降的完整链路。
11. **`sdk.land()`2026-09-13阶段C端到端测试出现过一次卡死超时，排查过程中提出的"A0的z冻结跟PX4的`land_detector`打架"这条理论已经被后续复测证据推翻，不是确认过的问题，记录下来提醒不要重复踩坑（v3-g新增，性质是"排查过程记录"不是"确认的风险"）**——第一次测试`sdk.land()`卡死时，一度怀疑是A0近地边界"range失效就冻结高度估计"的处理跟`pt4ctrl`的`AUTO_LAND`状态机（`get_landed()`判据`des.p(2)-odom.p(2)<-0.5`）打架，导致PX4自己的`landed_state`卡在`IN_AIR`不变、`disarm`发不出去。但这条推理链条有一处没有验证过的跳跃（pt4ctrl的`odom.p(2)`是PX4自己EKF2融合之后再读回来的，不是直接读我们发的原始值，两者不能划等号），且**用完全相同、未做任何修改的代码重新测了一次，`landed_state`干净地走完`ON_GROUND→IN_AIR→ON_GROUND`，`land()`没有卡住**——同样冻结的z，一次卡死一次正常，说明"z冻结"不是决定性因素，第一次卡死更可能是当时会话里几个小时内大量手动`ros2 topic pub`/service诊断调用留下的残留状态偶然干扰，不是这段代码稳定复现的bug。基于这条理论做的代码改动（`out.pose.pose.position.z`换成`self._p_state[2]`）已经撤回，`uwb_imu_fusion_node.py`维持A0原样不变。**真正的偶发卡死根因目前没有查清楚**，如果以后要继续排查，建议用一个干净重启过的容器（不要在跑了很久、被大量手动指令poke过的容器上测）多跑几次"起飞→goto→land"确认复现率，从排除法入手，不要直接从代码逻辑猜。这条也是一次值得记住的教训：不能因为"刚改完A0、紧接着测试就报错"这种时序上的巧合就直接把两者绑在一起下结论，需要先证明因果关系。

---

## 6. 代码量估算（用户直接问的，量级参考，不是精确报价）

**估算方法**：不是凭空猜，是拿`contest_mission`包里已经写好、风格/复杂度可比的现有文件做锚点——这个包目前12个核心文件（节点+纯函数helper）加起来1972行，单个ROS2节点类文件普遍落在**170-280行**区间（`fire_pillar_aim_node.py` 282行、`pillar_detector_node.py` 282行、`qr_apriltag_detect_node.py` 232行、`scenario_reset_node.py` 260行、`position_cmd_relay_node.py` 168行），纯函数helper文件普遍在**70-110行**（`orbit_flight_helper.py` 69行、`ground_scan_helper.py` 105行、`fire_pillar_approach_helper.py` 107行）。这些数字已经包含了这个项目一贯的**高密度中文注释风格**（本会话通篇都是这个风格，注释解释"为什么"往往比代码本身长）——如果实际写的时候注释密度明显更低，下面的数字可以打个7-8折；如果延续现在这个项目的注释习惯，下面的数字是比较贴近的。

| 部分 | 具体文件 | 估算行数 | 参考锚点 |
|---|---|---|---|
| 第一部分 | `formation_follower_node.py`（新文件） | 220-280 | 跟`fire_pillar_aim_node.py`复杂度相当（订阅+定时器+坐标转换+service） |
| 第一部分 | `uwb_ground_truth_node.py`+`uwb_imu_fusion_node.py`修z融合逻辑（v3-g订正，改现有文件，不是`contest_mission`包） | 50-80 | 不再新增`position_cmd_relay_node`模式；改成`uwb_imu_fusion_node.py`拆分z观测源+加边界判断，量级参考`position_cmd_relay_node.py`原`agl_hold`估算 |
| 第一部分 | `fire_pillar_aim_node.py`补发话题（改现有文件） | 10-15 | 一个新publisher+一行publish调用 |
| 第一部分 | `actuator_action_node.py`改造成真实RC override驱动（v3-e新增，改现有文件） | 60-100 | 现有文件86行纯状态记录，新增"按配置持续publish+定时释放"逻辑，量级参考`position_cmd_relay_node.py`里`_publish_with_orbit_yaw`这类"定时/条件发布"分支 |
| **第一部分小计** | | **330-455** | |
| 第二部分 | `capabilities.py`（8类能力、约14-15个方法，v3-e新增第9条`trigger_alarm`） | 380-480 | 参考`qr_apriltag_detect_node.py`(232)+`mission_judge_node.py`(203)组合复杂度，新增能力量级不大（一次性HTTP/串口调用+短超时） |
| 第二部分 | `reliability.py`（ACK重发协议+状态确认封装） | 150-200 | 这个项目里没有直接对应的现成逻辑可比，按中等复杂度helper估 |
| 第二部分 | `_rclpy_runtime.py`（节点生命周期封装） | 80-120 | 纯粹是boilerplate封装，逻辑不复杂 |
| 第二部分 | `__init__.py` | 15-25 | 只做re-export |
| **第二部分小计** | | **625-825** | |
| 第三部分 | Dockerfile+启动脚本(`.sh`/`.bat`)+`.devcontainer`配置+示例任务模板 | 150-250 | 大部分是配置/glue代码，不是复杂逻辑 |
| 第四部分 | `mission_vocab.py`（事件名/动作名词汇表常量） | 20-30 | 纯常量定义 |
| 第四部分 | 长距离分段插值helper | 30-50 | 参考`orbit_flight_helper.py`（69行）单函数量级 |
| 第四部分 | 全流程程序主文件（单份代码，RECON分支：航线+双火情响应，含立柱选择/双检测器监听/两个响应子流程） | 300-380 | 比`fire_pillar_aim_node.py`(282)更复杂一些，接近这个包里复杂度最高的文件量级 |
| 第四部分 | 同一份主文件的SUPPLY分支（编队+STANDBY事件分派+两个响应子流程） | 180-240 | 参考`mission_judge_node.py`(203)量级（跟上一行是`if role==...else...`的两个分支，不是两个独立文件，这里分开估算只是为了对照复杂度，不代表要拆成两份代码） |
| **第四部分小计** | | **530-700** | |

**回答用户的两个具体问题**：
- **"全流程程序"（第四部分）单独的代码量**：约**530-700行**（一份代码，RECON+SUPPLY两个角色分支+共享的词汇表/helper）。
- **整个方案（第一到第四部分全部加起来）**：约**1605-2155行**（v3-e因为`actuator_action_node.py`改造+`trigger_alarm`能力，比v3-d的估算多了50-55行左右，量级没有实质变化），中位数量级仍在**1800行**左右——这个总量跟`contest_mission`包现有12个文件的1972行几乎是同一个量级，作为一个交叉验证：这次新增的工作量大致相当于**再造一个跟现有`contest_mission`包同等规模的东西**，不是一个小改动，但也远没有到"重写整个仿真栈"那种量级。

**这个估算没有考虑的东西**（会让实际数字往上浮动）：单元测试代码（第5节验证清单提到的好几处"建议先写单元测试"，测试代码通常跟被测代码是同一个数量级，如果算上测试，实际总量可能要再加50-100%）、调试/踩坑过程中产生的临时脚本（这个项目从DEBUG_JOURNAL.md的记录看，历史上每个功能点平均都要经过2-3轮问题排查，实际花的时间/中间产出会明显多于"一次写对"的理想估算，但这些踩坑记录本身通常不计入最终代码量）。

---

## 7. 明确排除范围

**真实机构本身的物理动力学仿真**（Gazebo里没有抓取臂/投放舱的物理模型去响应`mavros/rc/override`信号——v3-e起`sdk.do_action()`触发的**信号链路**本身是真实的，见1.6节，被排除的只是"机构动起来"这个物理效果的仿真建模）、正式阶段7.4/官方SDK的完整实现（本方案的`contest_sdk`是过渡性质的、够用的SDK，不等同于以后官方阶段8要做的那个）、GCS网页端任务编排UI（GCS只需要新增订阅转发逻辑展示通知，不需要做任务编排的可视化界面）、双机直连物理链路（2.3节"链路B方案二"，本方案默认走路由器转发）、SROS2/DDS层面的细粒度访问控制（3.1节，本方案靠容器级隔离，不给选手签发DDS证书/权限文件）、选择性局部运行时状态复位服务（3.2节，本方案换选手直接重启三个仿真栈容器，不做只复位部分状态的精细化方案）、**声光装置本身的硬件驱动代码+GCS后端对应接口的具体实现**（2.1节第9条只定了`sdk.trigger_alarm()`这一层SDK接口该长什么样、可靠性要求多高，GCS机器上具体怎么把这个请求转成"点亮/发声"这个硬件动作——串口协议、USB继电器库、还是别的——是GCS那边的实现细节，不在本方案范围内，需要跟GCS开发者另外对接）、**抓取/投放/发射三个动作具体对应哪个RC通道/PWM值/持续时长**（1.6节已经说明这是真实硬件接线细节，方案层面拿不到，需要跟负责机构接线的人确认后再填，不是本方案能替你决定的）。
