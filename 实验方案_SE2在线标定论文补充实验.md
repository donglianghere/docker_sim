# 《基于滑窗最小二乘的局部-全局坐标系在线标定方法》补充实验方案

针对 `论文正文_多机SE2坐标在线标定方法_润色版.docx`（2026-08-22 版）的素材缺口
分析与实验设计。2026-08-23 制定。

---

## 零、总体判断

润色版正文的**理论部分已经完整**（闭式解、噪声传播、CRLB、抗差设计、SE(3)推广），
**实证部分几乎为空**：

| 章节 | 名义内容 | 实际内容 |
|---|---|---|
| §4.1 | 与现有方法比较 | 文献对比表，无任何实验数据 |
| §4.2 | 实验环境与方法论 | 只有 RTF 波动区间和"必须用非零真值"两条方法论提醒 |
| §4.3 | 真机泛化验证 | 基线 0.29 m、σ≈0.05 m 两个标定值 + 式(21) 理论推算，无实测误差分布 |
| §4.4 | 尚待完成的实验 | 四项，全部未做 |

全文唯一的定量实证是 §3.2 的落点预测对照（每机 2 次测量）。§2.6 写"与实测滑窗
收敛后 1°–2° 量级吻合"，但**这个数字在正文里没有任何数据、曲线或表格支撑**，
出处只是 `DEBUG_JOURNAL.md` 的口头记录。这是投方法类期刊的硬伤。

以下按"不补不能投 / 补了才完整 / 加分项"三档列缺口，再给四层实验方案。

---

## 一、素材缺口清单

### P0 致命缺口（不补则方法类论文不可投）

**G1 核心估计器的精度验证完全缺失。**
没有 θ* 收敛曲线、没有稳态误差统计、没有与真值（30°/−45°）的误差表。一篇提出
估计器的论文必须回答"它到底准到什么程度、多久收敛"。当前正文对此零数据。

**G2 噪声传播模型与 CRLB 声明没有实验支撑。**
式(12)(13) 给出了两个可证伪的定量预测：单段角度噪声 8.1°、W=50 收敛后 1.15°。
式(15) 声称达到 CRLB。这三条**必须有蒙特卡洛或实测方差曲线与之对照**，否则
"渐近有效估计量"这个摘要级论断只是纸面推导，审稿人一定会要求验证。

**G3 与批量最小二乘的对比（表3 第1项）空缺。**
这是回答"为什么要滑窗、而不是更简单的全历史批量求解"的唯一论据。§2.5、§4.1
两处都声称滑窗能跟踪时变映射，但没有任何数据。这是审稿人的必问项。

### P1 重要缺口（缺则贡献声明打折）

**G4 参数敏感性（W、d_min）空缺（表3 第2项）。**
表1 给出 d_min=0.5 m、W=50，依据是式(12)(13) 的理论计算——但没有实验说明这组
取值是否合理、偏离后性能如何退化。该实验同时是 G2 的载体，可合并。

**G5 §2.4 恒定偏置免疫性只有定理、没有实验。**
式(10)(11) 的结论非常干净（旋转精确无偏、平移原样继承），但补一组注入偏置的
对照实验成本极低，且能让这一节从"推导"升级为"推导+验证"。

**G6 §2.7 抗差扩展未实现、未验证，而摘要/结论的口径偏强。**
摘要写"设计了基于 Huber 损失的迭代重加权抗差方案"，§6.1 结论并列陈述"设计了
应对 UWB 多径观测野值的抗差扩展"。§2.7 末尾虽注明"仅完成设计、尚未验证"，但
摘要与结论容易被读成已验证。两条路：①补 E4/E14 实验，把口径坐实；②在补完前
把摘要/结论改为"给出并数值验证"或"提出设计"。**推荐走①**，成本很低（纯数值
实验半天可出）。

**G7 §3.2 修复方案未实现、未验证（表3 第3项）。**
这是全文最有说服力的一条证据链——"现象→定量预测排除竞争假说→修复→效果验证"。
现在只做到第二步，第三、四步缺失，导致 §3.2 目前是一个"已知缺陷"而不是"已解决
贡献"。补上后 §3.2 的分量会显著上升。

**G8 §5 可观测性讨论纯定性。**
"纯噪声下方向多样性非必要、存在方向相关偏置时才必要"这个二分论断很好，但没有
任何数据。用数值实验可以直接量化（单方向 vs 多方向，有偏置 vs 无偏置 四格对照）。

### P2 加分缺口（审稿人会问，可部分归入后续工作）

**G9 计算开销没有实测。** 论文声称 O(W)/O(1) 增量更新、适合机载嵌入式，但没给
Jetson Orin NX 上的单次更新耗时与 CPU 占用。项目本来就有真机平台，成本极低，
且与"面向机载平台"的项目主线呼应。

**G10 多机链式复合的实际误差没测。** §1.1/§2.1 声称"多机部署仅需并行调用 + 一次
简单坐标复合"，但没有给两机各自标定后复合出的机间相对位姿误差。标题里有"多机"，
这一条是标题的直接兑现，仿真里有 Gazebo 真值可算，成本低。

**G11 §4.3 真机双标签测向只有理论值 σ_yaw≈13.9°，没有实测角度误差分布。**
式(21) 是可证伪的预测，补一组台架实测就能验证，且直接强化"本文方法优于短基线
直接测姿"这一核心对照论证。

**G12 观测异步/时延未建模——这是本次审读新发现的一处理论缺口。**
`origin_setter_node._maybe_record_segment()` 构造 v_i 时用的是"里程计回调触发那
一刻最新的一帧 UWB"（`self._latest_uwb_xy`），**没有做任何时间戳对齐**。UWB 仿真
配置 `publish_rate_hz=10`、`latency_ms=30`，即端点数据的陈旧程度在 0~130 ms 之间
随机抖动。恒定速度下两个端点的固定时延会共同抵消（v_i 不受影响），但**抖动部分
不抵消**：1 m/s 飞行时抖动引入的端点位置误差可达 0.1 m，**超过 σ=0.05 m 本身**，
在加减速/转弯段还会叠加时延项残差。式(12)(13) 的噪声模型完全没有包含这一项。
建议：做消融实验量化，并至少在 §6.2 局限中承认；如影响显著，则在 §2.5 补一段
"观测异步引起的等效噪声"，反而成为一个新的细化贡献。

**G13 观测噪声水平/发布率的标度关系未验证。** 式(13) 预言 std ∝ σ，扫 σ 即可验证，
可并入数值实验，几乎零成本。

**G14 统计显著性不足。** §3.2 每种条件只有 2 次测量。所有仿真实验应做 N≥10 次
重复（不同随机种子/轨迹），报 mean±std，而非单次数值。

**G15 §4.2 实验环境描述不满足可复现性要求。** 缺：宿主机配置、ROS2/Gazebo 版本、
场景尺寸与障碍物配置、飞行轨迹定义、DLIO 配置、UWB 仿真参数（σ=0.05 m、
latency=30 ms、rate=10 Hz）、真值来源（Gazebo `model_states`）、重复次数。必须补齐。

**G16 图表缺口。** 已有图1–图4。缺：图5 收敛曲线（G1/G2）、图6 参数敏感性（G4）、
图7 批量 vs 滑窗跟踪（G3）、图8 抗差前后（G6）、图9 真机测向误差分布（G11），
以及一张汇总各场景 RMSE/稳态误差/收敛时间的**实验结果表**（当前正文一张实验
结果表都没有）。

---

## 二、实验方案（四层，按成本从低到高）

设计原则：**先用不依赖容器重建的手段把 P0 缺口补掉**，把需要重建镜像的改动一次性
打包，只重建一轮。

### L0 纯数值蒙特卡洛（宿主机 Python，不动容器，预计 1 天）

新建 `docker_sim/scripts/paper_exp/mc_se2_sim.py`：按式(1) 合成 u_i，加噪声得 v_i，
复用与 `origin_setter_node` 完全一致的滑窗累加与 `atan2(S,C)` 读数逻辑。

| 编号 | 实验 | 自变量 | 因变量 | 补的缺口 | 出图 |
|---|---|---|---|---|---|
| E1 | 噪声传播与 CRLB 验证 | W∈{1,5,10,20,50,100}，d∈{0.3,0.5,1,2} m，σ∈{0.02,0.05,0.1} m，每组 2000 次 | std(θ̂) 实测 vs 式(13) vs CRLB 式(15) | G2,G4,G13 | 图5a |
| E2 | 恒定偏置免疫 | b∈{0,0.5,2.0} m × 4 个方向 | θ̂ 偏差、t 偏差 | G5 | 表 |
| E3 | 批量 vs 滑窗跟踪时变映射 | 角度漂移率 ω∈{0.1,0.5,2}°/s | 跟踪滞后、稳态误差 | G3 | 图7 |
| E4 | Huber 抗差 | 野值比例 p∈{0,5,10,20}%，幅度∈{0.5,2,5} m | 加权前/后 θ̂ 误差、崩溃点 | G6 | 图8 |
| E5 | 可观测性四格对照 | {单方向,多方向} × {无偏置,方向相关偏置} | θ̂ 方差与偏差 | G8 | 图6b |
| E6 | 观测异步消融（合成版） | 端点时延抖动 0~130 ms，速度 0.3/1/2 m/s | θ̂ 方差相对式(13) 的膨胀倍数 | G12 | 表/图 |

E3 的理论对照可一并给出：滑窗对线性漂移的稳态滞后约为 ωWΔt/2。

**这一层就能把 G2/G3/G5/G6/G8/G13 全部补齐，且完全可复现、可给上千次统计。**

### L1 录包 + 离线复算（真实 DLIO/UWB 数据，飞一次扫无限次参数，预计 1–2 天）

前置改动（只改宿主机脚本，**不需要重建镜像**）：
1. `scripts/record_rosbag.sh` 的 `TOPICS` 增加 4 个话题：
   `/NX01/uwb/pose_abs`、`/NX02/uwb/pose_abs`、
   `/NX01/origin_setter/yaw_estimate`、`/NX02/origin_setter/yaw_estimate`
   （`/NX0x/origin_setter/yaw_sample_count` 也建议一并加；真值 `/plug/model_states_plug`
   和 `/NX0x/dlio/odom_node/odom` 已在列表中）。
2. 新建 `docker_sim/scripts/paper_exp/se2_calib_offline_eval.py`：从 bag 读三路数据，
   复刻分段/滑窗逻辑，支持任意 W/d_min、批量模式、Huber 开关、离线注入野值与偏置、
   时间戳对齐模式切换。

| 编号 | 实验 | 说明 | 补的缺口 |
|---|---|---|---|
| E7 | 真实数据上的 W/d_min 敏感性 | 与 E1 理论曲线叠加，形成"理论—合成—真实"三级印证 | G4,G2 | 图6a |
| E8 | 真实数据上的批量 vs 滑窗 | 直接用真实 DLIO 累积漂移，不需人为注入偏置 | G3 |
| E9 | 时间对齐消融 | 现状"最近帧" vs 时间戳线性插值对齐，扫飞行速度 | G12 |
| E10 | UWB 残差分布统计 | 静止段/巡航段残差直方图、正态性检验、野值率 | G6 动机, G13 |

E10 的价值：§2.7 现在的动机是假设性的（"UWB 在室内存在多径……"），有了实测野值率
就变成实证动机。

### L2 仿真在线闭环（需重建镜像，改动一次性打包，预计 2–3 天）

需要的代码改动（**建议全部改完后请用户一次性重建 sim-world + flight-stack**）：

*sim-world 镜像*
- (a) `src/uwb_sim/uwb_ground_truth_node.py` 增加观测污染参数：
  `bias_xy`（恒定偏置）、`bias_drift_rate`（时变/空变偏置）、`outlier_prob`、
  `outlier_magnitude_m`，默认全 0 保持现有行为不变。
- (b) `docker/entrypoints/sim-world-entrypoint.sh` 的 `SPAWN_YAW` 改为可由环境变量
  覆盖（默认仍是 NX01=30°、NX02=−45°），供 E12 全角域扫描。

*flight-stack 镜像*
- (c) `src/uwb_origin_bridge/origin_setter_node.py` 实现 §2.7 的 Huber 重加权
  （参数开关，默认关），并发布残差/权重诊断话题。
- (d) 目标点消费端持续重变换修复（§3.2 提出的方向）：跟踪目标点在全局系下的原始
  输入，每次 θ* 更新重新变换、重新发布局部目标，直到到达。
- (e) 视 E9 结论决定是否上线时间戳对齐的端点插值。

| 编号 | 实验 | 设计 | 补的缺口 | 出图 |
|---|---|---|---|---|
| E11 | **收敛精度主实验** | 两机 30°/−45°，自标定动作 + 任务飞行，记录 θ*(t) 与真值误差；N=10 次重复 | G1,G14 | 图5b + 结果表 |
| E12 | 真值角度全域扫描 | θ0∈{0,15,30,60,90,135,180,−45}°，验证无符号/象限错误 | G1，呼应 §4.2 方法论 | 表 |
| E13 | §3.2 修复前后对穿对比 | 修复前/后各 ≥5 次对穿，比落点误差 | G7,G14 | 图4 扩展 |
| E14 | 多机链式复合误差 | 由两机各自 (θ*,t) 复合机间相对位姿，比 Gazebo 真值 | G10 | 表 |
| E15 | 闭环污染验证 | 用 (a) 注入偏置/野值，闭环复现 E2/E4 | G5,G6 | 表 |
| E16 | 仿真侧计算开销 | 单次更新耗时、节点 CPU/内存 | G9 | 表 |

注意 §4.2 已记录的方法论陷阱：仿真配置的发布率需乘 RTF（实测 0.27–0.53）才是真实
频率，所有涉及"多少秒收敛"的结论必须换算后陈述。

### L3 真机台架（Jetson Orin NX，预计 1 天）

| 编号 | 实验 | 说明 | 补的缺口 |
|---|---|---|---|
| E17 | 双标签测向实测误差分布 | 台架上以已知参考朝向为真值，采集 ≥500 帧，统计 σ_yaw，与式(21) 预测的 13.9° 对照 | G11 | 图9 |
| E18 | Jetson 计算开销 | 单次更新耗时、CPU/内存占用 | G9 |
| E19 | 真机 UWB 静态噪声与野值率复测 | 坐实表1 的 σ≈0.05 m 与 §2.7 的野值动机 | G13,G6 动机 |

---

## 三、执行顺序与依赖

```
L0（今天可开始，零依赖）─────────────────────► 补齐 G2 G3 G5 G6 G8 G13
        │
        ├─ 改 record_rosbag.sh + 写离线复算工具（不需重建）
        │        └─ 录一次飞行 ──► L1 ──────► 补齐 G4 G12，强化 G3
        │
        └─ 打包 (a)(b)(c)(d)(e) 五处代码改动
                 └─ 【请用户重建 sim-world + flight-stack 各一次】
                          └─ L2 ──────────► 补齐 G1 G7 G10 G14 G16
                                   └─ L3 ─► 补齐 G9 G11
```

关键点：项目的 docker-compose 没有挂载 `src/` 和 `entrypoints/`，任何节点代码改动
都必须重建镜像。因此 (a)–(e) 五处改动**必须一次改完再交给用户重建**，避免多轮
build 浪费时间。

---

## 四、实验做完后的正文结构调整建议

1. **第4章重写**为：
   - 4.1 实验平台与参数设置（补齐 G15 的全部可复现性信息）
   - 4.2 数值验证：噪声传播与 CRLB、偏置免疫、抗差、可观测性（E1–E6）
   - 4.3 仿真闭环验证：收敛精度、角度全域、多机复合（E11/E12/E14）
   - 4.4 工程问题的修复与验证（E13，把 §3.2 补成完整闭环）
   - 4.5 真机验证：方法族泛化与机载计算开销（E17/E18）
   - 4.6 与现有方法的比较（现 4.1 的表2 + E8 的批量对比数据）
2. **表3 删除或大幅缩减**，只保留真正的后续工作（SE(3) 推广、物理基线与运动基线
   融合），不再把主线实验列为"尚待完成"。
3. **摘要与 §6.1 的抗差表述**在 E4/E15 完成前先降级为"提出并数值验证"，完成后再
   恢复正面陈述。
4. **§2.6 增补或 §6.2 承认观测异步项**（E6/E9 的结论决定放哪）。
5. §2.6 那句"与实测滑窗收敛后 1°–2° 量级吻合"改为引用 E11 的正式结果。

---

## 五、优先级速览

若时间极其有限，按此顺序砍：

1. **必做**：E1（CRLB 验证）、E3（批量对比）、E11（收敛精度主实验）——这三条撑起
   论文的实证骨架，缺任何一条都不完整。
2. **强烈建议**：E4（抗差，因为摘要已经写了）、E13（§3.2 闭环，性价比最高的贡献
   升级）、E7（真实数据敏感性）。
3. **有余力则做**：E2、E5、E9、E14、E17、E18。
4. **可留后续工作**：SE(3) 实现、物理基线与运动基线融合、E12 全角域扫描。

---

# 附：执行进展（2026-09-03，仿真侧）

用户要求"把仿真侧的都做了"。L0全部跑完出结果，L1工具写完并自检通过，L2代码
改动全部完成、等待重建镜像后执行实验。真机侧(L3)未动。

## L0 数值实验：已完成，结果如下

脚本 `scripts/paper_exp/mc_se2_sim.py`（估计器逻辑从 `se2_core.py` 导入，那份是
机上 `origin_setter_node.py` 算法部分的逐行副本；已用一致性测试确认两份实现在
含野值数据上给出**完全相同**的θ*）。全套约14秒跑完，输出在
`docker_sim/paper_exp_results/`。

| 实验 | 结论 | 补上的缺口 |
|---|---|---|
| E1 | 论文式(12)预测单段8.10°，实测8.28°；式(13)预测W=50时1.146°，实测1.13°（比值0.989）。**理论与实测吻合，CRLB论断成立** | G2 G4 G13 |
| E2 | 任意恒定偏置下θ*误差为**数值零**(0.000e+00°)，平移误差恒等于\|b\| | G5 |
| E3 | ω=0.1°/s、飞行600 s：滑窗稳态误差−1.16°（解析滞后−1.23°），批量−25.41°；RMSE 1.62° vs 25.55°。**批量误差随时间线性发散，滑窗有界** | G3 |
| E4 | 10%观测被5 m野值污染：RMSE 30.66°→**1.74°**；无野值时的效率代价仅1.18°→1.22° | G6 |
| E5 | 纯噪声下单方向/多方向标准差1.13° vs 1.17°（**论断①成立**）；γ=0.2的方向相关偏置下，单方向θ*偏差+11.29°恰等于arctanγ=11.31°且残差停在噪声底(比值1.00，**完全不可检测**)，多方向偏差降到+5.71°、残差抬升到1.11倍（**可检测**），量化了论断② | G8 |
| E6 | "取最新一帧"(机上现状)与理想时间对齐几乎无差别(RMSE 0.16° vs 0.15–0.18°)，最差是蛇形1 m/s下−0.09°偏差；**朴素时间戳插值反而更差**(0.25–0.37°) | G12 |

### L0跑出来的一个新发现（论文当前没有，建议补进2.6节）

机上实现的位移段是**首尾相接**的，相邻两段共享同一次绝对观测，噪声不独立。
对S做Abel求和可得

  Var(θ̂) ≈ σ²·[Σ‖u_i−u_{i+1}‖² + ‖u_1‖² + ‖u_W‖²] / (Σd_i²)²   (*)

两个极端：方向随机时 E‖u_i−u_{i+1}‖²=2d²，(*)化简**回到论文式(13)**；方向一致
（直线飞）时中间项全部消掉，只剩两个端点项，Var=2σ²/(Σd_i)²，**比式(13)小整整
W倍**（标准差小√W倍）——中间那些观测噪声两两抵消，等效基线是整条轨迹的总位移
而不是单段位移。E1实测：W=50、方向一致时标准差0.162°，(*)式预测0.162°，而式(13)
预测1.146°，差7.1倍≈√50，吻合。

这不违反CRLB：式(15)的下界是在"各段噪声独立"模型下推的，真实观测模型噪声相关、
Fisher信息本来就更大。**建议论文2.6节把(*)式补上**，它同时解释了E6为什么几乎
测不出异步误差（异步表现为绝对观测的位置域误差，在同一个差分结构里被同样抵消），
也给第5节的方向多样性讨论加了一层：纯噪声下方向多样性不仅不必要，在首尾相接的
实现里方向一致反而**更准**。

### 由实验结论直接做出的一个工程决定

原计划的L2改动 **(e) 时间戳对齐** 取消实施。E6显示朴素插值把RMSE从0.16°抬到
0.25–0.37°（插值混合两帧噪声、破坏了上面说的噪声抵消结构），是负优化。离线
工具里两种配对方式都保留（`--sweep`会跑E9消融），等真实bag复算确认之后再定论。

## L1 离线复算工具：已完成，自检通过

* `scripts/record_rosbag.sh` 增录4路话题：`/NX0x/uwb/pose_abs`、
  `/NX0x/uwb/pose_truth`、`/NX0x/origin_setter/yaw_estimate`、
  `/NX0x/origin_setter/yaw_sample_count`。
* `scripts/paper_exp/se2_calib_offline_eval.py`：读bag→复刻分段/滑窗→扫
  W/d_min/批量/Huber/时间配对，出CSV；`--fig`画E11收敛曲线，`--aggregate`
  做N次重复的mean±std（补G14）。`--self-test`会用rosbag2_py合成一个bag再读
  回来跑通全流程，**不需要真实bag就能验证工具本身是通的**（已通过：合成30°
  真值，复算30.005°）。
* 真值取的是**逐时刻**的θ_true(t)=yaw_truth(t)−yaw_odom(t)，不是固定的spawn
  yaw常数——这样DLIO自身的yaw漂移也算进评估里。

## L2 代码改动：已完成，**等待重建镜像**

需要重建 `sim-world` 和 `flight-stack` 两个镜像。所有开关都走环境变量，重建
之后做实验不用再改代码、不用再重建。

| 改动 | 文件 | 默认行为 |
|---|---|---|
| (a) 真值旁路 + 观测污染注入 + 观测率/时延限制 | `src/uwb_sim/uwb_sim/uwb_ground_truth_node.py` | 只新增`pose_truth`发布，其余全部默认关，行为不变 |
| (b) spawn yaw 环境变量可覆盖（按度） | `docker/entrypoints/sim-world-entrypoint.sh` | 默认仍是NX01=30°、NX02=−45° |
| (c) θ*的Huber抗差 + 残差诊断话题 | `src/uwb_origin_bridge/.../origin_setter_node.py`、`launch/uwb_origin_bridge.launch.py` | 抗差默认**关**，关闭时逐字节等价于原实现；诊断话题无条件发 |
| (d) 目标点持续重变换（§3.2修复） | `src/ego_planner_bridge/.../rviz_goal_bridge_node.py` | 默认**开**，`GOAL_RETRANSFORM=false`退回修复前，用于E13对照 |
| (e) 时间戳对齐 | — | **取消实施**，理由见上面E6 |
| 环境变量透传 | `docker-compose.yml` | 三个服务共14个新变量，默认值全部等于改动前行为 |

具体命令见 `scripts/paper_exp/README.md`。

## 顺带查实的一条事实（论文§4.2必须交代）

`/{ns}/uwb/pose_abs` 是在 `model_states_cb` 里逐帧发的，真实发布率等于world里
`gazebo_ros_state` 插件的 `update_rate`（各world都是**100 Hz仿真时间**），
**且完全没有走延迟队列**；节点参数 `publish_rate_hz=50`/`latency_ms=30` 只作用
于 `/frame_align/*`。即：仿真里的绝对定位源比真实UWB乐观得多。论文报仿真精度时
必须写明，或用新加的 `UWB_ABS_RATE_HZ=10 UWB_ABS_LATENCY_MS=30` 重跑一组对照。
