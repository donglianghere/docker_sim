# 全流程改进任务清单（2026-09-24 制定）

面向"按动作得分"的比赛场景，对选手全流程程序（`contestant_template/`）+ 配套 SDK/
飞行栈的改进清单。每项都写清楚：**为什么要做**（现状+证据）、**怎么改**（落到文件）、
**验收标准**、**工作量**。按优先级排序，不按提出顺序。

现状基线（2026-09-24 实测，`双机全流程巡检版示例.py` 这条链路）：

| 项 | 数值 |
|---|---|
| 选手代码量 | 5 个文件、纯代码 1326 行（含注释 1721 行） |
| 单轮全流程耗时 | 巡检版约 10 分 40 秒；绕飞版约 13.5 分钟 |
| 用到的 SDK 能力 | 27 个，约 240 处调用，无一处绕过 SDK 直接用 rclpy |
| 双机事件 | 6 种，全走 `mission_events`（ACK+1Hz 重发+去重） |
| 已知致命风险 | `goto()` 超时抛 `GotoTimeoutError`，示例无人捕获 → 整轮任务中断 |

---

## 优先级总表

| # | 任务 | 优先级 | 工作量 | 阻塞得分？ |
|---|---|---|---|---|
| T1 | 任务段防中断兜底（异常不终止整轮） | **P0** | 0.5 天 | 是，已实际发生 |
| T2 | `goto_direct()` 补显式超时 | **P0** | 1 小时 | 是（无限等待） |
| T3 | 现场已知目标"直达"开关 | **P0** | 0.5 天 | 否，但省 3~6 分钟 |
| T4 | 纯航点巡场程序（不做任何检测） | P1 | 0.5 天 | 否，调试/踩场用 |
| T5 | 一键配置/启动/运行脚本 | P1 | 1 天 | 否，现场效率 |
| T6 | 通报时截图回传（位置+时间+目标） | P1 | 1.5 天 | 否，评分证据 |
| T7 | 全流程瘦身到 300~400 行 | P2 | 1.5 天 | 否 |
| T8 | 真机移植方案 | P2 | 评估 0.5 天 + 联调 2~3 天 | 是（最终交付） |

---

## T1 任务段防中断兜底 【P0】

**现状与证据**：`goto()` 默认 60 秒超时、超时抛 `GotoTimeoutError`；示例里 15 处
`except` 全是它的**子类** `GotoUnreachableError`（`exceptions.py:87`），父类无人捕获。
`main()` 只有 `finally: sdk.shutdown()`（`双机全流程巡检版示例.py:163-170`），没有
任何兜底。2026-09-24 上午实测：编队第一个航点判不到点 → 超时 → **整个程序退出，
飞机悬在空中**，后面两段任务（地面火情、高层火情）全部没做，分全丢。
规划器在立柱膨胀区/障碍附近卡住重规划是已知会发生的（今天还遇到 3# 立柱那次）。

**改法**：
1. 三个全流程程序里，每一段任务用 `try/except ContestSdkError` 包起来：失败就打印
   原因、播报声光、`return_home_and_land()`，**继续做下一段**，最后统一汇总哪几段
   成功。侦察机和任务机两侧都要做。
2. 新增 `safe_goto(sdk, x, y, z, retry_xy=None)` 包装：超时后先原地 `goto_direct`
   退半步，再重试一次；仍失败就抛给上层的段级兜底。
3. `sdk.goto()` 的超时按航段长度给（现在统一 60 秒）：短段 30 秒、跨场 90 秒，避免
   "还在正常飞就被判超时"和"卡住了还傻等 60 秒"两头不讨好。

**涉及**：`双机全流程示例.py` / `双机全流程巡检版示例.py` / `双机灭火任务示例.py`，
新增一个 `任务容错.py`（或放进 SDK 的 helpers）。

**验收**：人为把某个航点设到障碍物内部（比如 `(0,0)` 3# 立柱中心）跑全流程，程序
必须跳过这一段、继续做完后面两段、两机都正常降落；日志里有明确的"第 N 段失败，
原因 X，继续下一段"。

---

## T2 `goto_direct()` 补显式超时 【P0】

**现状**：`goto_direct(x, y, z, timeout=None)`（`capabilities.py:2708`）默认**不设
超时**（2026-09-15 按要求删掉的），走精准伺服旁路，正常几秒到位；但只要
`precision_servo_node` 不回 `arrived`，就**无限期等待**——按动作计分的场景里等同
死锁。示例里 10 处调用全在降落前的收尾段。

**改法**：不改 SDK 默认值（那是刻意的设计），改**调用点**：示例里每处显式传
`timeout=20.0`，并 `except ActionFailedError` → 退回普通 `land()` / 继续流程。
SDK 文档里补一句"选手程序里不建议用默认的 None"。

**验收**：断掉 `precision_servo_node`（或改个话题名）后跑单段示例，20 秒内必须
走到降落，不能卡住。

---

## T3 现场已知目标"直达"开关 【P0】

**现状**：现场目视能判断的信息（火情大概在哪、在哪根柱子、哪个高度）**无法喂给
程序**，只能间接靠删常量。搜索/巡检是全场扫的：地面弓字 20 个航点约 3 分钟，高层
巡检 3 点 × 4 次检测约 4 分钟。

**改法**：每个任务段文件顶部加一组"现场填空"常量，默认 `None` 时行为一字不变：

```python
# ——— 现场可填（看得见就填，填了就跳过搜索直奔目标）———
KNOWN_GROUND_FIRE = None      # (x, y)：地面火点世界坐标
SEARCH_AREA_OVERRIDE = None   # [(x1,y1),(x2,y2)]：缩小搜索矩形
KNOWN_FIRE_BUILDING = None    # (x, y)：着火的那根立柱
KNOWN_FIRE_HEIGHT = None      # 1.5 或 2.5
KNOWN_FIRE_FACE = None        # (x, y)：着火面朝向哪根柱子/哪个方向
EXTRA_OBSTACLES = []          # [(x, y, 半径), ...]：现场看到的额外障碍
```

逻辑：`KNOWN_GROUND_FIRE` 填了就直接飞过去悬停解算+通报（保留解算这一步，坐标仍
以二维码解算为准，人填的只当"去哪看"）；`KNOWN_FIRE_BUILDING/HEIGHT/FACE` 填了
就跳过巡检直接飞正对位置；`EXTRA_OBSTACLES` 合并进已有的 `PILLARS` 列表，
`pull_waypoints_out_of_circles()` 自动把航点挪出来。

**已经能改的**（写进现场速查卡，不用改代码结构）：`AREA`（搜索矩形）、
`CRUISE_AGL_M`、`OVERLAP`、`BUILDINGS`（删到只剩一根）、`INSPECT_HEIGHTS_M`
（只留一个高度，检测 12 次→6 次）、`PILLARS`+`CLEARANCE_M`、`SUPPLY_POINT`、
`ROUTE`。示例是**卷挂载**进容器的（`-v $(pwd):/workspace`），**改完直接重跑，
不用 build、不用重启仿真**。

**验收**：填上 `KNOWN_FIRE_BUILDING=(-4.5,7.0)`、`KNOWN_FIRE_HEIGHT=2.5` 后，高层
段从起飞到发射破窗弹 ≤ 90 秒（当前约 4 分钟）；全部留空时行为与现在完全一致。

---

## T4 纯航点巡场程序（不做任何检测） 【P1】

**目的**：踩场/验证场地坐标/验证规划器能不能过每个关键点，不受识别影响；也是
出问题时"先排除飞行链路"的最小复现程序。

**内容**：新增 `contestant_template/纯航点巡场示例.py`，按全流程会经过的关键点
依次飞、每点悬停 2 秒、打印到点时间与真实位置偏差，**不订阅任何检测、不动舵机、
不发任何事件**。默认航点序列（世界坐标，全部来自
`fire_drill_room_layout.yaml`，不硬编码第二份）：

1. 起飞点上空 2.0 m
2. 编队航线四点 `(7,-9.5) (7,9.5) (-7,9.5) (-7,-9.5)`（含过仿地模块、绕障碍圆柱）
3. 地面弓字航线的 4 个角点（不是全部 20 点，只验证四角可达）
4. 物资点 `(-4,-6)` 上空 1.5 m
5. 地面火点 `(0,-4)` 上空 1.5 m
6. 三根立柱的三条边中点（高层巡检点）× 两个高度 1.5/2.5 m
7. 每根立柱的正对瞄准位置（standoff 3 m）
8. 返航降落

参数：`--agl`、`--hold`、`--only 1,2,5`（只飞其中几组）、`--dry-run`（只打印航点
不起飞）。全程开转场定高。

**验收**：连续跑 3 轮，每轮所有航点均到位（偏差 < 0.3 m）、无超时、无卡死；输出
一张"航点 → 实到坐标 → 偏差 → 耗时"的表，可直接贴进踩场记录。

---

## T5 一键配置/启动/运行脚本 【P1】

**现状**：`contestant_template/运行仿真.sh` 已经能"重建仿真 + 起两个任务容器"，
但**首次配置**还是散的：Docker 装没装、镜像从哪来、`.env` 要不要改、`xhost` 授权、
`net.core.rmem_max=32MB`（不设会丢包）、`brltty` 抢 CH340（声光板）……这些现在
分散在 README/调试日志里，选手（和现场助教）不可能记得住。将本机地面站容器同步到192.168.2.100

**改法**：三个脚本，放在 `contestant_template/`（选手只拿这一个目录）：

| 脚本 | 做什么 |
|---|---|
| `一键配置.sh` | 检查/安装 docker + compose 插件；`docker load` 离线镜像包（或 pull）；写 `/etc/sysctl.d/60-ros2-dds-buffers.conf` 并生效；`systemctl mask brltty`；写默认 `.env`；`xhost` 授权；最后打印一份"检查通过/未通过"清单 |
| `一键启动环境.sh` | 调 `scripts/start_sim.sh`：起仿真、等 PX4 自检、**逐路验证四个相机真的出图**、打印本次场景坐标（这套已经做好了，包一层给选手） |
| `一键运行.sh` | = 现在的 `运行仿真.sh`，但默认任务改成 `我的任务.py`，加 `--task` 切换；跑完自动把两机日志收到 `logs/日期-时间/` 下 |

Windows 侧对应 `一键运行.bat`（已有 `运行仿真.bat` 可扩展）。

**验收**：一台**干净的** Ubuntu 22.04 机器，从拿到压缩包到飞机起飞，只需要依次
执行这三个脚本，中途不需要查任何文档、不需要手敲 docker 命令。

---

## T6 通报时截图回传（位置 + 时间 + 目标信息） 【P1】

**需求**：通报**地面火情**、**高层火情**、**物资点**这三个时刻，相应飞机截一张图，
图上打上位置、时间、目标信息，传回地面站。

**现状（要注意的约束）**：
- 选手容器（`contestant-sdk:latest`）里**没有 PIL / cv2 / cv_bridge**，只有
  numpy 1.21.5 —— 在选手侧解码 `sensor_msgs/Image` 再画字，要么加依赖（镜像变大），
  要么手写解码+画字（难看且易错）。
- 飞控栈容器里 **cv2 4.5.4 + cv_bridge 都有**，检测节点
  （`camera_tag_detect_node.py:80`）本来就已经把每帧转成了 bgr8、而且**知道检测框
  在哪**——在那边截图还能顺带把框画上。
- 选手容器只挂了 `-v $(pwd):/workspace`，没挂 `/logs`。

**推荐方案（B 方案）**：截图放在飞控栈侧做，SDK 只发一条指令。
1. 检测节点（或新增一个 `snapshot_node`）订阅一个 `snapshot/request` 话题，收到后
   取**最近一帧**（不重新等）、画上检测框、叠加文字：事件名、目标类别/ID、目标世界
   坐标、飞机世界坐标、本机时间（UTC+8）、帧时间戳，存成
   `/logs/evidence/{ns}_{事件}_{时间戳}.jpg`，并发布 `snapshot/result`（文件路径 + 元
   数据）。
2. SDK 新增 `sdk.snapshot(event, camera='down'|'front', **meta) -> str`：发请求、等
   结果（超时 3 秒，**失败只警告不抛**——截图绝不能拖垮任务，跟 `trigger_alarm()`
   同一个原则），返回文件路径。
3. 三个通报点各加一行：地面火情（`地面火情搜索示例.py` 悬停解算后）、高层火情
   （`高层火情巡检版示例.py` 对准后）、物资点（精准降落抓取前）。
4. 回传地面站：仿真下 `/logs` 是宿主机 `runtime_logs/` 的挂载，地面站直接能读；
   真机上复用已有的 hw-fleet 代理，加 `GET /api/hw-fleet/{ns}/evidence`（列表）和
   `/evidence/{文件名}`（取图）两个透传接口，前端加一个"证据"页。
   **不要**把图片塞进 `mission_events`——那是 `std_msgs/String` + RELIABLE + 1Hz
   重发的控制通道，几百 KB 的 base64 会把它压垮。

**验收**：跑一轮全流程，`runtime_logs/evidence/` 下恰好出现 3 张图，每张图上能看清
目标框、目标世界坐标、飞机坐标、时间；地面站页面上能看到这 3 张图；人为把截图节点
关掉，任务流程**不受任何影响**（只多几条警告）。

---

## T7 全流程瘦身到 300~400 行 【P2】

**目标**：一个**单文件**全流程程序，纯代码 300~400 行，保留所有关键点的实现（不是
把逻辑藏进黑盒 API），当前这条链路是 1326 行。

**做法：把"机制"沉到 SDK，把"策略"留给选手。** 逐项拆：

| 现在在选手代码里的东西 | 行数 | 处置 |
|---|---|---|
| `FireWatcher` 后台检测线程 + 打断航段 | ~60 | 移入 SDK：`sdk.watch_for(class_id, on_found)` |
| `descend_onto()` 边瞄准边降落 | ~55 | 移入 SDK：`sdk.descend_onto(class_id, handoff_agl=0.7)` |
| `aim_at_fire()` 对准+复查+修坐标 | ~60 | 移入 SDK：`sdk.aim_and_refine(class_id)` |
| 火点归属判别（射线+表观尺寸估距） | ~70 | 移入 SDK：`sdk.attribute_target(candidates)` |
| `standby_point()` 待命点几何 | ~35 | 移入 SDK geometry_helpers |
| `inspection_order()` 巡检顺序 | ~10 | 保留（策略） |
| 舵机抓取/投放/发射三件套 | ~25 | 移入 SDK：`sdk.grab()/release()/launch()` |
| 双机握手（就位/破窗/降落通知） | ~50 | **保留**（关键点，必须看得见） |
| 弓字/绕飞/巡检航线生成 | ~30 | 已在 SDK，保留调用 |
| 各段主流程（飞哪、看什么、什么时候通报） | ~300 | **全部保留**（这才是"选手代码"） |

**预估**：串场 ~90 + 地面段 ~110 + 高层段 ~130 + 公共常量/main ~40 ≈ **370 行**，
落在目标区间。移出去的约 305 行进 `contest_sdk`（有单测、有文档，不算选手代码）。

**注意**：注释率现在 42%，瘦身后仍应保留关键决策的注释（踩过的坑不能丢），
所以"纯代码 370 行"对应文件总长约 600 行。这一条要跟你确认口径：**300~400 是纯
代码行还是文件总行数**。

**验收**：瘦身版与现版跑同一场景，结果一致（两处火情都发现、坐标偏差同量级、
耗时不增加），且 `diff` 能看出每一个关键动作仍在选手代码里显式写着。

---

## T8 真机移植方案 【P2】

**结论先说**：飞行/任务逻辑这一层**基本不用改**（SDK 已经是同一套，`运行真机.sh`
只是 `运行仿真.sh --real`：`ROS_DOMAIN_ID=20`、DDS 走飞机网卡、不重建仿真）。
真正的工作量在**环境与外设**，以及**没有 Gazebo 真值之后怎么验证**。

**必须逐项过的清单**：

| # | 项 | 现状 | 动作 |
|---|---|---|---|
| 1 | `DEPLOY_TARGET=hw` + `docker-compose.hw.yml` | 骨架已有，三个控制器都能跑但**未经真机验证** | 单机地面通电验证每个节点起得来 |
| 2 | 定位：真 UWB + 测距雷达（`uwb_imu`） | 仿真同一套代码路径 | **今天新加的原点锁定判据在真机更关键**；`max_odom_p2p_m`(0.15) 可能要按真机噪声放宽，已参数化 |
| 3 | 双机 MAVROS 串扰 | **未修复**：两架 `tgt_system` 都是 1，共用 `/uas1` | 改 NX02 的 `AGENT_INDEX` + `MAV_SYS_ID`，上电前必须先解决 |
| 4 | 舵机 | NX02 MAIN7/9 已真机验证（800 抓紧 / 2000 松开）；**NX01 未验证** | NX01 舵机配置+验证 |
| 5 | 声光板 | CH340 串口，真板已验证；`brltty` 会抢占设备 | 部署脚本里 `systemctl mask brltty`（并入 T5） |
| 6 | 相机 | 仿真是"可切换关节的单相机"；真机是 vision-stack 容器 + cam0/cam1 | `set_camera_view()` 在真机的等价实现要确认（是切物理云台还是切话题） |
| 7 | 网络缓冲 | Jetson 上 `net.core.rmem_max` 不设成 32MB 会丢包 | 并入 T5 配置脚本 |
| 8 | 电量 | 仿真打了"不掉电"补丁；真机三段任务要三次起降 | 改成每段落地之后（T 已完成）**正好方便中途换电**，但要确认单段耗时 < 单块电池续航 |
| 9 | 真值验证手段 | 仿真靠 `/plug/model_states_plug`；真机没有 | 改用 UWB 绝对位姿 + 起降点实测偏差 + 录 rosbag 事后对齐 |
| 10 | 部署打包 | `package_hw_deploy.sh` 已有（排除仿真专属文件） | 按现在的新增文件复核一次排除清单 |
| 11 | 仿地/定高 | 依赖 `uwb_imu` 的 z=离地高度，真机雷达是真实的 | 逻辑不变，但坡道响应滞后（实测上坡瞬间掉 0.3 m）要在真场地复测 |

**建议的联调顺序**（每步都能单独止损）：单机地面通电 → 单机起降 → 单机 `纯航点巡场
示例.py`（T4，无识别）→ 单机地面火情识别 → 双机通信 → 双机编队 → 全流程。

---

## 建议的执行顺序

1. **今天就能做**：T1 + T2（半天，直接消除"整轮丢分"风险）
2. **赛前必做**：T3（现场应变）+ T5（现场部署）+ T4（踩场工具）
3. **有余量再做**：T6（评分证据）→ T7（可读性/可维护性）
4. **并行推进**：T8 的清单第 3、4、6 项是硬件相关，越早开始越好

---

*本清单对应代码状态：`docker_sim` @ `6258ad8`（2026-09-24）。行数、耗时、失败案例
均为当日实测，不是估计值。*
