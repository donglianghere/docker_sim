# 论文大纲：基于Docker容器化的双机协同飞行仿真平台设计与实现

> 本大纲从 `README.md`、`TODO.md`、`docker_sim/DEBUG_JOURNAL.md`
> （2026-08-07~08-20，237条日期条目，仿真侧）、`read_hw.md`
> （2026-08-18~08-20，真机（Jetson单机）侧调试归档）、
> `赛项适配度评估报告.md`、`真机部署与Docker开发知识梳理.md` 蒸馏而来，
> 供确认方向后展开成全文初稿。定位：系统设计与实现型论文，面向机器人/
> 导航定位/系统仿真类中文核心期刊。
>
> **2026-08-20补充说明**：`read_hw.md`记录的是本项目在真实Jetson平台上
> （单机、未装桨叶的台架/dry-run验证）已经实际发生的调试过程，不是"计划
> 中的未来工作"——这份材料把全文的证据基础从"仿真验证+移植方案设计"
> 升级为"仿真验证+真机复现同类问题+真机独有的新发现"，第3/4/5节已按此
> 更新，是本次大纲修订的核心变化。

## 标题（备选）

1. 面向多机容器化仿真集成的三重一致性框架及其在双机协同飞行平台中的应用
2. 容器化多机异构仿真系统的一致性保障方法与双机协同飞行验证
3. 基于Docker容器化的双机协同飞行仿真平台设计与实现
4. 五子系统集成的双无人机协同飞行仿真平台：架构与关键技术

（备选1/2直接把"三重一致性框架"顶到标题里，最贴合"提出理论、以平台验证"
的定位，投偏理论/方法类栏目优先选；备选3/4更偏"系统设计与实现"传统表述，
投工程应用类栏目、或期刊明确要求"设计与实现"体例时选；四者共用同一份
正文骨架，仅摘要开篇一两句需要跟着标题调整强调重点，建议定刊后再定标题）

---

## 核心论点（全文的理论骨架，先立住这一条，再往下展开每一节）

**多机器人容器化仿真集成的本质，是把一组原本按"单进程、单机、单机器人"
隐式假设开发的异构软件栈（SLAM/规划/控制/仿真器），降级装配进一个多容器、
多主机、多智能体的分布式现实中；这个降级过程中出现的几乎所有集成故障，
都可以归约为三类一致性契约的破坏——时间一致性、空间一致性、通信一致性。**

本文不满足于报告"发现了哪些bug、怎么修的"，而是提出这一"三重一致性"
分类框架作为理解和预判此类集成故障的统一视角：每一类给出可判定的失效
特征（如何在下次遇到类似现象时快速定位属于哪一类、进而缩小排查范围），
并在空间一致性维度下把"局部-全局坐标系SE(2)在线标定"进一步形式化为
可独立复用的方法（含滑窗最小二乘公式）。三重一致性问题能够被持续发现、
修复并跨机器迁移复现，依赖于本文第二个体系性贡献——patch化的可复现
构建范式，它把"发现问题→定位根因→改代码→固化→在新环境复现"这个循环
本身工程化、可审计化。

**框架的跨环境验证与边界发现（已据完整证据挖掘修正，下方为修正后
表述——早期草稿"仿真从未触发并发问题"的说法已被证据推翻，写作时
不要采用）**：本文进一步把该框架从仿真环境迁移到真实Jetson单机平台
（未装桨叶的台架/dry-run验证）做交叉检验，发现两类现象：①三重一致性
框架中的多个具体问题在真机上以"物理量纲对应版本"复现（如IMU厂商SDK
原始输出单位为g而非m/s²、真实雷达前倾角度与仿真设定值不一致需重新
标定），验证了该框架并非仅对仿真容器化场景成立；②真机上暴露出一类
框架原有三个维度都无法归类的问题——多线程并发写共享状态（DLIO内部
对每帧点云发布采用不节流的detached线程）。本文把这一发现命名为"并发
一致性"。**完整案例证据挖掘（详见
`论文大纲_容器化机器人软件集成一致性故障分类方法.md`第6.5节）进一步
显示，这类问题在仿真阶段就已经独立出现过一次**（DLIO的`debug()`
detached线程与主线程竞争访问GICP对象致段错误，约2026-08-08），当时
的修复只处理了那一个具体线程，未触发对代码库其它同类detached线程
调用点的系统性审查；真机阶段的`publishToROS()`/`publishKeyframe()`
竞态是**同一失效模式在不同代码路径上的第二次复发**，其修复本身又
引出第三次变种（线程排队积压）。由此得到的更精确论点是：**并发一致
性故障不是仿真环境结构性无法触发，而是点状修复无法根除类别性风险
——真机更高的负载/时序抖动只是提高了复发概率、加速了复发速度，不是
"从0到1"创造了这类问题**。这一更精确的论点取代了"纯仿真验证对时序
敏感型故障存在结构性覆盖盲区"这一过强表述，改为更可辩护的"点状修复
不足以覆盖类别性风险，需要跨环境的持续验证与系统性代码审查配合"。
全文围绕这些体系性贡献组织，双机协同飞行仿真平台与真机单机验证是
共同的验证载体，不是论文本身要报告的终点。

## 摘要要点（写作时展开）

- 背景：无人机集群协同作业（应急/物流/巡检）对低成本、可复现的多机仿真验证
  平台有迫切需求，但主流仿真方案（PX4+Gazebo）默认不解决"多机容器化部署+
  跨机坐标统一+SLAM/规划/控制异构系统集成"这一整套工程问题；已有工作多
  停留在"报告踩过的坑"层面，缺少能迁移到其他多机容器化系统的统一理论视角。
- 方法：设计并实现了基于Docker的双机协同飞行仿真平台（集成DLIO/mighty/
  ego-planner-swarm/四种板外控制器/PX4 v1.16 SITL/经典Gazebo/UWB六大
  功能模块）作为验证载体；以此为基础，**提出容器化多机异构仿真集成的
  "三重一致性"故障分类框架**（时间一致性/空间一致性/通信一致性），并在
  空间一致性维度下**形式化了一种基于滑窗最小二乘的局部-全局坐标系SE(2)
  在线标定方法**，同时给出支撑上述问题可持续、可复现修复的**patch化
  工程范式**。
- 验证：三重一致性框架下归纳的典型失效模式在仿真平台均有实测复现与修复
  证据（QoS策略不匹配、跨容器共享内存丢包、命名空间隔离缺失、仿真/挂钟
  时间基准分裂等）；SE(2)标定方法的收敛精度与理论预期（噪声传播估算）
  吻合；仿真平台完成起飞-轨迹跟踪-悬停-降落-再起飞全流程与机间避障对穿
  测试（最小间距>3m）验证。进一步在真实Jetson单机平台完成台架/dry-run
  验证，复现并修复了IMU单位换算、LiDAR-IMU外参重标定、RC失控保护死锁
  等物理对应问题，并定位修复了DLIO发布线程竞态（真机阶段发现的这一
  竞态与仿真阶段更早出现过的一次同类DLIO线程竞态属于同一失效模式在
  不同代码路径上的复发，由此提出"并发一致性"作为框架边界的补充维度），
  同时给出DLIO与Point-LIO两种SLAM后端在同一Jetson平台上的负载/内存
  实测对比数据。
- 意义：三重一致性框架、SE(2)标定方法与patch化范式均不依赖本文具体选用
  的算法栈，可迁移应用于其他多机器人容器化仿真/部署系统的故障预判与
  工程组织；仿真到真机验证过程中"并发一致性"维度的完整复发链条进一步
  说明，点状修复不足以根除类别性风险，需要跨环境验证与系统性代码审查
  配合，为该类系统的验证方法论提供了从"个案排障经验"上升到"可复用
  方法论"的路径。

---

## 1 引言

### 1.1 研究背景与意义
- 无人机集群协同（应急救援/物流配送/巡检）的应用需求增长，2026年全国
  人工智能应用技术技能大赛"无人机装调检修工"赛项即以此为背景（引用
  `赛项适配度评估报告.md` 的赛项结构作为应用场景佐证）。
- 现有开源方案痛点：PX4/ArduPilot生态的SITL、DLIO/FAST-LIO/Point-LIO等
  SLAM算法、mighty/ego-planner-swarm等集群规划器，彼此独立发布、依赖版本
  互不兼容，"集成"本身就是被低估的工程难题——这正是本文工作的切入点。
- 容器化（Docker）为多子系统版本隔离+可复现部署提供了天然方案，但引入了
  跨容器DDS通信的新问题（本文关键技术部分的直接动机）。

### 1.2 国内外研究现状
- 待补充文献综述（建议按三条线组织，写作时用WebSearch/学术检索补充最新
  引用，本大纲不臆造文献）：
  1. 无人机集群仿真平台：PX4 SITL + Gazebo/AirSim 生态，多机扩展方案
     （如XTDrone、Fast-Planner多机分支）。
  2. 激光惯性里程计（LIO）在小型无人机上的应用：DLIO/FAST-LIO2/Point-LIO
     等算法路线对比（本文3.3节的技术选型部分可与此呼应）。
  3. 多机器人坐标系统一/相对定位：基于UWB的分布式定位、SLAM子图对齐方法
     （本文3.2节θ*标定方法的定位）。
- 本文与已有工作的差异化：已有平台多聚焦单一环节（要么是规划算法论文自带
  的简化仿真、要么是纯容器化部署教程），少有工作系统性报告"五系统集成
  仿真平台"从0到端到端可用过程中的完整工程问题谱系与解法。

### 1.3 本文工作与贡献
1. 提出容器化多机异构仿真集成故障的**"三重一致性"分类框架**（时间/空间/
   通信一致性），把分散的、案例式的排障经验提升为可预判、可迁移的统一
   理论视角——这是全文最核心的理论贡献，第3节按此框架的三个维度组织，
   而非按开发时间顺序或子系统模块罗列。
2. 在空间一致性维度下，**形式化提出一种基于滑窗最小二乘的局部-全局坐标系
   SE(2)在线标定方法**，同时提供不依赖仿真真值的硬件可迁移实现路径。
3. 提出并验证一套**patch化的可复现工程范式**，作为三重一致性问题能够被
   持续发现、修复、固化、并跨机器/跨架构复现的工程基础设施。
4. 将三重一致性框架从仿真环境迁移到真实Jetson单机平台做交叉验证，**发现
   并形式化"并发一致性"作为框架边界的第四维度**——完整案例证据显示该
   失效模式在仿真、真机两个环境跨越约11天、经三次不同代码路径复发，
   据此提出"点状修复无法根除类别性风险，需要系统性代码审查配合跨环境
   验证"这一更精确的方法论观察（详见方向A大纲第3.3.5/7.3节，取代早期
   "仿真结构性无法触发"的过强表述）。
5. 以双机协同飞行仿真平台与真机单机台架验证为共同载体，完成上述理论
   框架与方法在真实工程场景下的实测验证，并给出面向双机真机组网的
   后续路线图。
- 强调贡献1、2、3、4具有超出本平台本身的可迁移价值（不依赖DLIO/mighty/
  PX4这组具体选型），贡献5是载体与实证，写作时避免让读者误以为本文只是
  "记录了一个项目的开发过程"。

### 1.4 论文组织结构

---

## 2 系统总体架构

### 2.1 硬件与软件基线
- 宿主机（开发/仿真侧）：Ubuntu 24.04；容器内：Ubuntu 22.04 + ROS2
  Humble + PX4 v1.16 + 经典Gazebo（非Harmonic，需说明选型原因——生态
  兼容性/DLIO等上游依赖）。
- 真机侧：Nvidia Jetson Orin NX Super 16G，JetPack 6.2，8核CPU（
  DLIO稳态负载线程数75~76、1分钟load average 15~25，是本文4.6节负载
  实测的硬件背景）。**如实标注当前完成度**：单机已完成不装桨叶的台架/
  dry-run全流程验证（真实Mid-360雷达+IMU接入、DLIO/Point-LIO两种SLAM
  后端均已跑通、px4ctrl状态机验证），尚未进行真实带桨试飞与双机真机
  组网——图中应用不同底色/图例明确区分"仿真验证完成"/"真机台架验证
  完成"/"真机待完成（带桨试飞、双机组网、视觉模块）"三档，不要用单一
  的"已完成/待完成"二元划分掩盖中间状态。

### 2.2 五子系统集成架构（对应CLAUDE.md定的系统构成）
- DLIO（激光惯性里程计，SLAM定位源）
- mighty / ego-planner-swarm（两条独立规划器路线，可切换）
- ros2_px4_stack / px4ctrl / so3ctrl / pt4ctrl（板外控制器，四选一，逐一
  说明各自控制律来源：转发position/velocity setpoint交给PX4内环 vs
  板外几何姿态控制）
- 经典Gazebo（物理仿真+双机PX4 SITL）
- UWB（`uwb_sim`真值模拟节点，含在线SE(2)标定）
- 建议画一张整体架构图：三类容器（sim-world / flight-stack-nx01 /
  flight-stack-nx02）+ 容器间ROS2话题/TF流向 + 关键桥接节点
  （`gt_odom_bridge`/`uwb_origin_bridge`/`frame_align_bridge_node`/
  `ego_planner_bridge`）。

### 2.3 传感器配置
- 顶置激光雷达（Mid-360，水平安装——注明08-13"雷达安装角拍板水平安装"
  这一设计决策的确定过程）
- 前视相机（d435，当前因`realsense_gazebo_plugin`双机初始化时序崩溃被禁用，
  作为"已知局限"在此处如实说明，呼应第5节展望）
- 下视相机（尚未实现，同上）
- 此处**必须如实反映现状**：不要包装成"已完整实现三类传感器"，按
  `赛项适配度评估报告.md`的结论，传感器部分只有顶置雷达链路完整。

### 2.4 容器化部署架构
- 三镜像划分：`Dockerfile.base`（公共基础）/ `Dockerfile.sim-world`
  （Gazebo+双机SITL+UWB真值）/ `Dockerfile.flight-stack`（DLIO+规划器+
  控制器，双实例）——说明划分依据（关注点分离+复用父镜像层节省构建/存储）。
  本节只交代"镜像怎么划分"这一静态结构事实；三层代码组织与patch机制这套
  支撑一致性问题可持续修复的工程范式，作为完整论证放在3.4节展开，此处
  不重复，只做一句话前向引用。
- 运维可观测性设计：tmux八窗口监控面板（子系统健康位红绿灯）、
  `record_rosbag.sh`滚动录制+`save_incident.sh`事故留证、镜像
  导出/迁移脚本（`bundle.sh`）——体现"可复现实验+可追溯排错"的系统工程
  素养，适合作为区别于"纯算法论文"的系统论文亮点。

### 2.5 可配置参数矩阵
- `LOCALIZATION_SOURCE`（gt/uwb_slam/uwb_imu，含三个单机变体）×
  `PLANNER`（mighty/ego_planner）× `CONTROLLER`（ros2_px4_stack/px4ctrl/
  so3ctrl/pt4ctrl）= 理论18种主组合，是本平台面向"算法对比研究"的核心
  设计（不同定位源/规划器/控制器的可插拔对比，是很多论文缺乏的能力）。
- 建议第4节直接引用README.md的实测状态表格（哪些组合已验证/哪些确认
  不可用/哪些无记录），作为图表出现。

---

## 3 关键技术：容器化多机异构仿真集成的三重一致性框架

> 本节是全文的理论核心。**不按开发时间顺序、也不按子系统模块**组织，
> 而是先给出统一的分类理论（3.0），再把所有具体案例作为该理论的实例
> 挂到对应维度下（3.1/3.2/3.3），最后给出支撑三重一致性问题得以被
> 持续修复的工程基础设施（3.4）。每个具体bug只作为"实例佐证"出现，
> 不再是叙事主体——写作时注意控制每个案例的篇幅（2-4句话讲清"现象/
> 判据/根因/解法"即可），避免退化回流水账。

### 3.0 三重一致性：一个统一的故障分类框架

**核心命题**：DLIO、mighty/ego-planner-swarm、px4ctrl等每一个子系统在
最初设计时，都隐式假定自己运行在"单进程、共享内存地址空间、单一时钟源、
单一坐标系原点"的环境里。当这些子系统被组合进"多容器、多主机、多智能体"
的分布式仿真/部署系统时，凡是这组隐式假设被打破而没有被显式重建的地方，
就会表现为集成故障。本文提出的三重一致性框架，把这类故障按"被打破的
隐式假设"归为三类：

| 一致性维度 | 隐式假设（单体场景下天然成立） | 分布式现实中被打破的方式 | 典型失效特征 |
|---|---|---|---|
| **时间一致性** | 所有数据共享同一时钟源、时间戳携带足够信息供下游插值/去畸变 | 不同插件/进程解析到不同时钟源（仿真时间vs挂钟时间）；消息转换丢弃时间戳字段；反馈链路端到端延迟超过控制律稳定裕度 | 进程不崩溃但"卡死"或"数值缓慢发散"，最难定位 |
| **空间一致性** | 只有一个坐标系原点、一个身份，节点名/话题名/frame_id天然不冲突 | 多智能体共享同一DDS域/ROS graph，命名空间未被库代码支持；传感器安装外参未纳入模型；多机局部坐标系需要显式映射到统一全局系 | 要么直接崩溃（撞名/反序列化异常），要么"收敛到一个错误但稳定的解"（更隐蔽） |
| **通信一致性** | 发布者与订阅者进程内直接持有对方指针/引用，数据必达 | 分布式发布-订阅系统里QoS策略、传输通道可用性、进程环境初始化都变成需要双方显式对齐的契约 | 双方都显示"已发现对方"，但静默丢包/不通信，无任何报错日志 |

三类维度并非互斥标签，而是**排障优先级的检索索引**：遇到新的集成故障时，
先问"这是时间、空间还是通信契约被打破" 可以迅速把排查范围从"整个系统"
收窄到对应维度的检查清单，这是本框架相对于"逐个排查具体子系统"的效率
优势，也是它能迁移到其他多机器人容器化系统的原因——框架本身不依赖
DLIO/PX4等具体选型。

### 3.1 时间一致性：跨进程时钟基准与时序契约

- **判据**：凡是"两个原本各自独立运行的模块，现在需要联合使用带时间戳
  的数据（插值/去畸变/滤波/反馈控制）"的地方，都要显式核对：时钟源是否
  一致？时间戳信息在跨进程/跨消息类型转换时是否完整保留？端到端延迟是否
  满足下游算法对更新率的隐含假设？
- **实例一（时钟源分裂导致死锁）**：Gazebo同一`gazebo_ros::Node`机制下，
  IMU插件`now()`解析为真实挂钟时间，LiDAR插件`now()`解析为仿真时间，
  相差十余数量级；DLIO内部按数值阈值（1e14）误判传感器类型并采用错误
  时间戳缩放，IMU缓冲区永远查不到匹配区间——进程存活但全部线程阻塞在
  `futex_wait`，无崩溃无报错，是本项目排查耗时最长的一类问题。统一改用
  `rclcpp::Clock(RCL_SYSTEM_TIME)`后消除分裂。
- **实例二（时间戳信息在消息转换中丢失）**：Gazebo Livox插件早期实现
  经`sensor_msgs::convertPointCloudToPointCloud2`legacy转换只保留x/y/z，
  丢弃逐点`offset_time`，导致DLIO永远走不进逐点去畸变主路径、退化为
  精度更低的整帧刚体先验配准。改为直接构造含`timestamp`字段的五通道
  PointCloud2解决。
- **实例三（端到端延迟超出控制律稳定裕度）**：`uwb_imu`定位源下，位置
  反馈需经PX4内部EKF2+MAVLink往返，实测速率上限约48Hz；`px4ctrl`/
  `so3ctrl`这类要求companion computer持续高频喂姿态目标的板外几何控制器
  在该反馈速率下两次实测炸机，而直接转发setpoint、把姿态解算交给PX4
  内环的`ros2_px4_stack`不受影响——同一个"控制律对反馈率的隐含假设"，
  在架构选型层面即可规避，无需修复某一处bug。
- **推广**：LiDAR-IMU外参未建模导致重力对齐从初始化第一步就错误（详见
  3.2空间一致性——外参本质是空间变换，但其"必须在信号被下游算法消费前
  完成"这一时序约束，也可归入时间一致性检查清单，体现两个维度并非
  严格互斥，写作时以主归类为准即可，不必强行切割）。

### 3.2 空间一致性：从命名空间隔离到SE(2)在线标定

本节按"隔离粒度从细到粗"组织三层空间一致性问题，最后给出可独立复用的
形式化方法（3.2.3）。

#### 3.2.1 进程内命名空间隔离
- 上游DLIO launch文件从未把`namespace`参数传给`Node()`，双机共享同一
  ROS graph时话题名/节点名直接撞名，两机SLAM互相污染对方状态，最终以
  CDR反序列化异常崩溃告终；配套的TF frame_id（`odom`/`base_link`）
  同样需要按`${namespace}/xxx`前缀化，否则即使话题分开、`/tf`上的
  frame名依然全局唯一性冲突。本文提出的"namespace透传+frame_id前缀化"
  修复模式，对任何"直接复用开源单机ROS2包做多机化"的场景都具有一般性。

#### 3.2.2 传感器外参空间一致性
- 雷达30°前倾安装未在DLIO外参矩阵（`baselink2imu/R`、`baselink2lidar/R`）
  中建模，重力对齐从初始化第一步即错误，导致里程计**系统性**发散——
  区别于噪声累积型漂移，"飘的没边"而非"慢慢跑偏"本身就是外参错误的
  诊断特征，可作为该类问题的通用判据写入方法论小结。

#### 3.2.3 多机局部-全局坐标系SE(2)在线标定方法（本文形式化方法贡献）
- **问题定义**：每个智能体的SLAM局部坐标系原点/朝向由起飞点位置与
  spawn朝向决定，彼此独立且对全局系存在未知刚体变换
  T=(R(θ), t)∈SE(2)；真机场景下没有仿真里可预置的偏移量，必须现场标定。
- **方法**：
  1. 平移锚点：静止条件下取UWB样本（数量≥5、标准差≤0.15m判可信），
     与SLAM里程计位置求均值差，得到该时刻的平移锚点，持久化并支持
     容器/进程重启后自动恢复。
  2. 旋转在线估计：飞机每移动超过阈值`d_min`记一段位移对
     (局部位移向量, 全局位移向量)，滑窗（窗口大小`W`）最小二乘求解
     θ*=atan2(S,C)（S、C为滑窗内位移对的正弦/余弦分量加权和，写作时
     给出完整推导），飞机一开始移动即自动收敛，不需要额外的标定动作。
  3. 平移重算：每次θ*更新后，复用同一个固定锚点重新计算平移
     t=uwb_lock-R(θ*)·local_lock并重新广播，保证SE(2)变换整体自洽。
  4. 硬件可迁移实现：跨机map系对齐从"仅能用仿真真值直接计算差值"，
     升级为"查询已标定的`world→{ns}/map`TF、经tf2链式相乘得到"，
     仿真验证通过后真机部署无需更改任何计算逻辑，只需替换UWB数据源
     的输入话题。
- **精度验证与噪声传播**：UWB噪声实测标准差≈0.05m，单段最小位移
  0.5m对应角度噪声量级≈8°，W=50段滑窗平均后理论收敛到1~2°，与实测
  诊断话题输出吻合——这组"噪声→角度不确定度→滑窗收敛精度"的传播关系
  可作为方法本身的定量支撑，建议写成公式+实测对照图。
- **方法的边界（写入局限性，不回避）**：该方法保证的是"变换本身持续
  收敛"，不保证"已经使用旧变换发布出去的下游数据（如一次性发布的
  目标点）会被追溯修正"——这是一个通用的架构教训：**在线标定方法的
  收敛性，不等价于其消费者的正确性，除非消费者本身设计为持续重新查询
  最新变换**。2026-08-19对穿测试实测到的约0.1~0.15m系统性偏差即源于
  此（`rviz_goal_bridge_node`一次性变换目标点、后续θ*收敛不回溯）。

### 3.3 通信一致性：分布式发布-订阅系统的隐式契约显式化

- **判据**：在单进程场景下，"发布者发的消息，订阅者一定能收到"是无需
  声明的默认事实；一旦发布者与订阅者分处不同容器/不同进程，这件事就
  变成一组需要显式核对的契约——QoS策略是否兼容、传输通道（共享内存/
  网络）是否可用、双方进程环境是否已完成必要初始化。三者的共同失效
  特征是**静默**：discovery阶段双方都能"看见"对方，数据平面却收不到
  任何消息，且无任何报错或警告，是本框架下最难被动排查发现、最需要
  提前预判的一类问题。
- **实例一（QoS Reliability策略不匹配）**：DLIO点云订阅默认RELIABLE，
  Gazebo Livox插件按BEST_EFFORT发布，DDS QoS兼容性规则决定二者不通——
  这是唯一一个此类问题里"能查到`ros2 topic info -v`报QoS不兼容警告"的
  实例，其余两个实例均无警告可查，体现"有报错的QoS问题"实际上是这类
  故障里最容易的一种。
- **实例二（跨容器共享内存不可用）**：`network_mode: host`只共享网络
  命名空间，Fast-DDS默认为"同机"通信启用共享内存传输，但容器间默认
  不共享`/dev/shm`（需显式`ipc: host`）——SHM传输在此场景下静默丢包、
  不产生任何错误或警告，是本项目中最难与"QoS不兼容"类问题区分开的
  一个案例（两者外部症状几乎一致：discovery成功，数据收不到）。
- **实例三（进程环境初始化契约）**：entrypoint脚本bash严格模式
  （`set -euo pipefail`）与ROS2 ament setup脚本对未绑定变量的冲突、
  Gazebo GUI因`GAZEBO_RESOURCE_PATH`/`OGRE_RESOURCE_PATH`等环境变量
  从未被`source /usr/share/gazebo/setup.sh`而OGRE渲染初始化空指针
  崩溃——两者本质都是"进程假定某个环境初始化步骤已经发生，但容器化
  部署脚本没有显式保证"，且后者极易被误诊断为GPU硬件加速兼容性问题
  （本项目曾在这个错误方向上排查过一轮），提醒"环境初始化契约"类问题
  的第一步应该是核对环境变量本身，而非急于怀疑更底层的硬件/驱动。

### 3.4 支撑三重一致性问题可持续修复的工程基础设施：patch化构建范式

三重一致性框架描述"故障是什么、为什么发生"，但让这些修复能够被
**发现→定位→固化→跨机器/跨架构复现**，依赖一套独立的工程范式：

- **三层代码组织**：`staging/`（第三方原始代码，不进版本控制，按固定
  commit拉取）、`vendor/`（体量较小、直接托管改动的衍生代码）、`src/`
  （本项目原创的桥接/胶水代码，三层各自明确进版本控制的边界）。
- **patch叠加构建**：镜像内最终代码 = 原始代码 + 全部patch按固定顺序
  叠加，每个patch对应3.1~3.3中一个具体一致性问题的修复，patch文件名
  与内容注释即是"故障分类→根因→修复"的可审计记录。
- **与替代方案的权衡**（对比论证，体现这是刻意的设计取舍而非默认选择）：
  相较于"直接fork第三方代码进本仓库"（体积不可控）和"运行时volume
  挂载源码调试"（镜像不再自包含、多机复现能力打折扣），patch化方案
  用"改动记录的显式性"换取"仓库体积可控+镜像自包含+可在不同架构上
  重新生成"，代价是需要额外的patch生成/校验流程。
- **这一范式与三重一致性框架的关系**：没有可复现的构建体系，三重
  一致性框架发现的每一个具体修复都只是"这台机器上碰巧解决了一次"；
  patch化范式保证这些修复能可靠地迁移到目标架构（如Jetson
  Orin NX Super的arm64平台），是本文"仿真验证→真机移植"路线图能够
  成立的工程前提。

---

## 4 实验验证

### 4.1 实验环境与配置
- 硬件：开发机配置（用于编译/仿真的宿主机规格，需补充：CPU核数/内存，
  影响`BUILD_JOBS`并发编译的实测参考）。
- 场景：森林类场景（`hard_forest`，约127棵树，300m×38m走廊状分布，
  密度适合LiDAR SLAM验证）作为主验证场景；`simple_room`作为SLAM/全流程
  快速回归场景。

### 4.2 组合覆盖矩阵实测结果
- 直接复用README.md表格：18种`LOCALIZATION_SOURCE×PLANNER×CONTROLLER`
  组合中，明确"✅实测通过"（uwb_slam系5种+uwb_imu系2种，共7种，
  2026-08-13后确认）与"⛔实测确认炸机禁止使用"（uwb_imu×px4ctrl/
  so3ctrl共4种，根因为反馈频率不足）两类，其余为"接口设计推断、
  未实测"，需如实标注三类状态，不夸大覆盖率。

### 4.3 端到端飞行验证
- 起飞-轨迹跟踪-悬停-降落-再起飞完整序列测试（2026-08-19），验证
  `traj_server`降落卡死问题修复前后对比（修复前需人工kill进程绕过，
  修复后两架均"发降落指令后第一次尝试即成功"）。
- 机间避障对穿测试专门设计（NX01: (3,0)→(6,4)，NX02: (6,0)→(3,4)，
  同高度z=1.2强制水平避让）：全程最小间距3.13m（>3m安全阈值），
  无碰撞风险；同时暴露并修复了"假性到达终点"FSM逻辑缺陷
  （`EXEC_TRAJ`分支只判断局部目标是否等于全局终点，未校验飞机实际
  位置），修复后重规划次数由约30次提升至69/100次，验证了FSM确实在
  坚持逼近真实终点而非提前放弃。

### 4.4 定位与坐标对齐精度
- θ*标定收敛过程（诊断话题`/{NS}/origin_setter/yaw_estimate`实测曲线，
  建议补充实测收敛曲线图：段数vs估计角度）。
- 目标点变换偏差实测：两次独立对穿测试中，按真实spawn yaw
  （30°/-45°）重新预测的落点与实测落点误差量级0.1~0.15m，验证了
  3.2节所述"一次性变换未随θ*持续修正"这一架构局限的定量影响。

### 4.5 三重一致性框架的验证闭环与现有方案对比
- 本节承担全文"落回理论"的收口任务：用一张"一致性维度→具体实例→修复前
  失效现象→修复后实测证据"的总表，把3.1~3.3列出的全部实例与4.2~4.4的
  实测结果一一对应，证明三重一致性框架不是事后总结出来的分类标签，而是
  确实覆盖了本平台实测遇到的全部集成故障——这张表是检验"框架是否自洽、
  是否有遗漏"的关键证据，写作时逐条核对3.1~3.3每个实例是否都能在4.2~4.4
  找到对应实测记录，找不到的要么补实验要么弱化该实例的表述。
- 与现有方案对比：与不做容器化隔离（裸机部署）方案在"环境复现耗时/依赖
  冲突发生率"上的对比；与单容器/非patch化方案在"可维护性"上的定性对比；
  若条件允许，与其他开源多机仿真平台（如XTDrone多机分支）做功能覆盖度
  对比表——这组对比服务于"验证载体本身的工程价值"，与三重一致性框架的
  普适性论证是两件事，不要混在一起写。
- **当前素材缺口（需用户决策）**：现有DEBUG_JOURNAL.md记录以"问题-修复"
  叙事为主，缺少系统性、可重复的量化基准测试（如：不同SLAM后端定位精度
  对比表、不同控制器跟踪误差RMSE对比、容器化前后部署耗时对比）。三重
  一致性框架本身的定性论证不依赖这批数据即可成立，但"框架的实用价值有
  多大"这类问题的说服力依赖量化证据。写全文前建议评估是否安排一轮标准化
  基准测试，还是先以现有定性证据成文、按审稿意见再补。

---

## 5 结论与展望

### 5.1 主要结论
- 首先回扣核心论点：三重一致性框架把本平台实测遇到的全部集成故障
  （3.1~3.3共9个具体实例）纳入一个统一、可判定、可迁移的理论视角，
  SE(2)在线标定方法与patch化工程范式是该框架在"空间一致性"维度与
  "工程可持续性"维度上的两个具体产出——三者共同构成一个自洽的体系：
  框架回答"故障是什么类型"，方法回答"某一类具体问题怎么解"，范式回答
  "解出来的方法怎么保证能被复现、被迁移"。三者脱离本文选用的DLIO/PX4等
  具体算法栈仍然成立，这是本文区别于"项目开发报告"的核心判据。
- 其次总结验证载体（双机协同飞行仿真平台）本身的完成度：如实总结当前
  系统边界——按`赛项适配度评估报告.md`结论，当前系统仅完整覆盖"双机
  协同避障飞行"这一条主线，视觉感知/AI建模（赛项任务2，权重20%）、
  精准降落/仿地飞行/物资投送（任务4部分子项）仍是空白，这一"诚实的
  边界声明"应保留在论文里，避免过度宣称。

### 5.2 局限性
- 目标点坐标变换未随在线旋转标定持续修正的架构缺口（3.2节，已定位
  未修复）。
- DLIO长基线位置漂移的真实量级尚未在"非对抗性设计"的平稳任务剖面下
  实测过（当前仅有高机动对穿测试这一极端场景的间接数据）。
- 前视/下视相机链路未实现，GUI相机插件在双机场景下存在初始化时序
  崩溃已知问题。
- 真机移植尚处方案设计与骨架代码阶段，尚无真实Jetson硬件联调数据。

### 5.3 未来工作
- 面向Jetson Orin NX Super 16G的8阶段真机移植路线图（系统级准备→
  镜像改造→地面站改造→单机联调→单机试飞→UWB标定验证→双机组网→
  相机模块独立立项），可直接引用
  `真机部署与Docker开发知识梳理.md`1.2节路线图表格。
- 视觉感知/目标识别模块建设（呼应赛项任务2空白）。
- 持续修正型目标点变换、长基线漂移的系统性实测与补偿方案。

---

## 写作前需要决策/补充的事项（供确认，不是大纲内容本身）

1. **量化对比实验是缺口**——中文核心期刊对"系统设计与实现"类论文通常仍
   要求一定的定量对比表（如4.5节），当前素材以定性排障记录为主。是否
   在写全文前安排一轮补充测试（如标准化的定位精度/跟踪误差/资源占用
   基准），还是先以现有定性证据成文、后续投稿时按审稿意见补充？
2. **目标期刊定位未定**——不同期刊（如偏机器人本体的《机器人》、偏系统
   仿真的《系统仿真学报》、偏工程应用的《计算机工程与应用》）对篇幅、
   理论深度、公式化要求不同，建议先定1-2个候选期刊查一下投稿要求
   （字数/图表数/是否要求中英文摘要+关键词对照）再定稿全文篇幅。
3. **图表清单**（建议全文至少包含）：①系统总体架构图；②三容器/关键
   桥接节点数据流图；③DLIO发散问题7层根因排查时间线图；④θ*标定收敛
   曲线；⑤18组合实测覆盖矩阵表；⑥对穿避障测试轨迹图（含最小间距
   曲线）；⑦真机移植路线图。
4. **作者信息/单位/基金项目**等论文元信息本大纲未涉及，需用户提供。

---

（大纲完，确认方向无误后按此展开全文初稿，预计正文6000-8000字，
另加图表7-9幅、参考文献20-30篇。）
