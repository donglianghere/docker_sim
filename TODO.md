# 待办清单

三个镜像（`mighty-base:humble`、`sim-world:latest`、`flight-stack:latest`）已经能正常 `docker compose build` 出来，接下来要处理的是实测发现的功能性问题。

## P0 —— 已确认会出错，必须修

1. ~~修 `patches/mighty_lidar_frame_id.patch`~~ **已完成**。手写 diff 文本在空白字符上极易出错（试过手动改 hunk 头行数，byte 级比对显示内容对得上但 `git apply` 依然报 `patch does not apply`，换 `patch --dry-run` 才发现要 fuzz=2 才能勉强套上，说明手改文本还是有细微出入）。最终做法：直接在 `staging/mighty_ws_src/mighty` 里编辑源码、用 `git diff` 自动生成 patch、再 `git checkout` 把 staging 还原成干净的上游状态，保证 patch 是字节级精确的。已按 Dockerfile 里同样的 `git apply --check && git apply` 流程验证通过。

2. ~~修 `patches/mighty_sensor_mount.patch`~~ **已完成**。同样用「编辑 `staging/mighty_ws_src/mighty` 源码 + `git diff` 生成 + `git checkout` 还原」的方法，Mid-360 前倾角改成 `0.5236` 弧度（30°），已按 Dockerfile 里同样的 `git apply --check && git apply` 流程验证通过。

3. ~~核对/重写 `patches/ros2_px4_stack_dynus.patch`~~ **已完成**。真实源码比原 patch 假设的版本多了一段 docstring，行号全部对不上，不是简单改 hunk 头能救的，直接在 `staging/ros2_px4_stack` 里把 README 描述的三处改动（新增 `import time as _time`；新增 `_set_px4_param`/`_kick_offboard` 两个方法 + `_publish_setpoint` 里 `create_rate` 改 `time.sleep`；`get_thrust`/`get_angular` 里 `m=2.906`/`hover_thrust=0.50` 硬编码改读 `VEHICLE_MASS_KG`/`VEHICLE_HOVER_THRUST` 环境变量）逐一重做，`py_compile` 语法检查通过，`git diff` 生成新 patch，按 Dockerfile 同样的 `git apply --check && git apply` 流程验证通过。`_kick_offboard` 目前只是定义了方法、没有被自动调用（跟原 patch 设计一致，作者注释里写的是"补上这个不是必须的"），如果要在 SITL 里自动解锁+切 OFFBOARD，还需要在合适的地方（比如 `takeoff_and_track_trajectory` 或 `main()`）加一处调用——这个不在原 patch 范围内，暂不处理。

4. ~~修 `spawn_agent.launch.py` 缺失~~ **已完成**。排查发现比预想复杂：`mighty` 包里现成的 `spawn_quadrotor.launch.xml`/`_with_asus` 两个文件本身是废件——依赖一个根本不存在的 `dynus` 包（`$(find-pkg-share dynus)`），没法用；而真正能跑的多机参考实现 `onboard_mighty.launch.py` 又会把 `mighty_node` 规划器本体一起启动，不能直接照搬进 `sim-world`（规划器只应该跑在 `flight-stack` 里，两边都起会重复）。核实过 `flight-stack-entrypoint.sh` 调 `onboard_mighty.launch.py` 时传的是 `sim_env:=none`，不会触发 spawn 相关节点，两边不冲突。最终新写了一个精简的 `patches/mighty_spawn_agent_launch.patch`（新增文件型 patch，同样用「在 staging 里建文件 + `git add -N` + `git diff` 生成 + `git clean` 还原」的方法生成），只提取 `onboard_mighty.launch.py` 里 `robot_state_publisher`+`spawn_entity.py` 这两个节点，不带规划器；参数（`namespace`/`instance`/`x`/`y`/`z`/`yaw`）跟 `sim-world-entrypoint.sh` 第36行原本就在传的完全对得上，**entrypoint 脚本本身不用改**。已把这个新 patch 接进 `Dockerfile.sim-world`（只接进 sim-world，flight-stack 不需要），`git apply --check && git apply` 验证通过。

## P0 修完后 —— 重新构建验证

5. ~~重新构建三个镜像~~ **已完成**。`sim-world:latest`、`flight-stack:latest` 已重新 build 成功（`mighty-base:humble` 没改，沿用旧的）。

## P1 —— 端到端跑起来，暴露更多真实问题

0. ~~`docker compose up` 三个容器全部 `unbound variable` 崩溃退出~~ **已完成**。两个 entrypoint 脚本都开着 `set -euo pipefail`，紧接着 `source /opt/ros/humble/setup.bash`——这个文件内部第8行引用 `$AMENT_TRACE_SETUP_FILES` 没给默认值，`set -u` 下直接判定"未绑定变量"报错退出。这是 ROS2 ament setup 脚本和 bash 严格模式的经典冲突，不是这次改动引入的。修法：`set -eo pipefail`（去掉`-u`）打底，source 那几行前后单独包一层 `set +u` / `set -u`，source 完重新打开 `-u` 继续兜后面手滑打错变量名的错误。`docker/entrypoints/sim-world-entrypoint.sh`、`docker/entrypoints/flight-stack-entrypoint.sh` 都改了，已经在现有镜像里 live 验证过 source 这段不再报错——**但因为 entrypoint 脚本是 build 时 `COPY` 进镜像的，这个修复要重新 build 一次 `sim-world`/`flight-stack-nx01` 才会生效**，重新跑 `docker compose up` 之前先 `docker compose build sim-world flight-stack-nx01`。

6. `docker compose up` 完整跑一次，盯着日志看 spawn 流程、PX4 SITL、MAVROS 连接、mighty 规划器、DLIO 里程计有没有正常起来。**基本跑通**——三个容器全部稳定运行、无崩溃，Gazebo+双机PX4 SITL+MAVROS(已确认真实连上PX4)+mighty+DLIO+UWB 全部起来了。跑了好几轮，期间连续发现并修好了五个新问题（都是首次真跑才暴露，之前只编译过没运行过）：
   - ~~`uwb_sim` 报 `No executable found`~~ **已完成**。包里缺了 ROS2 `ament_python` 包标准该有的 `setup.cfg`（`ros2 pkg create --build-type ament_python` 会自带这个文件），没有它 setuptools 把 console_scripts 装到默认的 `bin/`，而 `ros2 run`/`ros2 pkg executables` 只认 `lib/<pkg>/`，导致"文件明明在磁盘上、ros2就是找不到"。`uwb_sim` 是这个仓库自己写的第一方代码，不走patch机制，直接补了 `src/uwb_sim/setup.cfg`。live验证过 `ros2 pkg executables uwb_sim` 之后能列出来了；后来 `docker compose up` 里也实测确认 UWB 节点真的启动成功（日志：`UWB ground-truth sim up: agents=['NX01', 'NX02']...`）。
   - ~~flight-stack 里 `mighty` 报 `ros2: error: unrecognized arguments: --params-file`~~ **已完成**。`ros2 launch` 命令本身不支持裸 `--params-file`（那是 `ros2 run` 的语法），`onboard_mighty.launch.py` 也没有接收外部yaml覆盖的机制。新增 `patches/mighty_onboard_vehicle_profile.patch`，给 `onboard_mighty.launch.py` 加一个 `vehicle_profile` launch参数，把传入yaml里 `mighty_node.ros__parameters` 那部分 merge 到内部读到的 `mighty.yaml`/`hw_mighty.yaml` 参数字典上（跟已有的 `hw_parameters` merge 是同一个模式）；`flight-stack-entrypoint.sh` 里调用方式从 `--params-file /opt/config/vehicle_profile.yaml` 改成 `vehicle_profile:=/opt/config/vehicle_profile.yaml`。这个patch只接进 `Dockerfile.flight-stack`（`onboard_mighty.launch.py` 只在 flight-stack 里被调用）。`docker compose up` 里实测确认 `mighty_node` 真的用这份参数跑起来了。
   - ~~`ros2_px4_stack` 的 `dynus_mavros.launch.py` 报 `TypeError: 'NoneType' object is not iterable`~~ **已完成**。这个launch文件读 `INIT_X`/`INIT_Y`/`INIT_Z`/`INIT_ROLL`/`INIT_PITCH`/`INIT_YAW` 六个环境变量拼进一个静态TF（`world_mocap` -> `${VEH_NAME}/init_pose`），`flight-stack-entrypoint.sh` 从没设置过这几个变量，全是`None`，传进 `Node(arguments=[...])` 直接报错。这几个值本质是"这架飞机的初始位姿"，必须跟 `sim-world-entrypoint.sh` spawn这架飞机时用的坐标（`x:="$((i*3))" y:="0" z:="3" yaw:="0"`）对上，两边用同一个 `AGENT_INDEX`（1-based，NX01=1/NX02=2，跟sim-world循环里的`i`是同一个编号）算出一致的值，直接在 `flight-stack-entrypoint.sh` 里 `export INIT_X="$((AGENT_INDEX * 3))"` 等六行。重新build后 `docker compose up` 里已实测跑通：`dynus_mavros.launch.py` 9个节点全起来，TF发布坐标 NX01=(3,0,3)/NX02=(6,0,3)，正好对上两架飞机在Gazebo里的spawn位置。
   - ~~`track_dynus_traj` 崩溃 `ModuleNotFoundError: No module named 'pymavlink'`~~ **已完成**。`ros2_px4_stack` 里 `track_dynus_traj.py`/`dynus_offboard_node.py` 都 `from pymavlink import mavutil`，但 apt 源里只有 `ros-humble-mavlink`/`ros-humble-libmavconn` 这两个C++库，没有 `python3-pymavlink` 这个包（`apt-cache search` 确认过），只能 pip 装。`Dockerfile.base` 里补了 `pip3 install pymavlink`（挂了pip缓存mount）。重新build后 `docker compose up` 里实测确认这个错误不再出现（往下暴露了新问题，见下一条）。
   - ~~`track_dynus_traj` 接着崩溃 `ModuleNotFoundError: No module named 'scipy'`~~ **已完成**。`dynus_offboard_node.py` 第20行 `from scipy.spatial.transform import Rotation`，`numpy` 已经在（别的依赖带进来的，1.21.5），但 `scipy` 没装。jammy apt源里有 `python3-scipy`（1.8.0），补进 `Dockerfile.base`。顺带系统扫了一遍 `ros2_px4_stack` 全部 `.py` 文件的 import，确认除了这个之外只有 `snapstack_msgs2` 缺失（见第10项）。重新build后 `docker compose up` 里实测确认：`pymavlink`/`scipy` 这两个错误都不再出现，`track_dynus_traj` 节点终于能正常跑起来（进到"waiting for ROS services"这一步），三个容器这次全程没有一个崩溃退出——目前跑得最远的一次。
   - ~~MAVROS 从来没被启动过~~ **已完成**。`track_dynus_traj` 虽然不崩了，但日志刷了一串 `set_arming/set_mode/set_param not available, proceeding without it`——查下来是 `flight-stack-entrypoint.sh` 只启动了 `dynus_mavros.launch.py`（里面只有TF发布器+track_dynus_traj等节点，没有mavros_node），MAVROS本身从头到尾没人起过。翻了这个仓库自己的 `staging/ros2_px4_stack/scripts/tmux/dynus_tmux.py`（原作者的真实开发脚本）确认：MAVROS 应该是跟 `dynus_mavros.launch.py` **并列**启动的独立进程（`ros2 launch mavros px4.launch namespace:={veh}/mavros tgt_system:={mav_id}`），不是被它内部拉起来的——这也解释了为什么 `dynus_mavros.launch.py` 里声明了 `fcu_url` 参数但从来没在文件里被用到（那是要传给MAVROS的，传错了地方）。端口公式查了PX4官方 `ROMFS/px4fmu_common/init.d-posix/px4-rc.mavlink` 源码确认：`udp_offboard_port_local=14580+instance`（PX4监听）、`udp_offboard_port_remote=14540+instance`（PX4往外发），之前 `flight-stack-entrypoint.sh` 给NX01/NX02两边写死了同一个 `14540/14580`——三个容器全是 `network_mode: host` 共享网络栈，NX02不改端口会跟NX01真冲突。改成按 `AGENT_INDEX-1`（=PX4的0-based instance号）算偏移，在 `flight-stack-entrypoint.sh` 里补了 `ros2 launch mavros px4.launch namespace:="${NAMESPACE}/mavros" fcu_url:="udp://:$((14540+PX4_INSTANCE))@127.0.0.1:$((14580+PX4_INSTANCE))" tgt_system:="${AGENT_INDEX}"` 这一步，`dynus_mavros.launch.py` 那边传的 `fcu_url` 也同步改成一致的值。**重新build后 `docker compose up` 端到端实测通过**：NX01=`14540/14580`、NX02=`14541/14581`两边端口确实错开了；`track_dynus_traj` 日志从"全部not available"变成 `set_arming/set_mode/wp_clear/wp_push service is up!`；`mavros.altitude`/`mavros.global_position`/`mavros.imu` 话题出现"New subscription discovered...QoS不兼容"警告——这种警告只有真的有数据在发布时才会出现，说明MAVROS真的在从PX4收MAVLink遥测、不是空转。三个容器全程没有一个崩溃退出，是目前跑得最远的一次。
   - 顺带确认：Gazebo（gzserver）其实已经正常起来、两架飞机也真的spawn成功了，不是"没起来"——只是没GUI窗口，因为 `docker-compose.yml` 里 `USE_GAZEBO_GUI=false` 是默认关的，这是预期行为不是bug。
   - **还剩两个不阻塞的小尾巴，先记着不用管**：①`VER: your FCU don't support AUTOPILOT_VERSION, switched to default capabilities`——MAVROS启动握手时一个标准请求超时后自动降级用默认能力继续跑，是mavros自己设计好的良性回退，不是故障。②`set_param`这一个服务唯独没跟着转正（其他都好了），`mighty_node`一直有条`Default goal z is lower than the ground level`的ERROR级日志（从第一次跑起来就有，非致命，mighty该干的事没耽误）——这两个以后有空再深挖，不影响主链路。

7. ~~PX4双机SITL端口偏移规则~~ **已完成**。官方公式（`px4-rc.mavlink`：`14580+instance`/`14540+instance`）已经在上面MAVROS那条里查到并用上、而且端到端实测验证过了。`sim-world-entrypoint.sh`这边启动PX4只传了 `-i $((i-1))`，没有另外传端口参数——PX4自己的`px4-rc.mavlink`脚本会根据这个`-i`内部换算出的`$px4_instance`自动算端口，这部分本身没问题，不用改。

8. ~~`hospital`/`office`/`tunnel` 三个场景的 `GAZEBO_MODEL_PATH` 能否正确解析~~ **部分完成**。查了`base_mighty.launch.py`里`env`到world文件的完整映射（`hospital`/`office`/`office_faster`/`cave_start`/`cave_vertical`/`cave_person`/`hard_forest`/`easy_forest`/`easy_high_forest`/`medium_forest`/`dynamic_forest`/`forest3`/`yaw_benchmark`/`global_planner`/`multiagent_performance`/`path_push`/`ACL_office`/`ground_robot`/`ground_robot_forest`/`multiagent_testing`/`empty_wo_ground`/`empty`/`high_res_forest`/`static_uncertainty_test2/3/4`，共约29个env名对应17个world文件），逐个world文件里`grep model://`统计外部模型依赖：
   - **森林类world全部自包含**（`hard_forest`/`easy_forest`/`easy_high_forest`/`medium_forest`/`dynamic_forest`/`forest3`/`ACL_office`/`ground_robot_forest`/`empty`，以及tunnel对应的`simple_tunnel.world`）——障碍物都是内联定义的圆柱体，0个`model://`外部引用，不依赖`GAZEBO_MODEL_PATH`，本来就能正常跑，`tunnel`场景之前只是"没验证过"，实际没问题。
   - `hospital.world`（64个`model://`引用）、`office.world`（`model://willowgarage`）、`empty_wo_ground.world`（`model://sun`）**原来确实解析不了**——不是"能不能解析"的未知数，是实测就是错的：`GAZEBO_MODEL_PATH`只把`gazebo_models_external`这个根目录纳入搜索路径，但实际模型分别嵌在子目录`aws-robomaker-hospital-world/models/`和`osrf-gazebo_models/`下面，Gazebo按`<GAZEBO_MODEL_PATH段>/<模型名>/model.sdf`直接拼路径找，不会自己往子目录递归，根目录里既没有叫`willowgarage`/`sun`的东西也没把模型平铺在第一层，所以全部报`Unable to find uri`。改了`Dockerfile.sim-world`里的`GAZEBO_MODEL_PATH`，把这两层子目录都显式加进去，`office`/`empty_wo_ground`两个world这下应该能完整解析了（还没重新build+实测确认）。
   - `hospital.world`即使路径修好也**只能部分工作**：64个引用模型里有37个（`AdjTable`/`Toilet`/`ElderLadyPatient`等家具人物类）在`fetch_sources.sh`拉的`aws-robomaker-hospital-world`仓库里根本不存在（不是路径问题，是资产本身没有，猜测属于配套的另一个仓库，没拉取），只有27个`aws_robomaker_*`前缀的墙体/地板/走廊类模型是有的——场景能加载但家具人物会缺失、Gazebo日志里会报一堆`Unable to find uri`，不阻塞起飞但不完整，要补全的话得先找到那37个模型实际来源的仓库（没有瞎猜URL去clone）。
   - `high_res_forest`（`big_forest_high_res.world`）依赖的`models_high_res`/`terrain`两组资产，在已vendor的`osrf-gazebo_models`/`osrf-subt`里翻遍了也没有同名目录，**这个场景目前没法用**，需要先找到/下载对应资产，来源不确定，没有处理。
   - **推荐**：主链路验证/调DLIO用森林类场景就够了、而且更适合LiDAR SLAM（有真实3D几何结构，不是空场地），当前默认的`hard_forest`没问题；hospital等场景等以后真需要室内测试时再补资产。
   - 顺带查了下`hard_forest.world`的障碍物坐标分布：约127棵"树"（圆柱体，半径1.0~1.5m、高6m），分布在x:[0,301.6] y:[-19,18.8]的约300m×38m走廊状区域——密度对LiDAR SLAM是合适的，但发现**NX02的spawn点(6,0,3)离最近一棵树（编号136，坐标(7.03,1.02)，半径1.01m）只有1.45m，减去树干半径净空只剩约0.44m**，基本是贴着障碍物出生的（NX01在(3,0,3)情况好一些，净空约1.5m）。这很可能是当前"两机各自收敛到一个错误但稳定的位置"（见第9项最后一段）的原因之一——第一批关键帧/子图如果被一个几乎贴脸的障碍物主导，初始配准容易出偏差。还没有改spawn坐标验证这个猜测，是下一步可以试的方向。

11. **已尝试、未解决**：开Gazebo GUI可视化调试。宿主机不是无头服务器，有真实本地图形会话（`DISPLAY=:1`，Intel核显+Mesa，`glxinfo`确认`direct rendering: Yes`），`docker-compose.yml`里原来就留好了开GUI的位置（`USE_GAZEBO_GUI`+`DISPLAY`+X11 socket挂载），`xhost +local:docker`授权、挂载`/tmp/.X11-unix`、`USE_GAZEBO_GUI=true`之后：
    - 先试了默认（走DRI3硬件加速）：容器里看不到`/dev/dri`，`libGL error: failed to open /dev/dri/card1`，最终gzclient在OGRE的`Camera`空指针assert上崩溃，还连带把gzserver也崩了（entrypoint里两者是绑在一起的，一个崩另一个也活不了），整个sim-world容器崩溃退出。加了`devices: [/dev/dri:/dev/dri]`挂设备进去。
    - 挂了`/dev/dri`之后不再报"找不到设备"，但换成`gazebo::rendering::Scene`空指针assert，还是崩，说明不是纯设备直通的问题。又试了`LIBGL_ALWAYS_SOFTWARE=1`强制软件光栅化（排除GPU驱动兼容性这条线，宿主机X本身也是Intel核显+Mesa、不涉及NVIDIA闭源驱动跟容器内Mesa不匹配那类经典坑）——**结果完全一样**，还是`Camera`/`Scene`空指针assert崩溃。说明根子不在硬件加速开不开，是这个镜像里Gazebo Classic自带的OGRE1.9渲染引擎在容器环境下初始化就有问题（渲染上下文/场景对象创建失败），需要单独深挖（可能要查`~/.gazebo`渲染配置有没有缺失、OGRE版本兼容性等），不是能一次性解决的事。
    - gzserver崩溃后entrypoint会不断重试spawn流程，整个sim-world容器进入崩溃循环，已经**把改动全部回退**（`USE_GAZEBO_GUI=false`，`WORLD_ENV`换回`hard_forest`），确认恢复到无GUI但稳定可用的状态。`docker-compose.yml`里把开GUI需要的三处配置（`environment`里的`USE_GAZEBO_GUI`/`DISPLAY`/`LIBGL_ALWAYS_SOFTWARE`、`volumes`、`devices`）都注释保留、写了这次排查结论，以后要继续查这个可以从这里接着试。
    - ~~未解决~~ **已解决**。继续排查（用户要求联网查一下这个assertion现象），公开报告里能对上号的场景基本都是"渲染资源路径没配好导致OGRE初始化失败、返回空指针又没判空直接assert"，不是GPU/world复杂度问题。回头核实容器里`env`：`GAZEBO_RESOURCE_PATH`、`OGRE_RESOURCE_PATH`、`GAZEBO_PLUGIN_PATH`**全是空的**——`sim-world-entrypoint.sh`从头到尾从来没有source过`/usr/share/gazebo/setup.sh`这个Gazebo自己的环境脚本（`Dockerfile.sim-world`里单独手写的`GAZEBO_MODEL_PATH`只覆盖了"外部障碍物模型去哪找"这一件事，凑巧把主链路撑住了，掩盖了这个缺口）。手动实测：`docker run`里手动`source /usr/share/gazebo/setup.sh`之后，gzserver+gzclient按跟`gazebo_ros`标准launch文件完全一致的方式并发启动（不用额外拆分延迟），15秒+稳定不崩；此前怀疑的"gzclient/gzserver启动时序竞态"改法（先起gzserver、sleep 8秒、再单独起gzclient）也顺手试过、确实能避开一部分崩溃，但换个位置（从gzclient的Camera挪到gzserver自己的Scene）还是会崩——说明那只是"渲染资源缺失"这个真正问题的其中一种触发方式，不是根因，已经把这个分支改动撤掉了，改成在`sim-world-entrypoint.sh`source ROS环境那一段末尾加一行`source /usr/share/gazebo/setup.sh`，保留原来简单的一体化`ros2 launch mighty base_mighty.launch.py ... use_gazebo_gui:=true`启动方式即可。重新build sim-world镜像、`docker-compose.yml`里`USE_GAZEBO_GUI=true`+`DISPLAY`+`LIBGL_ALWAYS_SOFTWARE=1`+`/tmp/.X11-unix`挂载+`/dev/dri`设备直通全部启用，完整走一遍`docker compose up`（含双机spawn、PX4 SITL、UWB节点）：**全程没有任何崩溃日志，宿主机X server上确认出现了真实的Gazebo GUI窗口（`xwininfo`能看到"Gazebo"标题、3068x1936分辨率的窗口）**。GUI这个坑彻底解决了。

9. ~~`mighty_node` 用的是假状态，不是真实DLIO/MAVROS数据~~ **已完成**。查清楚了确实是bug不是设计：`flight-stack-entrypoint.sh` 之前传 `use_hardware:=false`，导致 `onboard_mighty.launch.py` 起了 `fake_sim_node`——它自己在内部积分一个"假状态"发到 `mighty_node` 订阅的 `state` 话题上，DLIO处理的Gazebo真实点云、MAVROS真的连上的PX4，规划器根本没在用。查了 `onboard_mighty.launch.py` 的分支逻辑和 `convert_odom_to_state.cpp` 源码（货真价实的转换节点，不是空壳）确认：`use_hardware:=true` + `use_onboard_localization:=true`（quadrotor默认走这条）才会启动 `convert_odom_to_state`，remap `odom->dlio/odom_node/odom`——这个话题名跟DLIO自己发布的完全对上。而且这也解释了之前一直没想通的疑点：entrypoint本来就已经在传 `use_frame_alignment:=true`、sim-world也起了`uwb_sim`——`use_hardware:=true`之后 `map_frame_id` 会变成每架飞机自己的`${NAMESPACE}/map`（不再是全局共享的`map`），要靠UWB的frame_align机制做跨机对齐，这一整套本来就是配套设计好的，只是`use_hardware`这一个开关被漏设了。已经把 `flight-stack-entrypoint.sh` 里的调用改成 `use_hardware:=true use_onboard_localization:=true`，重新build后 `docker compose up` 实测确认 `convert_odom_to_state` 进程真的起来了（不再是`fake_sim`），整体没有崩溃退出。

   但顺着这条线往下查（`ros2 topic hz /NX01/state` 报 `does not appear to be published yet`），挖到了**另一个更深的bug，已经一并修完**：`/NX01/state` 话题存在但没人真发布，说明 `convert_odom_to_state` 压根没收到DLIO的里程计。查到根子在 DLIO 自己的源码 `src/dlio/odom.cc:37`——点云订阅用裸整数 `1` 构造QoS，默认是 **RELIABLE**；而Gazebo那边Livox插件发布点云用的是 **BEST_EFFORT**（`livox_points_plugin.cpp:78`，符合sensor数据的常规约定；DLIO自己IMU订阅那行`odom.cc:43`也确实用了`rclcpp::SensorDataQoS()`，唯独点云这行漏了）——RELIABLE订阅者收不到BEST_EFFORT发布者的消息，是DDS QoS兼容性规则决定的，不是巧合。这是DLIO上游代码本身的问题，不是这个项目引入的（也解释了为什么这条warning从第一次跑`docker compose up`起就一直刷屏、之前一直当成背景噪音没深究）。新增 `patches/dlio_pointcloud_qos.patch`（同样用「编辑 `staging/dlio_ws_src/direct_lidar_inertial_odometry` 源码 + `git diff` 生成 + `git checkout` 还原」的方法），把这行改成跟IMU那行一致的 `rclcpp::SensorDataQoS()`，接进 `Dockerfile.flight-stack`（DLIO只在flight-stack编译，sim-world不需要），`git apply --check && git apply` 验证通过，重新build后确认点云话题两端QoS真的匹配上了（`ros2 topic info -v` 两边都是BEST_EFFORT）。

   但改完这个之后 `/NX01/state` 依然没数据，继续用 `ros2 topic info -v`/`ros2 topic hz` 一路排查，挖到了**第三层、也是最深的一个bug**：在 **sim-world 容器内部**测 `/NX01/mid360_PointCloud2`，能稳定收到约45Hz的真实点云；但从 **flight-stack-nx01**（另一个容器）看同一个话题，`ros2 topic info -v` 显示两边已经互相"发现"（GID、QoS都对得上），却一条消息都收不到——这是 ROS2+Docker+Fast-DDS 的经典坑：`network_mode: host` 只共享**网络命名空间**，discovery阶段靠组播能跨容器工作；但Fast-DDS默认还会为"同机"通信优先启用**共享内存(SHM)传输**，Docker容器间默认不共享 `/dev/shm`（`network_mode: host` 不包含这个，需要单独加 `ipc: host`），SHM传输在这种情况下会**静默丢包、不报任何错误/警告**——表现就是"发现了但收不到数据"，跟前面两层QoS不兼容的报错方式完全不一样，更难查。`docker-compose.yml` 里三个服务原来都没设置 `ipc:`，现在全部加上 `ipc: host`（发布/订阅双方都要加，只加一边没用）。**这是纯运行时配置，不需要重新build镜像**，只需要 `docker compose up`（可能需要先 `docker compose down` 确保容器带着新的ipc命名空间重新创建）。`docker compose config --quiet` 验证过语法没问题。~~还没有实测确认~~ **已完成**：`docker compose down` + `up` 之后，flight-stack-nx01 容器里也能测到点云数据了，跨容器SHM丢包的问题解决了。

   `ipc: host` 修完之后，`/NX01/dlio/odom_node/odom` 依然 `Publisher count: 0`——DLIO进程活着（`ps aux`能看到`dlio_odom_node`）、点云也确实收到了（订阅者数从1变2，`convert_odom_to_state`也发现了对方），但DLIO自己没输出过一帧里程计。继续排查挖到了**第四层，也是这条链路里最根本的一个坑**：`ros2 topic info -v /NX01/mid360/imu` 显示 **Publisher count: 0**——压根没有任何东西在发布IMU数据。DLIO（跟几乎所有LIO/LIO-SAM系统一样）需要点云+IMU两路输入才能做重力对齐/初始化，没有IMU连第一步都迈不出去，之前查的DLIO点云QoS、跨容器SHM，都只是让"点云能送到"，IMU这条输入链路从一开始就是空的。查到 `mid360.xacro`（`livox_laser_simulation_ros2`包）里IMU传感器那一整段**从来就是注释掉的**，Livox点云插件自己也不发布IMU。往下查这段注释代码本身：想启用的插件`libimu_plugin.so`其实是真实存在、已编译好的（`mighty`自己vendor的`gazebo_ros_imu_sensor.cpp`，逻辑照抄标准`gazebo_ros_pkgs`实现），但注释里写的参数是`<topicName>`/`<bodyName>`这套——读了插件真实源码确认它根本不认这几个字段，是通过`<ros><namespace>/<remapping>`这套gazebo_ros标准SDF约定来定位话题的，就算简单取消注释也是无效配置，DLIO照样收不到IMU。新增 `patches/livox_mid360_imu.patch`（同样「编辑`staging/mighty_ws_src/livox_laser_simulation_ros2`源码 + `git diff`生成 + `git checkout`还原」的方法），改成`<ros><namespace>${namespace}</namespace><remapping>~/out:=${topic}/imu</remapping></ros>`，让最终话题按跟点云一致的规则拼成`${namespace}/${topic}/imu`（即`NX01/mid360/imu`），跟`flight-stack-entrypoint.sh`传给DLIO的`imu_topic`对上。这个patch只接进 `Dockerfile.sim-world`（`ros2_livox_simulation`包在flight-stack里是`--packages-skip`掉的，不会用到这份xacro），`git apply --check && git apply` 验证通过，`docker build --check` 语法检查通过。**这次改的是sim-world这边的staging，要重新build sim-world才生效**（flight-stack不用动）。用户重新build+up之后反馈"里程计位置飘的没边了"——这说明链路本身已经通了（IMU话题、`/NX01/state`都有数据在流，不然根本不会有"位置"这个概念可言），但数值不对。顺着这个反馈查到**第五层，也是这条链路里数值层面最根本的一个坑**：DLIO 的 `dlio.yaml` 里 `extrinsics/baselink2imu/R`、`extrinsics/baselink2lidar/R` 原来都是**单位矩阵**（`t`也是随手抄的某个真实硬件标定值，跟这个仿真的实际安装完全对不上）——但 `mighty_sensor_mount.patch`（第2项）早就把雷达装成了30度前倾（`quadrotor.urdf.xacro`里joint origin `rpy="0 0.5236 0"`），我们这次新加的IMU传感器又是跟雷达挂在同一个link上、天然带着同样的30度倾斜。读了DLIO源码（`odom.cc`）确认这两个外参矩阵的语义是"把lidar/imu坐标系下的点变换到base_link坐标系"（`baselink2lidar_T`直接乘到点云上），单位矩阵意味着DLIO以为base_link是水平的，实际上传感器读数来自倾斜30度的坐标系——重力对齐从初始化第一步就是错的，这会让里程计**系统性**地飘（跟姿态估计从起点就错了30度有关，不是普通的噪声累积漂移，所以才会"飘的没边"而不是"慢慢跑偏")。新增 `patches/dlio_extrinsics_tilt.patch`（同样「编辑 `staging/dlio_ws_src/direct_lidar_inertial_odometry` 源码 + `git diff` 生成 + `git checkout` 还原」的方法），把两个R矩阵按URDF joint里同一个30度算出对应的标准`Ry(30°)`旋转矩阵（`cos30=0.866025, sin30=0.500001`，取值精度跟xacro里`0.5236`这个近似值保持一致），`t`也同步改成`z=0.2`（跟joint的xyz偏移一致）。接进 `Dockerfile.flight-stack`（DLIO只在flight-stack编译），`git apply --check && git apply` 验证通过，YAML语法用python校验过。**这次改的是flight-stack这边的staging，要重新build flight-stack才生效**（sim-world不用动）。

   用户重新build+重启之后反馈"两个飞机的里程计都飘的很厉害"——30度外参这个修正本身经live验证是对的（用 `ros2 topic echo` 抓过NX01的原始IMU读数 `linear_acceleration=(-4.900010391553508, 0.0, 8.487042957512802)`，代入这个新R矩阵算出来正好是`(0,0,9.8)`，即"传感器倾斜着装，但转到base_link系里读数就是纯重力、机体是水平静止的"这个物理事实），但飘还在，说明还有别的问题没查到，于是挖到了**第六层，也是导致"两架飞机都飘得没边"这个症状的真正根因**：`docker exec`进正在跑的容器实测（`ros2 node list`能看到"there are nodes in the graph that share an exact name"警告；`ps aux`发现NX02容器里`dlio_odom_node`进程根本不在了；`docker logs`翻到NX02那边最后是 `terminate called after throwing an instance of 'eprosima::fastcdr::exception::NotEnoughMemoryException'` 崩溃退出，崩溃前打印的内部状态已经是 `Distance Traveled :: 8877m`、`Accel Bias [xyz] :: 4.78 -5.00000000 3.69`——Y轴精确顶在硬编码的`abias_max`上限，而Y轴恰恰是30度俯仰倾转不影响的那根轴，说明问题不出在外参，出在别处）——查上游 `launch/dlio.launch.py` 源码，发现它的两个 `Node()`（`dlio_odom_node`/`dlio_map_node`）**从来没有设置过 `namespace=`**：`flight-stack-entrypoint.sh` 传的 `namespace:=NX01`/`namespace:=NX02` 这个launch参数，`ros2 launch` 对没有在launch文件里`declare`过的参数是静默接受、直接丢弃的，不会报错也不会生效。实际生效的只有 `pointcloud_topic`/`imu_topic` 两个显式remap（这两个输入话题本来就各自带了`NX01/`前缀，侥幸是对的），但**输出**话题（`dlio/odom_node/odom`等）和**节点名本身**（`dlio_odom_node`/`dlio_map_node`）全都是裸的全局名字，没有任何namespace前缀。`sim-world`/`flight-stack-nx01`/`flight-stack-nx02` 三个容器全是 `network_mode: host` + `ipc: host`，共享同一个ROS graph——这意味着NX01和NX02两台飞机的DLIO节点名、话题名、连带rclcpp内部的参数服务等全部撞在同一个名字上，两边的点云/里程计/关键帧全部互相串话，实测NX02那边最终是在处理混进来的、跟自己点云结构对不上的数据时CDR反序列化直接崩了；NX01这边虽然进程没死，但内部状态早被另一台飞机的数据污染，飘到几千米外、accel bias顶满上限——这就是"两架飞机里程计都飘得很厉害"的真正根因，跟前面那个30度外参修正是两个独立的问题（外参那个已经修对了）。

   额外确认了一个连带问题：就算话题不撞名，DLIO发布的TF（`odom`→`base_link`等frame_id）用的也是`tf2_ros::TransformBroadcaster`默认的全局`/tf`话题，不受节点namespace影响，而 `frames/odom`/`frames/baselink` 这些frame_id本身在 `cfg/params.yaml` 里也是裸的`"odom"`/`"base_link"`，没有per-vehicle前缀——查了 `mighty` 自己的 `onboard_mighty.launch.py`（第99/161/227/352行）确认它期望的TF命名约定是`${namespace}/base_link`、`${namespace}/odom`、`${namespace}/map`这种斜杠前缀（`robot_state_publisher`用标准的`frame_prefix`参数机制做到了，`static_tf_map_to_odom`节点也是照这个约定发布`${namespace}/map`->`${namespace}/odom`静态变换），DLIO这边如果继续用裸frame_id，即使两台飞机的odom话题分开了，`/tf`上的`base_link`/`odom`这两个frame名依然会被两台飞机同时占用、互相打架，下游任何做TF查找的节点（比如`local_sensing/pcl_render_node`需要把点云转到map系）拿到的会是不确定的哪一台飞机的变换。此外这个仓库自己的 `staging/mighty_ws_src/mighty/scripts/run_hw_red_rover.py`（真机部署脚本）里本来就是照 `namespace:={rover_name}` 这样调用 `dlio.launch.py` 的，说明"DLIO要支持namespace"是这个项目原本就该有、只是fetch的上游DLIO仓库本身没做的功能，不是我们凭空加的需求。

   新增 `patches/dlio_namespace.patch`（同样「编辑 `staging/dlio_ws_src/direct_lidar_inertial_odometry` 源码 + `git diff` 生成 + `git checkout` 还原」的方法），给 `dlio.launch.py` 补上真正生效的 `namespace` launch参数：`dlio_odom_node`/`dlio_map_node` 两个 `Node()` 都加上 `namespace=namespace`（没传时默认空字符串、行为不变，向后兼容非多机场景）；同时把 `frames/odom`/`frames/baselink`/`frames/lidar`/`frames/imu`（`dlio_odom_node`）和 `odom/odom_frame`（`dlio_map_node`）这几个frame_id参数也按`${namespace}/xxx`加前缀，跟mighty已有的约定对齐。接进 `Dockerfile.flight-stack`，`git apply --check` 验证通过。配套改了 `flight-stack-entrypoint.sh`：`pointcloud_topic`/`imu_topic` 不能再手动拼`${NAMESPACE}/`前缀了（现在Node自己的namespace会加一遍，两边叠加就变成`NX01/NX01/mid360_PointCloud2`这种双重前缀、订阅不到真实话题），改传相对话题名`mid360_PointCloud2`/`mid360/imu`，由namespace统一解析。**这次改的是flight-stack这边的staging，要重新build flight-stack才生效**。~~还没有实测确认~~ **已完成，实测确认了一部分**：重新build+`docker compose up -d --force-recreate flight-stack-nx01 flight-stack-nx02`之后，`ps aux`确认两边`dlio_odom_node`启动参数里都带上了`-r __ns:=/NX01`（`/NX02`同理）；`ros2 topic echo /NX01/dlio/odom_node/odom --once`确认`frame_id: NX01/odom`、`child_frame_id: NX01/base_link`，namespace真的生效了；`ros2 node list`里`dlio_odom_node`/`dlio_map_node`不再出现在"share an exact name"警告里（只是列表里现在还有别的静态TF发布节点——`/base_to_d455`、`/body_to_base_link`、`/init_pose_to_camera_init`、`/map_to_odom`——依然重名，这几个是mighty那边`onboard_mighty.launch.py`起的，同样是没传namespace的老问题，但都是静态变换发布器不是有状态积分器，不会导致里程计本身发散，先记下来不阻塞）；跑了几分钟没有再出现NX02那种`fastcdr::exception::NotEnoughMemoryException`崩溃退出。**namespace串话/崩溃这个根因确认修复有效。**

   但顺着这个修复继续往下验证时发现：namespace隔离之后，NX01和NX02各自"独立"地依然会飘——不再是几千米那种量级，但也远谈不上能用（`Distance Traveled`几分钟内累积到150~1600米不等、`Accel Bias`持续顶在`params.yaml`里配置的`abias_max: 5.0`/`gbias_max`上限附近来回摆），而且`ros2 topic echo`确认两架飞机在同一时刻收到的原始IMU读数**完全一致**（`linear_acceleration=(-4.900010391553508, 0.0, 8.487042957512802)`，跟第9项验证过的30度重力对齐物理量吻合，外参这块继续没问题）。挖到**第七层、也是当前还没解决的一个问题**：每次状态打印里`Registration :: ... deskewed points: 0`一直是0——查`src/dlio/odom.cc`里的传感器类型自动识别逻辑（第508~524行）：只认点云字段名里带`t`/`time`/`timestamp`的，识别不出来就把`this->deskew_`强制置`false`（第526~528行），DLIO因此永远跳过`deskewPointcloud()`那条逐点用IMU插值做运动畸变补偿的主路径（`preprocessPoints()`第538行的`if (this->deskew_)`分支进不去），退化成"整帧点云套一个IMU积分出来的先验刚体变换再整体配准"这种精度低得多的备用路径。根子在Gazebo这边`livox_points_plugin.cpp`（`OnNewLaserScans()`，第157~228行）：它往`CustomMsg`（`pp_livox`）里逐点都算了`p.offset_time`（第206~209行），但发布给DLIO订阅的`PointCloud2`走的是另一条路——`sensor_msgs::convertPointCloudToPointCloud2(cloud, cloud2)`（第223~224行），`cloud`只塞了`x`/`y`/`z`（第195~198行），没有任何channel，转出来的`PointCloud2`只有x/y/z三个字段，`offset_time`直接被扔掉了，DLIO永远看不到时间戳字段。没有逐点去畸变、加上mid360.xacro里给的采样点数偏稀疏（`<samples>1000</samples>`、水平`<samples>100</samples>`），GICP配准噪声较大，几何观测器（geometric observer）把这部分配准误差错当成加速度计零偏在修正，就顶到了`abias_max`上限——这大概率是当前"两架飞机各自还在飘"的真正原因，但**还没有做实这个因果链、也还没有修**，是下一步要处理的。

   顺着这个因果链动手修了：新增 `patches/livox_pointcloud_timestamp.patch`（同样「编辑staging + `git diff` 生成 + `git checkout` 还原」的方法），改 `livox_points_plugin.cpp`——不再走`sensor_msgs::msg::PointCloud` + `convertPointCloudToPointCloud2()`这条legacy转换（只能转x/y/z，逐点的`offset_time`进不去），改成直接用`sensor_msgs::PointCloud2Modifier`/`PointCloud2Iterator`构造一个x/y/z/intensity/timestamp五字段的`PointCloud2`（字段名/类型跟`include/dlio/dlio.h`里`dlio::Point`的`POINT_CLOUD_REGISTER_POINT_STRUCT`完全对上），`timestamp`是float64、复用了本来就在算的每点`elapsed_time`。接进`Dockerfile.sim-world`（这个插件只在sim-world编译），`git apply --check`通过，重新build sim-world编译无报错。

   重新build+`docker compose up -d --force-recreate`之后**实测炸出一个新问题**：DLIO彻底卡死——`ros2 topic hz`确认点云/IMU输入照常在流动、`/NX01/dlio/odom_node/odom`还在按固定100Hz发布（这是`publish_timer`独立定时器在吐最后一次的值，不代表在算新东西），但状态面板（`debug()`，每次成功配准一帧后才会打印）从"DLIO initialized!"之后再没打印过一次，`deskewed`/`keyframes`两个话题`ros2 topic hz`完全没有消息，位姿死死卡在`(0,0,0)`。进程本身没崩溃（`ps aux`能看到，CPU占用很低但不是0）。临时给`flight-stack-nx01`加了`cap_add: SYS_PTRACE`（调试完已经删掉，不是常驻配置）、容器里装了`gdb -p`挂上去看全部线程栈，43个线程全部在`futex_wait_queue`（线程池全闲），主线程也在里面——不像是某处死循环，更像是在等一个永远等不到的条件。往下查改用python直接订阅点云、打印`header.stamp`和逐点`timestamp`字段的原始值：`header.stamp: sec=518, nanosec=60000000`——这是**仿真时间**（Gazebo `/clock`，约十几分钟场景内时间)，不是真实挂钟时间！点云插件`node_->get_clock()->now()`在这个SDF/节点组合下解析到的是sim time（纳秒量级约5.18e11），远小于`odom.cc`里`getScanFromROS()`用来区分Hesai(`<1e14`)/Livox(`>1e14`)的1e14这个阈值——于是被误判成**Hesai**而不是Livox！Hesai分支的`extract_point_time`直接把这个原始double当"秒"用、不做任何`*1e-9`缩放（Livox分支才会缩放），于是`this->scan_stamp`变成约5180亿"秒"（几千年后）——而`imu_buffer`里的真实IMU时间戳（`callbackImu`用的是`imu->header.stamp`，来自`gazebo_ros_imu_sensor.cpp`里的`ros_node_->now()`，这个节点在当前配置下解析到的确实是**真实挂钟时间**，实测约1785644147秒，跟点云插件的sim time judgement完全不是一个时间基准）差了十几个数量级，`deskewPointcloud()`/`integrateImu()`里靠时间戳去`imu_buffer`里做区间插值/查找的逻辑因此永远够不到目标时间，第一帧之后就卡死，不报错也不崩溃——这是最难查的一种坑。同一个`gazebo_ros::Node::Get(sdf)`机制、两个不同sensor插件（IMU用的`gazebo_ros_imu_sensor.cpp`，雷达用的`livox_points_plugin.cpp`）解析出来的`now()`一个是真时间一个是仿真时间，没有深究是SDF配置差异还是节点创建时机差异导致的，但不影响修法：不依赖`node_->get_clock()->now()`是否为sim time，直接用`rclcpp::Clock(RCL_SYSTEM_TIME).now()`拿真实挂钟时间，跟IMU插件保持一致（也更贴近真实Livox硬件驱动的行为——真雷达压根没有"sim time"这个概念）。更新了`livox_pointcloud_timestamp.patch`，重新build+重启验证：**卡死解决了**——状态面板恢复正常打印、`Registration :: deskewed points`不再是0（实测两机稳定在250~400/帧），点云话题`header.stamp`确认是真实epoch秒（如`1785644147`），deskew主路径终于第一次真正跑起来了。

   但**继续观察发现第三层、目前还没解决的问题**：deskew真正生效之后，两架飞机不再无限发散（`Distance Traveled`会在起飞穩定后趋于平台期不再持续增长——NX02连续两次状态面板`Distance Traveled`完全一样都是1258.58米、`Position`几乎不变，说明不是在无限跑飞），但会在启动后大约1分钟内先跳到一个**明显错误的固定位置**再稳定下来（实测NX01停在`(6.8, 12.8, -55.4)`附近、NX02停在`(-83.2, -74.0, -177.4)`附近，`accel bias`部分轴仍会顶到`abias_max`），而地面真值（`/plug/model_states_plug`）此时两机应该分别在spawn点`(3,0,3)`/`(6,0,3)`附近基本没动。跟前两层bug（无限发散、进程卡死）性质不同，这次是"收敛到一个错的固定解"，还没有查这一层的根因（怀疑是启动阶段第一批关键帧/子图构建时的一次性错误配准，被当成参考系锁死了，但还没验证），是下一步要继续挖的。

11. **已改动，还没实测确认**：`MAVROS` 一直 `connected: false`，查到根子跟 DLIO 那条线完全无关，是另一个独立的架构缺口——PX4 SITL 进程本身就没启动起来：`/tmp/px4_NX01.log` 卡死在 `Waiting for simulator to accept connection on TCP port 4560` 不动（`ss -ulnp` 确认 MAVROS 要连的 UDP 14540/14580 端口压根没被监听，PX4 主循环大概率被这一步彻底堵死，后续 MAVLink 模块从没机会启动）。查到 PX4<->Gazebo 的 TCP 桥接、以及 MAVROS 用的 MAVLink UDP 服务，都是由 `libgazebo_mavlink_interface.so` 这个插件提供的，但这个插件**只存在于 PX4 官方自带的 iris 系列机体模型**（`PX4-Autopilot/Tools/simulation/gazebo-classic/sitl_gazebo-classic/models/iris/iris.sdf.jinja`）里——而 `sim-world-entrypoint.sh` 原来 spawn 的是 mighty 自己的 `quadrotor.urdf.xacro`（只有雷达+IMU的纯传感器载具，压根没有这个插件、没有电机模型）。也就是说 PX4 从物理仿真层面上从来没有真正连接过任何飞行器模型，不是这几轮改动（重力/相机/围墙）引入的新问题，是"mighty 传感器载具" + "PX4 SITL" 这套组合从架构上就没有把两者用 MAVLink 接口耦合起来。之前 TODO 里记录的"MAVROS 成功连接"的证据其实是间接推断（QoS警告），这次直接查了 `connected` 字段和端口监听情况，结论是没真连上。

    参照 PX4 官方自己的 `iris_rplidar`（`models/iris_rplidar/iris_rplidar.sdf`：用 SDF 的 `<include>model://iris</include>` + `<joint type="fixed">` 把电机机体和外挂传感器焊在一起）和 `sitl_multiple_run.sh`（多实例场景下 `jinja_gen.py` 渲染 `iris.sdf.jinja` 时按实例给 `mavlink_tcp_port=4560+N`/`mavlink_udp_port=14560+N`/`mavlink_id=1+N` 做偏移、再用 `gz model --spawn-file`spawn 的标准做法）这两个官方先例，新增 `docker/scripts/gen_iris_mid360_sdf.py`（不走patch机制的第一方脚本，类似 `src/uwb_sim`）：调 `jinja_gen.py` 按实例渲染 iris.sdf，再把 Mid-360 雷达+IMU 的 SDF 传感器定义（内容照抄 mid360.xacro 打了三个 patch 之后的最终版本，只是从 xacro 变量替换改成 Python 字符串格式化，传感器/插件命名都按 `${namespace}_livox`/`${namespace}_livox_plugin`/`${namespace}_livox_imu_plugin` 加实例前缀，避免像手工测试时那样因为裸名字导致跨实例 ROS 节点撞名崩溃）焊到 `base_link` 上，输出一份每实例独立的融合SDF。`sim-world-entrypoint.sh` 里原来 spawn mighty xacro 那步换成调这个脚本生成+`ros2 run gazebo_ros spawn_entity.py -file`直接从文件spawn（**不能用 `gz model --spawn-file`**——手动测试时这个 Gazebo 原生命令莫名挂起两分钟以上都不返回，怀疑是在等待联网访问 `models.gazebosim.org` 超时，这台机器没有稳定外网；换成 ROS2 的 `spawn_entity.py -file` 参数后实测正常，几十秒内完成）。`Dockerfile.sim-world` 里 `GAZEBO_MODEL_PATH` 补了 PX4 自己的 `sitl_gazebo-classic/models` 目录（`iris.sdf.jinja` 内部 `<include>` 了 `model://gps` 等 PX4 自带传感器模型，原来搜索路径里没有这一层，实测报过 `Unable to find uri[model://gps]`）。

    这次改动之后，sim-world 这边不再 spawn mighty 的 `quadrotor.urdf.xacro`——`mighty_disable_d435.patch`/`mighty_enable_gravity.patch`/`livox_mid360_visual_gravity.patch` 这几个针对该 xacro 的 patch 对**物理仿真**已经名存实亡（没有对应的 Gazebo 物理实体了），但 flight-stack 容器那边 `onboard_mighty.launch.py` 的 `robot_state_publisher` 仍然用同一份 xacro 渲染 TF（跟规划器等下游对接），这几个 patch 对 TF 描述内容仍然有意义，所以都保留没删。sim-world 这边原来给 `spawn_entity.py -topic robot_description` 提供数据的 `robot_state_publisher`（`mighty_spawn_agent_launch.patch` 里那个）现在也不再被调用（换成直接 `-file` 指定sdf），但patch本身留着没删，只是不再是当前生效路径，方便以后需要时对比/回退。

    ~~还没有实测确认~~ **实测过程一波三折，最终确认修复方向是对的，但又挖到两层新坑**：

    重新build+`docker compose up`之后，PX4日志还是卡在同一行没动，`GAZEBO_PLUGIN_PATH`确实设对了（entrypoint里`source setup_gazebo.bash`打印出来确认过），iris+mid360模型也确实成功spawn了（`Successfully spawned entity [NX01]`），但PX4实例0启动瞬间gzserver直接`exit code -6`崩溃，连带NX02也spawn不了。排查发现宿主机上有一整套**孤儿进程**在跑（之前某次`docker compose up`留下的完整gzserver+两个PX4+mavros等，`network_mode: host`下这些进程本质是宿主机普通进程，容器边界清理不掉，一直在跟每次新起的gzserver抢网络资源/发生实体命名冲突），清理之后重测。

    清理孤儿进程后自己又踩了一个坑：用`docker run ... sim-world:latest tail -f /dev/null`起临时调试容器时忘了加`--entrypoint bash`，Docker语义下`tail -f /dev/null`会被当成entrypoint.sh的参数而不是替代entrypoint，导致每次调试容器都在后台默默跑起一份完整的sim-world实例（用镜像里默认的`WORLD_ENV=hard_forest`），产生了一批新的、这次是自己制造的"孤儿"，之前排查到的孤儿有一部分很可能就是这么来的。改用`--entrypoint bash`之后环境才是真正干净的。

    在干净环境下手动单独测试（`empty.world` + 单独spawn iris+mid360 + 前台跑PX4）：**这次PX4真的打印出`Simulator connected on TCP port 4560.`了**——`GAZEBO_PLUGIN_PATH`这个根因确认修复有效。但紧接着刷屏`ERROR [simulator_mavlink] poll timeout 0, 25`，PX4没法完整启动。做了对照实验：spawn一个完全没改过的纯净iris（不带mid360）单独测，PX4完整启动成功、`Ready for takeoff!`，排除了环境/PX4版本本身的问题，坑出在mid360融合部分。逐步排查后确认：`gen_iris_mid360_sdf.py`里雷达`<sensor>`/`<plugin>`两处`<visualize>true</visualize>`（射线扫描线可视化，1000个采样点）是罪魁祸首——无GUI订阅者时Gazebo依然会为这个准备渲染数据，拖慢整个物理仿真循环的实时性，导致`mavlink_interface`插件发给PX4的数据流延迟、PX4那边poll超时。把这两处改回`false`（保留雷达本体的`<visual>`外观box不受影响，那个不吃性能）之后，同一个融合模型单独测试`Ready for takeoff!`，问题解决。已经改了`gen_iris_mid360_sdf.py`。

    ~~还没有走完整流程重新验证~~ **已完成**。重新build flight-stack镜像（这次build额外带上了08-14晚些时候新加的`SLAM_BACKEND=point_lio`可选后端、`DEPLOY_TARGET`真机骨架、`source_all.sh`重构，此前flight-stack:latest一直没重新build过，这几处改动其实没在镜像里生效过）之后，`docker compose up -d`用默认参数（`WORLD_ENV=simple_room`/`LOCALIZATION_SOURCE=gt`/`PLANNER=ego_planner`/`SLAM_BACKEND=dlio`）跑三容器：sim-world日志确认NX01/NX02两台iris+mid360融合模型都`Successfully spawned`，PX4两个实例都打印`Ready for takeoff!`；`ros2 topic echo /NX01(NX02)/mavros/state --once`确认两边`connected: true`（**排查小插曲**：第一次隔着`docker exec`查`ros2 node list`/`ros2 topic echo`死活查不到任何flight-stack自己的节点，只能看到gcs容器的rosbridge——查到是容器里`ros2-daemon`早先被一次用默认FastRTPS参数的`ros2`调用启动过、之后一直常驻，我后来导出的`RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`等环境变量只影响新起的CLI进程本身、管不到已经在跑的旧daemon，`ros2 daemon stop`杀掉重建之后才查到完整graph——以后再用`docker exec`进这两个flight-stack容器查ROS graph，记得这一步，不然会误判成"话题没在发布"）。三个容器全程稳定运行4分钟+无崩溃，日志里没有`Traceback`/`core dumped`/`terminate called`等崩溃特征；`USE_GAZEBO_GUI=true`下`wmctrl`确认宿主机上真实出现了"Gazebo"标题的GUI窗口。TODO.md #11这条端到端确认完成。

10. **已知缺口，暂不阻塞（用户已确认按此处理）**：`ros2_px4_stack` 的 `trajgen_offboard_node.py`/`track_gen_traj.py` 这两个备用节点 `from snapstack_msgs2.msg import ...`，但 `snapstack_msgs2` 这个包哪儿都没有（不在任何 workspace 里、apt也没有、fetch_sources.sh也没提过）。网上搜过，只找到它的ROS1前身 `snapstack_msgs`（`gitlab.com/mit-acl/fsw/snap-stack/snapstack_msgs`，catkin包不能直接用），公开渠道找不到一个真实可用的ROS2版本，不能瞎编URL去clone。当前用的是 `dynus_mavros.launch.py`/`track_dynus_traj.py`/`dynus_offboard_node.py` 这条"dynus"线，不碰这两个文件，不影响现在跑通。以后真要切到 "trajgen" 那条线，得先找到（或者自己按实际用到的字段手写一个）这个包，到时候再处理。

12. **新发现，还没修（需要用户同意才动`ego_planner`核心代码）**：`ego-planner-swarm`的`traj_server.cpp`（`staging/ego-planner-swarm/src/planner/plan_manage/src/traj_server.cpp`）一旦收到过一条bspline轨迹（`receive_traj_=true`）就永久按100Hz发布`position_cmd`（轨迹走完后自动切成"悬停在终点"继续发），全文没有任何停止/取消机制——这会导致"飞到某处再降落"这个常规操作序列卡死：`px4ctrl`的状态机（`vendor/px4ctrl_ros2/px4ctrl/src/PX4CtrlFSM.cpp`）明确要求`AUTO_LAND`必须从`AUTO_HOVER`触发，而`CMD_CTRL`状态下只要还在收`position_cmd`就不会掉回`AUTO_HOVER`，`降落指令会被直接拒绝`（实测报错`Reject AUTO_LAND, which must be triggered in AUTO_HOVER`）。2026-08-19验证"飞轨迹→降落→再起飞→飞回起飞点"整套序列时当场撞上，用手动`pkill traj_server`+`ros2 run ego_planner traj_server`重启这个操作层面的绕过办法解决（`ego_planner_docker_sim.launch.py`里这个节点没设`respawn=True`，杀了不会自动重启，需要手动补一个），没有改`traj_server.cpp`源码本身。真机上没有`docker exec`环境去手动kill进程，这个绕过办法不现实——如果以后真机部署要支持"飞到某处再降落"这类操作，需要给`traj_server`补一个能停止发布`position_cmd`的机制（比如订阅一个`Empty`/`Bool`话题，收到后清空`receive_traj_`标志位）。这属于改`ego_planner`核心逻辑，按`feedback_mighty_core_patch_authorization.md`的约定，需要用户明确同意才动手，这次先记录发现，没有征得同意去改源码。

    ~~还没修~~ ~~还没重新build验证~~ **已完成，实测确认降落卡死彻底解决**。用户重新build+`docker compose down`/`up -d`干净重启（原来直接`--force-recreate`部分容器踩了个坑——飞机还悬停在空中时单独重建flight-stack会导致px4ctrl内部FSM状态跟外部现实脱节，改成整体重来）之后：起飞→飞到目标点→不做任何人工干预，`docker logs`确认`px4ctrl`到达目标点约1秒后自动打印`From CMD_CTRL(L3) to AUTO_HOVER(L2)!`（这条日志格式跟其他状态转换不一样，没有`-->`符号，第一次grep漏看过），之后发降落指令**两架都在第一次尝试就成功**，`AUTO_HOVER-->AUTO_LAND-->MANUAL_CTRL`，`armed: false`。不再需要任何手动kill进程。`traj_server.cpp`的`cmdCallback()`加了`HOVER_AFTER_FINISH_SEC`(1.0s)超时判断，到达终点悬停满1秒后主动`receive_traj_=false`停止发布，把长期悬停职责交还给px4ctrl自己"CMD_CTRL cmd流断流0.5s自动回退AUTO_HOVER"这条本来就设计好的路径（不是新发明机制）。新增`patches/ego_planner_traj_server_stop_after_finish.patch`，接进`docker/Dockerfile.flight-stack`（跟在已有8个`ego_planner_*.patch`后面）。**验证方式的插曲，顺带挖出第13项那个不相关的既有问题**：只测"对干净文件单独apply"不够，因为Dockerfile按固定顺序把多个patch叠加应用在同一份`traj_server.cpp`上——搭了个临时目录模拟`COPY staging/ego-planner-swarm/src/planner /opt/ego_planner_ws/src`的目录拍平效果，按Dockerfile真实顺序完整跑一遍全部9个`ego_planner_*.patch`，确认这个新patch在链条最后仍能正常应用。**需要重新build flight-stack才生效**，还没有实测确认降落卡死的问题真的解决了。

13. **新发现，需要用户决定怎么处理（不是这次改动引入的，已经存在10天）**：验证第12项那个新patch时，按Dockerfile真实顺序完整测试全部9个`ego_planner_*.patch`，发现`ego_planner_terminal_collision_check.patch`和`ego_planner_waypoint_recursive_spin_fix.patch`两个在真实应用顺序下`git apply --check`都失败（`patch does not apply`）。根子是`staging/ego-planner-swarm`这个git checkout里有三个从**2026-08-09就一直没提交也没转成patch**的未跟踪改动（`git status`能直接看到，mtime都是08-09 22点多）：`bspline_opt/src/bspline_optimizer.cpp`（跟`ego_planner_terminal_collision_check.patch`内容完全一样，大概率当初生成patch时漏了`git checkout`还原，属于良性冗余，文件已经是patch想要的最终状态，不影响功能）、`plan_manage/include/ego_planner/ego_replan_fsm.h`+`plan_manage/src/ego_replan_fsm.cpp`（一份**没有任何patch文件、没有任何记录提到过**的swarm坐标对齐WIP功能，251行改动，`declare_parameter("swarm/use_frame_alignment", ...)`那一段，照mighty的`/frame_align`模式补的机间坐标变换）。第二份是真问题——`ego_planner_waypoint_recursive_spin_fix.patch`要改的`planNextWaypoint()`递归spin崩溃修复（复现条件：给一个障碍物内的目标点规划失败、紧接着再给一个自由空间合法目标点，会SIGABRT整个`ego_planner_node`）跟这份WIP改的是同一个文件、context冲突，导致这个修复补丁在真实build里大概率一直在悄悄apply失败——Dockerfile这里"apply --check失败就echo警告、不让整个build失败"的容错设计本意是防止单个patch冲突拖垮整条build流水线，代价是这类静默失败很容易被埋没在几十分钟build日志里没人注意到。**直接证据**：`docker exec`进当前跑着的`flight-stack-nx01`容器查`ros2 param list /NX01/ego_planner_node`，能看到`swarm/use_frame_alignment`/`swarm/num_agents`这两个参数——证明当前这个刚完成起飞/飞行/降落全套验证的镜像里，`ego_planner_node`用的正是这份08-09遗留WIP版本。

    **更正（原判断错了）**：逐行通读完整280+行diff后发现，这份WIP的`planNextWaypoint()`那段改动，跟`ego_planner_waypoint_recursive_spin_fix.patch`的内容**逐字完全一致**——不是两份互相冲突的独立改动，是WIP在recursive_spin_fix已经生效的基础上继续往上叠加新功能，只是最后忘了拆分成patch归档。也就是说当前镜像**同时带着`terminal_collision_check`和`waypoint_recursive_spin_fix`两个修复**，SIGABRT崩溃风险不存在，之前"大概率没带上递归spin崩溃修复"这个结论是只看了不到50行diff就下的，判断错了。真正的风险是**这280+行代码目前完全没有任何版本控制保护**（没有patch文件、没有被Dockerfile引用），一旦有人在`staging/ego-planner-swarm`里跑`git checkout .`/`git clean`，会连带把已验证过的两个修复一起丢掉。**没有动手处理**——用户已要求先搁置，这属于改`ego_planner`核心逻辑范畴，按`feedback_mighty_core_patch_authorization.md`的约定，继续做完转成正式patch还是丢弃，需要用户明确同意才动手。

    ~~没有动手处理~~ **已获同意，归档完成**。新增`patches/ego_planner_swarm_frame_alignment.patch`，接进`docker/Dockerfile.flight-stack`（位置在`waypoint_recursive_spin_fix`之后、`traj_server_stop_after_finish`之前）。提取基线时踩了个坑——这份WIP是叠在`waypoint_recursive_spin_fix`之上写的，不是从干净upstream直接改的，第一次用错基线提取导致重建结果比原始WIP多出`manual_target_goal_marker`那段代码；改用跟Dockerfile完全一致顺序的临时目录重新算出正确基线才提取对。提取时还发现WIP相对正确基线**意外丢了**`manual_target_goal_marker.patch`加的`displayGoalPoint()`调用（8行，不是swarm功能的一部分，来源不明），生成新patch时手动排除了这处丢失，不让归档动作把这个已修好的bug带着一起退回去。用临时目录完整模拟Docker COPY拍平效果，按真实顺序把全部10个`ego_planner_*.patch`跑了一遍全部`apply --check`通过，重建结果跟原始WIP逐字节diff确认一致（除了故意排除的那8行）。顺带把另一处同样10天未跟踪、内容已确认跟`terminal_collision_check.patch`完全一样的`bspline_optimizer.cpp`stray diff也`git checkout`还原掉，`staging/ego-planner-swarm`现在完全干净。**默认`swarm/use_frame_alignment=false`，没有启用这个功能、没有接环境变量、没有做仿真验证，纯粹是把代码挪进版本控制**，需要重新build flight-stack才会把这个patch编译进镜像（不影响任何已验证场景的默认行为）。

14. **新发现，还没修（需要用户同意才动`ego_planner`核心代码）**：`ego_replan_fsm.cpp`的`case EXEC_TRAJ`分支判断"是否到达终点"时（第564行起），只检查`(local_target_pt_ - end_pt_).norm() < 1e-3`（这段重规划的局部目标是不是就是全局终点）和轨迹时间是否跑完，**从来没检查飞机实际飞到的位置`pos`离`end_pt_`还有多远**——只要某次重规划的局部目标锁定成全局终点、且该段轨迹时间跑完，FSM就直接判定"到达目标"切回`WAIT_TARGET`，不管飞机真实位置在哪。2026-08-19专门设计一次两机路径真正交叉的对穿飞行测试（NX01 world(3,0)→(6,4)，NX02 world(6,0)→(3,4)，同高度z=1.2强制走水平避让）时实测撞上：两机全程没有碰撞风险（最小间距3.13m，全程>3m），但都远没有飞到目标点就"假性宣布成功"进了`WAIT_TARGET`（NX01差2.3米，NX02几乎原地没动、还一度往反方向多飞了2米多）。之前两次"各自飞不冲突目标点"的测试没触发这个问题，因为路径不冲突时swarm avoidance不需要做实质性规避、轨迹能顺利飞完全程——这次专门设计冲突路径才第一次暴露。这是`ego-planner-swarm`upstream本来就有的逻辑缺口，不是这次改动引入的，只是这次的swarm avoidance/frame alignment验证（第13项）让轨迹段被系统性压缩得更短，更容易触发这个"假性到达"条件。属于改`ego_planner`核心FSM逻辑范畴，按`feedback_mighty_core_patch_authorization.md`的约定需要用户明确同意才动手，这次只定位根因并记录。

    ~~还没修~~ **已获用户同意，改完了，还没重新build验证**。给"轨迹时间跑完"这个分支补上跟旁边`REPLAN_TRAJ`分支同样的`(end_pt_ - pos).norm() > no_replan_thresh_`距离判断——没真到终点就`REPLAN_TRAJ`继续尝试，真到了才按原逻辑宣布`WAIT_TARGET`，复用已有阈值，没引入新参数。新增`patches/ego_planner_exec_traj_reach_check.patch`，接进`docker/Dockerfile.flight-stack`（`ego_planner_swarm_frame_alignment`之后、`ego_planner_traj_server_stop_after_finish`之前）。按Dockerfile真实顺序（全部11个`ego_planner_*.patch`）在模拟目录里完整测试过，全部`apply --check`通过，改动位置跟另外4个也碰这个文件的patch没有冲突。**需要重新build flight-stack才生效，还没有重新跑对穿测试确认修复真的有效**（也没确认会不会跟`traj_server_stop_after_finish`那个降落修复有交互——理论上不会，因为新分支只在"确实没到终点"时触发，但两者都涉及"飞机什么时候算飞完"，值得一起重新测）。

    ~~还没有重新跑对穿测试确认~~ **已重新build+重测，修复本身确认有效，但测出了第15项那个更深层的问题**。同样的对穿路径重测，`EXEC_TRAJ->REPLAN_TRAJ`次数从~30次涨到69/100次——证明飞机确实在坚持重规划、不再"假性宣布成功"就撒手不管，修复逻辑生效。跟`traj_server_stop_after_finish`没有冲突，降落依然一次成功。但最终停留位置这次意外地跟修复前几乎一样，查`mavros/local_position/pose`（DLIO自己的位置估计）对比Gazebo地面真值才发现：DLIO自己估计的位置其实已经很接近目标点了（这也是这次修复正确判定"到达"、真正退出REPLAN循环的原因），但跟真实位置差了约2.6米——问题不在FSM逻辑，在DLIO这次高机动对穿飞行场景下产生了明显里程计漂移，详见第15项。

15. ~~新发现，还没处理：DLIO里程计漂移~~ **结论已更正，不是DLIO漂移**。原判断（DLIO里程计相对地面真值漂移约2.6米）是只看一次差异就归因，没有可重复的量化验证。用户指出真正的深层问题后重新核算：两次对穿测试的目标点坐标是手算的——拿"起飞点锁定那一刻"（θ*=0，只是"没有位移样本"的占位默认值，不是真的测出旋转为0）的偏移量，直接用世界坐标相减得到局部坐标，没有乘旋转矩阵；但两机真实出生朝向固定是30°/-45°（`SPAWN_YAW`硬编码值，θ*后续也确实收敛到这两个值）。用真实30°/-45°重新预测"不做旋转补偿会落在世界系哪里"：NX01预测(3.60,4.97)对实测(3.67,4.88)/(3.64,4.91)，NX02预测(6.70,4.95)对实测(6.84,5.03)/(6.75,4.89)——两次独立测试、两架飞机，误差都在0.1-0.15米量级，精确解释了"偏离"现象。真正的问题不是DLIO里程计精度，是**目标点变换的架构缺口**，见第16项。

16. **新发现，需要用户决定怎么处理**：`uwb_slam`模式下，目标点从`rviz_goal_world`到`term_goal`是**一次性**TF变换（`rviz_goal_bridge_node.py`的`_cb()`回调只在收到消息那一刻查一次TF），转换结果发布后就是固定值，之后规划器拿这个固定的`end_pt_`去飞，不会再变。但`origin_setter_node.py`的`world_frame->map_frame`这条TF的旋转分量（θ*）会随飞行持续在线修正（`_republish_with_theta()`），也就是说：**连续修正的只有方位角，没有任何机制把这个修正回头应用到已经发出去的目标点上**。如果目标点是在θ*还没收敛准（比如刚起飞、位移样本还很少）的时候发出去的，规划器会一直朝着一个按错误旋转换算出来的、偏离真实意图的世界位置飞——2026-08-19两次对穿测试复现的"偏离"现象（见第15项）就是这个bug的具体实例。这是`uwb_slam`模式多机坐标对齐架构一个真实存在的设计缺口，真机部署如果同样存在"起飞后θ*才逐渐收敛、但目标点在早期已经发出"这种时序，会系统性地把目标点送错地方。可能的修复方向（还没有讨论细节/没有征得同意实施）：`rviz_goal_bridge_node`不再"收到一次转一次就完事"，改成持续跟踪目标点的world系"真值"，每次θ*/TF更新都重新变换、重新发布`term_goal`，直到规划器真正到达为止。

    **2026-08-19深入讨论，用户暂缓不做**。用户进一步指出这套θ*在线估计的深层假设问题：所有修正都建立在"起点（`_local_lock`/`_uwb_lock`这对平移锚点）永远精确"这个假设上，只连续修正方位角，但SLAM误差累积下平移也会累积——`_maybe_record_segment()`里θ*用的分段位移`u_i`是首尾相接的短基线相对测量（每段只要`rotation_seg_min_disp_m`=0.5m），相对不容易被累积漂移污染，但`_broadcast_and_persist()`算平移时用的"当前DLIO绝对位置 - `_local_lock`"是从起飞那一刻算起的**长基线**位移，恰恰最容易被累积误差污染，θ*估得再准也救不了这条。查了`rotation_seg_min_disp_m=0.5`/`rotation_window_size=50`/`sample_window_sec=2.0`几个参数确认：数学上θ和锚点各只需要1个数据点/1段位移就能精确解（`rotation_min_segments`默认就是1），滑窗到50段纯粹是为了平均掉UWB噪声（实测std≈0.05m，单段0.5m位移对应的角度噪声量级~8°，50段平均后收敛到~1-2°，跟实测吻合）——如果按"平移锚点也跟着最近一段位移滑动"这个方向修，理论上不需要额外测量，复用θ*估计本身已经在收集的分段数据即可，但要在"重定多勤"和"锚点噪声多大"之间权衡。

    **最终决定不做的理由**：比赛场地20×25米，但漂移量级取决于总飞行路程不是场地尺寸，如果实际任务是相对平稳的单段/少数几段飞行（不是刻意设计的高机动对穿+避障那种极端场景），真实漂移量可能远小于第15项那次误判的2.6米，现在去建一套连续重定锚点的架构，很可能是在解决一个还没证实真的存在、或者影响很小的问题。而且第15项已经证明"2.6米偏差"那次实测本身就是坐标计算错误，不是真实漂移，**这个项目到目前为止还没有一次真正用ground truth测出过DLIO在正常（非对抗性设计）任务剖面下的真实漂移量**。更务实的路径：①现有的`set_origin_from_uwb`服务本身可以随时重新调用，如果实际任务有自然的悬停/检查点时机，手动重新触发锁定就能把误差归零，不需要改代码；②真要判断这事值不值得投入，应该先用比赛会用的、相对平稳的任务剖面实测一次真实漂移量级，而不是继续纸面讨论——但这个实测这次也没有做，用户问完"接下来有哪些工作"直接转向了别的方向，这条先搁置，不确定优先级。

    **2026-08-20用正确旋转补偿重测对穿飞行，问题在"应用层用法"层面已确认可规避（不代表第16项这个架构缺口本身被修了）**。用户要求把"对穿重测+旋转补偿"这个候选项也一并完成。关键发现：项目里其实已经有一个做法完全正确的目标点发送工具——`scripts/dual_goal_input.py`（2026-08-12就重写过，`send_goals()`在发送前对每架飞机现查一次`world -> {ns}/map`这条TF，用当时的θ*+锚点正确做SE(2)变换）。2026-08-19那两次对穿测试之所以复现"偏离"，是因为验证时用的是手写`ros2 topic pub`直接发布，绕开了这个已验证工具、自己手算了坐标（还漏乘旋转矩阵）——不是这个工具本身有问题，是**测试方法没用对已有的正确工具**。

    这次重测复用`DualGoalInput.send_goals()`的逻辑（写了个一次性脚本`send_crossing_goals.py`，导入`dual_goal_input.py`做同一套TF查询，不进repo，纯本次验证用），分两阶段：①起飞+锁定起飞点后，先发一段短途非冲突路径（各自飞约2m）让`origin_setter_node`积累位移样本，θ*从锁定时的占位值0°收敛到29.6°/-47.2°（真实spawn yaw 30°/-45°）；②θ*收敛后，再用同一个工具发真正的对穿目标点（NX01 world(3,0)→(6,4)，NX02 world(6,0)→(3,4)，z=1.2，跟2026-08-19那次完全一样的路径设计）。

    **结果——两架都真正飞到了目标点附近，且是这次是真实发生了机间近距）**：NX01最终(5.86,4.40)，平面误差0.43m（对比2026-08-19那次2.3m）；NX02最终(2.78,3.78)，平面误差0.31m（对比2026-08-19那次几乎原地没动）——两次的残余误差都落在`GOAL_RADIUS=0.3m`附近，基本就是规划器自己的到点容差，不是系统性偏离。全程最小间距1.61m（t=21.5s，比2026-08-19那次3.13m更近，说明这次是真的飞了会冲突的路径、swarm avoidance真被触发），仍然安全（高于`EGO_SWARM_CLEARANCE=1.0m`），全程无碰撞风险。两架`ego_planner_node`日志确认都进入`WAIT_TARGET`，是真到达不是假到达（跟第14项`exec_traj_reach_check`修复的验证结论一致）。

    **结论**：`traj_server`停发布修复（第12项）、`exec_traj_reach_check`到点判断修复（第14项）、以及"用现有`dual_goal_input.py`而不是手算坐标发目标点"这三者叠加后，2026-08-19复现的"假成功"+"偏离目标点"两个现象都不再出现。**但这不代表第16项描述的架构缺口（一次性TF变换、θ*后续收敛不会回头修正已发目标点）已经被修复**——这次刻意规避了这个缺口（先用短途飞行让θ*收敛、再发真正目标点），如果真机部署时目标点必须在θ*还没收敛时就发出（比如任务一上来就要飞长距离、没有"先飞一小段热身"这一步），第16项的问题依然会复现。第16项的状态维持"用户决定暂缓"不变，这次只是证明"按正确方法使用现有工具"能规避已知的复现场景，不改变对架构缺口本身优先级的判断。

    **额外发现的一个操作层面坑（已记录到DEBUG_JOURNAL，不是代码bug）**：`docker exec`进容器执行`ros2`命令时，如果不手动`export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`+`export CYCLONEDDS_URI="file:///tmp/docker_sim_cyclonedds.xml"`，会出现"进程明明在跑、话题却发现不到/间歇性发现不到"的假象——因为这两个变量是entrypoint.sh在自己的shell里运行时`export`的，只在entrypoint那条进程树内有效，`docker exec`起的新进程看不到，会retreat到默认RMW/CycloneDDS配置，可能连不上真正锁定`lo`网卡的那个DDS域。以后所有`docker exec ... ros2 ...`诊断命令都要带上这两个export，不能只做`ros2 daemon stop`。

17. **已完成（2026-08-20），已重新build并端到端HTTP验证通过**：GCS网页地面站阶段6——机队启停控制。`gcs/backend/app.py`新增三组接口：`GET/POST /api/fleet/sim/{status,start,stop}`（对`docker_sim-sim-world-1`/`docker_sim-flight-stack-nx01-1`/`docker_sim-flight-stack-nx02-1`三个容器直接`docker start`/`stop`，start顺序sim-world在前、stop倒序）、`GET/POST /api/fleet/hw/{ns}/{status,start,stop}`（SSH到`nx01`/`nx02`跑`docker compose -f docker-compose.hw.yml up -d`/`down`/`ps`，复用已有的`HW_TARGETS`/`_ssh_run`）。**跟方案文档8.2节阶段6原文的一个刻意偏离**：仿真这边没有按原方案写的"本地`docker compose up/down`"实现，改成直接对已有容器`docker start`/`stop`——因为镜像build按项目约定必须用户手动跑（不是容器B的职责），"机队启停"这个按钮的前提本来就是容器已经用`./start.sh`/`docker compose up`建过至少一次，改用`docker compose`会引入"容器B内部起docker compose、但实际由宿主机dockerd建容器"这套sibling-container场景下卷挂载路径要跟宿主机对齐的复杂度（还得在镜像里另装`docker-compose-plugin`），对"启停已存在的容器"这个需求没必要。前端`gcs/frontend/index.html`新增"🚀 机队启停"面板（仿真机队一组启停按钮+三容器状态；真机机队按NX01/NX02分别一组按钮+状态），用`fleetCtl`/`renderFleetPanel()`维护，跟已有的调参面板（`paramsPanelOpen`）互不影响。**已验证**：`app.py`/`index.html`内联JS语法检查通过；仿真`docker start`/`docker stop`两个方向直接对着当前这台开发机真实存在的三个容器（当时状态`exited`）跑通——启动后三者都变`running`，随后按倒序停止又都回到`exited`，跟测试前状态一致。**已补充验证（build完成后）**：`docker compose up -d backend`用新镜像重建容器后，直接curl真实跑了一轮`/healthz`（200）、`/api/fleet/sim/status`+`start`+`stop`（exited→running→exited，状态跟测试前一致）、`/api/fleet/hw/NX01/status`+`NX02/start`（无SSH别名，均正确502包装而非裸异常）、`/api/fleet/hw/NX99/status`（未知机身，404）、CORS预检（`OPTIONS`返回`access-control-allow-origin: *`），全部经过真实FastAPI路由层，不是绕过路由的纯逻辑单测。**仍未验证**：前端新面板没有真实/模拟浏览器点击过（这次直接curl测的是接口，没有再走`gcs_ui_sim.py`那套rosbridge模拟，因为这三个新接口全部走HTTP、不经过rosbridge）；`hw`那组接口的SSH路径本身（不是错误处理逻辑）这台开发机没有`nx01`/`nx02`别名，只能等真机部署时端到端验证。

18. **新发现（2026-08-21）**：`docker-compose.yml`的`WORLD_ENV`一直是硬编码字面量`WORLD_ENV=simple_room`，不是`${WORLD_ENV:-simple_room}`——跟`LOCALIZATION_SOURCE`/`PLANNER`/`CONTROLLER`等其它同类开关的写法不一致，导致README.md里文档的用法`WORLD_ENV=hard_forest ./start.sh`实际上完全不生效（宿主机环境变量传不进去，永远用的是`simple_room`）。**已修复**，改成`${WORLD_ENV:-simple_room}`，默认值不变，可以通过环境变量覆盖（这个仓库目前没有`.env`文件，要么在调用`docker compose`/`./start.sh`前`export WORLD_ENV=hard_forest`，要么临时建一份`.env`写`WORLD_ENV=hard_forest`——后者对`docker compose`每次调用都生效，前者只对当次shell有效，且必须在**每一次**触碰到`sim-world`服务的`docker compose`命令前都设置，否则某次漏设会被compose判定成"配置变了"，把`sim-world`容器`Recreate`回默认的`simple_room`，这次实测踩过一次）。

19. **新发现（2026-08-21），机队启停功能验证时顺带发现的操作坑，已记录进TODO#16**：`docker exec`进容器跑`ros2`命令，如果不手动`export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`+`export CYCLONEDDS_URI="file:///tmp/docker_sim_cyclonedds.xml"`，会出现"进程明明在跑、话题却发现不到/间歇性发现不到"的假象——这两个变量是entrypoint.sh在自己的shell里`export`的，只对entrypoint那条进程树有效，`docker exec`起的新进程不会继承，会退化成默认RMW/CycloneDDS配置连不上真正锁定`lo`网卡的那个DDS域。以后所有`docker exec ... ros2 ...`诊断命令都要带上这两个export，不能只做`ros2 daemon stop`（这是另一类已知坑，两者要分清楚）。

20. **测hard_forest场景，发现两个独立情况——一个已确认修复，一个是新的真实问题**：用户要求测hard_forest场景（森林密集障碍物）下规划器的实际表现。

    **情况一：早前记录的gzserver在森林场景下`exit -6`崩溃，已经在不知情的情况下被修复了**——`WORLD_ENV=hard_forest`修复+补齐后，`docker compose up sim-world`完整走了一遍双机spawn（169+个障碍物模型），全程没有崩溃、`docker inspect`确认`RestartCount=0`，gzserver稳定跑起来。判断：早前（"简单室内场景 simple_room"那条记录）判断"森林场景exit -6是复杂度问题、暂不深挖"，但后来排查Gazebo GUI崩溃（TODO第11项）时发现的真正根因——`sim-world-entrypoint.sh`从来没有`source /usr/share/gazebo/setup.sh`，`GAZEBO_RESOURCE_PATH`/`OGRE_RESOURCE_PATH`全是空的——很可能同时也是当初森林场景崩溃的根因（同一类"渲染资源路径没配好"问题），第11项那次修复顺带把这个问题也修好了，只是当时没有回头重测过。**建议**：这次只验证了"不崩溃"，没有把默认值从`simple_room`改回`hard_forest`（不确定验证得够不够全面，稳妥起见先保留`simple_room`当默认，`hard_forest`已经能通过`WORLD_ENV=hard_forest`环境变量正常使用）。

    **情况二：一个新发现的真实问题——hard_forest下MAVROS↔PX4连接间歇性断开，阻塞解锁**。带着`LOCALIZATION_SOURCE=uwb_slam`起两机、等DLIO/mavros就绪后尝试起飞，`mavros/state`的`connected`字段持续在`true`/`false`间反复横跳（周期大约5-10秒，观测了40秒以上没有自愈），`armed`卡在`false`，`mode`在`OFFBOARD`/空字符串间跟着一起跳。查`/tmp/px4_NX01.log`（sim-world容器内PX4自己的日志）确认PX4侧最终确实打印了`Ready for takeoff!`（`ekf2 missing data`/`heading estimate not stable`等preflight警告后来都消失了，不是数据链路本身没通——`ros2 topic hz /NX01/mavros/vision_pose/pose_cov`确认稳定100Hz），问题出在MAVROS到PX4的心跳/连接层面，不是EKF2数据源问题。`docker stats`确认host有充足余量（32核、当时load average 4-7，`sim-world`容器只用了约1.6个核），排除了"host整体资源不够"这个解释；判断更可能是Gazebo Classic的lockstep协议本身对**单步物理仿真延迟**敏感（不是总CPU不够，是"每一步"计算耗时变长——hard_forest比simple_room多了169+个障碍物的碰撞检测/渲染负载），导致PX4基于仿真时钟发出的心跳跟MAVROS基于真实墙钟的心跳超时窗口错位，间歇性判定"心跳超时"。**这是一个新发现的、独立于GUI崩溃/exit -6的真实问题，这次没有进一步深挖或修复**（需要的话，方向大概是调大MAVROS心跳超时容忍度，或者想办法降低hard_forest单步物理仿真的延迟峰值，比如简化碰撞几何体），只测出"目前hard_forest场景下解锁不稳定，双机没能真正起飞"这个结论，没有完成原计划的对树避障飞行测试。

    **顺带确认了另一件事**：TODO第9项末尾记录过NX02出生点(6,0,3)离最近的树（编号136，坐标(7.03,1.02)，净空约0.44m）很近的担忧——这次实测DLIO日志里NX02确实在初始化阶段出现过一次`[getNextPose] GICP LOST TRACK: only 0 correspondences (< 64)`+`[updateKeyframes] REJECTED new keyframe: implied speed 863.99m/s`（明显是被这棵近树干扰了初始配准），但DLIO自身的数值校验/关键帧拒绝机制正确拦截了这次坏更新（保留最后已知良好的位姿，没有让坏数据污染发布出去的里程计），后续`/NX01/dlio/odom_node/odom`位置读数正常（接近0,0,0，飞机还没动），这一次的近树担忧被证实是真实存在的风险，但当前系统的防御机制生效了，没有酿成实际问题——只发生过1次，之后20秒窗口内没有复现。

## 已验证没问题，不用管

- DLIO 的 `pointcloud_topic`/`imu_topic` launch 参数——跟 `flight-stack-entrypoint.sh` 传的对得上。
- `BUILD_JOBS` 并发限制 + ccache 缓存 + apt 索引刷新——都已经在最近几次构建里验证生效。
