# 2026大赛任务系统全流程开发——可执行实施清单

版本：2026-09-13 v4（同步实现方案v3-g——同一次执行前一致性核查的第二轮订正：A2（`agl_hold`模式）整体废弃，新增A0直接修`uwb_ground_truth_node.py`/`uwb_imu_fusion_node.py`的z融合逻辑；B3第5条`set_altitude_mode`删除；D3两处`sdk.set_altitude_mode('agl')`调用删除；风险清单/代码量估算同步调整。v3版本本身的其它订正——`CONTROLLER=pt4ctrl`、A5新增、θ*收敛判据删除、B3第10条新增、E6新增——都保留不变）
依据：`2026大赛任务系统全流程任务仿真实现方案.md`（v3-g）——本清单只做"方案→可执行任务"的拆解，不引入任何新的架构决策；如果执行过程中发现某一项跟实现方案的结论冲突，以实现方案为准，回头改方案，不要在这份清单里悄悄改设计。

**开始阶段A之前的环境前置检查（v3新增，不是contest_mission代码改动，是运维检查）**：核查时发现当前`gcs/cyclonedds_gcs.xml`+GCS容器实测处于"真机模式"配置（`ROS_DOMAIN_ID=20`、IP写死`192.168.2.x`），不是仿真模式（`ROS_DOMAIN_ID=21`+127.0.0.1回环）——**开始阶段A的容器内测试前，先确认/切换回仿真模式**，否则`ros2 topic echo`等验收手段可能连错网络域、看不到数据，容易被误判成"代码没生效"。同时确认`docker-compose.yml`（或`.env`/启动命令）里`CONTROLLER`实际生效值是`pt4ctrl`（不是默认值`px4ctrl`——`px4ctrl`+`LOCALIZATION_SOURCE=uwb_imu`是README.md记录的禁止组合，会直接炸机），`LOCALIZATION_SOURCE`是`uwb_imu`。

**怎么用这份清单**：每一项是一个可以独立完成、独立验证的最小任务，标了涉及的文件、具体改动点、依赖的前置项、验收标准。按"阶段"顺序做（阶段内部的编号顺序也是建议的实施顺序，不是随便排的），每做完一项在这份文件里把`- [ ]`改成`- [x]`当进度记录用。**每完成一个阶段末尾的"分段验证"，必须手动build+实测确认过，才能进入下一阶段**——这是实现方案第5节"不建议一次性串联全流程"这条建议的具体落地。

**关于docker build**：所有涉及镜像内容变化的项（改`contest_mission`包代码、新增`contest_sdk`/`contestant-sdk`镜像）都需要重新build——按项目约定，build由用户手动执行，本清单每个阶段末尾都会提示"这里需要手动build"，不会自动跑`docker build`/`docker compose build`。

---

## 阶段A：第一部分——仿真栈基础能力扩展（`contest_mission`包内）

不碰`mighty`/`ego_planner`核心代码，只改`contest_mission`包自己的文件+`flight-stack-entrypoint.sh`里`ros2 run contest_mission ...`那几行。按风险从低到高排序。**例外：A0**——执行前一致性核查发现A2（原`agl_hold`模式方案）的设计前提本身有问题，根子在`uwb_origin_bridge`包的定位融合逻辑，不是`contest_mission`包能解决的，用户明确要求这次一起修，所以A0破例改了`contest_mission`包之外的文件，其余A1/A3/A4/A5仍然只碰`contest_mission`包。

### A0. 修`uwb_ground_truth_node.py`+`uwb_imu_fusion_node.py`的z融合逻辑（对应方案1.1/1.1a节，v4订正新增，替代原A2）

- [x] 文件1：`src/uwb_sim/uwb_sim/uwb_ground_truth_node.py`
- 改动点：
  - `_publish_abs_and_truth`方法里，`pose_msg.pose.orientation`目前强制`x=0.0`/`y=0.0`（现第305-306行，"roll/pitch强制置0"），改成从`self.latest_poses[name].orientation`（Gazebo model_states真实姿态四元数，函数一开始已经取到`q = self.latest_poses[name].orientation`）直接透传roll/pitch分量到输出四元数，不再强制置0。**注意不要动`/{ns}/uwb/pose_truth`那条话题**（现第259-270行`if self.publish_truth_pose`分支）——那条线是"机上模块不许订阅"的评估专用旁路，这次改的是`/{ns}/uwb/pose_abs`（机上模块本来就允许订阅的模拟传感器读数），两条线不能混。
  - 文件头注释（现1-62行）补充说明这个决定的理由：这不是"开天眼"，是模拟一个精度良好的姿态传感器（类比0.2/0.4节"传感器能力可以直接建模，上帝视角信息不能写代码"的区分原则）。
- 依赖：无。
- 验收标准：容器里`ros2 topic echo /<ns>/uwb/pose_abs`，确认飞机做出滚转/俯仰机动时，输出四元数的roll/pitch分量能跟着变化（不再永远是0）。

- [x] 文件2：`src/uwb_origin_bridge/uwb_origin_bridge/uwb_imu_fusion_node.py`
- 改动点：
  - roll/pitch的来源从`_imu_cb`订阅的`mid360/imu`（现172-181行，解出的`self._last_roll`/`self._last_pitch`）改成从`_uwb_cb`订阅的`uwb/pose_abs`（文件1改完之后这条消息已经带有效roll/pitch）里解出——线加速度/角速度部分（现183-206行的位置/速度互补滤波传播）仍然要用`mid360/imu`，只是姿态角这两个分量换了数据源，不要把整个`_imu_cb`删掉。
  - 新增参数：`range_topic`（默认`mavros/hrlv_ez4_pub`）、`range_min_valid_m`（参考原方案1.1节的0.19-0.2米，留量取0.25）。
  - 新增订阅`sensor_msgs/Range`（`range_topic`），回调里缓存最近一次`range`读数+时间戳。
  - `_uwb_cb`（现208-237行）里x/y/z统一处理的`for i in range(3)`循环（现233-236行）需要拆开：x/y两路（`i=0,1`）保持现有逻辑不变（数据源仍是`uwb/pose_abs`）；z这一路改成——先算`compensated_height = latest_range * cos(roll) * cos(pitch)`（`roll`/`pitch`用当前最新值），再检查`latest_range`是否在`range_min_valid_m`以上且不超时（参考现有`_on_control_tick`风格的超时判断），有效时才用`compensated_height`去更新`self._p_state[2]`/`self._v_state[2]`的互补滤波修正（公式跟现有x/y那两行同构，只是数据源换了），无效时跳过z这一路的修正、只保留`_imu_cb`已经在做的纯IMU积分传播（不额外处理，本来就不受这个改动影响）。
  - 文件头注释（现1-64行）补充说明这次改动的理由和"为什么z的观测源要跟x/y分开处理"。
- 依赖：跟文件1顺序做（文件2依赖文件1改完之后`uwb/pose_abs`才有有效的roll/pitch可读）。
- 验收标准：
  1. 容器里全程飞行（起飞→巡航→降落），`ros2 topic echo mavros/local_position/odom`（或`<ns>/dlio/odom_node/odom`一类下游读回话题，具体以`local_position_readback_node`重发的话题名为准）观察z读数：飞过仿地模块时能跟着地形起伏变化（不是保持世界系固定值不变）。
  2. 手动让飞机做一定倾角的机动（比如快速侧飞/俯仰），确认z读数没有因为倾斜角度变化而产生系统性偏差（对照没有补偿时的表现，确认补偿生效）。
  3. 观察起降瞬间（`range`跌破`range_min_valid_m`），确认z读数不会因为这一刻的无效/异常range值产生跳变。

### A1. `fire_pillar_aim_node.py`补发`fire_pillar_aim_pose`话题（对应方案1.2节，最小改动，先做）

- [x] 文件：`src/contest_mission/contest_mission/fire_pillar_aim_node.py`
- 改动点：
  - `__init__`里新增一个publisher：`self.aim_pose_pub = self.create_publisher(PositionCommand, 'fire_pillar_aim_pose', 10)`（跟现有`self.aim_pub`/`self.staging_pub`同一种写法，紧挨着加在第111-112行附近）。
  - `_on_control_tick`方法（现文件第236-261行）里，`aim_cmd`组装完之后已经有`self.aim_pub.publish(aim_cmd)`（现第250行），紧接着加一行`self.aim_pose_pub.publish(aim_cmd)`——**注意这是对外广播用的独立话题，不是把`fire_pillar_aim_cmd`改名**，`fire_pillar_aim_cmd`继续给`position_cmd_relay`的`pillar_aim`模式消费，`fire_pillar_aim_pose`是给第四部分程序读的只读广播，两者内容一致但语义不同（一个是控制指令，一个是状态广播），不能合并成一个话题，避免以后"改控制指令格式"和"改对外广播格式"两件不相关的事被迫绑在一起改。
  - 顺手在文件头注释里补一句这个新话题的说明（跟着现有"命中后悬停点发到fire_pillar_aim_cmd...任务机等待点发到fire_pillar_staging_pose"那句话一起改，第131-135行附近）。
- 依赖：无。
- 验收标准（对应方案第5节"synthetic注入确认发布内容正确"）：容器里用`ros2 topic echo /<ns>/fire_pillar_aim_pose`，triggering一次立柱火情检测命中（可以复用之前验证阶段1-7时用过的手段），确认这个话题在`_locked_aim`产生的那一刻开始按`control_rate_hz`频率发布，内容跟同时`echo`的`fire_pillar_aim_cmd`一致。

### A2.（已废弃，见A0）

原方案打算给`position_cmd_relay_node.py`新增`agl_hold`模式，执行前一致性核查发现这个设计的前提站不住脚（PX4的高度主参考源是`vision_pose`不是气压计，`uwb_imu`模式下"世界系Z"这个概念本身不存在，见方案1.1节改写说明）——改成在定位融合层（`uwb_origin_bridge`包）一次性修好，具体任务挪到了A0，`position_cmd_relay_node.py`不需要改，回到"维持不变"名单。这个编号保留空位、不重新占用，避免跟其它已经写好的引用（A3/A4/A5）对不上号。

### A3. 新增`formation_follower_node.py`（对应方案1.4/1.4.1/1.4.2节，本阶段最复杂的一项）

- [x] 新文件：`src/contest_mission/contest_mission/formation_follower_node.py`
- 建议内部实现顺序（一个文件内的分步骤，不是分成多个文件）：
  1. **先写`LeaderPathBuffer`类**（`append(x, y, t)`往缓冲区追加长机轨迹点、`point_at_arc_length_behind(follow_distance_m)`按弧长回溯查询目标点）——这是全节点里逻辑最复杂、最值得先独立写、独立跑单元测试的部分，不要跟ROS节点逻辑混在一起写，方便按方案第5节"用synthetic轨迹数据测试，不要直接上真实双机飞行验证"这条建议单独验证。
  2. **再写`FormationGeometry`/`FormationControlStrategy`两个抽象基类** + `TandemGeometry`（调用`LeaderPathBuffer.point_at_arc_length_behind()`）+ `LeaderFollowerStrategy`（把目标点发到`~/rviz_goal_world`或等价的既有目标输入接口，具体接口名参照现有`ego_planner_bridge`的`rviz_goal_bridge_node`用法确定）的具体实现——按方案1.4.1节给的接口代码框架实现，`LineAbreastGeometry`/`VirtualStructureStrategy`只需要占位（`raise NotImplementedError`或注释说明"预留接口，这次任务不实现"），不需要真的写出内容。
  3. **最后写节点本体`FormationFollowerNode`**：
     - 参数：`geometry_type`（默认`'tandem'`）、`strategy_type`（默认`'leader_follower'`）、`follow_distance_m`、`leader_namespace`（跟随谁，运行时通过service/参数设置，不能硬编码）、`enabled`（默认`False`，对应1.4.2节"节点常驻但默认不生效"）。
     - 订阅：跨命名空间订阅`/<leader_namespace>/dlio/odom_node/odom`（`leader_namespace`运行时可变，意味着这个订阅需要能在收到"启用"指令时动态创建/切换，不能在`__init__`时就固定订阅哪个namespace）。
     - 新增一个`~/set_formation`（自定义service或者复用标准`~/set_parameters`+一个额外的"enable"参数，具体二选一时优先选标准`~/set_parameters`——这个包里`position_cmd_relay`/`fire_pillar_aim_node`都是这个模式，保持接口风格一致，减少SDK那边`capabilities.py`要适配的接口种类）：SDK的`sdk.start_formation_follow()`最终调的就是这个。
     - 定时器：`create_timer(1.0/rate_hz, ...)`（`rate_hz`取5-10Hz区间，方案1.4.2节给的范围），`enabled=True`时才真正调用`compute_target()`+`track()`，`enabled=False`时空跑不发布任何东西。
  4. **每架飞机的`flight-stack-entrypoint.sh`都要起这个节点**（1.4.2节"每架飞机都要起，角色运行时决定"）：在`flight-stack-entrypoint.sh`里`ros2 run contest_mission fire_pillar_aim_node ...`那几行附近（现第1221-1230行左右）新增一行`ros2 run contest_mission formation_follower_node --ros-args -r __ns:=/${NAMESPACE} &`（跟现有几行的写法保持一致，具体参数透传方式照抄现有几个`ros2 run contest_mission ...`调用的模式）。
  5. `setup.py`里`entry_points`加一行`'formation_follower_node = contest_mission.formation_follower_node:main'`。
- 依赖：无强依赖，但建议在A1/A2做完之后再做（风险从低到高的顺序，也让实施者先熟悉这个包里现有节点的写法习惯）。
- 验收标准（对应方案第5节）：
  1. **先做synthetic单元测试**：给`LeaderPathBuffer`喂几种典型轨迹形状（直线、直角拐弯、连续S弯）的synthetic坐标点序列，确认`point_at_arc_length_behind()`返回的点落在"缓冲区折线上往回数固定弧长"的位置，不是简单的"直线插值抄近道"（这是方案专门强调过的坑，之前版本设计错过一次，见DEBUG_JOURNAL.md历史记录）。
  2. 节点本身：`enabled=False`时确认不发布任何目标（默认待命状态）；手动调`~/set_parameters`把`enabled`设为`True`+指定`leader_namespace`，确认能收到对方odom后开始按频率发布目标。
  3. 暂不要求这一步就上双机真实编队飞行验证——那是阶段E跟第四部分联调时才做的事。

### A4. `actuator_action_node.py`改造成真实`mavros/rc/override`驱动（对应方案1.6节，v3-e新增）

- [x] 文件：`src/contest_mission/contest_mission/actuator_action_node.py`（现86行，纯状态记录）
- 改动点：
  - `package.xml`加一行`<depend>mavros_msgs</depend>`（现在没有这个依赖）。
  - 新增publisher：`self.override_pub = self.create_publisher(OverrideRCIn, 'mavros/rc/override', 10)`（`from mavros_msgs.msg import OverrideRCIn`）。
  - 新增动作→硬件映射的参数（每个`action_name`对应一个RC通道号+目标PWM值+`hold_duration_s`，具体几个值**目前是未知数**，见下面"未决问题"——先做成参数，不要硬编码，方便真实通道确定后直接改launch参数不用改代码）。
  - `_on_set_parameters`收到有效`action_name`后：启动一个`create_timer`（周期对应≥10Hz），定时器回调里持续发布`OverrideRCIn`（只设置映射表里配置的那个/那几个通道，其余17个通道全部填`OverrideRCIn.CHAN_NOCHANGE`即65535，不要动别的通道），累计时长达到`hold_duration_s`后：①再发一帧把该通道也设成`CHAN_NOCHANGE`释放控制权，②取消定时器，③`self._publish_status(f'done:{action_name}')`（现有逻辑）。
  - 原来"记录状态"的部分（`self._last_action`等）继续保留，不用删。
- 依赖：无强依赖，可以跟A1/A2/A3并行，但建议放在A3之后做（这个节点是"有限时长的持续发布"模式，跟`formation_follower_node`的"定时器+条件发布"是同一种写法，建议实施者先写完A3熟悉这个模式再来做这个更简单的版本）。
- **未决问题（不是这一项能自己决定的，需要先问清楚再填代码里的默认值）**：三个动作（`grab_supply`/`drop_supply`/`horizontal_launch`）具体对应哪个RC通道、什么PWM值、需要保持多久，这是真实机构/遥控器接线的硬件细节，方案层面没有答案——**这一项实施前需要先跟负责机构接线的人确认这三个映射关系**，拿到之前只能先用占位值（比如统一先填通道9、PWM 2000、保持1秒）让链路跑通，不代表这几个占位值是最终真实值。
- 验收标准（对应方案1.6节"未决问题2"提到的验证手段）：
  1. 容器里`ros2 param set /<ns>/actuator_action action_name grab_supply`触发一次，同时`ros2 topic echo /<ns>/mavros/rc/override`，确认：①配置的通道在`hold_duration_s`内持续输出目标PWM值（不是只发一帧）；②时长结束后，该通道变回`65535`（`CHAN_NOCHANGE`）；③其余17个通道全程都是`65535`，没有被意外改动；④`action_status`最终收到`done:grab_supply`。
  2. 按方案第5节风险清单第8条，SITL里额外确认一下`CHAN_NOCHANGE`释放后，如果场景里配置了真实RC输入模拟（如果SITL支持模拟一路"未拨动"的RC信号），飞控行为符合预期（不会因为释放瞬间产生奇怪的PWM跳变）——这一步如果SITL本身没有真实RC输入可模拟，可以跳过，留到真机阶段验证。

### A5. `precision_servo_node.py`拆成"仅居中定位"/"精降抓取"两种模式（对应方案1.7节，v3新增，执行前一致性核查发现的缺口）

- [x] 文件：`src/contest_mission/contest_mission/precision_servo_node.py`（现226行）
- 改动点：
  - 新增参数`servo_mode`（取值`'center_only'`/`'precision_land'`，无默认值或给个安全默认，强制调用方显式设置）、`converge_hold_duration_s`（水平误差收敛后需要持续稳定这么久才算"真的收敛"，避免瞬间抖动误判，建议0.5-1.0秒）。
  - `_on_control_tick`（现171-209行）里，水平误差收敛判断（现194行`if horizontal_error <= threshold`）之后按`servo_mode`分叉：
    - `center_only`：不进入现有的下降逻辑，改成累计"持续收敛时长"，达到`converge_hold_duration_s`后：①新增一个publisher把当前`_odom_xy_z_yaw`的x/y发出去（比如`~/centered_pose`，`geometry_msgs/PointStamped`或复用`PositionCommand`，跟A1对`fire_pillar_aim_pose`的做法保持同一种风格），②发一次"完成"通知（复用`actuator_action_node`那种`action_status`字符串广播模式，比如`~/servo_status`发`'centered'`），③停止发布`precision_land_cmd`（不再继续控制，把控制权交还给`position_cmd_relay`当前其它模式）。
    - `precision_land`：现有逻辑不变（收敛后开始下降），但下降到`min_descent_z`后**新增**：不再是"停在那个高度悬停"，而是调用现有的降落触发接口（复用现有`TakeoffLand`消息降落触发，跟`sdk.land()`底层是同一个接口，不需要新造降落机制），订阅`mavros/state`等`armed`变`false`确认真正落地，然后往`~/servo_status`发`'landed'`。
  - 两种模式共享现有的水平误差收敛计算逻辑（现140-169行`_on_detections`不用改），只是收敛后的分支行为不同，不要把两种模式拆成两个文件/两个节点。
- 依赖：无强依赖，可以跟A1-A4并行，但建议放在A4之后做（同样是"给现有节点加一个新分支行为"的模式，跟A2/A4是同一类改动）。
- 验收标准：
  1. `center_only`模式：容器里手动切参数触发，飞机悬停在目标tag上方水平有偏移时，确认只修水平不下降；水平误差进入阈值并稳定`converge_hold_duration_s`后，确认`~/centered_pose`发出的坐标跟当时的实际odom x/y一致，且之后`precision_land_cmd`停止发布（`ros2 topic hz`确认频率归零）。
  2. `precision_land`模式：确认现有下降行为没有回归（跟改造前行为一致），额外确认下降到`min_descent_z`附近后触发真正降落（`armed`变`false`）、`~/servo_status`收到`'landed'`，且**降落后能重新`sdk.takeoff()`**（这一步需要在`CONTROLLER=pt4ctrl`环境下测，验证方案5节风险清单第10条提到的"落地→再起飞"链路，不要等到D5串联时才第一次测）。

### 阶段A手动build+分段验证

- [ ] 上面A0/A1/A3/A4/A5全部改完后（A2已废弃，见上），交给用户手动`docker build`（或`docker compose build`对应的flight-stack+`uwb_sim`/`uwb_origin_bridge`相关镜像），build完成后**先只做A0/A1/A3/A4/A5各自的独立验收标准**（不要提前联调编队/瞄准/动作跟第四部分程序的交互），全部通过后再进入阶段B。

---

## 阶段B：第二部分——选手SDK（`contest_sdk`）

不依赖第四部分的任何任务专属代码，可以在阶段A的build结果之上单独开发+做synthetic测试（不需要真的起双机飞行）。

### B1. 建包骨架

- [x] 新目录：`contest_sdk/`（建议跟`src/contest_mission`平级，作为一个独立的Python包，不是ROS2功能包——它不需要`colcon build`，选手容器里`pip install`或者直接`COPY`进镜像即可，具体打包方式留给B1实现时按`contestant-sdk`镜像的Dockerfile一起定）。
- [x] 按方案2.2节的分层建空文件：`__init__.py`、`exceptions.py`、`_rclpy_runtime.py`、`capabilities.py`、`reliability.py`。
- [x] 写`exceptions.py`（先写这个，因为后面几层都要用）：`TakeoffTimeoutError`、`GotoTimeoutError`、`ActionFailedError`、`TeammateUnreachableError`、`DetectionTimeoutError`——每个异常类的`__init__`按方案2.2.2节要求带上"可能原因"提示文本（不是只报"超时"），比如`TeammateUnreachableError`默认消息里带"请确认队友程序是否已经启动、两台设备是否在同一个路由器/WiFi下"这类排查提示。

### B2. `_rclpy_runtime.py`（L1）

- [x] 封装`rclpy.init()`/后台spin线程/节点创建/`rclpy.shutdown()`的生命周期，对上层只暴露"给我一个能创建publisher/subscriber/client的节点句柄"这一个接口，`capabilities.py`不直接碰`rclpy`模块。
- [x] 顺手实现2.4节"`ROS_DOMAIN_ID`一致性校验"：初始化时读环境变量，跟约定值（21）不一致就直接抛一个`contest_sdk`自定义异常（可以放进`exceptions.py`，比如`EnvironmentMisconfiguredError`），报错信息直接提示"请检查ROS_DOMAIN_ID环境变量"。

### B3. `capabilities.py`（L2）——按2.1节的能力清单逐条实现，建议顺序：

- [x] 1. `wait_for_detection(class_id, timeout)` → 订阅`vision/detections`过滤，超时抛`DetectionTimeoutError`，返回值是B1定义的简单数据类（不是`vision_msgs/Detection2D`，对应2.2.1节封装完整性要求）。**实现时的字段命名调整**：方案示例代码写的是`det.local_x`/`det.local_y`，实现改成了`det.bbox_x`/`det.bbox_y`（+`bbox_width`/`bbox_height`/`score`/`stamp_sec`）——理由见`capabilities.py`里`Detection`类的docstring：单帧2D检测本身解不出世界坐标，沿用`local_x`容易让选手误以为能直接喂给`goto()`。
- [x] 2. `set_mission_state(state)`/`get_mission_state()` → 封装`mission_state.py`契约（vendor到新建的`contest_sdk/mission_state_helpers.py`，零ROS依赖，方便独立单元测试）。**核查结论**：读过`is_valid_mission_state()`源码后确认它*已经*支持"顶层值:子状态"带冒号写法（`state.split(':', 1)[0]`只取冒号前半部分校验），不需要扩展，vendor过来的拷贝逐字一致，没有做任何校验逻辑改动。
- [x] ~~5. `set_altitude_mode('agl'|'world_z')`~~（**v4订正：整条不实现**——A2已废弃，`uwb_imu`模式下AGL全程默认生效于定位融合层，没有"world_z"可选项，不需要这个开关接口，见方案2.1节第5条订正）。
- [x] 6. `world_to_local(x, y, z)` → 走TF查询，只在SDK内部处理`frame_id`，对外只收发`(x,y,z)`三元组（2.2.1节要求）。参考`ego_planner_bridge/rviz_goal_bridge_node.py::_to_local()`同一套`lookup_transform(target_frame, 'world', Time(), timeout=Duration(seconds=0.0))`查询方式，外层用`_poll_until()`轮询+打印进度代替内部死等非零timeout。
- [x] 4. `do_action(name)` → 封装`actuator_action_node`的`~/set_parameters`+`~/action_status`轮询，超时抛`ActionFailedError`。
- [x] 7. `start_formation_follow(follow_distance_m)`/`stop_formation_follow()` → 一次性触发调用，调阶段A3新增的`formation_follower_node`的`~/set_parameters`，**这里只是"发一次调用+确认对方接受了参数"，不做任何持续订阅/计算**（1.0节架构原则，SDK这层绝对不能实现控制回路本身）。
- [x] `takeoff()`/`land()`/`goto(x,y,z)` → 虽然2.1节没有单独编号，但方案2.3节"链路A"三条可靠性要求（常驻publisher、发送+等待状态反馈、service化阻塞语义）主要就是针对这几个接口，实现时要落实这三条，不是简单发一次topic就返回。`goto()`最终选用现成的`waypoint_queue`（`nav_msgs/Path`，单点）+`waypoint_state`确认+`waypoint_cancel`打断这一套接口（`ego_planner_bridge/rviz_goal_bridge_node.py`已有实现），不是`rviz_goal_world`单点话题，理由见`capabilities.py::goto()`docstring。`land()`超时新增了`LandTimeoutError`（未复用`TakeoffTimeoutError`，见`exceptions.py`该类docstring说明的理由）。
- [x] 3. `send_to_teammate(event, **kwargs)`/`on_teammate_event(event_name, callback)` → 依赖B4（reliability.py）先做完，放最后实现。
- [x] 8. `generate_orbit_waypoints(cx, cy, radius, z_agl, num_points)`/`generate_ground_scan_waypoints(...)` → 把`src/contest_mission/contest_mission/orbit_flight_helper.py`（现69行）、`ground_scan_helper.py`（现105行）这两个纯函数的逻辑**复制一份**进`contest_sdk`（不要跨包`import contest_mission`，2.1节第8条已经讲过原因），改成`contest_sdk`自己的模块（比如`contest_sdk/geometry_helpers.py`，从`capabilities.py`或`__init__.py`re-export成`sdk.generate_orbit_waypoints(...)`）。**这两个函数本身跟ROS无关（纯几何计算），复制不会产生"以后两边改法不一致"的维护负担太大的问题**——如果实现时发现`orbit_flight_helper.py`/`ground_scan_helper.py`本身逻辑还在变动，也可以考虑抽成一个两边都依赖的独立小包（方案2.1节第8条提到的备选方案），哪种做法更合适留给实现时按实际情况判断，两种都符合方案要求。已实现为新文件`contest_sdk/geometry_helpers.py`，`DroneSDK`同时提供实例方法转调+保留模块级纯函数两种调用方式。
- [x] 9. `trigger_alarm(pattern, duration_s=None)`（v3-e新增，对应方案2.1节第9条/"链路D"）→ 联系GCS所在机器触发声光装置，具体走什么协议（HTTP调GCS后端一个新接口/串口/其它）**目前是未知数，需要先跟GCS开发者对接确定接口形式**（不是这一项能自己决定的）。实现要点：内部做一次简单确认+短超时（几秒量级），超时/失败**吞掉异常、只打警告日志**，不向选手抛出去（方案2.3节"链路D"明确这是唯一一处刻意不向选手暴露异常的能力，因为声光装置故障不该拖垮任务逻辑）——这跟B3其它方法"超时要抛`contest_sdk`异常"的一般模式刻意不一样，实现时不要图省事直接复用其它方法的错误处理模板。已实现为HTTP POST占位调用（`DEFAULT_ALARM_URL`是明显的占位地址，等GCS接口定下来后再改）。
- [x] 10. `center_on_target(class_id, timeout)`/`precision_land_and_confirm(class_id, timeout)`（v3新增，对应方案2.1节第10条/A5新增的`precision_servo_node`两种模式）→ 分别触发A5的`servo_mode='center_only'`/`'precision_land'`并阻塞轮询`~/servo_status`（前者等`'centered'`，同时读回`~/centered_pose`发布的坐标作为返回值；后者等`'landed'`），超时抛`ActionFailedError`一类（跟`do_action`共用异常类型，不需要新造）。**执行前一致性核查时确认的一个额外缺口**：D6用到的`fire_pillar_aim_node`的`~/reset_aim`service目前没有任何B3方法能覆盖（现有8条能力都是各自对接一个特定的节点接口，没有"调用任意service"这种通用封装）——建议在这一条顺手补一个薄封装，比如`reset_aim()`（专门对接`fire_pillar_aim_node`的`~/reset_aim` Trigger service），不需要做成通用的"调任意service"接口，按需专门封装这一个就够。`cancel_goto()`/`reset_aim()`两个B3清单未编号列出、但D4/D6节已经在用的方法也已补上。**阶段D写`mission.py`时又补的三个方法**（v3-g追加，方案2.1节第11条）：`read_fire_pillar_staging_pose(timeout)`/`read_fire_pillar_aim_pose(timeout)`（订阅`fire_pillar_aim_node`常驻广播的`~/fire_pillar_staging_pose`/`~/fire_pillar_aim_pose`取最新值，读不到超时抛`DetectionTimeoutError`，跟`get_mission_state()`是同一种"持续订阅取最新值"模式）、`get_local_position(timeout)`（暴露SDK内部本来就有的`_odom_xyz`，返回自己当前局部坐标）。**2026-09-14阶段E联调时又补的三个方法**（用户提出的新场景，`precision_servo_node`从2种模式扩到4种）：`precision_land_at(x,y,timeout)`（`servo_mode='coordinate_land'`，不看视觉、直接拿已知坐标跟里程计作差做分级下降+`AUTO_LAND`交接，适合"起飞前记录起降点坐标、返回时精确降落"这类场景，验证过69.5秒/5.3厘米误差）、`goto_direct(x,y,z,timeout)`（`servo_mode='coordinate_goto'`，同样不看视觉，三维同时收敛不下降，用户提出的"goto的不避障直达版"，⚠️不走`ego_planner`没有避障能力、且底层P控制只适合短距离最后一段精修不适合替代`goto()`跑长距离，验证过2.5米/9.82秒/0.196米误差）、`local_to_world(x,y,z,timeout)`（跟已有的`world_to_local()`对称补上的方法，B3原文漏掉的一个方向）。这几个方法+对应`precision_servo_node.py`的改动详见`DEBUG_JOURNAL.md` 2026-09-14多条记录，目前只在容器里`docker cp`验证过，还没有走`docker build`固化进镜像。

### B4. `reliability.py`（L3）

- [x] 实现应用层ACK+重发协议（方案2.3节"链路B"）：每条`mission_events`消息带唯一ID（建议UUID或者`f"{namespace}-{monotonic_counter}"`），发送方1Hz重发直到收到对方ack或超时（30-60秒，方案2.3节延迟预算建议值），接收方按ID去重。
- [x] `mission_events`话题QoS：`RELIABLE` + `history depth=10` + 可选`TRANSIENT_LOCAL`（方案2.3节第3条）。
- [x] 这一层被B3的`send_to_teammate`/`takeoff`等方法内部调用，选手完全无感（对上层暴露"阻塞直到确认，超时抛异常"这一个语义）——已定义好`ReliableEventChannel.send_and_wait_ack()`/`register_event_handler()`接口，B3实现时直接转调即可。

### B5. `__init__.py`

- [x] 只`from .capabilities import DroneSDK`+`from .exceptions import *`对外暴露，不导出`_rclpy_runtime`/内部实现细节。
- [x] `DroneSDK.__init__(self, namespace, role, teammate_namespace)`——三个参数都必填（方案1.5节/2.2.1节示例代码），构造时顺手做B2提到的`ROS_DOMAIN_ID`校验（转调`RclpyRuntime`，未重复实现）。

### B6. 可调试性打点（方案2.2.2节，贯穿B3各个方法，不是单独一步，但单列出来提醒不要漏）

- [x] `takeoff()`/`goto()`等阻塞方法内部，等待期间用一个后台`Timer`或者在轮询循环里每2-3秒打印一行中文进度（复用B1/B2的日志机制，不直接用`self.get_logger()`裸ROS2日志，包一层"SDK自己的进度打印"，方便以后要不要输出到文件/GUI而不是print时统一改）。实现为`DroneSDK._progress()`（包一层`print()`）+`_poll_until()`（自带轮询循环的方法用）/`_run_with_progress_ticker()`（转调`reliability.py`这类内部循环不可拆分的方法用，后台线程定时打印，不碰`reliability.py`一行代码）两个helper，PROGRESS_INTERVAL_S=2.5秒。
- [x] 逐个检查B1定义的异常类的报错文本，确认每个都带了"可能原因"提示，不是只报"超时"两个字。核查结论：7个异常类全部带"可能原因"分点提示，未发现需要重写的问题；新增的`LandTimeoutError`同样带了3点可能原因。

### 阶段B验收（对应方案第5节"每个能力方法写单元测试/集成测试"）

- [x] `reliability.py`的ACK重发协议：写synthetic丢包/延迟测试（不需要真实网络环境，可以在本机用两个rclpy节点+人为丢弃/延迟消息模拟）——实现为`test_reliability.py`，用自建`FakeNetwork`/`VirtualClock`抽象出可注入丢包规则/延迟拍数的假网络层，比"两个真实rclpy节点"更彻底地不依赖ROS2环境，8个场景全部通过。
- [ ] `capabilities.py`每个方法：至少一个"正常路径成功"+一个"超时报出正确的异常类型"的测试用例。**本次实现范围说明**：这次会话按需求方明确指示，`takeoff`/`goto`/`do_action`等依赖真实ROS2话题通信的方法这次只做`python3 -m py_compile`语法检查（已全部通过），不起容器测试——集成测试推迟到阶段E跟真实flight-stack联调时做。这次独立写了单元测试的只有`generate_orbit_waypoints`/`generate_ground_scan_waypoints`（`test_geometry_helpers.py`，18个用例）+`mission_state`格式校验含冒号子状态（`test_mission_state_helpers.py`，6个用例），两者都不需要ROS2环境、`python3`直接跑通过。
- [ ] 交给用户手动build`contestant-sdk`镜像雏形（哪怕阶段C还没做完整的launcher脚本，先能在一个装了ROS2 Humble+CycloneDDS的容器里`import contest_sdk`跑通就算这一步过关）。

---

## 阶段C：第三部分——选手开发/运行环境

跟阶段B可以部分并行（Dockerfile骨架可以先搭，装SDK的步骤等B完工后再补），但完整验收需要B先完成。

- [x] `contestant-sdk`镜像Dockerfile：基于跟flight-stack一致的ROS2 Humble+`rmw_cyclonedds_cpp`基础镜像（不需要mighty/PX4那些重依赖），`COPY`进`contest_sdk`包+`pip install`。
- [x] `运行仿真.sh`（Linux/macOS）+`运行仿真.bat`（Windows）：包一层`docker run --rm --network host -v "$(pwd)":/workspace contestant-sdk:latest python3 /workspace/我的任务.py`，加错误捕获（Docker未安装/未启动时打印人话提示，参照方案3节"脚本要包一层错误捕获"）。
- [x] 示例任务模板`我的任务.py`：预置`from contest_sdk import DroneSDK`+一个最小可跑通的示例（比如只做起飞→飞到一个点→降落），让选手一开始就能看到"改这几行就能让飞机动起来"的效果，不需要凭空从空文件开始写。
- [x] （可选，方案②）`.devcontainer/devcontainer.json`，指向同一个`contestant-sdk`镜像。
- [x] 3.1/3.2节提到的边界/复位机制不需要新写代码——3.1节靠"选手镜像本来就不包含飞行栈源码"这个事实自然成立（检查一下`contestant-sdk`的Dockerfile确实没有`COPY`任何`sim-world`/`flight-stack`目录进去就行）；3.2节的复位流程是操作层面的约定（`docker compose restart sim-world flight-stack-nx01 flight-stack-nx02`），不需要写代码，写进选手/赛事方操作手册即可。

### 阶段C验收

- [x] 双击（或命令行执行）`运行仿真.sh`，确认选手侧感知不到任何`docker`字样，能看到中文进度输出（阶段B6打的日志）——2026-09-13实测通过：起飞/goto/降落三段中文进度全部正常显示，中途发现并修复了`Dockerfile.contestant-sdk`漏装`mavros_msgs`/`vision_msgs`/`quadrotor_msgs`三个消息包+`goto()`/`cancel_goto()`漏了连发几次防护这两个真实bug（见DEBUG_JOURNAL.md对应条目），修复后`goto()`验证完全正常（起飞→精确飞到目标点→"已到达"）。
- [x] 确认`contestant-sdk`容器里`ros2 topic list`能看到仿真栈的话题（网络配置对了），但容器本身文件系统里找不到`mighty`/PX4固件等飞行栈源码（3.1节隔离验证）——2026-09-13实测通过，`find`确认镜像里无任何飞行栈源码，`docker run --network host`能立即看到完整仿真栈话题列表。⚠️ 中途`sdk.land()`出现过一次卡死超时，排查后确认不是A0改动的稳定bug（复测同样代码就正常了，见"已知的坑"第12条+方案5节风险清单第11条完整记录），不是阶段C这部分（选手环境本身）的缺陷，不影响这两条验收本身通过。

---

## 阶段D：第四部分——全流程任务程序（最终交付物）

这一层是选手/开发者写的普通Python程序，依赖阶段A（新能力）+阶段B（SDK）都已经完成并且build验证过。

### D1. 词汇表 + 通用helper

- [x] `mission_vocab.py`：方案4.1节的4个跨机事件名常量（`GROUND_FIRE_FOUND`/`HR_FIRE_LOCKED`/`RECON_LAUNCH_DONE`/`SUPPLY_READY`）+ 3个动作名字符串（`grab_supply`/`drop_supply`/`horizontal_launch`）。
- [x] **已知坐标常量（v3新增，执行前一致性核查发现的缺口）**：4个航线途经点（4.2节）、`supply_point`、3根候选立柱坐标——这几个题目允许提前给的坐标，**直接写成`mission_vocab.py`（或程序自己的常量模块）里的字面量常量**，不是读`fire_drill_room_layout.yaml`（选手程序跑在`contest_sdk`容器里没有这个文件/没有`contest_mission`包，见方案0.4节订正、3.1节隔离设计）；仿真环境里这几个数值可以直接抄`fire_drill_room_layout.yaml`当前的值（抄一次、写死，不是运行时读取），真机部署时换成主办方现场给的实际数值即可，改的是常量的值，不是读取方式。
- [x] 立柱确定性编号排序函数（方案4.3.2节："先比X坐标、X相同再比Y坐标"字典序）——纯函数，写在全流程程序自己的一个小helper文件里（不需要收进`contest_sdk`，这是任务专属逻辑，2.1节第8条已经说明这种不构成打包问题）。
- [x] 长距离分段插值函数（方案4.2节"按最大步长3米自己插值出中间途经点"）——同样是纯函数，本次任务专属，放程序自己的helper文件。

### D2. 主程序骨架

- [x] 新文件（建议名`mission.py`，放在选手工作目录/示例模板里，不是`contest_mission`包的一部分）：
  - CLI参数：`--namespace`、`--role`（`recon`/`supply`）、`--teammate-namespace`（方案1.5节的构造参数）。
  - `sdk = DroneSDK(namespace=args.namespace, role=args.role, teammate_namespace=args.teammate_namespace)`。
  - 顶层按`if sdk.role == 'recon': ... else: ...`分两个分支（方案4.3节"代码结构提示"，同一份文件，不是两个文件）。

### D3. 第一部分任务：航线编队飞行（方案4.2节）

- [x] RECON分支：`sdk.takeoff()` → 用D1的分段插值函数把4个途经点拆成≤3米步长的子航点，逐段`sdk.goto()`。（**v4订正**：不再调用`sdk.set_altitude_mode('agl')`——这个SDK方法已删除，AGL全程默认生效，不需要显式切换。）
- [x] SUPPLY分支：`sdk.takeoff()` → `sdk.start_formation_follow(follow_distance_m=3.5)`。（**执行前一致性核查订正**：之前这里写"确认θ*收敛判据"是套用了`uwb_slam`模式的机制，这次任务实际用`uwb_imu`定位——`origin_setter_node`是`uwb_slam`专属节点，`uwb_imu`模式下不存在θ*收敛这个环节，两机局部系天然对齐，不需要任何等待/查询收敛信号，确认起飞完成就可以直接调`start_formation_follow`。）
- [x] 两机都到位后：SUPPLY角色`sdk.land()`；RECON角色不降落，直接进入D4。
- [x] **这一步先只跑RECON分支单独验证**（方案第5节"第四部分先单机验证"），SUPPLY分支等阶段E的双机联调再测。**2026-09-13实测**：第一轮实测直接复现`GotoTimeoutError`，查出一个真实的系统性坐标系bug——`sdk.goto()`只认局部坐标，但D1的`mission_constants`存的是世界坐标（按用户明确要求，字面量抄自yaml），`mission.py`好几处直接传给`goto()`没做转换；`sdk.center_on_target()`等方法文档也错误写着"返回世界坐标"，实际是局部坐标，跨机广播会传错坐标给队友。已修复：`capabilities.py`补`local_to_world()`跟`world_to_local()`对称+订正3处文档错误措辞，`mission.py`改了8处调用点（详见DEBUG_JOURNAL.md对应条目）。**修复后验证分两层**：①单独调用`sdk.goto()`（从已稳定悬停状态触发）证实坐标转换本身完全正确，飞机干净地飞到目标点；②但完整跑`mission.py`（`takeoff()`后立刻第一次`goto()`）连续两次都在目标点附近（差0.5~0.8米）卡住不收敛、60秒超时——这是跟坐标系bug**无关**的独立现象。**2026-09-14继续排查**：查清楚了根因是`ego_planner`的到达判据"假性完成"——`ego_replan_fsm.cpp`判断轨迹是否到达终点时，比较的是自己规划的B样条在当前时刻的取值（`evaluateDeBoorT`），不是真实里程计，规划轨迹时间一跑完就判定"到达"、切`WAIT_TARGET`不再发指令，`pt4ctrl`0.5秒后检测到指令断流切`AUTO_HOVER`锁定飞机当时的真实位置（可能还没真正飞到目标）；这个问题在真机部署仓库（`docker_real`）里也被独立发现过、明确记录为"需要改成检查真实里程计+配超时兜底，但目前没做"的已知限制，不是仿真专属。顺带核实到一个相关但不同的bug（`planner_manager.cpp::reboundReplan()`的`size_t`下溢死循环，真机2026-08-31用gdb定位过），已移植`patches/ego_planner_rebound_replan_arclength_underflow.patch`到`docker_sim`并rebuild验证，专门构造小距离挪动场景确认守卫会被正确触发、触发后不卡死。**但这个死循环补丁只是加固、不解决"假性完成"本身**——rebuild后重新完整跑`mission.py`确认D3第一段（起飞后立刻第一个goto()）仍然稳定卡住、60秒超时，不是偶发；同一批测试还观察到`sdk.land()`超时的现象连续出现了第3次（跟"假性完成"场景是否有因果关系未确认，只记录）。**D3目前仍未验收通过**，"假性完成"本身要不要修（比如给起飞后第一个goto()之前插入稳定等待、或者放宽`rviz_goal_bridge_node.py`的到达阈值、或者给`ego_planner`加真实里程计到达判据+超时兜底）等用户决定。**2026-09-14重大插曲+订正**：排查中发现`docker-compose.yml`的`LOCALIZATION_SOURCE`默认值是`uwb_slam`（不是这次任务约定的`uwb_imu`），某次`docker compose up -d`重建容器时（为了让重build的flight-stack镜像生效）因为shell里没有手动export这几个变量，悄悄回退成了默认值——之前记录的"起飞后加3秒等待触发`Coord2Index`网格越界+飞到5.75米天花板+`sdk.land()`超时"这几条，**很可能是在错误的`uwb_slam`（FAST-LIO2，这个大房间场景下本来就没验证过、容易自己发散）环境下测的**，已建`docker_sim/.env`固定三个变量（`LOCALIZATION_SOURCE=uwb_imu`/`PLANNER=ego_planner`/`CONTROLLER=pt4ctrl`）避免再次误回退。修复环境后重新隔离验证：`takeoff()`本身完全正常（干净爬升到1.0米附近稳定），单独`goto()`到2米外的点也一次干净到达；但**完整重新跑一次带`TAKEOFF_SETTLE_WAIT_S=3.0`的`mission.py`，D3第一段仍然稳定卡死**（这次日志确认`Coord2Index`/`in obstacle`零命中，排除了那次极端症状是环境问题的假象）——**"假性完成"本身是真实、独立存在的问题，不是环境误配置造成的假象，"起飞后加3秒等待"这个缓解方向没有解决它**，`ego_planner_rebound_replan_arclength_underflow.patch`那次死循环验证发生在环境事故之前，结论不受影响、依然有效。**2026-09-14再插曲+修复**：手动重跑D3第二段`(7,-10)->(7,10)`（穿过`obstacle_cylinder`正中间）时飞机直接撞上障碍物（真实碰撞，不是"假性完成"那种卡住不动），排查确认是`ego_planner`的`grid_map`参数太紧——`grid_map/map_size_x/y`（占据栅格缓冲区）原来42x30米、`grid_map/local_update_range`（局部感知窗口，代码里是半宽）原来15米（等于`fsm/planning_horizon`，没有留余量，upstream官方默认是局部窗口5.5<规划视野7.5，这套配置方向反了）；另外查清楚`grid_map.cpp::cloudCallback()`每帧都会把局部窗口清空重建、只信任"这一帧"点云里的点（不是log-odds概率累积地图），障碍物可能因为LiDAR角分辨率不够、扫描花样凑巧没扫到而在某一帧"闪烁消失"（`docker_real`的`read_hw.md`独立记录过同一个现象）。用户直接给出新参数：缓冲区60x60x5米、局部窗口40x40米（半宽20.0）——`grid_map/map_size_x/y`是`ego_planner_docker_sim.launch.py`硬编码默认值已改成60.0，`grid_map/local_update_range_x/y`改成`.env`里`EGO_LOCAL_UPDATE_RANGE_XY=20.0`固定。**rebuild `flight-stack`镜像后用当初撞机的NX01原样重跑一遍触发过事故的`(7,-10)->(7,10)`场景，这次干净到达，轨迹能看出真实绕障动作，`land()`正常**——1次样本，不是多次重复统计意义上的"稳定修复"，但比之前"100%复现撞机"是质的改善。这次事故顺带发现并绕过了`scenario_reset_node`的`~/reset_scenario`service死锁（改用底层`/plug/set_entity_state`直接瞬移飞机归位），这个死锁本身没有修，只是绕过。

### D4. 双火情响应总控（方案4.3节状态机骨架）

- [x] RECON分支：`search_itinerary`（弓字形地面航段+3根立柱依次绕飞航段），`ground_fire_done`/`hr_fire_done`两个标志位，后台并行监听两种`class_id`的检测（不是分段判断），命中后`sdk.cancel_goto()`打断当前航段、执行对应子流程、恢复剩余航段。
- [x] SUPPLY分支：降落后进入`STANDBY`循环，同时监听`GROUND_FIRE_FOUND`/`HR_FIRE_LOCKED`两种消息，分派到对应子流程。
- [x] 两个标志位都`True`且两机都已降落后，RECON飞回起降点`sdk.land()`。

### D5. 地面火情子流程（方案4.3.1节）

- [x] RECON侧：命中`apriltag:2`后调用`sdk.center_on_target('apriltag:2', timeout=...)`（B3第10条新增，对接A5的`precision_servo_node`模式A`center_only`），阻塞直到节点广播"已居中"并返回收敛时的坐标，读到的坐标就近似等于地面火情的世界坐标，`sdk.send_to_teammate(GROUND_FIRE_FOUND, x=..., y=...)`。
- [x] SUPPLY侧：`sdk.goto(supply_x, supply_y, 巡航AGL高度)`（`supply_x`/`supply_y`是D1里写进`mission_vocab.py`（或程序自己的常量模块）的字面量常量，**不是从`fire_drill_room_layout.yaml`读**——选手程序跑在`contest_sdk`容器里没有`contest_mission`包/这份yaml文件，`layout_loader.py`是`contest_mission`包内部给flight-stack节点用的，选手程序碰不到；z统一用D3那条航线用的巡航AGL高度）→`sdk.precision_land_and_confirm('apriltag:0', timeout=...)`（B3第10条新增，对接A5的`precision_servo_node`模式B`precision_land`，精确对准+下降+真实autoland，阻塞直到确认落地）→`sdk.do_action('grab_supply')`→`sdk.takeoff()`（**重新起飞，落地抓取后必须再起飞才能继续任务，这条链路依赖`CONTROLLER=pt4ctrl`支持反复起降，建议提前在A5验收阶段就单独测过，不要留到这里才发现问题**）→`sdk.goto(消息里的坐标, 巡航AGL高度)`→`sdk.do_action('drop_supply')`（投放不需要落地，悬停触发即可）→回`STANDBY`。

（v3订正：之前这里"复用`precision_servo_node.py`视觉伺服逻辑""需要确认是否已暴露居中接口"这两处标注的疑点，执行前一致性核查已经确认——现有接口确实只支持精降一种用途，缺口属实，已经正式列进阶段A5，不再是"发现后临时处理"的开放问题。）

### D6. 立柱绕飞+高层火情子流程（方案4.3.2节）

- [x] 立柱选择：3个候选坐标用D1新增的字面量常量（不是读yaml）+ D1的确定性编号排序 + "离当前位置最近，同距离取编号小的"选择规则（平局兜底：距离差小于极小阈值时按编号排序取小的那个，方案4.3.2节给了1#/3#到地面火情点距离精确相等的具体例子，实现时可以直接拿这组坐标当单元测试用例）。
- [x] 绕飞航点：`sdk.generate_orbit_waypoints(cx, cy, radius=2.5, z_agl=2.0, num_points=12)`（阶段B3第8条新增的SDK方法）。
- [x] 高层火情命中：`sdk.cancel_goto()`打断绕飞 → `sdk.read_fire_pillar_staging_pose(timeout=...)`（B3第11条新增，取代最初设想的"直接订阅话题"，也取代了实现中间态的几何近似占位函数）拿等待点 → `sdk.send_to_teammate(HR_FIRE_LOCKED, staging_x=..., staging_y=..., staging_yaw=...)`。
- [x] SUPPLY侧：`sdk.goto(等待点)` → `sdk.send_to_teammate(SUPPLY_READY)` → 等`RECON_LAUNCH_DONE`。
- [x] RECON侧：等到`SUPPLY_READY` → `sdk.do_action('horizontal_launch')` → `sdk.read_fire_pillar_aim_pose(timeout=...)`（B3第11条新增，同上取代占位近似）拿自己锁定的瞄准点 → `sdk.send_to_teammate(RECON_LAUNCH_DONE, aim_x=..., aim_y=..., aim_yaw=...)` → 调`fire_pillar_aim_node`的`~/reset_aim`（`sdk.reset_aim()`，B3第10条附带补的薄封装） → 标记`hr_fire_done=True`，恢复`search_itinerary`。
- [x] SUPPLY侧：收到`RECON_LAUNCH_DONE` → `sdk.goto(消息里的瞄准点)` → `sdk.do_action('horizontal_launch')` → `sdk.goto(自己起降点)` → `sdk.land()`。

### D7. 4.4节20个通知点落地

- [x] 不需要单独一步一步做，建议在D3-D6写每一段逻辑的同时，对照方案4.4节的表格，在对应位置顺手加`sdk.set_mission_state(...)`或者事件已经在`send_to_teammate`里天然发出去了的就不用重复加。写完D6之后，回头对着20点表格逐条核对一遍有没有漏掉（尤其是纯"对外通知、不影响任务逻辑"的第5/6/8/9/16/19/20几个`mission_state`点，容易在写代码时顺着逻辑往下写就忘记打）。
- [x] 第3点"避障重规划事件"按方案4.5节结论**不做**（没有干净的现成信号，需要patch `ego_planner`核心代码才能做，不在这次范围）。

---

## 阶段E：联调验证（对应实现方案第5节的验证顺序，逐条来，不要跳步）

- [ ] E1. 双机跑第一部分任务（D3两个分支一起跑）：验证编队跟随实际表现（拐弯处是否抄近道；`uwb_imu`模式下不存在θ*收敛环节，重点看的是UWB定位噪声本身导致的抖动）。
- [ ] E2. 双火情响应阶段单独验证（可以让RECON角色直接从地面火情点附近开始摆位置测试，不用每次都从头飞完整条航线）：弓字形航点+检测命中中断逻辑、跨机消息+ACK协议、立柱选择规则（重点测1#/3#平局情况）、双机投射顺序。
- [ ] E3. **角色对调后重跑一遍**：把`--role`参数在两架物理飞机之间互换，确认行为完全对调、没有任何残留的机身编号硬编码（这是方案1.5节双机对称性原则的最终验证点，务必不要跳过）。
- [ ] E4. 最后串联全流程：从起飞到两个火情都处理完、两机都返航降落，全程跑一遍，同时用`ros2 bag record`记录`mission_state`/`mission_events`话题日志，方便复盘。
- [ ] E5.（v3-e新增）**RC override链路+声光装置各自单独验证一遍，不要等到E4串联时才第一次测**：A4验收标准里的`ros2 topic echo mavros/rc/override`确认+B3第9条`trigger_alarm`的实际触发效果（真的能让GCS那边的装置响应），这两项对硬件依赖比较重，建议提前到E1之前单独跑，避免E4串联失败时分不清是任务逻辑问题还是这两个新硬件接口的问题。
- [x] E6.（v3新增）**"精降落地→抓取→重新起飞"这条链路单独验证一遍，不要等到D5/E4才第一次测**：`CONTROLLER=pt4ctrl`+`LOCALIZATION_SOURCE=uwb_imu`组合下，触发A5的`precision_land`模式落地→确认`armed`变`false`→`sdk.takeoff()`重新起飞，单机测试即可（不需要双机），确认这条"落地后还能再起飞"的链路本身可靠，因为这是方案里第一次真正依赖`pt4ctrl`"支持反复起降"这个特性的地方，建议跟E5一样提前到E1之前单独验证。**2026-09-14/15完成**：过程中连环发现并修复4个bug（阈值比悬停精度还紧、连续下压设计本身不可靠改成分级下降+AUTO_LAND交接、`_set_relay_mode()`阻塞调用死锁、relay_mode接管逻辑顺序bug导致精降完成后卡死），NX01/NX02用纯build出来的镜像（不靠overlay）跑通完整链路：起飞→到物资点→精降抓取(68.69秒)→抓取→重新起飞→飞回起降点降落。顺带按用户提出的场景扩展出`coordinate_land`（给定坐标精降，替代普通goto+land用于精确返航，验证过69.5秒/5.3厘米误差）和`coordinate_goto`（不避障直达版goto，验证过2.5米/9.82秒/0.196米误差，明确只适合短距离最后一段、不能替代goto()跑长距离）两个新模式，详见`DEBUG_JOURNAL.md`2026-09-14/15多条记录。

---

## 已知的、实施过程中大概率会撞到的坑（摘自实现方案第5节风险清单，提前标出来降低意外程度）

1. `uwb_imu`定位方式本身已真机验证，但这套**容器化仿真环境**里还没实测跑过，阶段A/D的第一次实测都要留意这一层wiring问题，不是怀疑定位算法本身。
2. `uwb_imu_fusion_node`z融合层的起降边界阈值（A0提到的`range_min_valid_m`）需要实测调，不要指望一次性猜对（v4订正：不再是"agl_hold模式"，是A0新增的z观测源有效性判断）。
3. 编队跟随精度取决于`uwb_imu`定位本身的噪声水平（v3订正：不经过`origin_setter_node`/θ*，那是`uwb_slam`专属机制，这次任务不适用）。
4. 地面火情坐标精度依赖`precision_servo_node`视觉伺服收敛精度，建议实测残余像素偏移换算成地面距离误差，确认明显小于`mission_judge_node`的1米判定半径。
5. 双检测器并行监听+中断恢复逻辑（D4）容易漏掉"处理A事件时刚好又检测到B事件"这种交错情况，建议单独写单元测试模拟几种到达顺序。
6. `ego_planner`长距离目标容易卡在`GEN_NEW_TRAJ`重规划死循环，D3的分段插值是应对措施不是根治，E1/E4验证时重点观察。
7.（v3-e新增）A4的RC通道/PWM值/持续时长目前是占位值，不是真实硬件映射，实施前务必先跟负责机构接线的人确认，不要拿占位值直接当最终配置用。
8.（v3-e新增）声光装置的GCS侧接口目前不存在，需要跟GCS开发者另外对接才能让B3第9条`trigger_alarm`真正联通，阶段B验收时这一条可能因为GCS侧接口还没ready而暂时卡住，不代表`contest_sdk`这边实现有问题。
9.（v3新增）`CONTROLLER`必须是`pt4ctrl`，不能用默认值`px4ctrl`——`px4ctrl`+`LOCALIZATION_SOURCE=uwb_imu`是README.md记录的禁止组合（2026-08-12实测炸机），开始阶段A实测前先确认启动配置/环境变量里`CONTROLLER`实际生效值。
10.（v3新增）A5新增的"精降到位后触发真正autoland"这段是新代码，且SUPPLY抓取后"重新起飞"这条链路是第一次真正依赖`pt4ctrl`"支持反复起降"这个特性，建议按E6单独提前验证，不要等D5/E4串联时才第一次测。
11.（v3新增）`mission_judge/checkpoints`里的checkpoint实际命名是`nx01_return_pad`/`nx02_return_pad`（带命名空间前缀、小写），如果D3要用这个checkpoint做权威确认，查询时注意拼接`namespace.lower()+'_return_pad'`，不是查一个叫`return_pad`的单一条目。
12.（v4新增，2026-09-13阶段C端到端测试踩过一次坑，但排查后确认不是A0本身的稳定bug）**`sdk.land()`出现过一次卡死超时**，当时怀疑是A0的z冻结逻辑跟PX4的`land_detector`打架，但用完全相同代码复测一次`land()`就正常了（`landed_state`正常走完`ON_GROUND→IN_AIR→ON_GROUND`），说明"z冻结"不是决定性原因，更可能是当次会话大量手动诊断指令留下的残留状态偶然干扰。基于错误推理做的代码改动已撤回，`uwb_imu_fusion_node.py`维持A0原样。真正的偶发卡死根因还没查清楚，详见方案5节风险清单第11条完整记录。**阶段E全流程串联前建议留意`land()`是否偶发卡死，如果再次出现，优先用"干净重启容器+多跑几次"的排除法定位，不要直接假设是A0的问题去改代码。**

---

## 明确不在这份清单范围内的事（跟实现方案第7节"明确排除范围"一致，不要在实施过程中顺手扩大范围）

**真实机构本身的物理动力学仿真**（v3-e起改成走真实`mavros/rc/override`信号链路了，见A4——被排除的只是Gazebo里"机构真的动起来抓取/投放"这个物理效果，不是整个"真实触发"这件事）、**正式阶段7.4/官方SDK的完整实现**（注意不是指本清单阶段B要做的`contest_sdk`——`contest_sdk`是这次任务够用就行的过渡性质SDK，是本清单范围内必须做的东西；被排除的是以后"阶段8"面向所有未来赛题的正式官方SDK，覆盖范围/稳定性要求都更高，是一个独立的、更大的项目，不是这次顺手能做完的）、GCS网页端任务编排UI、双机直连物理链路、SROS2/DDS细粒度访问控制、选择性局部运行时状态复位服务、`ego_planner`避障重规划事件上报（需要patch核心代码，不做）、**声光装置的硬件驱动代码+GCS后端对应接口的具体实现**（B3第9条只定了`contest_sdk`这一侧`trigger_alarm()`该长什么样，GCS机器上具体怎么把请求转成"点亮/发声"是GCS那边的实现，不在这份清单范围内）。
