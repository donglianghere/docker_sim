# docker_sim 对照《无人机装调检修工（飞行器人工智能技术应用方向）》赛项要点适配度评估

评估日期：2026-08-12
评估依据：项目根目录 `CompetitionOverview.pdf`（2026年全国行业职业技能竞赛——第四届全国人工智能应用技术技能大赛，无人机装调检修工/飞行器人工智能技术应用方向，赛项竞赛要点·指导版）
评估范围：`docker_sim` 目录全部内容（README.md、TODO.md、DEBUG_JOURNAL.md、docker-compose.yml、patches/、src/、config/ 等）

> 本文档是从 `DEBUG_JOURNAL.md` 2026-08-12 条目蒸馏出的独立报告，便于直接查阅结论，不用去调试日志里翻记录。历史排查细节仍以 `DEBUG_JOURNAL.md` 为准。

---

## 一、结论先行

**当前系统只覆盖赛项五大任务里"双机协同避障飞行"这一个技术点的仿真验证阶段（且该链路本身还未完全稳定），完全没有覆盖赛项名称里的核心——视觉感知/AI建模这条主线，整体适配度低。**

---

## 二、逐任务比对

赛项实操比赛四个任务权重：任务1 25% / 任务2 20% / 任务3 25% / 任务4 25%，另有任务5 职业素养 5%。

| 赛项任务 | 权重 | 当前覆盖度 | 主要缺口 |
|---|---|---|---|
| 任务1 飞行器智能部件安装测试 | 25% | 部分 | 顶置激光雷达(Mid-360)装调链路完整；**前视相机(d435)因 `realsense_gazebo_plugin` 双机初始化时序崩溃bug被禁用，下视相机完全未实现**（patch注释原话"前视/下视这套需求本来就还没做完"）；身份识别模块、距离传感器（UWB仿真外）、智能作业装置均无任何代码或规划；且全部工作停留在Gazebo仿真里，无对应真实硬件安装/调试记录 |
| 任务2 任务目标信息采集与数据处理 | 20% | **空白** | 无摄像头取流、图像/数据采集、清洗标注、数据集制作、模型训练/部署、识别精度标定、可视化呈现相关代码；全项目搜索"yolo/detect/vision/识别"等关键词只出现在文档/变量名注释里，无实际实现 |
| 任务3 飞行器智能功能集成与验证 | 25% | 较好但未收口 | 路径规划(mighty自研+ego-planner两条路线)、局部避障、双机协同(机间轨迹广播防撞+UWB frame_align坐标对齐)均已实现并做过仿真联调，是投入最多、也最接近赛项要求的部分；但**DLIO SLAM定位链路仍未完全收敛稳定**（最新条目：两架飞机会收敛到一个错误但稳定的固定位置，根因还在排查），依赖真实SLAM（而非Gazebo真值）的组合还不能视为验证通过 |
| 任务4 飞行器系统联调与智能化作业 | 25% | 大部分空白 | 目标识别（依赖任务2，无）、仿地飞行（未见相关功能，当前是固定高度/森林走廊场景飞行）、精准降落（只有MAVROS `AUTO.LAND`，非视觉/标志引导）、环境交互与协同作业（"协同"仅指双机避让飞行，不含抓取/投送）、物资投送（无payload释放机制）均缺失，仅轨迹生成/自主避障可复用任务3成果 |
| 任务5 职业素养与安全规范评价 | 5% | 不直接适用 | 考核的是选手参赛过程行为，与系统代码无直接对应；但项目自身的事故留证机制(`save_incident.sh`)、滚动录制(`record_rosbag.sh`)、健康状态监控(`status_monitor.py`+tmux状态栏)体现的工程规范性，可作为指导选手养成"留痕排查"习惯的教学素材 |

---

## 三、关键差距与建设优先级

按赛项权重排序：

1. **视觉感知与AI建模主线（任务2，20%）**——从0开始建设：先解决前视/下视相机仿真崩溃问题（或改用无该bug的相机插件、直接上真实D435真机测试），再搭建"图像采集→标注→训练→部署→可视化"的最小闭环。
2. **定位链路稳定性（任务3基础，直接决定任务3/4能否通过）**——DLIO收敛问题是当前最紧迫的阻塞项，不解决，后续所有依赖真实SLAM的功能都无法验证。
3. **精准降落/仿地飞行/环境交互/物资投送（任务4大部分子项，25%）**——目前完全空白，需要新增功能而非修bug。
4. **身份识别模块、距离传感器（UWB外）、智能作业装置的选型与集成（任务1剩余部分，25%）**——需要先明确赛项要求的具体设备清单，再针对性开发/采购。
5. **仿真到真机的落地路径**——CLAUDE.md里定的最终目标是移植到Jetson Orin NX，但目前完全没有真实硬件的适配/联调记录；赛项本身要求"实景野外作业环境"操作，仿真验证过的功能仍需要一轮真机复现才能真正算数。

---

## 四、备注

- 本评估基于2026-08-12的项目状态（DLIO收敛问题、θ*旋转对齐NaN修复均标注为"还没有重新部署验证"），后续项目状态变化后建议重新核对 `TODO.md`/`DEBUG_JOURNAL.md` 最新条目再更新本报告。
- 完整排查过程记录见 `DEBUG_JOURNAL.md` 2026-08-12条目"对照《无人机装调检修工（飞行器人工智能技术应用方向）》赛项竞赛要点，评估docker_sim当前适配度"。
