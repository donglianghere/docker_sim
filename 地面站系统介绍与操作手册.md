# 地面站（GCS）系统介绍与操作手册

> 文档版本：2026-08-23 首版
> 适用范围：`docker_sim/gcs/` 目录下的网页地面站（Ground Control Station），覆盖本机仿真联调场景（GCS + sim-world + flight-stack 全在一台开发机）和真机部署场景（GCS 与 1～2 台 Jetson Orin NX 物理分离）。
> 编写依据：`docker_sim/gcs/` 现有代码（`app.py`、`docker-compose.yml`、`entrypoint.sh`、`Dockerfile`、`frontend/index.html`）、`docker_sim/网页端地面站控制面板方案.md`、`docker_sim/真机部署操作清单.md`、`docker_sim/scripts/up_gcs.sh`，以及 `docker_sim/DEBUG_JOURNAL.md` 中与 GCS 相关的调试记录。文中"实际完成情况"均以代码现状为准，方案文档中未落地的部分会明确标注。

---

## 目录

- [第一部分 系统组成与原理](#第一部分-系统组成与原理)
- [第二部分 部署篇](#第二部分-部署篇)
- [第三部分 环境设置篇](#第三部分-环境设置篇)
- [第四部分 命令操作篇](#第四部分-命令操作篇)
- [第五部分 图形界面操作手册](#第五部分-图形界面操作手册)
- [第六部分 常见问题与已知限制](#第六部分-常见问题与已知限制)
- [第七部分 安全边界与权限说明](#第七部分-安全边界与权限说明)
- [附录 A：后端 API 一览表](#附录-a后端-api-一览表)
- [附录 B：常用命令速查](#附录-b常用命令速查)

---

## 第一部分 系统组成与原理

### 1.1 地面站在整个平台中的位置

`docker_sim` 是一套基于 Docker 的双机（未来可扩展多机）无人机飞行仿真系统，核心是三个容器：

| 容器 | 内容 |
|---|---|
| `sim-world` | 经典 Gazebo 仿真环境 + 双机 PX4 v1.16 SITL + UWB 真值节点 |
| `flight-stack-nx01` / `flight-stack-nx02` | 每架飞机一份：DLIO 激光雷达里程计 + 规划器（mighty 或 ego-planner）+ 板外控制器（ros2_px4_stack / px4ctrl / so3ctrl / pt4ctrl） |

真机部署时，`sim-world` 不存在，`flight-stack-hw` 分别跑在每台 Jetson Orin NX 上，飞机与飞机之间、飞机与地面站之间靠 ROS2 DDS 网络通信。

**地面站（GCS）是这套系统的"人机交互层"**：操作员不直接 SSH 进每台设备敲命令，而是通过地面站提供的网页，对整个机队做监控、参数配置、起降控制、容器启停。它本身**不参与飞行控制闭环**（不发送位置/姿态指令），只做三类事：

1. **看**——订阅 DDS 域里的话题，把飞行状态、SLAM 里程计、UWB 定位、健康监控等信息可视化；
2. **点**——把起飞/降落/锁定起飞点这类标准操作，转成一次 ROS2 话题发布或 service 调用；
3. **管**——启停容器、改配置文件、SSH 远程操作 Jetson 这类浏览器原生做不到、必须靠一个有特权的后端代劳的操作。

### 1.2 总体架构：两个容器 + 一个静态前端

地面站自身由两个 Docker 容器组成，浏览器不在容器里，在操作员自己的笔记本/平板上打开：

```
┌─────────────────────────────────────────────────────────┐
│                      操作员浏览器                          │
│        http://<GCS-IP>:8080   （网页地面站界面）            │
└───────────────┬───────────────────────────┬───────────────┘
                 │ WebSocket :9090            │ HTTP :8000
                 │ (rosbridge协议)             │ (REST + Bearer Token)
                 ▼                            ▼
     ┌───────────────────────┐   ┌─────────────────────────────┐
     │   容器 gcs (gcs-gcs-1)  │   │  容器 backend (gcs-backend-1) │
     │  - rosbridge_websocket │   │  - FastAPI (app.py)          │
     │    (:9090)             │   │  - 挂 docker.sock            │
     │  - http.server 静态前端 │   │  - 挂宿主机 ~/.ssh            │
     │    (:8080)             │   │  - 挂 HOST_REPO_PATH(只读)    │
     │  - rviz2（按需启动）     │   └──────────┬────────────────────┘
     │  network_mode: host    │              │ docker.sock / SSH
     └───────────┬─────────────┘              │
                 │ DDS (CycloneDDS/FastDDS)   │
                 ▼                            ▼
     ┌─────────────────────────┐   ┌──────────────────────────┐
     │ 仿真: sim-world +        │   │ 真机: Jetson NX01 / NX02   │
     │ flight-stack-nx01/nx02  │   │ flight-stack-hw (SSH远程)  │
     │ (本机 docker exec)       │   └──────────────────────────┘
     └─────────────────────────┘
```

两个容器分工的原则（见《网页端地面站控制面板方案.md》第二章）：

- **`gcs` 容器**：只需要完整 ROS2 Humble 环境和 DDS 网络访问权限（`network_mode: host`），不需要任何宿主机特权。承担"桥接"角色——把 ROS2 话题/service 转成浏览器能用的 WebSocket JSON（rosbridge 协议），同时用 Python 内置 `http.server` 直接把静态前端文件（`frontend/index.html`）serve 出来，省掉第三个容器。
- **`backend` 容器**：唯一挂载 `/var/run/docker.sock`（等于拿到宿主机 Docker daemon 的完整控制权）和宿主机 `~/.ssh`（免密登录 Jetson）的容器，是这套系统里**权限最集中**的一点。只负责浏览器/rosbridge 做不到的几件事——本质上都是"改一个只在启动/运行时读一次的东西 + 重启/启停相关容器或进程"：
  1. 改地面站自己的 CycloneDDS 网络配置并重启 `gcs` 容器；
  2. SSH 到 Jetson 改真机 flight-stack 的规划/避障调参白名单，并远程重建容器；
  3. 本地仿真机队 / 远程真机机队的启停；
  4. 起飞（含自动标定动作）——因为标定脚本必须在 flight-stack 容器内部用 `docker exec` 执行，浏览器做不到；
  5. 诊断信息：容器环境变量、原生 SLAM 频率、资源消耗（`docker stats` / `nvidia-smi`）；
  6. 按需启动 Gazebo/RViz2 图形界面窗口。

起降/降落/锁定起飞点这三个最高频的飞行操作，**不经过 backend**，前端直接用 `roslib.js` 对 `gcs` 容器的 rosbridge（9090 端口）发起调用——这是 2026-08-20 落地时对原方案的简化：既然 rosbridge 本身就能从浏览器直接发话题/调 service，就不必再绕一层后端转发。

### 1.3 容器 A（`gcs`）详解

镜像来源：`gcs/Dockerfile`，基础镜像 `ros:humble-ros-base-jammy`。因为宿主机（Ubuntu 24.04）没有 ROS2 Humble 的官方 apt 包（Humble 只对应 22.04），地面站本身也要跑在一个容器里，但这个镜像**只装 ROS2 底座 + GCS 需要跨机订阅的自定义消息定义包**（`quadrotor_msgs`、`traj_utils`、`decomp_util`/`decomp_ros_msgs`/`decomp_rviz_plugins`），不编译 DLIO/mighty/ego_planner/px4ctrl 这些算法实现——GCS 只需要能解析消息类型、加入 DDS 域看话题/调 service，不需要真的跑这些算法节点。

容器内长期运行两个进程（由 `entrypoint.sh` 拉起）：

1. `ros2 launch rosbridge_server rosbridge_websocket_launch.xml port:=9090` —— 把 ROS2 话题/service/参数转成 WebSocket JSON；
2. `python3 -m http.server 8080 --directory /opt/frontend` —— serve 静态前端。

`network_mode: host` 下这两个端口就是宿主机端口，不需要额外的 `-p` 映射。容器 `CMD` 是 `sleep infinity`，两个后台进程作为子进程存活，日常可以 `docker exec -it docker_sim-gcs-1 bash` 进去用 `ros2` CLI 排查，其中一个进程崩溃不会导致容器退出。

镜像里还装了 `ros-humble-rviz2` 和两份预置的 RViz 配置（`gcs/rviz/multi_mighty.rviz`、`gcs/rviz/multi_ego_planner.rviz`），配合 X11 转发（`DISPLAY` 环境变量 + `/tmp/.X11-unix` 挂载）可以直接在这个容器里弹出 RViz 可视化窗口，见第五部分。

**RMW（DDS 中间件实现）联动切换**是 `entrypoint.sh` 里最关键的一段逻辑：`PLANNER=ego_planner` 时切到 `rmw_cyclonedds_cpp`（读取 `cyclonedds_gcs.xml`），否则（`PLANNER=mighty`）保持默认 FastDDS。这条规则必须和 `flight-stack`/`sim-world` 两边的 entrypoint 保持一致——三处只要有一处没跟着联动，GCS 和飞机就会用不同的 DDS 实现，互相发现不了，GCS 什么都监控不到。

### 1.4 容器 B（`backend`）详解

镜像来源：`gcs/backend/Dockerfile`，基础镜像 `python:3.11-slim`（不需要 ROS2，不发布/订阅任何话题，保持轻量）。除 Python 依赖外只装了三样东西：`openssh-client`（SSH 到 Jetson 用）、Docker CLI 静态二进制（对挂进来的 `docker.sock` 发命令）、`docker compose` CLI 插件（v2，动态传参重建容器用）。

后端（`app.py`）按功能分六大块，与前端页面的六个折叠面板一一对应：

| 功能块 | 关键接口 | 作用 |
|---|---|---|
| 1. GCS 自身网络配置 | `GET/POST /api/gcs-network` | 改本机 `cyclonedds_gcs.xml` 的网卡 IP / 对端 IP，改完自动 `docker restart` 让配置生效 |
| 2. 真机调参白名单 | `GET/POST /api/hw-params/{ns}` | SSH 改 Jetson 上的 `.env`（仅限规划/避障调参 8 项白名单），远程 `docker compose -f docker-compose.hw.yml up -d` 重建容器生效 |
| 3. 机队启停 | `GET/POST /api/fleet/sim/*`、`GET/POST /api/fleet/hw/{ns}/*`、`POST /api/gcs/stop` | 仿真三容器/真机 flight-stack-hw/GCS 自己的启停 |
| 4. 起飞（含自动标定） | `POST /api/sim/{ns}/takeoff` | `docker exec` 到 flight-stack 容器执行起飞动作 + 自动标定飞行脚本，仅限仿真场景 |
| 5. 诊断信息 | `GET /api/sim/{ns}/env`、`GET /api/sim/{ns}/slam-hz`、`GET /api/container-stats`、`POST /api/clear-logs` | 容器环境变量、原生 SLAM 发布频率、CPU/内存/网络/GPU 占用、清空日志 |
| 6. GUI 窗口按需启动 | `POST /api/sim/gazebo-gui`、`POST /api/gcs/rviz` | 在宿主机 X server 上后台起 `gzclient`/`rviz2` |

完整接口清单见[附录 A](#附录-a后端-api-一览表)。

**鉴权机制**：所有 `/api/*` 路径要求请求头 `Authorization: Bearer <GCS_BACKEND_TOKEN>`，共享密钥形式（不是登录系统，符合"单/少数操作员内部工具"定位）。`GCS_BACKEND_TOKEN` 环境变量为空字符串时鉴权退化成不校验——这不是"更安全的默认值"，只是保留本地纯仿真调试时不想每次都带鉴权头的退路；`gcs/docker-compose.yml` 里已经给了一个非空默认值，正常走 compose 起容器不会落到这个分支。`/healthz` 端点和所有 `OPTIONS` 预检请求豁免鉴权。Token 只存在浏览器本机 `localStorage`，不经过任何服务端 session。

**安全边界（务必知悉）**：`backend` 容器同时挂了宿主机 `/var/run/docker.sock`（等于拿到宿主机 Docker daemon 完整控制权）和宿主机 `~/.ssh`（能免密登录 Jetson），是这套系统里权限最集中的一点。当前实现**没有 TLS**，Token 以明文形式过网络，共享密钥能防"同一局域网里凑巧扫到这个端口的人随手一点"，**防不住真正的中间人/恶意内网设备**。这套系统的部署前提是**受信任的本地/内网环境**——如果以后要暴露到不受信任网络，必须先上 HTTPS，不能原样部署。

### 1.5 前端（`frontend/index.html`）详解

前端是一个**不需要构建步骤的静态 HTML**（不是 Vue/React SPA），内联 JavaScript，通过 `<script src="roslib.min.js">` 引入 rosbridge 官方 JS 客户端（build 阶段就 curl 进镜像，不依赖运行时 CDN——现场大概率没有公网出口）。

**核心设计原则：动态发现，不硬编码场景**。前端不写"如果是双机就……"这类场景分支，只做一件事：订阅 rosbridge 自带的 `rosapi`（`/rosapi/topics`），正则提取形如 `/NX01/mavros/state` 中的命名空间 `NX01`，看到几个命名空间就渲染几张飞机卡片。这条原则天然覆盖仿真单机/双机、真机单机/双机全部场景，以后扩展到 3 机、4 机也不用改前端代码。

页面整体分左右两栏：

- **左栏**：品牌头部 + 连接状态 + 六个功能折叠面板入口（顶部链接栏）+ 每架飞机的三张状态表格（飞行参数 / 空间对齐 / 子系统健康状态）+ 每架飞机的操作按钮（锁定起飞点 / 起飞 / 降落）。
- **右栏**：三个标签页——规划地图（俯视示意图 + 点击发目标点 + 航线规划）、俯视相机、前视相机。

前端所有信息来源分两类：

1. **实时话题订阅**（不经过 backend，直连 `gcs` 容器的 rosbridge :9090）：`mavros/state`、`dlio/odom_node/odom`、`uwb/pose_abs`、点云频率、姿态推力、起飞点标定的 θ* 在线估计、规划器心跳、航点队列进度、`/tf` 与 `/tf_static`（俯视地图用）、当前规划轨迹等；
2. **按需查询**（走 backend :8000，需要鉴权）：容器环境变量、原生 SLAM 频率、资源消耗、机队/GCS 主机启停状态。

详细的界面元素与操作步骤见[第五部分](#第五部分-图形界面操作手册)。

### 1.6 网络原理：DDS、CycloneDDS 与"两条独立的轴"

ROS2 底层通信基于 DDS（Data Distribution Service），本项目里飞机之间、飞机与地面站之间要能"发现"彼此、正确收发消息，取决于三件事：

1. **RMW 实现一致**：要么都用默认 FastDDS，要么都显式切到 `rmw_cyclonedds_cpp`——不能一边 FastDDS 一边 CycloneDDS，互相发现不了。本项目按 `PLANNER` 环境变量联动决定（`ego_planner` → CycloneDDS，`mighty` → 默认 FastDDS），`sim-world`/`flight-stack`/`gcs` 三处 entrypoint 必须同步。
2. **`ROS_DOMAIN_ID` 一致**：GCS 侧固定为 `20`，必须和 `docker-compose.hw.yml` 里真机配的值一致。
3. **CycloneDDS 网络接口/对端配置正确**（仅当切到 CycloneDDS 时才生效）：`cyclonedds_gcs.xml` 里的 `NetworkInterface address` 决定本机用哪张网卡收发，`Peer address` 是静态对端发现配置。

方案文档提出的核心设计原则是**"几台物理机"和"几架飞机"是两条独立的轴**，只在真机场景下才会重合：

| 轴 | 决定什么 | 仿真场景 | 真机场景 |
|---|---|---|---|
| **网络拓扑轴**（几台物理机） | CycloneDDS 配置（本机回环 / 静态 Peer / Discovery Server） | 永远 1 台机器，配置不变 | 单机 = 2 台机器（GCS + 1 Jetson），双机 = 3 台机器，配置要跟着变 |
| **机队规模轴**（几架飞机） | `docker compose` 起哪些服务 + `NUM_AGENTS`/`LOCALIZATION_SOURCE` | 单机只起 `sim-world`+`flight-stack-nx01`；双机三个都起 | 单机 1 台 Jetson 跑 `single_uwb_slam`；双机 2 台 Jetson 各跑 `uwb_slam`+`NUM_AGENTS=2` |

这也是为什么**本机仿真联调**（`scripts/up_gcs.sh`）和**真机部署**（`scripts/up_gcs.sh`）对 `cyclonedds_gcs.xml` 的处理完全不同：前者会自动把 `NetworkInterface` 改成回环地址 `127.0.0.1`（同机联调，用真实网卡会出现"能发现对方、但数据包送不到"的静默丢包），后者绝不会自动改这个文件（真机场景是站点专属的真实网卡 IP，改错直接连不上现场设备），只做格式检查。

### 1.7 与已有仿真操作台（`launch_control.py`/`watch_sim.sh`）的关系

在网页地面站之前，`docker_sim/scripts/` 下已有一套 tmux + curses + `docker exec` 的操作台（`launch_control.py`、`watch_sim.sh`、`status_monitor.py` 等）。这套工具对"仿真里 3 个容器全在一台机器上"这个假设依赖很深，真机分布式部署后大部分功能会失效。网页地面站的目标就是**替代它成为统一的操作入口**，同时兼顾仿真调试和真机部署两种场景。目前两套工具并存，仿真本地调试仍可使用 `launch_control.py`，但真机场景只能用网页地面站。

---

## 第二部分 部署篇

### 2.1 前置条件

**软件**：
- 宿主机 Docker + Docker Compose v2（`docker compose version` 确认）
- 图形桌面环境（如需使用 Gazebo GUI / RViz2 按钮，需要本机有 X server；纯远程真机监控不需要）
- 已完成 `docker_sim/README.md` 描述的主系统部署（`fetch_sources.sh` 拉取源码、构建 `sim-world`/`flight-stack` 镜像），本机仿真联调场景依赖这些容器已能正常起飞

**网络**（真机部署场景）：
- GCS 与各 Jetson 处于可互通的网段（同网段最简单；跨网段需要额外的路由/VPN 配置，不在本手册范围）
- 已按《真机部署操作清单.md》阶段 0 完成 SSH 免密登录配置（见 [3.3 节](#33-ssh-免密登录配置nx01nx02)）

**代理**（如宿主机需要代理才能访问 Docker Hub / apt 源）：构建镜像前 `export http_proxy=...; export https_proxy=...`，两个 Dockerfile 都已配置 `build.network: host` 让 `127.0.0.1` 代理在构建阶段能被正确访问。

### 2.2 目录结构

```
docker_sim/gcs/
├── Dockerfile                    # 容器A(gcs)镜像：ROS2底座+rosbridge+rviz2+自定义消息包
├── docker-compose.yml            # gcs + backend 两个服务
├── entrypoint.sh                 # 容器A entrypoint：起rosbridge(9090)+静态前端(8080)
├── .env                          # GCS_BACKEND_TOKEN / HOST_REPO_PATH（本机专属，需按需修改）
├── cyclonedds_gcs.xml.example    # CycloneDDS配置模板（占位符IP）
├── cyclonedds_gcs.xml            # 实际生效配置，含站点专属IP，已加入.gitignore不进git
├── frontend/
│   ├── index.html                # 前端主文件（唯一功能代码文件）
│   └── konghe_logo.png
├── backend/
│   ├── Dockerfile                # 容器B(backend)镜像
│   ├── app.py                    # FastAPI后端主文件
│   └── requirements.txt
└── rviz/
    ├── multi_mighty.rviz         # PLANNER=mighty时用
    ├── multi_ego_planner.rviz    # PLANNER=ego_planner时用
    └── README.md
```

配套的一键脚本在仓库根目录 `docker_sim/scripts/` 下：`up_gcs.sh`（本机仿真联调）、`up_gcs.sh`（真机部署）。

### 2.3 镜像构建

> ⚠️ 按项目约定，**镜像构建操作由用户手动执行**，不由自动化流程代为执行。以下命令供操作员在宿主机终端手动运行。

```bash
cd docker_sim
docker compose build sim-world           # 如仿真容器尚未构建，先构建主系统
docker compose build flight-stack-nx01

cd gcs
docker compose build                     # 构建 gcs + backend 两个镜像
```

`gcs/Dockerfile` 构建耗时较长（`rviz2` 会拉入完整 Qt5 + VTK + assimp/ogre-vendor 依赖链，约 500MB/600+ 包），已拆成多个 `RUN` 层并加了 `-o Acquire::Retries=3`，网络抖动时通常只需重试最后一层，不必整层重来。

### 2.4 本机仿真联调部署

适用场景：GCS + `sim-world` + `flight-stack-nx01`/`nx02` 全部跑在同一台开发机，用于日常调试网页地面站功能，不涉及真实 Jetson。

**一键方式**（推荐）：

```bash
cd docker_sim
./scripts/up_gcs.sh
```

该脚本按顺序自动完成：

1. `xhost +local:root` 授权，让容器内以 root 身份运行的 `gzclient`/`rviz2` 能连上宿主机 X server；
2. `docker compose up -d` 起 `sim-world` + `flight-stack-nx01`/`nx02`；
3. 轮询等待两架飞机的 PX4 都打印 `Ready for takeoff`（最长等 180 秒）；
4. 起（或确认）`gcs` + `backend` 两个容器（若 `cyclonedds_gcs.xml` 不存在会自动从 `.example` 复制一份）；
5. 把 `cyclonedds_gcs.xml` 的 `NetworkInterface address` 改成本机联调专用的 `127.0.0.1`（若尚未是该值），并重启 `gcs` 容器应用新配置；
6. 用 `ros2 topic list` 验证 rosbridge 真的连上仿真（最长等 20 秒）；
7. 打印浏览器地址和后端鉴权 Token。

脚本跑完后按提示打开 `http://localhost:8080`，并在页面"🔑 后端鉴权Token"面板填入脚本打印的 Token。

**手动方式**（理解每一步在做什么，或脚本失败时排查用）：

```bash
cd docker_sim
xhost +local:root                          # GUI窗口用，无图形桌面可跳过
docker compose up -d                       # 起主系统三容器

cd gcs
[ -f cyclonedds_gcs.xml ] || cp cyclonedds_gcs.xml.example cyclonedds_gcs.xml
docker compose up -d                       # 起 gcs + backend

# 手动确认/修改 NetworkInterface address 为 127.0.0.1（本机联调专用）
# 编辑 cyclonedds_gcs.xml 后：
docker restart gcs-gcs-1

# 验证：
docker exec gcs-gcs-1 bash -c \
  "source /opt/ros/humble/setup.bash; export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp CYCLONEDDS_URI=file:///opt/cyclonedds_gcs.xml; ros2 topic list | grep NX01"
```

### 2.5 真机部署

适用场景：GCS 跑在开发机/固定值守机上，`flight-stack-hw` 跑在物理分离的 Jetson Orin NX 上（1 台或 2 台）。前提：

- 已完成《真机部署操作清单.md》阶段 0（SSH 免密登录、`~/.ssh/config` 别名 `nx01`/`nx02`）；
- `gcs/cyclonedds_gcs.xml` 已手动配置为**真实网卡 IP**（不是本机联调用的 `127.0.0.1` 或占位符），见 [3.5 节](#35-cyclonedds-网络配置)。

**一键方式**：

```bash
cd docker_sim
./scripts/up_gcs.sh
```

该脚本会：

1. 检查 `cyclonedds_gcs.xml` 是否已从占位符/回环地址改成看起来像真实 IP（未改则直接报错退出，不会帮你自动改——改错真机 IP 风险太大）；
2. 起（或确认）`gcs` + `backend` 两个容器，并**强制 `docker restart gcs-gcs-1`**（compose 不会因为 bind mount 的文件内容变了就自动重启容器，必须显式重启才能保证用的是当前文件内容）；
3. 验证 GCS 自身（rosbridge + backend）没有崩溃（**不验证飞机话题**——此时 Jetson 大概率还没启动，这是正常状态）；
4. 打印浏览器地址和后端鉴权 Token。

脚本跑完后，接下来在网页"📡 远程真机"面板对每台 Jetson 点"启动"（会 SSH 远程执行 `docker compose -f docker-compose.hw.yml up -d`），详见[第五部分](#54-远程真机面板)。

**与本机联调脚本的关键区别**：

| | `up_gcs.sh` | `up_gcs.sh` |
|---|---|---|
| 是否会自动修改 `cyclonedds_gcs.xml` | 会（改成 `127.0.0.1`） | **不会**，只做格式检查 |
| 是否本地起 `sim-world`/`flight-stack` | 会 | 不会（飞控栈在 Jetson 上，由网页远程启停） |
| 验证内容 | 飞机话题（`/NX01/mavros/state` 等） | 仅 GCS 自身进程存活 |

---

## 第三部分 环境设置篇

### 3.1 `GCS_BACKEND_TOKEN`（后端鉴权令牌）

定义位置：`gcs/.env`。

```bash
GCS_BACKEND_TOKEN=gcs-lan-only-change-me
```

**⚠️ 这不是一个"更安全的默认值"，只是把之前一直在用的默认值固定下来，避免每次重建容器时悄悄漂回 compose 文件里的默认值**（曾经因为没显式设置、容器每次重建都换默认 Token，浏览器保存的旧 Token 全部 401，排查成本很高）。正式/长期部署前应替换为自己生成的随机串：

```bash
# 生成一个随机 Token 的示例
openssl rand -hex 16
```

改完 `gcs/.env` 后需要 `cd gcs && docker compose up -d` 让 backend 容器读取新值，并在网页"🔑 后端鉴权Token"面板里同步填入新值保存。

### 3.2 `HOST_REPO_PATH`

定义位置：`gcs/.env`。

```bash
HOST_REPO_PATH=/home/robots/ai_uav/docker_sim
```

**含义**：`docker_sim` 仓库根目录在**宿主机**上的绝对路径。backend 容器里执行的 `docker compose up -d`（本地仿真机队启动用）实际是通过挂进来的 `docker.sock` 指挥**宿主机**的 dockerd 干活——dockerd 创建新容器时 bind mount 的源路径必须是它自己（宿主机）认识的真实路径，不能是 backend 容器自己文件系统里的路径，所以必须显式告诉它宿主机侧的绝对路径，没法自动探测。**换一台机器部署，这个值必须跟着改成那台机器上的实际路径**，否则"本地仿真"面板的启动会失败或挂载错位。

### 3.3 SSH 免密登录配置（nx01/nx02）

真机场景下，backend 容器通过复用宿主机 `~/.ssh`（只读挂载）来 SSH 到 Jetson，**不新增一套凭证管理**。配置步骤（详见《真机部署操作清单.md》阶段 0）：

```bash
# 1. 本机（GCS）确认/生成 SSH key
ssh-keygen -t ed25519   # 已有 key 可跳过

# 2. 拷贝公钥到每台 Jetson
ssh-copy-id <jetson用户名>@<Jetson IP>       # NX01
ssh-copy-id <jetson用户名>@<另一台Jetson IP>  # NX02

# 3. 在 ~/.ssh/config 里配别名，别名必须精确是 nx01 / nx02（app.py 里写死）
cat >> ~/.ssh/config <<'EOF'
Host nx01
    HostName <NX01的IP>
    User <jetson用户名>

Host nx02
    HostName <NX02的IP>
    User <jetson用户名>
EOF

# 4. 验证免密登录
ssh nx01 echo ok
ssh nx02 echo ok
```

配置完成后，backend 容器内部的 `ssh nx01 "<远程命令>"` 才能正常工作——这是"真机调参白名单"（3.6节的姊妹功能，见 [4.4 节](#44-ssh-远程操作)）和"远程真机机队启停"（[5.4 节](#54-远程真机面板)）两大功能的前提。

### 3.4 X11 / xhost 授权（GUI 窗口用）

只有需要使用"启动Gazebo图形界面"或"启动RViz"两个按钮时才需要（纯监控/纯真机场景可跳过）：

```bash
xhost +local:root
```

**原理**：容器里以 root 身份运行的 `gzclient`/`rviz2` 属于与宿主机不同的 X client，X server 默认按 UID 做访问控制（`SO_PEERCRED`），必须显式授权才能连接。`xhost` 的 ACL 存活于当前图形会话内，`docker restart`/重新 `up` 容器不会清掉，只有注销/重启宿主机图形会话才需要重新执行。`up_gcs.sh` 已经把这一步自动包含在内，日常联调无需单独执行；若跳过图形界面按钮想临时手动补授权，随时可以单独执行上面这条命令。

### 3.5 CycloneDDS 网络配置

模板文件：`gcs/cyclonedds_gcs.xml.example`；实际生效文件：`gcs/cyclonedds_gcs.xml`（含站点专属 IP，已加入 `.gitignore`，不会被提交，需要每次部署手动准备）。

```bash
cd gcs
cp cyclonedds_gcs.xml.example cyclonedds_gcs.xml
```

需要改的两处：

```xml
<NetworkInterface autodetermine="false" address="127.0.0.1" .../>   <!-- 1. 本机(GCS)实际用来跟Jetson通信的网卡IP -->
<!-- <Peer address="..."/>  -->                                      <!-- 2. Jetson的静态IP（需要先取消注释） -->
```

1. **`NetworkInterface address`**：本机（GCS）实际用来跟 Jetson 通信的网卡 IP，用 `ip a` 查询确认。
   - **本机仿真联调场景**：必须填 `127.0.0.1`（回环地址）。注意 CycloneDDS 的这个字段要求填 IP，不认接口名——早期记录里"改成 `lo`"的做法实测会直接报 `does not match an available interface` 崩溃，已更正为填回环 IP。
   - **真机部署场景**：填 GCS 本机与 Jetson 直连/同网段的那张网卡的真实 IP。
2. **`Peer address`**（Discovery 段，默认注释掉）：真机部署时需要取消注释并填 Jetson 的静态 IP——这是"反过来"的关系：GCS 侧填 Jetson 的 IP，Jetson 侧（`docker-compose.hw.yml` 的 `GCS_PEER_ADDRESS`）填 GCS 的 IP，**双向都要配，只配一边发现不了对方**。

改完文件后必须 `docker restart gcs-gcs-1` 才会生效（compose 不会因为 bind mount 文件内容变化自动重启容器）。网页"⚙ GCS网络设置"面板本质就是这一段操作的可视化版本，见 [5.2 节](#52-gcs-网络设置面板)。

单机阶段（GCS + 1 台 Jetson）用静态 Peer 配置已经足够；双机阶段（GCS + 2 台 Jetson）建议升级成 CycloneDDS Discovery Server（把"两两互相配对"简化成"大家都连一个中心"），目前该项在方案文档里标注为待实施。

### 3.6 `PLANNER` 环境变量与 RMW 联动

真机部署时，`gcs` 容器的 `PLANNER` 环境变量**必须跟真机 `flight-stack-hw` 那边实际用的值一致**（不能只依赖默认值 `ego_planner`），因为它决定 GCS 侧走 CycloneDDS 还是默认 FastDDS：

```bash
PLANNER=mighty docker compose up -d      # 真机跑 mighty 规划器时，GCS也要带这个变量起
```

两边不一致会导致 DDS 互不发现，GCS 页面永远显示"暂时没发现任何飞机命名空间"。

### 3.7 真机调参白名单（8 项）

backend 通过网页"每架飞机调参面板"（见 5.3 节飞行参数表格旁的调参入口）暴露的白名单参数，只包含"调坏了最多飞得不好看、不会导致失控"的规划器/动力学参数：

| 参数 | 默认值 | 含义 |
|---|---|---|
| `V_MAX` | 1.0 | 最大速度 |
| `A_MAX` | 3.0 | 最大加速度 |
| `J_MAX` | 5.0 | 最大加加速度 |
| `OMEGA_MAX` | 0.10472 | 最大角速度 |
| `GOAL_RADIUS` | 0.3 | 到达目标点判定半径 |
| `GOAL_SEEN_RADIUS` | 3.0 | 目标点可见判定半径 |
| `EGO_OBSTACLES_INFLATION` | 0.6 | ego-planner 障碍物膨胀半径 |
| `EGO_SWARM_CLEARANCE` | 1.0 | ego-planner 机间安全间距 |

**明确排除**（这几类要改必须走 SSH 手动改 `.env`，不做成网页一键操作）：

- `PLANNER`/`CONTROLLER`/`LOCALIZATION_SOURCE`/`SLAM_BACKEND` 这类模式选择——切到没验证过的代码路径风险不可控；
- `NAMESPACE`/`AGENT_INDEX`/`NUM_AGENTS` 这类机身份标识——改错会导致双机串话/命名冲突；
- `FC_SERIAL_DEVICE`/`UWB_SERIAL_DEVICE`/`HW_NETWORK_INTERFACE` 这类硬件接线参数——改错直接连不上飞控/UWB 模块。

---

## 第四部分 命令操作篇

### 4.1 一键脚本

| 命令 | 场景 |
|---|---|
| `./scripts/up_gcs.sh` | 本机仿真联调（GCS+sim-world+flight-stack全在一台机器） |
| `./scripts/up_gcs.sh` | 真机部署（GCS 单独起，飞控栈在 Jetson 上远程启停） |

两者互不通用，混着跑会导致 `cyclonedds_gcs.xml` 被改错场景对应的值，务必根据当前场景选择对应脚本。

### 4.2 `docker compose` 常用命令

在 `docker_sim/gcs/` 目录下执行：

```bash
docker compose build                  # 构建 gcs + backend 镜像（需手动执行，不自动build）
docker compose up -d                  # 起两个容器
docker compose down                   # 停止并删除两个容器
docker compose logs -f gcs            # 跟踪 gcs 容器日志（rosbridge崩溃时看这里）
docker compose logs -f backend        # 跟踪 backend 容器日志
docker restart gcs-gcs-1              # 改完 cyclonedds_gcs.xml 后必须手动重启才生效
docker exec -it docker_sim-gcs-1 bash # 进 gcs 容器手动跑 ros2 CLI
```

在 `docker_sim/`（根目录）下执行，控制仿真主系统三容器：

```bash
docker compose up -d                          # 起 sim-world + flight-stack-nx01/nx02
docker compose down                           # 停止并删除三容器
docker start docker_sim-sim-world-1 \
  docker_sim-flight-stack-nx01-1 \
  docker_sim-flight-stack-nx02-1              # 仅启动已建好的容器（网页"本地仿真"面板走这条路径）
docker stop docker_sim-sim-world-1 \
  docker_sim-flight-stack-nx01-1 \
  docker_sim-flight-stack-nx02-1
```

### 4.3 `ros2` CLI 手动操作（网页按钮的等价命令）

以下命令是网页"起飞"/"降落"/"锁定起飞点"按钮实际发出的操作，跨机 DDS 打通后可以在 GCS 本机直接执行（`docker exec -it docker_sim-gcs-1 bash` 进容器后跑），不需要 SSH：

```bash
# 起飞点一键锁定（起飞前，飞机静止不动时执行一次）
ros2 service call /NX01/set_origin_from_uwb std_srvs/srv/Trigger

# 起飞 / 降落（长驻订阅，可反复触发；仅对 REPEATABLE_CONTROLLERS即px4ctrl/so3ctrl/pt4ctrl有效）
ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 1}"   # 起飞
ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 2}"   # 降落

# CONTROLLER=ros2_px4_stack 时（只支持起飞一次），改用 touch 起飞门
docker exec docker_sim-flight-stack-nx01-1 touch /tmp/takeoff_go

# 查看当前话题列表（排查DDS是否互通）
ros2 topic list

# 查看原生SLAM发布频率（网页"SLAM频率(真实)"列的等价命令）
ros2 topic hz /NX01/dlio/odom_node/odom --window 30
```

### 4.4 SSH 远程操作

```bash
ssh nx01 echo ok                                           # 验证免密登录
ssh nx01 "cat ~/docker_sim/.env"                            # 查看Jetson当前.env
ssh nx01 "cd ~/docker_sim && docker compose -f docker-compose.hw.yml up -d"    # 启动真机flight-stack
ssh nx01 "cd ~/docker_sim && docker compose -f docker-compose.hw.yml down"     # 停止真机flight-stack
ssh nx01 "cd ~/docker_sim && docker compose -f docker-compose.hw.yml ps --status running --services"  # 查询运行状态
```

### 4.5 故障排查命令速查

```bash
curl localhost:8000/healthz                       # backend是否存活，返回 {"status":"ok"}
docker logs gcs-gcs-1 --since <时间戳>              # 查gcs容器日志（--since过滤重启前旧日志，避免误判）
docker logs gcs-backend-1                          # 查backend容器日志
docker exec gcs-backend-1 printenv GCS_BACKEND_TOKEN  # 查当前生效的鉴权Token
docker inspect --format '{{.State.Status}}' docker_sim-sim-world-1   # 查某容器运行状态
docker stats --no-stream                           # 查容器CPU/内存/网络占用
docker exec docker_sim-sim-world-1 nvidia-smi       # 查GPU占用（仅sim-world容器装了显卡直通）
```

---

## 第五部分 图形界面操作手册

打开浏览器访问 `http://<GCS-IP>:8080`（本机联调即 `http://localhost:8080`）。首次打开需要先在"🔑 后端鉴权Token"面板填入 Token（见 [3.1 节](#31-gcs_backend_token后端鉴权令牌)），否则涉及 backend 的功能会全部返回 401。

### 5.1 页面整体布局

页面分左右两栏（中间有可拖动的分隔条 `dragHandle`，可调整左右宽度比例）：

- **左栏**（自上而下）：品牌头部（Logo + 系统名）→ 连接状态提示 →（如无飞机被发现，显示"DDS域里暂时没发现任何飞机命名空间"提示）→ 每架飞机的操作控制区 → 三张状态表格 → 底部一排功能链接（7 个）及其对应的可展开配置面板。
- **右栏**：三个标签页——🗺️ 规划地图（默认激活）、📷 俯视相机、🎥 前视相机。

### 5.2 顶部/底部功能链接栏（7 项）

页面底部一排可点击链接，点击展开/收起对应的折叠面板（再点一次收起；展开时链接本身会高亮标出"当前已展开"）：

#### 5.2.1 🔑 后端鉴权Token

- **作用**：设置调用 backend 时携带的身份令牌，必须与 backend 容器的 `GCS_BACKEND_TOKEN` 环境变量一致。
- **操作**：点击展开 → 输入框填入 Token → 点击"保存"。
- **说明**：只存在本机浏览器 `localStorage`，不上传到任何地方，换一台浏览器/清缓存需要重新填。**不影响**起飞/降落/锁定起飞点——那几个直接连 rosbridge，不经过这层鉴权。

#### 5.2.2 ⚙ GCS网络设置

- **作用**：配置 GCS 这台机器自己用哪张网卡 IP、跟哪个 Jetson IP 互相发现，即第三部分 3.5 节介绍的 `cyclonedds_gcs.xml`。这是 DDS 网络层设置，管的是"GCS 能不能收到飞机的话题"，不是发给飞机的指令。
- **操作**：展开面板会自动加载当前配置到"本机网卡IP"和"Jetson对端IP"两个输入框 → 修改后点击"保存并重启GCS容器"。
- **注意**：保存后会重启 `gcs-gcs-1` 容器，网页会短暂断开重连（属正常现象，rosbridge 断线重连逻辑会自动处理，见 [6 节](#第六部分-常见问题与已知限制)）。**本机仿真联调不需要碰这个**，只有真机部署才需要。若"Jetson对端IP"填了值但文件里 Peer 段当前是注释状态，会报错提示先手动编辑文件取消注释，或把该输入框留空跳过。

#### 5.2.3 💻 GCS主机

- **作用**：关闭地面站自己（`gcs-gcs-1` 前端+rosbridge、`gcs-backend-1` 后端自己）。
- **操作**：展开面板 → 点击"关闭 GCS"（红色危险按钮）。
- **重要限制**：**只有停止，没有启动按钮**——一旦两个容器都停了，就不会再有任何进程响应浏览器发来的请求，网页上"关完还能再点一次开"这件事本身无法实现。重新拉起只能回到宿主机终端手动 `docker start gcs-gcs-1 gcs-backend-1`，或重新执行 `up_gcs.sh`。**不影响**仿真/真机那边正在跑的飞控软件——只关地面站自己。

#### 5.2.4 🖥️ 本地仿真面板

- **作用**：本机仿真软件栈（`sim-world` + 双机 `flight-stack` 三个容器）的开关机。
- **区别提醒**：这里管的是"软件/容器有没有在跑"，跟下面表格里的"起飞/降落"按钮是完全不同的两件事——**必须先在这里把容器启动起来，飞机才会出现在状态表格里，才能点起飞**。
- **操作**：
  1. 展开面板，可从下拉框选择三个启动参数（不选则沿用当前默认值）：
     - **定位来源**：`gt`（Gazebo真值）/ `uwb_slam`（真实DLIO SLAM）/ `uwb_imu`（UWB+IMU融合）
     - **规划器**：`mighty` / `ego_planner`
     - **控制器**：`ros2_px4_stack` / `px4ctrl` / `so3ctrl`
  2. 点击"用以上参数启动仿真机队"——后端执行 `docker compose up -d` 动态传参重建容器（不是简单的 `docker start`，因为这几个参数是容器*创建*时读取的环境变量，必须重新 `up` 才能应用新值）；
  3. 点击"停止仿真机队"（红色危险按钮）——`docker stop` 三个容器（不删除）。
  4. 面板内另有两个 GUI 按钮：
     - **启动Gazebo图形界面**：在宿主机 X server 上后台起 `gzclient` 连接已运行的 `gzserver`（要求 `sim-world` 容器已在 running 状态，且宿主机已完成 [3.4 节](#34-x11--xhost-授权gui-窗口用) 的 `xhost` 授权）；
     - **启动RViz**：在 `gcs` 容器里后台起 `rviz2`，自动按当前 `PLANNER` 选择配置文件（`multi_mighty.rviz`/`multi_ego_planner.rviz`）。
  5. 如果点击 GUI 按钮几秒内没弹出窗口，先在宿主机终端跑一次 `scripts/up_gcs.sh`（内含 `xhost` 授权），再点一次按钮。

#### 5.2.5 📡 远程真机面板

- **作用**：真机 `flight-stack-hw` 容器的开关机——SSH 到对应 Jetson 执行 `docker compose -f docker-compose.hw.yml up`/`down`，**按机身（NX01/NX02）分开操作**，方便只重启出问题的那一台，而不是"一个按钮控制全部"。
- **操作**：展开面板，对每个机身分别有"启动"/"停止"两个按钮，点击即触发对应 SSH 命令。
- **前提**：宿主机已完成 [3.3 节](#33-ssh-免密登录配置nx01nx02) 的 SSH 免密登录配置。
- 同样是"软件有没有在跑"，不是飞行指令。

#### 5.2.6 📊 资源消耗面板

- **作用**：查看 `sim-world`/`NX01`/`NX02` 三个容器当前的 CPU / 内存 / 网络 I/O / GPU 占用，纯监控，不会对飞机做任何操作。
- **操作**：展开即自动查询并展示。GPU 利用率只在 `sim-world` 那一行显示（GPU 是整机层面统计，nvidia-smi 本身不按容器拆分，只有 `sim-world` 容器实际使用 GPU 做 Gazebo 渲染）。

#### 5.2.7 🗑 清空记录文件（仿真，双击确认）

- **作用**：删除 rosbag / 容器持久化日志 / 姿态推力调试日志等运行时记录文件（**不删** `incidents/` 下手动留证的文件夹——留证功能设计初衷就是不受滚动清理影响），**仅限仿真场景**。
- **操作**：**双击**该链接，5 秒确认窗口内再次操作才会真的执行（防止误触的破坏性操作）。

### 5.3 飞机状态表格（每架飞机自动出现一组）

只要 DDS 域里出现某个命名空间（如 `NX01`）的关键话题，对应的整套卡片/表格行就会自动出现，不需要手动添加。

**表 1：飞行参数**

| 列 | 含义 |
|---|---|
| 飞机 | 命名空间（NX01/NX02） |
| SLAM位置 x,y,z (m) | DLIO 里程计位置 |
| UWB位置 x,y,z (m) | UWB 绝对定位 |
| 姿态 r,p,y (deg) | 横滚/俯仰/偏航角 |
| 推力/油门 | 板外控制器输出的姿态目标推力值 |

**表 2：空间对齐**

| 列 | 含义 |
|---|---|
| 飞机 | 命名空间 |
| θ*估计 | 局部坐标系相对全局系的在线旋转角估计（弧度），飞机一动起来就自动收敛 |
| 样本段数 | 参与估计的滑窗段数 |
| 标定状态 | 起飞点是否已锁定（未锁定时显示"未标定"，不会强行假装存在一个默认值） |

**表 3：子系统健康状态**

| 列 | 含义 | 掉线判定阈值 |
|---|---|---|
| 点云频率 | 激光雷达点云发布频率 | 2000ms 无新消息判掉线 |
| SLAM频率(收到/真实) | 左：网页通过 rosbridge 实际收到的速率；右：容器内原生 `ros2 topic hz` 查到的真实发布速率（每20秒刷新一次） | 2000ms |
| 规划器频率 | 规划器心跳（仅 `PLANNER=mighty` 时有对应节点发布，`ego_planner`下显示"不监控"） | 2000ms |
| 板外控制器 | 板外控制器（px4ctrl/so3ctrl/pt4ctrl）心跳，`ros2_px4_stack`模式下不适用 | 2000ms |
| MAVROS链路 | 飞控通信链路状态 | 6000ms |

**关于"左/右两个SLAM频率经常不一样"**：这不代表 SLAM 真的变慢——`nav_msgs/Odometry` 消息自带两个 6x6 协方差矩阵，序列化成 JSON 后对单线程的 `rosbridge_server` 吞吐量压力较大，高速率下容易被丢弃；右侧"真实"频率是绕过 rosbridge、在容器内直接查询得到的准确值，可信度更高，但因为查询本身要等几秒收集样本，不能像左侧那样高频刷新。

各字段若超过预期时间没有收到新消息，会在界面上变灰提示可能断流；系统启动后有 90 秒宽限期，避免刚起来还没收到第一帧就被误判为"掉线"。

### 5.4 每架飞机的操作按钮

每张飞机卡片下方三个按钮：

| 按钮 | 对应动作 | 二次确认 |
|---|---|---|
| **锁定起飞点** | 调用 `/NX01/set_origin_from_uwb` service（飞机静止时执行一次，作为坐标系标定的固定锚点） | 无（幂等操作） |
| **起飞** | 板载控制器为 `px4ctrl`/`so3ctrl`/`pt4ctrl` 时发布 `TakeoffLand` 话题（连发 3 次防止 DDS discovery 未跟上导致白发）；为 `ros2_px4_stack` 时改为 `touch` 起飞门文件（**该链路只支持起飞一次，降落后不能再起飞**）。起飞的同时后台自动执行一段自标定飞行（悬停 → 前进 1.1 米 → 退回起飞点，约十几秒），为 θ* 在线估计积累初始样本 | 有（浏览器 `confirm()` 弹窗二次确认） |
| **降落** | 转发降落指令给 mavros | 无（幂等操作） |

按钮上如附带提示文字/图标颜色，表示对应操作当前是否可执行（例如仿真机队未启动、容器未 running 时会提示先启动机队）。

### 5.5 右侧面板：规划地图

标签页 "🗺️ 规划地图"，主体是一块俯视示意图画布（`mapCanvas`），叠加显示：

- 每架飞机的实时位置（按 `/tf`、`/tf_static` 换算到世界系或 UWB 绝对坐标系）；
- 当前规划轨迹（`ego_planner`: `optimal_list`；`mighty`: `traj_committed_colored`，尚未实测确认 frame，暂不渲染）；
- 可选叠加的障碍物栅格图（`ego_planner`: `grid_map/occupancy_inflate`；`mighty`: `occupancy_grid`）。

**工具栏操作**：

- **坐标来源**下拉：`世界系(θ*标定)` / `UWB绝对坐标(免标定)`——前者依赖起飞点已锁定+θ*已收敛，后者不需要任何标定，直接用 UWB 原始绝对坐标；
- **叠加障碍物**下拉：选择要显示哪架飞机的占据栅格图，或"不叠加障碍物"。

**发送单个目标点**：在地图上点击任意位置 → 弹出确认面板，选择目标飞机 + 填写相对起飞点的飞行高度（米）→ 点击"发送目标点"，或点击"取消"放弃。

**航线规划（多点）**：

1. 点击"开始规划航线"进入规划模式；
2. 依次点击地图上多个位置，逐点加入航线；
3. 可"撤销上一点"或"清空"重新开始；
4. 选择目标飞机 + 填写统一飞行高度（米）→ 点击"发送航线"；
5. 点击"退出规划"离开规划模式。

**队列状态与取消**：地图下方显示各飞机当前的航点执行进度（依赖机载 `rviz_goal_bridge_node.py` 反馈的 `waypoint_progress`/`waypoint_state` 话题），可对每架飞机点击对应按钮取消其当前航点队列。

### 5.6 右侧面板：摄像头（俯视相机 / 前视相机）

两个标签页结构相同，各自独立配置：

- **信号源**下拉：`MJPEG`（浏览器原生 `<img>` 标签直接播放，无需转码）/ `H.264(经本机转码)`（原始信号是 RTP+H.264 硬编码推流，浏览器无法直接播放，需要先在**宿主机**运行转码脚本 `scripts/rtp_h264_to_mjpeg.py` 把它转成 MJPEG，见下方说明）；
- **地址**输入框：填视频流地址（如 `http://192.168.x.x:8080/stream.mjpg`）；
- **应用**按钮：确认地址，开始播放；
- 播放区域：图像自动撑满并居中显示（`object-fit: contain` 保持原始宽高比），支持**鼠标滚轮缩放**、**拖动平移**。

地址和信号源选择均保存在浏览器 `localStorage`（按标签页+信号源分开存），不经过 backend。

**H.264 转码前置操作**（仅当摄像头输出是 RTP+H.264 硬编码流时需要）：

```bash
python3 docker_sim/scripts/rtp_h264_to_mjpeg.py --udp-port 5000 --http-port 8081
```

该脚本依赖宿主机已安装 `gst-launch-1.0`（GStreamer）及 `jpegenc`/`multipartmux`/`avdec_h264` 元素，独立运行、不受 GCS 容器管理，**没有断流自动重启机制**，如摄像头断流需手动重新运行。转码成功后，网页对应标签页"信号源"选择"H.264(经本机转码)"，地址填 `http://<本机IP>:8081/stream.mjpg`。

---

## 第六部分 常见问题与已知限制

| 问题现象 | 原因 | 处理方式 |
|---|---|---|
| 页面顶部一直显示"DDS域里暂时没发现任何飞机命名空间" | ①仿真/真机机队还没启动；②`PLANNER` 环境变量与真机不一致导致 RMW 不匹配；③本机联调时 `cyclonedds_gcs.xml` 还是真机 IP 没切回 `127.0.0.1` | 先确认机队已启动；核对两边 `PLANNER` 一致；核对当前场景对应的 CycloneDDS 配置是否正确 |
| 改 GCS_BACKEND_TOKEN 或网络设置后，所有 backend 相关面板变成 401 | 浏览器 `localStorage` 里保存的 Token 与 backend 容器实际生效的值不一致（常见于容器被重建、`.env` 没被正确读取） | `docker exec gcs-backend-1 printenv GCS_BACKEND_TOKEN` 核对实际值，网页重新填入保存 |
| 点"改GCS网络设置"报 CORS / "Failed to fetch"，看不到具体错误原因 | 历史 bug（已修复）：早期版本 `CORSMiddleware` 注册顺序在鉴权中间件之前，401 提前返回时没有经过 CORS 层，浏览器只会报语焉不详的网络错误，看不到 401 详情。当前代码已修正顺序 | 确认使用的是修复后的镜像版本；如仍复现，检查浏览器 Network 面板的真实状态码而非仅看 Console 报错文案 |
| 启动 Gazebo GUI / RViz 按钮点了没反应或窗口崩溃 | ①宿主机未执行 `xhost +local:root` 授权；②`DISPLAY` 环境变量在容器创建时被固化成空字符串（`docker exec` 不会重新解析宿主机当前 `$DISPLAY`，只继承容器创建时那份） | 先跑一次 `scripts/up_gcs.sh`（含 xhost 授权）；已知问题已通过在 `docker exec` 时显式传 `-e DISPLAY=:1` 规避，如仍失败检查宿主机是否确实是单一图形会话、`DISPLAY` 是否为 `:1` |
| RViz2 打开但界面空空如也，收不到任何话题 | `docker exec` 起的新 shell 只继承容器*创建时*就设好的环境变量，不会继承 entrypoint 运行时 `export` 的 `RMW_IMPLEMENTATION` | 已在后端启动逻辑中显式 `export RMW_IMPLEMENTATION=rmw_cyclonedds_cpp`（仅 `PLANNER=ego_planner` 时）后再起 `rviz2`；手动执行需注意同样显式 export，不能依赖 `.bashrc` |
| RViz2 里"走廊可视化"（PolyhedronArray）显示插件加载失败 | 缺少 `decomp_rviz_plugins` 依赖的 `ros-humble-pcl-conversions` 包，早期镜像未装 | 确认使用的镜像已包含该依赖（当前 Dockerfile 已修复），其余 Display（TF/PointCloud2/Path 等）不受影响 |
| "关闭GCS"点了之后网页失去响应，想再打开却没有按钮 | 设计上的天然限制——容器都停了不会再有进程响应 HTTP 请求，"网页上关掉自己又在同一网页重新打开"无法实现 | 回宿主机终端 `docker start gcs-gcs-1 gcs-backend-1`，或重新运行 `up_gcs.sh` |
| "本地仿真"面板启动失败，提示 `HOST_REPO_PATH未配置` | `gcs/.env` 里 `HOST_REPO_PATH` 未设置或不正确 | 按 [3.2 节](#32-host_repo_path) 核对并设置正确的宿主机绝对路径 |
| 前视/下视摄像头功能缺失 | 方案文档早期规划阶段的已知阻塞项——彼时前视/下视相机尚未集成到仿真/真机平台 | 目前已通过 MJPEG/RTP转码两种信号源接入俯视/前视相机标签页，若相机硬件/仿真话题尚未就绪，对应标签页仅展示占位提示 |
| RTP+H.264 视频转码脚本停止后画面卡住不动 | 脚本没有"gst子进程挂了自动重启"的容错机制（刻意不做，避免把正常抖动误判成需要重启） | 手动重新运行 `scripts/rtp_h264_to_mjpeg.py`，网页点一次"应用"按钮重新连接 |
| "机队启停"面板对真机（NX01/NX02）操作没有反应或报 SSH 错误 | `~/.ssh/config` 里 `nx01`/`nx02` 别名未配置或配置错误；backend 容器挂载的 `~/.ssh` 路径与实际宿主机用户不符 | 参照 [3.3 节](#33-ssh-免密登录配置nx01nx02) 核对配置，宿主机终端先手动 `ssh nx01 echo ok` 验证 |

---

## 第七部分 安全边界与权限说明

1. **`backend` 容器是这套系统权限最集中的单点**：同时持有宿主机 Docker daemon 完整控制权（`docker.sock`）和对 Jetson 的免密 SSH 登录权。任何能访问这个容器 API 的人，理论上都能通过它间接控制宿主机上任意容器、以及所有配置了免密登录的 Jetson。
2. **鉴权是共享密钥，不是账号系统**：`GCS_BACKEND_TOKEN` 只能防止"同一局域网内偶然扫到端口的人随手操作"，**无法防御真正的中间人攻击**——当前没有 TLS，Token 以明文形式在网络上传输。
3. **部署前提是受信任的本地/内网环境**。若未来需要把地面站暴露到不受信任的网络（如公网、办公网），**必须先补齐 HTTPS/TLS**，不能原样部署；此外也应考虑把 `docker.sock` 挂载收紧为仅允许特定操作的 socket proxy，而不是给予完整 Docker daemon 控制权（当前实现尚未做这一步，属已知待改进项）。
4. **真机调参白名单已做服务端校验**，前端传入不在白名单内的键会被拒绝（HTTP 400），防止误操作或前端 bug 直接把任意字符串写入 Jetson 的 `.env`。
5. **破坏性操作均有确认机制**：清空记录文件要求双击 + 5 秒窗口确认；起飞要求浏览器 `confirm()` 二次确认；停止/关闭类按钮统一标红（`danger` 样式）以做视觉区分。
6. **`Peer address`/站点 IP 等敏感配置文件不进 git**：`gcs/cyclonedds_gcs.xml` 已加入 `.gitignore`，每次真机部署需要按 [3.5 节](#35-cyclonedds-网络配置) 手动准备，避免站点专属网络信息随代码仓库扩散。

---

## 附录 A：后端 API 一览表

基础地址：`http://<GCS-IP>:8000`。除 `/healthz` 与 `OPTIONS` 请求外，均需请求头 `Authorization: Bearer <GCS_BACKEND_TOKEN>`。

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/healthz` | 健康检查，无需鉴权 |
| GET | `/api/gcs-network` | 读取当前 CycloneDDS 网络配置 |
| POST | `/api/gcs-network` | 写入 CycloneDDS 网络配置并重启 `gcs` 容器 |
| GET | `/api/hw-params/{ns}` | 读取指定机身（NX01/NX02）真机调参白名单当前值 |
| POST | `/api/hw-params/{ns}` | 写入白名单参数到 Jetson `.env` 并远程重建 `flight-stack-hw` |
| GET | `/api/fleet/sim/status` | 仿真三容器（sim-world/nx01/nx02）运行状态 |
| GET | `/api/fleet/sim/params` | 仿真机队当前实际生效的 `LOCALIZATION_SOURCE`/`PLANNER`/`CONTROLLER` |
| POST | `/api/fleet/sim/start` | 按选定参数启动/重建仿真机队 |
| POST | `/api/fleet/sim/stop` | 停止仿真三容器 |
| GET | `/api/fleet/hw/{ns}/status` | 指定 Jetson 上 `flight-stack-hw` 运行状态 |
| POST | `/api/fleet/hw/{ns}/start` | SSH 远程启动指定 Jetson 的 `flight-stack-hw` |
| POST | `/api/fleet/hw/{ns}/stop` | SSH 远程停止指定 Jetson 的 `flight-stack-hw` |
| POST | `/api/gcs/stop` | 延迟停止 GCS 自身两个容器（`gcs`+`backend`） |
| POST | `/api/sim/{ns}/takeoff` | 触发指定飞机起飞 + 后台自动标定飞行，仅限仿真场景 |
| GET | `/api/sim/{ns}/env` | 查询指定 flight-stack 容器的 `PLANNER`/`CONTROLLER` 环境变量 |
| GET | `/api/sim/{ns}/slam-hz` | 查询容器内原生 SLAM 发布频率（较慢，不建议高频轮询） |
| POST | `/api/clear-logs` | 清空运行时日志/rosbag（不删 `incidents/`），仅限仿真场景 |
| GET | `/api/container-stats` | 查询仿真三容器的 CPU/内存/网络 I/O + GPU 占用 |
| POST | `/api/sim/gazebo-gui` | 在宿主机 X server 上后台启动 `gzclient` |
| POST | `/api/gcs/rviz` | 在 `gcs` 容器内后台启动 `rviz2`（自动按 `PLANNER` 选配置） |

---

## 附录 B：常用命令速查

```bash
# ===== 部署 =====
./scripts/up_gcs.sh                 # 本机仿真联调一键起
./scripts/up_gcs.sh                    # 真机部署一键起（GCS侧）

# ===== 手动构建镜像（需手动执行）=====
cd docker_sim && docker compose build sim-world && docker compose build flight-stack-nx01
cd docker_sim/gcs && docker compose build

# ===== 容器管理 =====
cd docker_sim/gcs
docker compose up -d / down
docker restart gcs-gcs-1                  # 改cyclonedds_gcs.xml后必须手动重启
docker exec -it docker_sim-gcs-1 bash
curl localhost:8000/healthz

# ===== ROS2 手动操作（进gcs容器后执行）=====
ros2 topic list
ros2 service call /NX01/set_origin_from_uwb std_srvs/srv/Trigger
ros2 topic pub --once /NX01/takeoff_land quadrotor_msgs/msg/TakeoffLand "{takeoff_land_cmd: 1}"
ros2 topic hz /NX01/dlio/odom_node/odom --window 30

# ===== SSH远程（真机）=====
ssh nx01 echo ok
ssh nx01 "cd ~/docker_sim && docker compose -f docker-compose.hw.yml up -d"
ssh nx01 "cd ~/docker_sim && docker compose -f docker-compose.hw.yml down"
```
